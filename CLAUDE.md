# راهنمای توسعه — حساب لند (پروژه حسابداری)

اپلیکیشن حسابداری شخصی/خانوادگی، چندکاربره، فارسی و راست‌به‌چپ، با تاریخ شمسی.
PHP خام (بدون فریم‌ورک و بدون Composer) + MySQL/MariaDB + PDO.

**پیش‌نیاز اجرا:** PHP ≥ ۸.۰ (تست‌شده تا ۸.۴)، افزونه‌های `pdo_mysql`, `gd`, `zlib`, `mbstring`.

---

## نقشه‌ی پروژه

```
config/       تنظیمات (فقط config.example.php در گیت است؛ config.php محرمانه و ignore شده)
includes/     لایه‌ی مشترک — همه‌ی صفحات از اینجا شروع می‌شوند
api/          ~۵۰ اندپوینت JSON برای فراخوانی‌های AJAX
admin/        صفحات ویژه‌ی مدیر (کاربران، دسته‌بندی‌ها، آمار استفاده)
assets/       style.css، app.js، jalali-datepicker.js، آیکون‌ها، فونت وزیرمتن
mobile/       اپ فلاتر (اندروید و iOS) — مصرف‌کننده‌ی api/v1
*.php (ریشه)  صفحات کاربر
*.sql         schema.sql + ۱۸ فایل migration
```

### فایل‌های کلیدی `includes/`

| فایل | نقش |
|---|---|
| `db.php` | کلاس `Database::getConnection()` — PDO تک‌نمونه‌ای، `ERRMODE_EXCEPTION`، `EMULATE_PREPARES=false`. همچنین فشرده‌سازی gzip خروجی را روشن می‌کند و باید **پیش از هر خروجی** لود شود. |
| `auth.php` | کلاس `Auth` — نشست، ورود، «دستگاه مورد اعتماد» با کوکی، نقش‌ها. متدهای پرکاربرد: `initSession()`, `requireLogin()`, `requireAdmin()`, `userId()`, `isAdmin()`. |
| `csrf.php` | کلاس `Csrf` — `token()`, `field()`, `verifyOrFail()` با `hash_equals`. |
| `mailer.php` | کلاس `Mailer` — ارسال ایمیل با `mail()` یا SMTP (کلاینت SMTP دست‌نویس، چون Composer نداریم). |
| `password_reset.php` | کلاس `PasswordReset` — منطق بازیابی رمز، جدا از صفحه‌ها تا قابل تست باشد. |
| `functions.php` | ~۹۰ تابع کمکی (۱۰۸۸ خط) — بزرگ‌ترین فایل پروژه و مرکز منطق دامنه. |
| `header.php` / `sidebar.php` / `footer.php` | قالب مشترک صفحات. |
| `add_tx_sheet.php` / `edit_tx_modal.php` | فرم‌های مشترک ثبت و ویرایش تراکنش. |

### توابع مهم `functions.php`

- **تاریخ شمسی:** `toJalali()`, `jalaliToGregorian()`, `gregorianToJalali()`, `today()`, `startOfWeek()`, `startOfJalaliMonth()`, `startOfJalaliYear()`, `jalaliMonthLength()`, `jalaliWithWeekday()`
- **ورودی/خروجی:** `postParam()`, `getParam()`, `jsonResponse()`, `h()` (escape)، `sanitizeAmount()`, `isValidDate()`, `toPersianDigits()`, `formatMoney()`
- **دامنه:** `walletBalances()`, `totalBalance()`, `budgetStatuses()`, `savingsGoalsWithProgress()`, `debtRemaining()`, `processRecurringTransactions()`, `financialEvents()`, `safeToSpend()`, `spendingInsights()`, `monthComparison()`
- **تنظیمات:** `getSetting()` / `setSetting()` روی جدول `app_settings`

---

## قواعد کدنویسی (هنگام افزودن قابلیت رعایت شود)

### الگوی استاندارد یک اندپوینت `api/`

هر فایل نویسنده‌ی داده در `api/` دقیقاً همین ترتیب را دارد:

```php
<?php
require_once __DIR__ . '/../includes/auth.php';
require_once __DIR__ . '/../includes/csrf.php';
require_once __DIR__ . '/../includes/functions.php';

Auth::initSession();
header('Content-Type: application/json; charset=utf-8');

if (!Auth::isLoggedIn()) { jsonResponse(['success' => false, 'message' => 'ابتدا وارد شوید.'], 401); }
if ($_SERVER['REQUEST_METHOD'] !== 'POST') { jsonResponse(['success' => false, 'message' => 'درخواست نامعتبر است.'], 405); }
Csrf::verifyOrFail(postParam('csrf_token'));

$userId = Auth::userId();   // همیشه از اینجا، هرگز از ورودی کاربر
```

### قواعدی که نباید شکسته شوند

1. **جداسازی کاربران:** هر کوئری روی داده‌ی کاربر باید `WHERE user_id = :uid` داشته باشد. `user_id` همیشه از `Auth::userId()` می‌آید، هرگز از `$_POST`. **استثنایی هم ندارد:** تنها استثنای قبلی `admin/all-transactions.php` بود که حذف شد (پایین‌تر در «آمار استفاده» توضیح داده شده).
2. **کوئری‌ها:** فقط `prepare()` + پارامتر نام‌دار. اگر ناچار به ساختن پویای `WHERE` شدید، مثل `transactions.php` عمل کنید: قطعه‌های ثابت در آرایه، مقادیر همیشه bind. رشته‌ی ورودی کاربر هرگز داخل SQL درج نشود.
3. **CSRF:** هر اندپوینتی که چیزی می‌نویسد `Csrf::verifyOrFail()` دارد. اندپوینت‌های فقط-خواندنی (`dashboard_stats`, `day_detail`, `category_transactions`, `savings_history`, `transaction_attachments`, `view_attachment`) عمداً ندارند — اگر روزی نویسنده شدند، باید اضافه شود.
4. **خروجی HTML:** هر مقدار متغیر با `h()` یا `htmlspecialchars` فرار داده شود.
5. **مسیرها:** لینک‌ها و آدرس دارایی‌ها با ثابت `APP_BASE_PATH` ساخته شوند تا نصب در زیرپوشه نشکند.
6. **تاریخ:** ذخیره در دیتابیس میلادی (`DATE`)، نمایش شمسی. تبدیل فقط با توابع خود پروژه — مبدأ الگوریتم PHP و `assets/js/jalali-datepicker.js` باید یکی بماند.
7. **پول:** ورودی با `sanitizeAmount()` پاک شود (ارقام فارسی و جداکننده‌ها را می‌فهمد) و با `formatMoney()` نمایش داده شود.

### دیتابیس و migration

- `schema.sql` جداول پایه را می‌سازد: `users`, `categories`, `transactions`, `debts`, `app_settings`. دسته‌بندی‌های پیش‌فرض را فقط وقتی درج می‌کند که جدول `categories` خالی باشد — بدون این شرط، اجرای دوباره روی دیتابیس دارای داده همه را تکراری اضافه می‌کرد.
- بقیه‌ی جداول و ستون‌ها با فایل‌های `migration_*.sql` اضافه شده‌اند (کیف‌پول، بودجه، چک، دارایی، پس‌انداز، تکرارشونده، آیکون دسته‌ها، ایندکس‌ها).
- **ترتیب اجرا الفبایی نیست.** ترتیب درست در آرایه‌ی `MIGRATIONS` داخل `deploy/migrate.sh` ثبت شده — تنها مرجع ترتیب همان است.
- `migration_repair.sql` هم `wallets`/`transfers` را می‌سازد و هم `budgets`/`savings_*`/`debt_payments`/`recurring_transactions` را، به‌شکل idempotent. دو فایل قدیمی‌تر که همین‌ها را غیر-idempotent می‌ساختند (`migration_wallets.sql` و `migration_p1.sql`) حذف شدند؛ اجرای دستی‌شان روی دیتابیس دارای داده خطر داشت و هیچ چیزی که `migration_repair.sql` نسازد نمی‌ساختند.
- `migration_indexes.sql` باید **بعد از** `migration_cheques_assets.sql` بیاید، چون روی جدول `cheques` ایندکس می‌زند. این فایل idempotent نیست (`ADD INDEX` بدون `IF NOT EXISTS`) و اجرای دوباره‌اش خطای «ایندکس تکراری» می‌دهد — `migrate.sh` این خطا را می‌شناسد و به‌عنوان «از قبل اعمال شده» ثبتش می‌کند.
- همه‌ی فایل‌ها به‌جز `migration_indexes.sql` ایدمپوتنت‌اند؛ روی MariaDB 10.11 تست شده که اجرای کامل روی یک دیتابیس دارای داده، داده را تغییر نمی‌دهد.
- **ردیابی migration:** جدول `schema_migrations` (نام فایل، چک‌سام، زمان اجرا) که `deploy/migrate.sh` می‌سازد و نگه می‌دارد.
  - `migrate.sh` بدون آرگومان فقط وضعیت را نشان می‌دهد؛ `--apply` اعمال می‌کند.
  - **جدول ردیابی به‌تنهایی مدرک نیست.** هر migration در `SENTINEL` یک شاهد دارد (جدولی یا ستونی که می‌سازد) و `--verify` ثبت را با ساختار واقعی می‌سنجد. علتش یک خرابی واقعی است: `--baseline` روی دیتابیس ایمپورت‌شده، `migration_p4` را «اجراشده» ثبت کرد در حالی که ستون `users.avatar` وجود نداشت؛ از آن به بعد `--apply` می‌گفت «چیزی برای اجرا نیست» و آپلود تصویر در اپ خطا می‌داد، بی‌آنکه این دو به هم ربط داده شوند. حالا `--baseline` فقط چیزی را علامت می‌زند که شاهدش واقعاً هست، و `--apply` این ناهم‌خوانی را خودش ترمیم می‌کند.
  - migration تازه = یک ردیف تازه در `SENTINEL` هم لازم دارد؛ بدون آن اسکریپت متوقف می‌شود.
  - **شاهدِ migration ای که فقط داده اضافه می‌کند، شکلِ `جدول~ستون=مقدار` دارد**
    (`app_settings~setting_key=more_categories_seeded`). ⚠ و آن شاهد عمداً
    خودِ داده نیست: اگر شاهد «دسته‌ی خودرو هست» می‌بود، کاربری که آن را حذف
    می‌کند باعث می‌شد `--apply` دوباره اجرایش کند و کارِ خودش را پس بزند.
  - `--baseline` برای دیتابیسی است که از قبل کامل است ولی ردیابی ندارد (مثل ایمپورت از هاست). فقط `BASELINE_SET` را «اجراشده» علامت می‌زند — یعنی migration هایی که پیش از راه‌اندازی ردیابی وجود داشتند. هر فایلی که بعداً اضافه شده باشد واقعاً اجرا می‌شود و baseline از رویش نمی‌پرد. (نسخه‌ی اول کل فهرست را علامت می‌زد و همین باعث می‌شد migration تازه بی‌سروصدا جا بماند.)
  - اگر فایل `migration_*.sql` تازه‌ای اضافه کنید و در آرایه‌ی `MIGRATIONS` ثبتش نکنید، اسکریپت با خطا متوقف می‌شود — عمدی است تا بی‌سروصدا نادیده گرفته نشود.
  - چک‌سام هر فایل ذخیره می‌شود؛ اگر migration اجراشده‌ای بعداً تغییر کند هشدار می‌دهد. **فایل اجراشده را ویرایش نکنید — migration تازه بنویسید.**
  - **`schema.sql` از این هشدار معاف است** (`CHECKSUM_EXEMPT`). قاعده‌ی بالا می‌گوید با هر تغییر ساختار باید به‌روزش کرد، پس چک‌سامش عمداً و مرتب عوض می‌شود. پیش از این همین یک هشدار دائمی بعد از هر استقرار می‌ساخت که کاری هم برایش نمی‌شد کرد — و هشدارِ همیشگی بدتر از نبودنش است، چون آدم را عادت می‌دهد هشدارها را نادیده بگیرد و آن‌وقت همین هشدار روی یک migration واقعی هم دیده نمی‌شود. `--apply` چک‌سام ثبت‌شده‌اش را تازه می‌کند تا جدول ردیابی واقعیت را بگوید.
- تغییر ساختار جدید = یک فایل `migration_*.sql` تازه (idempotent بنویسید: `IF NOT EXISTS` / بررسی ستون)، به‌علاوه به‌روزرسانی `schema.sql` برای نصب‌های تازه.

### ⛔ collation: چرا SQL درست، روی سرور می‌میرد

- **متغیرِ کاربری در مقایسه با یک ستون ممنوع است.** `FIND_IN_SET(name, @list)`
  روی سرور با `Illegal mix of collations (utf8mb4_persian_ci, utf8mb4_general_ci)`
  مرد و migration وسطِ کار ایستاد. علتش این است که `@var` همان «قابلیتِ تبدیلِ ۲»
  را دارد که یک ستون دارد، پس هیچ‌کدام به دیگری تبدیل نمی‌شود. **رشته‌ی ثابت
  قابلیتِ ۴ دارد و همیشه به collation ستون تبدیل می‌شود** — پس `name IN ('…','…')`
  بنویسید، نه `FIND_IN_SET` با متغیر.
- **⚠ و چرا محلی دیده نمی‌شود:** `mysql` بدون آرگومان با `latin1_swedish_ci` وصل
  می‌شود و آنجا **تبدیلِ charset** اتفاق می‌افتد، پس خطا نمی‌دهد. ولی
  `migrate.sh` با `--default-character-set=utf8mb4` وصل می‌شود و همان مقایسه
  می‌میرد. هر آزمونِ دستی روی SQL باید **همان آرگومان** را بدهد، وگرنه چیزی را
  می‌آزماید که روی سرور اجرا نمی‌شود.
- **`schema.sql` باید با seedِ خودش بخواند.** یک بار ستون‌های `icon`/`color` به
  `INSERT` اضافه شدند ولی به `CREATE TABLE` نه. روی نصبِ **موجود** نامرئی بود
  (آن ستون‌ها را `migration_category_icons.sql` قبلاً ساخته بود) ولی نصبِ
  **تازه** با «Unknown column 'icon'» بدونِ هیچ دسته‌بندی‌ای بالا می‌آمد.
- **`tests/test_migration_chain.php` هر دوی این‌ها را می‌گیرد** و با جهش سنجیده
  شده. کلِ زنجیره را روی یک دیتابیسِ **تازه‌ی** `utf8mb4_persian_ci` با اتصالِ
  `utf8mb4` اجرا می‌کند، بعد می‌سنجد که دسته‌بندی‌های پیش‌فرض واقعاً ساخته شده‌اند
  («بدون خطا اجرا شد» با «کار کرد» یکی نیست)، و آخر همه را دوباره اجرا می‌کند تا
  ایدمپوتنت بودن سنجیده شود. فهرستِ فایل‌ها را از خودِ آرایه‌ی `MIGRATIONS` در
  `migrate.sh` می‌خواند، پس فهرستِ دومی وجود ندارد که از آن عقب بیفتد.
  - این تست یک دیتابیسِ موقت می‌سازد، پس کاربرِ اپ (که طبق قانونِ جداسازی فقط
    روی `hesab_db` دسترسی دارد) نمی‌تواند اجرایش کند و **رد می‌شود، نه شکست**.
    روی ماشین توسعه با یک grantِ محدود فعال می‌شود:
    `GRANT ALL ON \`hesab\_chain\_%\`.* TO 'کاربرِ‌تست'@'localhost'`

---

## بررسی وضعیت هنگام ایمپورت (۱۴۰۵/۰۶/۰۵)

انجام شد روی کل سورس:

- ✅ `php -l` روی هر ۸۶ فایل PHP — بدون خطای نحوی (با PHP 8.4)
- ✅ اسکن کلیدواژه‌های محرمانه — هیچ رمز، توکن یا کلیدی در سورس نیست؛ فقط `config.example.php`
- ✅ هر ۴۸ اندپوینت `api/` بررسی ورود کاربر دارند
- ✅ ۲۰۴ فراخوانی `prepare()` در برابر ۸ `query()` (بدون پارامتر ورودی) — تزریق SQL دیده نشد
- ✅ **فونت اضافه شد** — `assets/fonts/Vazirmatn.woff2`، نسخه‌ی variable و subset‌شده (۸۵ KB در یک درخواست، به‌جای ۱۵۳ KB در سه). جزئیات در `assets/fonts/README.md`.
- ✅ **ردیابی migration اضافه شد** — جدول `schema_migrations` و `deploy/migrate.sh` (بالا توضیح داده شد).
- ✅ **مجموعه تست اضافه شد** — `bash tests/run.sh` (پایین‌تر توضیح داده شد).

---

## ⛔ قانون سرور — استقلال کامل پروژه

روی VPS مقصد، سرویس‌های دیگری (دو ربات) در حال کارند. **حساب لند کاملاً مستقل است.**

هیچ کد، اسکریپت یا دستوری در این مخزن اجازه ندارد:

- به پوشه، فایل، سرویس systemd، یا دیتابیس هیچ سرویس دیگری روی سرور دست بزند
- سرویس مشترکی را `stop` یا `restart` کند (فقط `reload`، آن هم بعد از تست موفق)
- فایل‌های پیکربندی مشترک را تغییر دهد: `nginx.conf`، `php.ini` سراسری، سایت‌های nginx دیگر، pool های دیگر، قوانین فایروال موجود
- کاربر دیتابیسی با دسترسی گسترده‌تر از `hesab.*` بسازد

منابعی که این پروژه **فقط** حق دارد استفاده کند:

| منبع | مقدار |
|---|---|
| پوشه | `/opt/hesab/app` |
| کاربر سیستمی | `hesab` |
| سایت nginx | `/etc/nginx/sites-{available,enabled}/hesab` |
| pool مربوط به PHP | `/etc/php/<نسخه>/fpm/pool.d/hesab.conf` |
| سوکت | `/run/php/php-hesab.sock` |
| دیتابیس / کاربر | `hesab_db` / `'hesab_user'@'localhost'` با `GRANT ... ON hesab_db.*` |
| بکاپ | `/opt/hesab/backups` و `/etc/cron.d/hesab-*` (backup، sessions، due، reminders، offsite) |

> **دام دسترسی `var/sessions`:** `deploy.sh` عمداً `chown -R root:root .` می‌زند تا PHP نتواند کد را عوض کند. ولی `session.save_path` این pool داخل خودِ پوشه‌ی اپ است (`var/sessions`)، پس همان دستور آن را هم مال root می‌کرد و PHP — که با کاربر `hesab` اجرا می‌شود — دیگر نمی‌توانست فایل نشست بنویسد. **خرابیِ حاصل هیچ ردی در لاگ نداشت:** هر درخواست یک نشستِ خالیِ تازه می‌گرفت، پس توکن CSRF هرگز نمی‌ماند و صفحه‌ی ورود در حلقه‌ی «نشست شما منقضی شده بود» گیر می‌کرد — هیچ‌کس نمی‌توانست وارد شود، و هر بار اجرای `deploy.sh` دوباره خرابش می‌کرد. `deploy.sh` حالا مثل `uploads`، پوشه‌ی `var` را هم برمی‌گرداند و نوشتنی بودنش را با `sudo -u hesab test -w` واقعاً می‌آزماید. قاعده ۸ در `test_api_contract.php` این را می‌سنجد.
>
> **دام دسترسی `config/config.php`:** فایل‌های اپ مال `root:root` هستند ولی PHP با کاربر `hesab` اجرا می‌شود. پس `chmod 640` به‌تنهایی یعنی «هیچ‌کس جز root»، و PHP دیگر کانفیگ را نمی‌خواند — کل سایت ۵۰۰ می‌دهد. یک بار `deploy/mail-setup.sh` همین کار را کرد و اپ خوابید. ترتیب درست: `chown root:hesab` بعد `chmod 640`، و در آخر واقعاً آزمودن با `sudo -u hesab test -r`؛ اگر خوانده نشد، برگشت به `644`. هر اسکریپتی که به دسترسی این فایل دست می‌زند باید همین کار را بکند (`deploy.sh` و `deploy/mail-setup.sh` می‌کنند).

سه لایه‌ای که این قانون را عملاً تضمین می‌کنند:

1. `deploy/vps-setup.sh` تابع `assert_allowed()` دارد؛ هر نوشتن خارج از فهرست بالا اسکریپت را متوقف می‌کند.
2. pool اختصاصی PHP با `open_basedir=/opt/hesab/app:/tmp:/usr/share/php` و `disable_functions` شامل `exec,shell_exec,system,proc_open,popen`.
3. PHP این اپ با کاربر `hesab` اجرا می‌شود، نه `www-data`.

هنگام افزودن هر اسکریپت یا قابلیت مربوط به سرور، همین قانون رعایت شود.

---

## نصب و استقرار

- راه‌اندازی روی VPS: `DEPLOY.md` — شامل قانون جداسازی و انتقال داده از هاست اشتراکی. **تنها راه نصب همین است**؛ پشتیبانی از هاست اشتراکی (Apache) برداشته شد.
- گزارش وضعیت سرور پیش از نصب (فقط خواندنی): `deploy/vps-preflight.sh`
- **تشخیص علت کندی (فقط خواندنی): `deploy/perf-report.sh`** — پایین‌تر توضیح داده شده
- اعمال migration ها با ردیابی: `deploy/migrate.sh`
- نصب روی VPS (پیش‌فرض نمایشی؛ با `--apply` اجرا می‌شود): `deploy/vps-setup.sh`
- به‌روزرسانی روی سرور: `./deploy.sh`
- تنظیم pool پی‌اچ‌پی روی نصب موجود: `deploy/tune.sh` (بدون آرگومان نمایشی، با `--apply` اعمال)
- بکاپ روزانه دیتابیس: `deploy/backup.sh --install-cron`
- **بردنِ بکاپ به بیرون از سرور: `deploy/backup-offsite.sh --setup`** — پایین‌تر توضیح داده شده
- **پاک‌سازی نشست‌های منقضی: `deploy/session-clean.sh --install-cron`** — پایین‌تر توضیح داده شده
- **یادآوری روزانه‌ی سررسیدها: `deploy/reminders.php --install-cron`** — پایین‌تر توضیح داده شده
- **سنجش و بازیابی بکاپ: `deploy/restore.sh`**
- افزودن استثنای سرویس‌ورکر به nginx روی نصب موجود: `deploy/nginx-sw.sh` (نمایشی؛ با `--apply` اعمال)
- روشن کردن HTTP/2 روی سایت (سرعت روی شبکه‌ی پرتأخیر): `deploy/nginx-perf.sh` (نمایشی؛ با `--apply` اعمال)
- تنظیم ارسال ایمیل: `deploy/mail-setup.sh`
- مدیریت کاربر از خط فرمان: `deploy/user-admin.php` (از جمله `--unlock ali` برای باز کردن قفل ورود)

`config/config.php` و `uploads/` هرگز وارد گیت نمی‌شوند و در استقرار دست‌نخورده می‌مانند.

---

## حساب بانکی و نمای کارت

- `wallets` علاوه بر نام و بانک، `card_number`، `account_number`، `iban` و `bank_code` هم دارد (`migration_wallet_cards`). این‌ها **حساس‌اند**: در فهرست حساب‌ها نمایش داده نمی‌شوند، فقط در نمای کارت باز می‌شوند، و در فایل بکاپ هم می‌آیند.
- **رنگی که کاربر انتخاب می‌کند همیشه برنده است.** کارت با دو رنگ ساخته می‌شود: رنگِ خودِ حساب و نسخه‌ی تیره‌ترش (`shadeColor()`). نسخه‌ی اول رنگ بانکِ انتخاب‌شده را جای رنگ کاربر می‌گذاشت، پس کاربر رنگ عوض می‌کرد و هیچ اتفاقی نمی‌افتاد.
- **نوع حساب:** نقدی / حساب بانکی / سایر. «کارت بانکی» برداشته شد (عملاً همان حساب بانکی بود) ولی حساب‌های قدیمی با این نوع دست‌نخورده‌اند و گزینه‌اش فقط برای همان‌ها در فرم می‌ماند. «سایر» اسم می‌گیرد: `wallets.kind_label` به‌علاوه‌ی فهرست `wallet_kinds` که برای دفعات بعد می‌ماند (`migration_wallet_kinds`).
- `bankPresets()` فهرست بانک‌ها با رنگ هویتی تقریبی است. **لوگو یا تصویر کارت هیچ بانکی در مخزن نیست** — علامت تجاری‌اند؛ کارت با گرادیان و طرح خودمان ساخته می‌شود. انتخاب بانک فقط رنگ پیش‌فرض را می‌گذارد و کاربر می‌تواند عوضش کند.
- اعتبارسنجی: کارت دقیقاً ۱۶ رقم، شبا دقیقاً ۲۴ رقم بعد از `IR`. ورودی با `digitsOnly()` پاک می‌شود و نمایش با `formatCardNumber()` / `formatIban()`.
- ستون‌ها ممکن است هنوز با migration نیامده باشند، پس `walletBalances()` و `api/save_wallet.php` وجودشان را می‌سنجند و بدون آن‌ها هم کار می‌کنند.

## فهرست‌های من

- `references.php` تنها جای افزودن و حذف فهرست‌های کمکی است: بانک‌های من، بانک‌های طرف مقابل، انواع دارایی، انواع حساب، و **دسته‌بندی درآمد و هزینه**. همه از `api/manage_reference.php` می‌گذرند و چیزی که جایی استفاده شده باشد قابل حذف نیست.
- پیش از این هر کدام ته صفحه‌ی مربوط به خودش قایم بود (بانک‌ها ته `cheques.php`، انواع دارایی ته `my-assets.php`) و کاربر پیدایشان نمی‌کرد. آن صفحه‌ها حالا فقط یک لینک به اینجا دارند. **فهرست تازه‌ای که اضافه می‌کنید هم باید همین‌جا بیاید، نه ته صفحه‌ی دیگری.**
- صفحه دکمه‌ی «بازگشت» دارد (`.page-back` + `.js-page-back`): اگر کاربر از داخل اپ آمده `history.back()` می‌زند، وگرنه به خانه می‌رود. صفحه‌ای که فقط از شیت ابزارها باز می‌شود بدون این راهِ بسته شدن نداشت.

## اشخاص (طرف مقابل)

- جدول `people` (نام + سمتِ آزاد) با `migration_people`. مدیریتش در «فهرست‌های من» است، مثل بقیه‌ی فهرست‌های کمکی.
- **ستون `counterparty_name` عمداً دست‌نخورده ماند و همچنان نامِ ذخیره‌شده است.** این جدول فقط فهرستِ کمکیِ پر کردنِ فرم است، نه جایگزینِ آن ستون. سه دلیل: رکوردهای قدیمی بدون کار کردن، دست نخوردنِ همه‌ی صفحه‌های نمایش، و اینکه حذف یک شخص از فهرست تاریخچه را خراب نکند.
- به همین دلیل، برخلاف بانک و نوع دارایی، **«استفاده شده» مانع حذف شخص نیست.**
- وصل شدن با `<datalist>` است نه `<select>`: ورودی آزاد باید کار کند، چون کاربر ممکن است اسمی را یک بار وارد کند و نخواهد به فهرست اضافه‌اش کند.
- `peopleDatalist()` یک بار در `includes/footer.php` رندر می‌شود تا همه‌ی فرم‌های همان صفحه (افزودن و ویرایش) از یک نسخه استفاده کنند. هر ورودی با `list="peopleList"` وصل می‌شود.
- **انتخابگر شخص** با `personPicker()` ساخته می‌شود: یک `<select>` از اشخاص به‌علاوه‌ی «سایر»، و زیرش همان ورودیِ متنیِ همیشگی که تا زدنِ «سایر» پنهان است. **سمت (همکار / مشتری / …) داخل منو نمی‌آید** بلکه به‌صورت یک نشان کنارِ عنوان دیده می‌شود — داخل منو نامِ بلند را می‌برید و فهرست را شلوغ می‌کرد. رنگش از `.asset-tag` می‌آید نه از رنگِ «تعامل»، چون کنارِ عنوانِ آبی شبیه ادامه‌ی خودِ عنوان دیده می‌شد. چیزی که به سرور می‌رود همچنان `counterparty_name` است، پس هیچ اندپوینتی عوض نشد. فرم ویرایش با `syncPersonPicker()` هماهنگ می‌شود: نامِ داخل فهرست انتخاب می‌شود، نامِ بیرون از فهرست «سایر» با ورودیِ پرشده.
- **گردش حساب هر شخص** در `person.php?name=…` است — همه‌ی چک، طلب و بدهی، و معامله‌های او با خالص‌ها. کلیدش *نام* است نه شناسه، پس رکوردهای قدیمی هم دیده می‌شوند.
- `migration_people` ستون `counterparty_name` را به `trades` و `trade_sales` هم اضافه می‌کند. `save_trade` و `sell_trade` وجودش را با `tableHasColumn()` می‌سنجند، پس پیش از اجرای migration هم نمی‌شکنند.

## دسته‌بندی: پیش‌فرضِ برنامه در برابر شخصیِ کاربر

- `categories.user_id` سه‌حالتی است (`migration_user_categories`):
  - `NULL` → دسته‌ی پیش‌فرضِ برنامه. همه می‌بینندش و فقط مدیر از `admin/categories.php` تغییرش می‌دهد.
  - عدد → دسته‌ی شخصیِ همان کاربر. فقط خودش می‌بیند، اضافه می‌کند و حذف می‌کند.
- **`categoryScopeSql()` / `categoryScopeParams()` تنها جایی‌اند که این معنا تعریف می‌شود.** هر کوئری روی دسته‌ها باید از همان‌ها رد شود — وگرنه یک صفحه دسته‌های شخصی را می‌بیند و صفحه‌ی بغلی نه. جاهایی که رد می‌شوند: `cachedCategories()`, `category-report.php`, `budget.php`, `recurring.php`, `api/add_transaction`, `api/update_transaction`, `api/save_budget`, `api/save_recurring`.
- **پنل مدیر فقط پیش‌فرض‌ها را می‌بیند و دست می‌زند** (`$defaultsOnly` در `admin/categories.php`). اگر دسته‌های شخصیِ کاربران هم آنجا فهرست می‌شدند، مدیر می‌توانست ناخواسته دسته‌ی کسی را حذف کند و تراکنش‌های او بی‌دسته می‌ماند.
- کوئریِ حذف در `api/manage_reference.php` شرط `user_id = :u` دارد، پس دسته‌ی پیش‌فرض (که `NULL` است) اصلاً پیدا نمی‌شود — کاربر عادی به هیچ روشی نمی‌تواند پیش‌فرض‌ها را پاک کند. تست جداسازی در `tests/test_user_categories.php` است.
- **این قاعده یک بار واقعاً شکست:** `data.php` (صفحه‌ی ورود اطلاعات از فایل) دسته‌ها را بدون scope می‌خواند. همان فهرست هم برای تطبیق نامِ ستون استفاده می‌شد و هم با `window.IMPORT_CATEGORIES` به مرورگر می‌رفت — یعنی نامِ دسته‌های شخصیِ همه‌ی کاربران در سورس صفحه دیده می‌شد، و یک ردیف وارد شده می‌توانست `category_id` کاربر دیگری را بگیرد. حالا `test_api_contract.php` قاعده ۶ هر `FROM categories` را جدا می‌سنجد تا این دوباره تکرار نشود.
  - نکته درباره‌ی خودِ آن تست: پنجره‌ی بررسی تا **اولین `;`** بریده می‌شود، نه تعداد ثابتی کاراکتر. نسخه‌ی اول ۶۰۰ کاراکتر می‌گرفت و به کوئریِ بعدی سرریز می‌کرد (`FROM wallets WHERE user_id = :u`) و شرطِ کیف پول را به حساب دسته‌بندی می‌گذاشت. کامنت‌ها هم با توکنایزر حذف می‌شوند، وگرنه توضیحی که *بالای* یک کوئریِ ناایمن نوشته شده تست را سبز می‌کرد. و شرط فقط در `WHERE`/`AND` شمرده می‌شود — `ORDER BY (user_id IS NULL)` صافی نیست.
- **شش دسته‌ی هزینه‌ی جاافتاده با `migration_more_categories` اضافه شدند:**
  خودرو، بیمه، دیون مذهبی، مالیات و عوارض، کارمزد و سود بانکی، جریمه و
  دیرکرد. «خودرو» عمداً از «حمل‌ونقل» جداست (آن یکی تاکسی و مترو است) و
  «جریمه و دیرکرد» از «قسط و وام» (قسط برنامه‌ریزی‌شده است، جریمه نه — و
  دیدنِ جداشان همان چیزی است که رفتار را عوض می‌کند).
  - **⛔ «سرمایه‌گذاری» عمداً نیامد** با اینکه در فهرستِ اپ‌های مشابه زیرِ
    هزینه‌هاست. خریدِ طلا یا سهام هزینه نیست؛ اگر هزینه حسابش کنیم گزارشِ
    هزینه دروغ می‌گوید و «پول قابل خرج» بی‌دلیل کم می‌شود. جای درستش از
    قبل هست: دارایی، معاملات، و خالص دارایی.
  - **⛔ نشانه‌ی `more_categories_seeded` در `app_settings` یعنی این فایل
    دیگر کاری نمی‌کند.** فقط برای ایدمپوتنت بودن نیست (شرطِ `LEFT JOIN`
    خودش آن را می‌دهد) بلکه برای این است که اگر کاربر یکی از این دسته‌ها
    را **عمداً حذف کند**، اجرای بعدی زنده‌اش نکند. بدونِ نشانه، هر deploy
    کارِ کاربر را پس می‌زد.
  - **⛔ جدولِ موقتش با collation صریح ساخته می‌شود.** `JOIN` روی `name`
    بین دو ستون است و هر دو «قابلیتِ تبدیلِ ۲» دارند، پس هیچ‌کدام به
    دیگری تبدیل نمی‌شود — همان چیزی که `FIND_IN_SET(name, @var)` را روی
    سرور کشت. اینجا رشته‌ی ثابتی نیست که خودش تبدیل شود.
  - آیکونِ `shield` برای «بیمه» به `categoryIconMap()` اضافه شد. قرض
    گرفتنِ `health` وسوسه‌کننده بود ولی بیمه‌ی ثالث ربطی به سلامت ندارد و
    دو دسته با یک آیکون در فهرست از هم تشخیص داده نمی‌شوند. **این را
    `test_migration_chain` گرفت**، نه بازبینیِ چشمی: قاعده‌اش این است که
    فقط یک دسته‌ی پیش‌فرض حق دارد آیکونِ `default` داشته باشد.
- «سود معاملات» و «زیان معاملات» عمداً پیش‌فرض (`user_id IS NULL`) ساخته می‌شوند؛ اگر شخصی می‌شدند هر کاربر یک نسخه‌ی تکراری می‌ساخت.
- **راه ورود به گزارش دسته‌بندی از خودِ داشبورد است** (`.breakdown-cta` در `dashboard.php`). پیش از این `category-report.php` فقط از شیت «بیشتر» پیدا می‌شد، در حالی که سؤالِ بعدیِ کاربر بلافاصله بعد از دیدن جمع هزینه‌ی ماه همان «این پول کجا رفت؟» است. دو دکمه‌ی جدا برای هزینه و درآمد، چون دو گزارش جدا هستند؛ هر کدام جمع همان جهت را نشان می‌دهد و با `?type=…&preset=this_month` مستقیم روی همان زبانه باز می‌شود. رنگ دکمه‌ها از `--out`/`--in` می‌آید تا با رنگی که کاربر داخل خودِ گزارش می‌بیند یکی باشد.

## ورود: سدِ حدس رمز

- `LoginThrottle` (`includes/login_throttle.php`) هر تلاش ناموفق را در جدول `login_attempts` ثبت می‌کند (`migration_login_throttle`). پیش از این، صفحه‌ی ورود هیچ سدی نداشت و می‌شد بی‌نهایت رمز امتحان کرد — در حالی که *بازیابی* رمز از قبل محدودیت داشت. یعنی در پشتی محکم بود و در جلویی باز.
- **دو کلید جدا شمرده می‌شوند و هر دو لازم‌اند:** نام کاربری (۵ در ۱۵ دقیقه) و IP (۲۰ در ۱۵ دقیقه). فقط IP کافی نیست چون مهاجم از چند آدرس می‌آید؛ فقط نام کاربری هم کافی نیست چون آن‌وقت می‌شود با تلاش عمدی، حسابِ *یک نفر دیگر* را قفل کرد.
- سقفِ IP عمداً بالاتر است چون پشت NAT (یک خانه، یک دفتر، اینترنت موبایل) چند نفر واقعی ممکن است یک IP داشته باشند.
- **سنجش پیش از `password_verify` است**، نه بعدش: آن تابع عمداً کند است و نباید به مهاجم هدیه شود.
- پنجره کشویی است، نه قفلِ ساعت‌دار — تلاش‌های قدیمی که از پنجره بیرون بروند خودبه‌خود آزاد می‌کنند، پس هیچ کار زمان‌بندی‌شده‌ای لازم نیست. `prune()` هم بعد از هر ورود موفق جدول را کوچک نگه می‌دارد.
- **ورود موفق فقط سابقه‌ی همان نام کاربری را پاک می‌کند، نه سابقه‌ی IP را.** وگرنه مهاجمی که یک حساب معتبر دارد می‌توانست بین هر ۲۰ حدس یک بار وارد شود و شمارنده را صفر کند.
- پیام خطای تلاش ناموفق دست‌نخورده مانده تا وجود حساب لو نرود؛ پیام قفل هم چون روی نامِ ناموجود هم می‌آید، چیزی لو نمی‌دهد.
- اگر migration اجرا نشده باشد، `available()` نادرست برمی‌گردد و ورود دقیقاً مثل قبل کار می‌کند — سد بی‌سروصدا خاموش می‌ماند، نه اینکه صفحه بخوابد.
- **در پشتی:** `php deploy/user-admin.php --unlock ali` (یا `--unlock --all` که شمارنده‌ی IP را هم پاک می‌کند). سد بین کاربر واقعی و مهاجم فرق نمی‌گذارد، پس خودتان هم می‌توانید قفل شوید.
- تست در `tests/test_login_throttle.php` است و مهم‌ترین بررسی‌اش این است که **در حالت قفل، رمزِ درست هم رد شود** — وگرنه سد بی‌فایده است.

## نشست، مهلت، و «۳۰ روز به خاطر بسپار»

- **`logout()` فقط برای خروجِ خواسته‌ی کاربر است** و عمداً اعتماد این دستگاه را هم باطل می‌کند. انقضای مهلت از `expireSession()` رد می‌شود که نشست را خالی می‌کند ولی به `trusted_devices` دست نمی‌زند.
- **این یک بار واقعاً شکسته بود و بی‌سروصدا:** `isLoggedIn()` هنگام انقضای مهلت `logout()` را صدا می‌زد، پس ردیف `trusted_devices` پاک می‌شد و `loginFromTrustedDevice()` بلافاصله بعدش چیزی برای کار کردن نداشت. یعنی همان مهلت یک‌ساعته‌ای که قرار بود «۳۰ روز به خاطر بسپار» از رویش پل بزند، خودِ آن را نابود می‌کرد — کاربر با وجود روشن بودنِ گزینه هر ساعت دوباره رمز می‌خواست، و هیچ خطایی هم دیده نمی‌شد.
- `expireSession()` نشست را `destroy` نمی‌کند بلکه خالی می‌کند و شناسه‌اش را عوض می‌کند، چون `loginFromTrustedDevice()` بلافاصله بعدش می‌خواهد در همان نشست بنویسد.
- تست در `tests/test_session_trust.php` است و هر سه حالت را با هم می‌سنجد — چون درست کردنِ یکی به قیمت دیگری آسان است: انقضا با دستگاه مورد اعتماد (باید بماند)، خروجِ کاربر (باید اعتماد باطل شود)، و انقضا بدون دستگاه مورد اعتماد (باید بیرون بیندازد).

### مهلت «ماندن در حساب» — یک تنظیم، و کشویی

- **`users.session_minutes` تنها مرجع است** (`migration_session_window`). ۰ یعنی
  «بدون مهلت» و **پیش‌فرضِ همه همین است**. ستون قدیمی `session_hours` عمداً حذف
  نشد (بکاپ‌ها و کد قدیمی) ولی هیچ‌جا خوانده نمی‌شود.
- فهرست مقدارهای مجاز فقط در `Auth::SESSION_WINDOWS` است — از ۱ دقیقه تا یک ماه،
  به‌علاوه‌ی «بدون مهلت». پروفایل و `api/update_session_pref` هر دو از همان
  می‌خوانند؛ **فهرست دوم نسازید**، وگرنه گزینه‌ای که کاربر می‌بیند هنگام ذخیره
  بی‌سروصدا به پیش‌فرض برمی‌گردد.
- همین یک عدد **هم** مهلت بی‌فعالیتیِ نشست را تعیین می‌کند **هم** عمر کوکی
  دستگاه مورد اعتماد. پیش از این دو چیز جدا بودند (یک ساعت در برابر ۳۰ روزِ ثابت)
  و کاربر نمی‌فهمید کدام حاکم است.
- **⛔ کشویی بودن یعنی هر سه تکه با هم جلو بروند** (`slideTrustedDevice()`):
  `expires_at` در دیتابیس، `expires` روی کوکی، و `last_used_at`. اگر یکی جا
  بماند، خرابی **بی‌صداست**: نسخه‌ی قبلی فقط سومی را می‌نوشت، پس مهلت ثابت بود و
  کاربری که هر روز اپ را باز می‌کرد هم سرِ سررسیدِ اول رمز می‌خواست.
- نشستِ زنده از `loginFromTrustedDevice()` رد نمی‌شود، پس `isLoggedIn()` هم
  کوکی را تمدید می‌کند — با سقفِ **روزی یک بار** (`trust_slid_at` در نشست)، وگرنه
  هر بارگذاری صفحه یک `UPDATE` می‌زد.
- عوض کردن مهلت در پروفایل فوراً کوکیِ همان دستگاه را هم هم‌تراز می‌کند
  (`refreshTrustForCurrentDevice()`). بدون آن، کاربر «یک ماه» را انتخاب می‌کرد و
  تا ورود بعدی هیچ اثری نمی‌دید — یعنی تنظیمی که کار نمی‌کند.
- «بدون مهلت» کوکی ده‌ساله می‌گیرد (`FOREVER_SECONDS`). اگر PHP فایل نشست را جمع
  کند، همان درخواستِ بعدی از کوکی دوباره وارد می‌شود و کاربر چیزی نمی‌بیند.
- **`logout()` فقط دستگاهِ خودش را باطل می‌کند، نه همه را.** تست این را جدا
  می‌سنجد؛ اگر روزی همه را پاک کند، کاربری که روی گوشی خروج می‌زند لپ‌تاپش را هم
  بیرون انداخته.
- تست در `tests/test_session_trust.php` است (۲۳ بررسی) و با آزمون جهش سنجیده
  شده: برگشت به مهلت ثابت، بی‌اثر کردن «بدون مهلت»، و باطل کردن همه‌ی دستگاه‌ها
  در خروج — هر سه شکست می‌دهند.

### `session.gc_maxlifetime`

- کاربر در پروفایل مهلتش را انتخاب می‌کند (`users.session_minutes`؛ بالاتر توضیح داده شد)، ولی پیش‌فرض `session.gc_maxlifetime` در PHP ۲۴ دقیقه است. `Auth::initSession()` آن را پیش از `session_start()` روی بزرگ‌ترین گزینه‌ی **محدود** (یک ماه) می‌گذارد. گزینه‌ی «بدون مهلت» بلندتر است ولی این عدد را بالا نمی‌برد: جمع شدن فایل نشست آنجا بی‌خطر است، چون همان درخواست بعدی از کوکی دستگاه دوباره وارد می‌شود.
- **⚠ و دقیقاً به همین دلیل، پوشه‌ی نشست خودش پاک نمی‌شود.** سه چیز دست به دست
  هم می‌دهند: `session.save_path` داخل خودِ پروژه است پس کرونِ `sessionclean`
  دبیان سراغش نمی‌آید؛ روی اوبونتو `gc_probability = 0` است پس خودِ PHP هم
  جمعشان نمی‌کند؛ و این خط مهلت را یک ماه می‌کند. با یک کاربر نامرئی است، با
  صدها کاربر ده‌ها هزار فایل در یک پوشه جمع می‌شود و آن‌وقت باز کردنِ خودِ پوشه
  کند می‌شود یا اینود تمام می‌شود. `deploy/session-clean.sh` همین را می‌برد:
  بدون آرگومان فقط گزارش می‌دهد، با `--apply` پاک می‌کند، و با `--install-cron`
  شبانه زمان‌بندی می‌شود.
  - **⚠ بیشترِ آن فایل‌ها اصلاً کاربر ندارند.** `login.php` هم `initSession()`
    می‌زند (برای توکن CSRF)، پس هر ربات و هر بازدیدِ گذری یک فایل جا می‌گذارد.
    روی سرور واقعی این عدد با سه کاربر به **۴۳۱۰ فایل** رسیده بود.
  - به همین دلیل **دو مهلتِ جدا** هست: نشستِ کاربرِ واردشده ۳۵ روز (بیشتر از
    بلندترین مهلتِ محدودِ اپ، وگرنه کاربرِ فعال بیرون می‌افتاد)، و نشستِ
    بی‌کاربر فقط ۲ روز. نسخه‌ی اول همه را ۳۵ روز نگه می‌داشت — آن استدلال برای
    فایلی که کاربری ندارد بی‌معنا بود و یعنی انبار کردنِ آشغال به مدت پنج هفته.
  - تشخیص با وجودِ رشته‌ی `user_id|i:` در فایل است، یعنی به قالبِ سریال‌سازیِ
    پیش‌فرضِ PHP بند است. **اگر آن قالب عوض شود، هیچ نشستی «کاربردار» دیده
    نمی‌شود و اسکریپت همه را بعد از دو روز پاک می‌کند** — یعنی بیرون انداختنِ
    همه‌ی کاربران، بی‌هیچ خطایی. محافظش این است: اگر پوشه پر باشد ولی در هیچ
    فایلی نشانه پیدا نشود، دست نگه می‌دارد و توضیح می‌دهد.
  - محافظ دوم روی مسیر است: اگر `SESSION_DIR` به `var/sessions` ختم نشود امتناع
    می‌کند. بدون آن یک متغیر محیطیِ اشتباه کافی بود تا جای دیگری پاک شود.
- **روی اوبونتو این تنظیم عملاً بی‌اثر است و مشکلی هم نیست:** آنجا `session.gc_probability = 0` است (PHP خودش جمع نمی‌کند) و `sessionclean` دبیان فقط `save_path`ِ داخل `php.ini` را پاک می‌کند، نه مسیر اختصاصیِ این pool را. پس این خط برای نصب روی هاست اشتراکی است که GC خودِ PHP فعال است.
- روی هیچ سرویس دیگری اثر ندارد: pool این اپ `session.save_path` خودش را دارد (`hesab.conf`) و `ini_set` فقط همان درخواست را عوض می‌کند، نه `php.ini` سراسری.
- **عارضه‌ی جدی‌ترش توکن CSRF بود.** توکن داخل همان فایل نشست است، پس با جمع شدن نشست از بین می‌رفت و فرمی که روی گوشی ساعت‌ها باز مانده بود، با submit شدن به `Csrf::verifyOrFail` می‌خورد — که آن موقع فقط `die()` با یک رشته‌ی خام می‌زد: صفحه‌ی کاملاً سفید، بدون قالب و بدون هیچ دکمه‌ای. بن‌بست کامل.
  - `verifyOrFail` حالا صفحه‌ی «نشست شما منقضی شد» را رندر می‌کند: قالب کامل، حالت شب، و دو راه خروج (خانه/ورود و «بازگشت»). مثل `offline.html` عمداً بدون وابستگی به `style.css` است.
  - **`login.php` عمداً `verifyOrFail` صدا نمی‌زند** بلکه `Csrf::validate()` را خودش می‌سنجد و در صورت کهنه بودن، فرم را با توکن تازه و پیام «نشست شما منقضی شده بود» دوباره نشان می‌دهد. صفحه‌ی ورود بیشتر از هر صفحه‌ی دیگری باز می‌ماند و بن‌بست شدنش دقیقاً بدترین حالت است. امنیت کم نمی‌شود: توکن نامعتبر همچنان وارد نمی‌کند.
  - `test_api_contract.php` قاعده ۷ هر سه را می‌سنجد.
  - **`isJsonRequest()` باید `X-Requested-With` را هم بپذیرد.** همه‌ی `fetch` های `app.js` این سرآیند را می‌فرستند ولی `Accept: application/json` نمی‌فرستند؛ بدون آن شرط، ردِ CSRF به یک فراخوان AJAX صفحه‌ی HTML می‌داد، `res.json()` می‌شکست و کاربر فقط «خطا در ارتباط با سرور» می‌دید.

## ورودِ اطلاعات از فایل: پیلود مرورگر داده است، نه دستور

- مرحله‌ی ثبت در `data.php` روی JSON ای کار می‌کند که خودِ مرورگر ساخته. یک بار `category_id` و `wallet_id` را همان‌طور که آمده بودند می‌نوشت — یعنی هر کاربری می‌توانست پیلود را دست‌کاری کند و تراکنشش را به دسته یا حسابِ **کاربر دیگری** بچسباند. با curl آزموده و تأیید شد.
- حالا هر دو در برابر مالکیتِ خودِ کاربر سنجیده می‌شوند: دسته باید از `categoryScopeSql()` رد شود *و* جهتش (درآمد/هزینه) بخواند، وگرنه `NULL` می‌شود؛ حساب باید مالِ خودش باشد وگرنه حساب پیش‌فرض می‌گیرد (همان قاعده‌ی `resolveWalletId()`، تا پول در هیچ‌کجا گم نشود).
- تست رگرسیونش در `tests/test_api_auth.php` است و **قربانی را خودش می‌سازد**؛ تکیه بر داده‌ی موجود یعنی روی دیتابیس خالی بی‌سروصدا رد می‌شود و چیزی را نگه نمی‌دارد.

## `tests/test_dead_code.php` — چیزی که `php -l` نمی‌بیند

- **متغیرِ استفاده‌شده ولی هرگز مقدارنگرفته.** همان باگی که دو بار زد (`categoryScopeParams($userId)` در `add_transaction` و `update_transaction`). با توکنایزر کار می‌کند و چهار حالت را جدا می‌شناسد تا هشدار الکی ندهد: تخصیصِ تجزیه‌ای (`[$a,$b] = …`)، ویژگیِ ایستا (`Mailer::$lastError`)، `&$ref` (که توکنش در PHP 8.1 عوض شده)، و پارامترهای خروجیِ ارجاعی (`preg_match(…, $m)`).
- **نکته‌ی کلیدی:** نسخه‌ی اول هر `(` را «پارامتر تابع» فرض می‌کرد و محافظه‌کارانه می‌پذیرفت — یعنی دقیقاً همان باگی که تست برایش نوشته شده بود را نمی‌گرفت. `isParamList()` حالا *تعریف* را از *فراخوانی* جدا می‌کند. با آزمون جهش سنجیده شده.
- **تجزیه‌ای در `foreach` هم تخصیص است:** `foreach ($rows as [$a, $b])`. پیش‌پویشِ تجزیه‌ای فقط دنبالِ `=` بعد از براکت بود، ولی اینجا `)` می‌آید — پس `$b` «بدون مقدار» دیده می‌شد. **هشدارِ الکی از نبودِ تست بدتر است**، چون آدم را وادار می‌کند کدِ بدتر بنویسد تا تست سبز شود (اینجا وسوسه این بود که `foreach` را به `$f[0]`/`$f[1]` بشکنیم). حالا اگر توکنِ *قبلِ* براکت `as` باشد هم تخصیص شمرده می‌شود. با دو جهش سنجیده شده: متغیرِ تعریف‌نشده‌ی معمولی، و متغیرِ تعریف‌نشده داخلِ همان حلقه‌ی تجزیه‌ای — هر دو گرفته می‌شوند.
- کد مرده (تابع یا اندپوینتِ بی‌فراخوان) فقط **گزارش** می‌شود، نه شکست.

## اندپوینت‌ها: چرا تستِ «بدون ورود» کافی نیست

- `tests/test_api_auth.php` سه لایه دارد و هر سه لازم‌اند:
  ۱. بدون ورود، هیچ اندپوینتی داده ندهد.
  ۲. **با ورود**، هیچ اندپوینتی ۵۰۰ ندهد.
  ۳. **مسیر واقعیِ پول** — ثبت و ویرایش یک تراکنشِ دسته‌دار با پیلود کامل.
- لایه‌ی ۳ از یک باگ واقعی درآمد: `api/add_transaction.php` و `api/update_transaction.php` تابع `categoryScopeParams($userId)` را صدا می‌زدند در حالی که `$userId` در آن دو فایل هرگز تعریف نشده بود. نتیجه، خطای کشنده‌ی PHP و پاسخ ۵۰۰ با بدنه‌ی خالی بود؛ در مرورگر فقط «خطا در ارتباط با سرور» دیده می‌شد. **ثبت و ویرایش تراکنشِ دسته‌دار — یعنی هسته‌ی اپ — کاملاً از کار افتاده بود.**
- `php -l` متغیر تعریف‌نشده را نمی‌گیرد و تست قرارداد فقط شکل کد را می‌بیند.
- **لایه‌ی ۲ هم به‌تنهایی کافی نبود:** خطِ باگ‌دار داخل `if ($categoryIdValue !== null)` بود، پس با ورودیِ خالی اصلاً اجرا نمی‌شد و تست سبز می‌ماند. هر اندپوینتی که شاخه‌ی شرطی دارد، دست‌کم یک بار باید با **پیلود کاملِ واقعی** صدا زده شود.

## ⛔ صفحه‌ها هم همان سه لایه را می‌خواهند — `tests/test_page_render.php`

- **همین سه لایه برای صفحه‌ها وجود نداشت.** `test_api_auth` فقط لایه‌ی
  اول را داشت («بدون ورود ۳۰۲ می‌دهد») و آن هم برای ۱۲ صفحه از ۳۹.
  **هیچ تستی در کلِ مجموعه یک صفحه را با کاربرِ واردشده باز نمی‌کرد.**
- **⛔ خطای کشنده‌ی وسطِ رندر، در تولید با `display_errors` خاموش،
  HTML نصفه می‌دهد**: نه خطایی، نه صفحه‌ی سفیدی که به چشم بیاید — فقط
  صفحه‌ای که ناگهان تمام می‌شود و کاربر فکر می‌کند اینترنتش قطع شده.
  `php -l` نمی‌گیردش و `test_dead_code` فقط شکل را می‌بیند. سابقه‌اش هم
  هست: `assets/manifest.php` مدت‌ها ۵۰۰ می‌داد و فقط تستی که واقعاً
  صدایش زد پیدایش کرد.
- **⛔ نشانه‌ی «رندر تمام شد» وجودِ `</html>` است، نه طولِ بدنه.** خطای
  کشنده خروجی را همان‌جا می‌بُرد، پس آن تگ هرگز نوشته نمی‌شود. این
  دقیق‌ترین سنجه است و به هیچ عددِ جادویی بند نیست.
- **⛔ کاربرِ آزمایشی در همه‌ی بخش‌ها داده دارد**، وگرنه هر صفحه شاخه‌ی
  «هنوز چیزی ثبت نکرده‌اید» را می‌گیرد و کدی که به ردیف‌های واقعی دست
  می‌زند اصلاً اجرا نمی‌شود — همان درسِ لایه‌ی ۲.
  - **⛔ و شمارشِ کلِ درج‌ها کافی نیست، هر جدول جدا سنجیده می‌شود.**
    نسخه‌ی اولِ fixture `next_date` نوشته بود به‌جای `next_due_date`؛ درج
    **بی‌صدا** رد شد و صفحه‌ی «تراکنش دوره‌ای» با فهرستِ خالی سنجیده
    می‌شد — یعنی همان شاخه‌ای که تست برایش ساخته شده اجرا نمی‌شد.
- **⛔ فهرستِ `EXPECT` بسته است.** هر `*.php` تازه‌ای در ریشه یا `admin/`
  که در آن نباشد تست را می‌شکند. بدونِ این، صفحه‌ی فردا بی‌صدا بیرونِ
  پوشش می‌ماند و تست به‌مرور بی‌ارزش می‌شد — همان قاعده‌ی آرایه‌ی
  `MIGRATIONS` و فهرستِ بسته‌ی فایل‌های بومی.
- **کاربرِ عادی هم جدا سنجیده می‌شود** و نباید از هیچ صفحه‌ی `admin/`
  محتوا بگیرد. این بررسی **هیچ‌جای دیگری وجود نداشت**: `test_api_auth`
  صفحه‌ها را فقط بدون ورود می‌سنجد.
- **⚠ تستِ دود است نه تستِ درستی:** ثابت می‌کند صفحه بالا می‌آید، نه
  اینکه عددهایش درست‌اند.
- **و همان اجرای اول یک باگِ واقعی پیدا کرد:** `admin/_nav.php` یک
  partial بود بدونِ نگهبانِ `APP_BASE_PATH`، و سایتِ nginx فقط
  `/deploy/`, `/tests/`, `/config/` و `/includes/` را می‌بندد —
  **`/admin/` را نمی‌بندد.** پس `https://…/admin/_nav.php` از بیرون
  قابل باز کردن بود و برای هر کسی، حتی واردنشده، **۵۰۰** می‌داد.
  نگهبانش اضافه شد و حالا انتظارش در `EXPECT` از نوعِ `blocked` است.
- تست ۱۱ بررسی دارد و با **شش جهش** سنجیده شده، همه گرفته شد: خطای
  کشنده در ابتدای صفحه، خطای کشنده **وسطِ** رندر (بدنه‌ی نصفه)،
  برداشتنِ نگهبانِ partial، برداشتنِ `requireAdmin()` از یک صفحه‌ی مدیر،
  صفحه‌ی تازه‌ی خارج از فهرست، و همان درجِ بی‌صدا شکست‌خورده‌ی fixture.

### ⛔ برند «حساب لند» — و چهار چیزی که عمداً عوض **نشدند**

نامِ برنامه از «دفتر مالی» به **«حساب لند» / HesabLand** تغییر کرد.
آنچه عوض شد: `APP_NAME` و `MAIL_FROM_NAME` در `config.example.php`، همه‌ی
رشته‌های برگشتیِ `defined('APP_NAME') ? … : '…'`، `app_name` اپ اندروید،
نامِ بسته (`ir.stland.hesabland`)، `Theme.HesabLand`، `rootProject.name`،
مسیرِ `download/hesabland.apk`، و متنِ همه‌ی مستندات.

**⛔ ولی چهار چیز دست نخوردند، و هر چهار به یک دلیل: نامِ ما نیستند،
نشانیِ چیزی هستند که همین حالا روی دستگاهِ کاربر یا داخلِ یک فایل نشسته.**

1. **کلیدهای `localStorage` و کوکی‌ها** (`daftar_theme`, `daftar_device`,
   `daftar_remember_user`, `daftar_asset_off`, `daftar_has_apk`,
   `daftar_sms_auto_on`, …). عوض کردنشان یعنی حالت شب، فیلترِ دارایی و
   **دستگاه مورد اعتماد** همه صفر می‌شوند و کاربر دوباره رمز می‌خواهد —
   بی‌هیچ خطایی، و بدونِ اینکه کسی ربطش را به تغییرِ نام پیدا کند.
2. **`VERSION` در `sw.js`** (`daftar-v3`). آن رشته پیشوندِ نامِ کش است؛
   عوض کردنش کلِ کشِ سرویس‌ورکر را پاک می‌کند و همان **تورِ نجاتِ
   «نسخه‌ی قبلیِ فایل»** را می‌برد که برای صفحه‌ی بی‌جان نوشته شده بود.
3. **`keyAlias = "daftar"`** — نامِ ورودیِ داخلِ فایلِ keystore. پایین‌تر
   در بخش اندروید توضیح داده شده.
4. **پسوندِ `.sthesab` فایلِ بکاپ.** کاربرانی که تا امروز بکاپ گرفته‌اند
   فایل‌هایی با همان پسوند دارند؛ عوض کردنش فقط دو نسلِ فایل می‌ساخت
   بی‌آنکه چیزی بهتر شود. (نامِ برند **داخلِ** فایل از `APP_NAME` می‌آید و
   خودبه‌خود به‌روز است.)

**⚠ و `APP_NAME` روی سرور با `git pull` عوض نمی‌شود**، چون
`config/config.php` وارد گیت نمی‌شود. تا وقتی آن یک خط دستی عوض نشود،
عنوانِ صفحه‌ها و موضوعِ ایمیل‌ها همان نامِ قبلی می‌ماند — یعنی همان
خرابیِ بی‌صدایی که قاعده ۲۲ برای گرفتنش نوشته شده، این بار از سمتِ
پیکربندی.

### ⛔ قاعده ۲۲ — نامِ برند فقط از `APP_NAME`

- `APP_NAME` در `config.php` هست و بیشتر جاها از همان می‌خوانند، ولی
  **چهار نقطه‌ی دیده‌شدنی** سخت‌کد مانده بودند: موضوعِ ایمیلِ یادآوریِ
  روزانه، فیلدِ `app` داخلِ فایلِ بکاپِ کاربر، متنِ ایمیلِ آزمایشیِ
  `user-admin.php`، و کامنتِ فایلِ cron.
- **خرابی‌اش برای مالکِ نصب بی‌صداست:** کسی که برند را عوض می‌کند اپ را
  باز می‌کند و همه‌چیز درست است — ولی ایمیل‌هایش با نامِ ما می‌روند و
  فایل‌های بکاپش نامِ ما را دارند. و آن ایمیل با cron می‌رود، پس
  خروجی‌اش را هیچ‌کس نمی‌بیند.
- الگوی مجاز `defined('APP_NAME') ? APP_NAME : '…'` روی همان خط است، تا
  نصبی که هنوز ثابت را ندارد نشکند.
- ⚠ با توکنایزر است نه grep: همین توضیح خودش نامِ برند را دارد و با
  جست‌وجوی متنی، تست روی فایلِ **سالم** هم قرمز می‌شد.
- ⚠ و یک اشتباهِ خودِ تست در همان اجرای اول: متغیرِ `$bad` با بلوکِ
  بررسیِ نحو مشترک بود و آن بلوک خالی‌اش نمی‌کند، پس خطاهای این قاعده
  در گزارشِ آن یکی هم تکرار می‌شدند — **تست دروغ می‌گفت کدام بررسی
  شکسته**. حالا `$badBrand` جداست.

## ⛔ `LICENSE` — نبودنش یعنی نمی‌شود فروخت

- تا امروز مخزن هیچ فایل پروانه‌ای نداشت. نبودنش نرم‌افزار را آزاد
  نمی‌کرد (پیش‌فرضِ حقوقی همان «تمام حقوق محفوظ» است) ولی برای خریدار،
  همکار، یا هر شخص ثالثی جای ابهام می‌گذاشت.
- **پروانه عمداً اختصاصی است، نه متن‌باز.** پروانه‌ی متن‌باز دقیقاً همان
  دارایی‌ای را واگذار می‌کند که قرار است فروخته شود.
- بندِ «واگذاری» صریح آمده: مالک می‌تواند حقوق را با قراردادِ کتبیِ جدا
  منتقل کند و آن قرارداد بر این پروانه مقدم است.
- **دو دارایی شخص ثالث استثنا شده‌اند** و پروانه‌ی خودشان را دارند:
  Chart.js (MIT) و وزیرمتن (SIL OFL 1.1). شرایطِ فونت به
  `assets/fonts/README.md` اضافه شد — پیش از این هیچ‌جا نوشته نشده بود،
  و برای واگذاری این نکته لازم است.

## نصب روی گوشی (PWA)

- `assets/manifest.php` عمداً PHP است نه JSON ایستا، چون `start_url` و `scope` باید مسیر مطلق درست را بدانند. یک آیکون هم با `purpose: maskable` می‌آید تا اندروید داخل کادر سفید نگذاردش.
- **⛔ آیکونِ `maskable` فایلِ جداست، نه همان `icon-512`.** اندروید آیکون
  را داخل شکلِ خودش می‌برد و فقط **۸۰٪ مرکزی** تضمین‌شده است. در آیکونِ
  عادی دورترین گوشه‌ی لوگو روی **۴۳.۸٪** می‌افتد — یعنی با ماسکِ دایره‌ای
  لبه‌ی میله‌ی بلندِ نمودار بریده می‌شود. `icon-512-maskable.png` همان لوگو
  را وسط‌چین و روی **۳۷.۱٪** دارد.
  - ساختنش هم دو بار غلط شد و هر دو یک ریشه داشت: **زمینه‌ی آیکون تخت
    نیست** (سایه‌ی ملایمِ #1B1B1B تا #111111). پس هر تکه‌ی بریده‌شده —
    چه خودِ لوگو، چه کلِ مربعِ کوچک‌شده — لبه‌اش با زمینه اختلافِ روشنایی
    داشت و به شکلِ یک **مستطیلِ روشنِ دیدنی** درمی‌آمد. راهِ درست
    «کلیدِ رنگ» است: زمینه خاکستریِ خنثی است و لوگو طلاییِ اشباع، پس
    فاصله‌ی `R−B` شفافیت را می‌سازد و درزی نمی‌ماند.
  - **تست خودِ تصویر را می‌سنجد، نه نامِ فایل را** (در `test_api_auth`):
    اگر کسی این ورودی را دوباره به `icon-512` برگرداند، هیچ فایلی گم
    نمی‌شود ولی تست قرمز می‌شود.
- **⛔ آدرسِ آیکون باید `?v=` داشته باشد** — با `iconUrl()`، نه مسیرِ خام.
  nginx به همه‌ی `.png` کشِ «یک سال، **immutable**» می‌دهد؛ با آدرسِ ثابت،
  عوض کردنِ فایلِ آیکون برای کسی که یک بار سایت را باز کرده **تا یک سال**
  هیچ اثری ندارد و تازه‌سازیِ صفحه هم کاری نمی‌کند (`immutable` یعنی
  مرورگر حتی درخواستِ شرطی نمی‌فرستد). با عوض کردنِ فایلِ آیکون،
  `VERSION` در `sw.js` هم باید بالا برود، چون `sw.js` آن‌ها را پیش‌کش
  می‌کند.
- ⚠ `assets/manifest.php` فقط `auth.php` را لود می‌کرد و آن `functions.php`
  را نمی‌آورد؛ اولین باری که مانیفست تابعی از آنجا صدا زد، **۵۰۰** داد
  بی‌آنکه هیچ ردی روی صفحه بگذارد — سایت درست بالا می‌آمد و فقط «افزودن
  به صفحه‌ی اصلی» بی‌صدا از کار می‌افتاد. `php -l` این را نمی‌گیرد و هیچ
  تستی هم آن فایل را صدا نمی‌زد؛ حالا `test_api_auth` می‌زند.
- **`sw.js` هیچ HTML ای کش نمی‌کند و این قابل مذاکره نیست.** همه‌ی صفحه‌ها عمداً `no-store` می‌گیرند (پایین‌تر در «سرعت» توضیح داده شده)؛ اگر سرویس‌ورکر HTML را کش کند همان باگِ سافاری برمی‌گردد — این بار بدتر، چون کشِ سرویس‌ورکر با تازه‌سازی صفحه هم پاک نمی‌شود. `test_api_contract.php` این را می‌سنجد (قاعده ۶).
- درخواست‌های ناوبری شاخه‌ی جدا دارند: همیشه از شبکه، و اگر شبکه نبود `offline.html`. کش فقط برای دارایی‌های ثابتِ داخل `assets/` است، آن هم به شکل «اول کش، بعد تازه‌سازی در پس‌زمینه».
- `offline.html` عمداً HTML ایستا با استایل درون‌خطی است — وقتی نشان داده می‌شود که هیچ چیزی از سرور نمی‌آید، پس نباید به `style.css` وابسته باشد.
- `sw.js` در ریشه‌ی اپ است چون دامنه‌ی کنترل یک سرویس‌ورکر نمی‌تواند بالاتر از پوشه‌ی خودش برود. مسیرهای داخلش نسبی حل می‌شوند، پس نصب در زیرپوشه هم کار می‌کند.
- **دام کش:** سایتِ nginx به همه‌ی `.js` کشِ «یک سال، immutable» می‌دهد. برای فایل‌های نسخه‌دار درست است ولی `sw.js` نسخه ندارد — با آن هدر، نسخه‌ی تازه ممکن است هرگز نرسد. `vps-setup.sh` استثنای `location = /sw.js` را دارد؛ روی نصب موجود با `sudo bash deploy/nginx-sw.sh --apply` اضافه می‌شود.
- **دارایی کش‌شده در پس‌زمینه دوباره گرفته نمی‌شود، و این عمدی است.** نسخه‌ی اول «stale-while-revalidate» بود و نتیجه‌اش **بدتر از نداشتنِ سرویس‌ورکر** شد: در هر ناوبری ۶ فایل (فونت، `style.css`، `app.js`، `chart.js`، آیکون — حدود ۲۲۰ کیلوبایت) دوباره از شبکه گرفته می‌شد با اینکه همه کش بودند، و روی دیتای موبایل با خودِ صفحه سرِ پهنای باند دعوا می‌کرد. اندازه‌گیری شد: **۶ درخواست در هر جابه‌جایی → ۰**. تازه‌سازی لازم نیست چون آدرس این فایل‌ها نسخه دارد (`?v=…`). قاعده ۶ در `test_api_contract.php` این را می‌سنجد.
- **کشِ دقیق خالی بودن نباید صفحه را بی‌جان کند.** بعد از هر deploy آدرس `app.js` عوض می‌شود (`?v=`)، پس کش خطا می‌خورد و فایل باید از شبکه بیاید. اگر همان یک درخواست روی اینترنت موبایل گیر می‌کرد، صفحه **کامل و خوش‌ظاهر** بالا می‌آمد — HTML و CSS رسیده بودند — ولی `app.js` اجرا نشده بود: صفحه اسکرول می‌شد و هیچ دکمه‌ای کار نمی‌کرد، بی‌هیچ نشانه‌ای از خرابی. بستن و باز کردن اپ درستش می‌کرد، چون بار دوم فایل می‌رسید. حالا سه لایه هست: کشِ دقیق → شبکه با مهلت `NET_TIMEOUT` → **نسخه‌ی قبلیِ همان فایل** (`cache.match(req, { ignoreSearch: true })`). کدِ یک نسخه عقب‌تر بی‌نهایت بهتر از صفحه‌ی بی‌جان است و دفعه‌ی بعد خودبه‌خود به‌روز می‌شود. با A/B روی سرورِ خوابیده سنجیده شد: پیش از اصلاح `504` با بدنه‌ی خالی، پس از آن `200` با ۱۵۵ کیلوبایتِ واقعی.
- **نسخه‌های قدیمی هر فایل پاک می‌شوند** (`dropOtherVersions`). بدون آن، هر deploy یک `?v=` تازه به کش اضافه می‌کرد و قبلی برای همیشه می‌ماند — کش روی گوشی کاربر بی‌پایان بزرگ می‌شد. در آزمون، بعد از دو deploy سه نسخه‌ی `app.js` کنار هم بودند؛ حالا همیشه یکی می‌ماند.
- **`<html class="js-loading">`** در `header.php` گذاشته می‌شود و اولین کارِ `app.js` برداشتنِ آن است. تا وقتی هست یعنی اسکریپت نرسیده؛ استایلش با تأخیر ۹۰۰ میلی‌ثانیه دکمه‌ها را کم‌رنگ می‌کند. بارگذاری عادی چند ده میلی‌ثانیه است و هرگز این حالت را نمی‌بیند. دلیلش همان باگ بالاست: سکوتِ کامل بدترین حالت بود — کاربر نمی‌فهمید صفحه هنوز آماده نیست یا خراب است.
- **`viewport-fit=cover` بدون آن، همه‌ی حاشیه‌های امن صفر می‌شوند.** `style.css` در پنج جا از `env(safe-area-inset-*)` استفاده می‌کند، ولی iOS این مقادیر را **فقط** وقتی می‌دهد که متای viewport شامل `viewport-fit=cover` باشد. نبودش هیچ خطایی نمی‌دهد — فقط نوار پایین زیر خطِ خانه می‌رود و عنوان صفحه زیر ناچ/Dynamic Island. مدت‌ها همین‌طور بود و در اسکرین‌شات کاربر دیده می‌شد. قاعده ۱۱ در `test_api_contract.php` این را می‌سنجد، و چون هر صفحه‌ای که سرآیند خودش را دارد (`login`, `setup`, `forgot-password`, `reset-password`) هم باید داشته باشد، همه‌شان سنجیده می‌شوند.
- **نوار بالا `env(safe-area-inset-top)` را در `padding` حساب می‌کند.** `apple-mobile-web-app-status-bar-style=black-translucent` عمدی است (نوار وضعیت هم‌رنگ اپ)، ولی همان باعث می‌شود وب‌ویو تا بالای صفحه کشیده شود؛ بدون این padding، عنوان زیر ناچ می‌رود.
- **⛔ داشتنِ `env(...)` در CSS کافی نیست — قاعده‌ای که *برنده می‌شود* باید داشته باشدش.** قاعده ۱۱ فقط وجودِ `viewport-fit=cover` را می‌سنجد، یعنی اینکه iOS اصلاً این مقادیر را بدهد؛ دو خرابیِ واقعی از زیرش رد شدند چون مقدارها داده می‌شدند و `env(...)` هم در فایل بود، ولی یک قاعده‌ی **بعدی** رویش می‌نوشت:
  - `.topbar` قاعده‌ی پایه `calc(13px + env(safe-area-inset-top))` داشت، ولی `@media (max-width:900px)` شورتهندِ `padding: 12px 14px` را بعدتر می‌نوشت و حاشیه‌ی بالا را کاملاً پاک می‌کرد.
  - `.page-content` فاصله‌ی کف ۹۶px ثابت داشت، در حالی که نوارِ شناور از کفِ پنجره `14 + env(bottom) + 62` بالا می‌ایستد — در مرورگر ۷۶px (کافی) ولی روی آیفون ۱۱۰px.
  - **هر دو در مرورگر بی‌عیب بودند** (چون `env(...)` آنجا صفر است) و فقط روی گوشیِ نصب‌شده دیده می‌شدند. قاعده ۱۳ در `test_api_contract.php` همین را می‌سنجد: آخرین قاعده‌ای که این فاصله را تعیین می‌کند باید `env(...)` داشته باشد.
- **مودال‌ها هم حاشیه‌ی امن می‌خواهند، نه فقط نوارها.** شیت‌ها از اول
  `env(safe-area-inset-bottom)` داشتند ولی `.modal-overlay` فقط `padding: 16px`
  بود. با `max-height: 88vh` و `align-items: center`، مودالِ بلند (مثل ویرایش چک
  بعد از اضافه شدنِ انتخابگر شخص) لبه‌ی بالایش روی **۵۱px** می‌نشست در حالی که
  ناچ تا **۵۹px** می‌آید — یعنی سرآیند و دکمه‌ی بستن زیرِ ناچ. اندازه‌گیری شد،
  حدس زده نشد.
  - `max-height` حالا `100%` است نه `88vh`: درصد روی جعبه‌ی محتوای همان لایه حل
    می‌شود که حاشیه‌ی امن و padding از آن کم شده، پس هیچ عددِ جادویی لازم نیست.
    `vh` روی iOS کلِ صفحه را می‌شمارد و نوار مرورگر را هم نادیده می‌گیرد.
  - `.modal-overlay` حالا `overflow-y: auto` هم دارد: با `align-items: center`
    تنها، بالای یک مودالِ بلندتر از لایه بیرون می‌افتد و **قابل اسکرول هم نیست**.
  - قاعده ۱۳ در `test_api_contract.php` حالا `.modal-overlay` را هم می‌سنجد.
- **نوار بالا روی موبایل هم چسبنده است، نه فقط دسکتاپ.** در مرورگر نوارِ خودِ سافاری بالای صفحه است و محتوا هرگز زیر ساعت نمی‌رود؛ در حالت نصب‌شده هیچ نواری نیست، پس با اولین اسکرول کارت‌ها می‌رفتند زیر ساعت و باتری و اپ اصلاً سرآیند نداشت.
- **آزمودنش هم واقعی است، نه حدسی:** کروم با `Emulation.setSafeAreaInsetsOverride` (از راه CDP) حاشیه‌ی امن را **واقعاً** تنظیم می‌کند، پس همان `calc()`ی که در `style.css` نوشته شده اجرا می‌شود. بازنویسیِ دستیِ قاعده‌ها با عددِ ثابت این را نمی‌سنجد — فقط طرحِ خودتان را دوباره می‌خواند. مقدارهای iPhone 15 Pro: بالا ۵۹px، پایین ۳۴px.
- با هر تغییرِ فهرست پیش‌کش، `VERSION` در `sw.js` بالا برود؛ نسخه‌های قدیمی هنگام `activate` پاک می‌شوند. (برای تغییرِ *منطق* بالا بردنش لازم نیست و حتی بد است: پاک شدنِ کش یعنی نسخه‌ی قبلیِ فایل‌ها — همان تورِ نجات — از دست می‌رود.)

## وام و قسط — برنامه‌ی مجازی، نه ردیفِ ذخیره‌شده

- رایج‌ترین بدهیِ خانوارِ ایرانی وامِ بانکی یا قرضِ صندوق با اقساطِ ماهانه است، ولی مدل فقط یک مبلغ و **یک** سررسید داشت. کاربر یا ۳۶ بدهیِ جدا می‌ساخت (دفترش غیرقابل خواندن می‌شد) یا کلِ وام را با سررسیدِ آخر ثبت می‌کرد — و آن‌وقت «آینده مالی» و «پول قابل خرج» قسطِ ماهِ بعد را **اصلاً نمی‌دیدند** و عددِ قابل خرج تا سه سال خوش‌بین می‌ماند.
- سه ستون روی `debts` (`migration_installments`): `installment_count`, `installment_every`, `first_installment_date`.
- **⛔ هیچ ردیفِ قسطی ذخیره نمی‌شود.** برنامه مثل تراکنش‌های دوره‌ای *مجازی* است و در `debtInstallments()` ساخته می‌شود. سه دلیل: ۳۶ ردیف به‌ازای هر وام جدول را سریع بزرگ می‌کند؛ پرداختِ واقعی از قبل در `debt_payments` هست و ردیفِ قسط نسخه‌ی دومی از همان حقیقت می‌شد؛ و عوض کردنِ تعداد اقساط یعنی بازسازیِ همه‌ی ردیف‌های پرداخت‌نشده.
- **باقیمانده‌ی تقسیم به قسطِ آخر می‌رود، نه پخش‌شده.** ۱۰ میلیون در ۳ قسط می‌شود ۳٬۳۳۳٬۳۳۳ + ۳٬۳۳۳٬۳۳۳ + ۳٬۳۳۳٬۳۳۴. وگرنه جمعِ اقساط با مبلغِ وام یکی نمی‌شد و کاربر یک بدهیِ چندریالیِ **ابدی** پیدا می‌کرد که هرگز تسویه نمی‌شود.
- **«کدام قسط پرداخت شده» از جمعِ پرداختی می‌آید، نه از تاریخ.** کاربر ممکن است دو قسط را یک‌جا بدهد یا زودتر بپردازد؛ پول پول است و ترتیبش اهمیتی ندارد.
- **⛔ بدهیِ قسطی نباید هم به‌صورت اقساط بیاید هم به‌صورت یک مبلغِ درشت.** `financialEvents()` شناسه‌های قسطی را از کوئریِ ساده کنار می‌گذارد؛ بدون آن، وامی که سررسیدِ آخرش داخلِ بازه است **دو بار** شمرده می‌شد. تست دقیقاً همان حالت را می‌سازد.
- ⚠ مبلغِ کمتر از تعدادِ اقساط، قسط‌های **صفر** می‌ساخت (۱ تومان در ۲ قسط) — هم در کارت «۰ تومان» نشان می‌داد هم تعهدِ صفر وارد آینده‌ی مالی می‌کرد. حالا در آن حالت بدهی مثل بدهیِ ساده رفتار می‌کند. **این را خودِ تست پیدا کرد، نه بازبینیِ چشمی.**
- سقفِ ۴۸۰ قسط (۴۰ سال ماهانه) در `api/add_debt.php` جلوی ساختنِ ده‌هزار رویداد را می‌گیرد.
- روی صفحه: یک **کلید** «قسطی است» که سه فیلد را باز می‌کند (اکثرِ ثبت‌ها ساده‌اند و نباید سه فیلدِ اضافه ببینند)، یک **خط** روی کارت («قسط ۳ از ۱۲ — بعدی ۱٬۰۰۰٬۰۰۰ در …»)، و مبلغِ قسط بعدی به‌عنوان پیش‌فرضِ فرمِ پرداخت.
- **تاریخچه‌ی پرداخت از قبل در `debt_payments` بود ولی هیچ‌جا رندر نمی‌شد**: کاربر می‌دید «۳ میلیون پرداخت شده» ولی نمی‌توانست بفهمد کِی و از کدام حساب. حالا در یک `<details>` جمع‌شونده زیر نوار پیشرفت است. ⚠ ستون `payment_date` است نه `paid_at`.
- تست در `tests/test_installments.php` است (۲۶ بررسی) و با سه جهش سنجیده شده: گم شدنِ باقیمانده، دوبار شمردنِ بدهیِ قسطی، و حساب کردنِ «پرداخت‌شده» از تاریخ به‌جای جمعِ پرداختی.

## ارزشِ روز و روندِ خالص دارایی

- **`assets.unit_price` بهای واحد هنگام ثبت است، نه ارزش.** صفحه‌ی دارایی همان را جمع می‌زد و «ارزش کل دارایی‌ها» می‌نامیدش. در اقتصادِ تورمی این حرفِ بی‌معنایی است: ارزشِ سکه‌ای که پارسال خریده شده هیچ ربطی به قیمتِ پارسال ندارد، و طلا و دلار ابزارِ اصلیِ پس‌اندازِ کاربر ایرانی‌اند.
- `asset_types.current_price` + `price_updated_at` (`migration_asset_prices`). ارزش با `COALESCE(at.current_price, a.unit_price)` حساب می‌شود — **اگر نرخ وارد نشده باشد همان بهای خرید می‌ماند، نه صفر** (رفتارِ قبلی حفظ می‌شود).
- **`assetSummaryRows()` تنها جای این محاسبه است** و هم صفحه هم عکسِ روزانه از آن می‌خوانند. `total_value` (به نرخِ روز) و `total_cost` (بهای تمام‌شده) جدا برمی‌گردند تا «سود/زیانِ محقق‌نشده» گفتنی باشد — بدون آن کاربر فقط یک عددِ بزرگ‌تر می‌بیند و نمی‌داند چقدرش رشدِ قیمت است و چقدرش پولی که گذاشته.
- **نرخ دستی است و از هیچ سرویسِ بیرونی نمی‌آید** — همان دلیلی که Chart.js را محلی کرد: اینترنتِ داخلی و تحریم. سرویسی که نصفِ روزها در دسترس نباشد، از نبودنش بدتر است.
- **ورودِ نرخ روی خودِ چیپِ نوعِ دارایی در «فهرست‌های من» است**، نه یک بخشِ تازه. آن صفحه از قبل شش فهرست دارد؛ کارتِ هفتمی به نام «نرخ‌ها» همان شلوغی‌ای بود که صفحه برای حذفش ساخته شد.
- **`net_worth_snapshots` اجزا را ذخیره می‌کند، نه جمع.** صفحه‌ی دارایی برای هر قلم یک کلیدِ روشن/خاموش دارد؛ با ذخیره‌ی جمع، روندِ «بدون طلا» یا «بدون حساب‌ها» ساختنی نبود.
  - نوشتن **بدون cron** است: اولین بازدیدِ روز ردیف را می‌سازد (همان الگوی `processRecurringTransactions`). `INSERT IGNORE` با کلیدِ `(user_id, snap_date)` یعنی بازدیدِ دوم چیزی اضافه **و بازنویسی** نمی‌کند — با `REPLACE` هر بارگذاری عددِ اول روز را خراب می‌کرد.
  - `recordNetWorthSnapshot()` عمداً هیچ استثنایی پرتاب نمی‌کند: کارِ جانبی است و نباید بارگذاری صفحه را بشکند.
  - ⚠ نامِ سطل‌ها باید دقیقاً با `kind` های `$portfolio` بخواند (`wallets`, `trade`, `cheques`, `debts`)؛ مفرد نوشتنشان همه را جزوِ «دارایی» می‌برد. جمعِ کل درست می‌ماند ولی اجزا غلط — و **بی‌صدا**، چون عددِ روی صفحه فرق نمی‌کند.
- سرآیندِ صفحه «خالص دارایی» شد نه «ارزش کل دارایی‌ها»، چون بدهی از آن کم می‌شود.
- اسپارک‌لاین عمداً بی‌محور و بی‌عدد است: اینجا شکلِ روند مهم است، مقدارِ دقیق را همان عددِ بزرگِ بالای صفحه می‌گوید. رنگش از روندِ کلی می‌آید (رشد `--in`، افت `--out`) و با **`onThemeChange`** دوباره رنگ می‌گیرد — نه `registerThemedChart` که آرایه‌ی رنگِ قاچ‌ها را می‌گیرد و برای این کار نیست.
- تست در `tests/test_insights.php` است و با سه جهش سنجیده شده: برگشت به بهای خرید، `REPLACE` به‌جای `INSERT IGNORE`، و برعکس شدنِ ترتیبِ تاریخچه.

## چرخه‌ی چک: چهار سرنوشت، نه دو تا

- چک تفاوت‌سازترین چیزِ این اپ برای بازار ایران است، ولی مدلش فقط `is_settled` صفر یا یک بود. در واقعیت چکِ دریافتی اغلب **خرج می‌شود** (به نفر سوم واگذار) و گاهی **برگشت می‌خورد**. کاربر ناچار بود چکِ خرج‌شده را «پاس شد» بزند — که پول را به اشتباه به حسابش اضافه می‌کرد — یا حذفش کند و تاریخچه را از دست بدهد.
- `cheques.status` چهار حالت دارد: `pending` / `cleared` / `bounced` / `endorsed` (`migration_cheque_status`).
- **⛔ `is_settled` عمداً نماند بلکه مقدارِ مشتق شد: `is_settled = 1 ⇔ status = 'cleared'`.** حذفش یعنی بازنویسیِ یک‌جای `walletBalances()` و همه‌ی تست‌ها — و آن‌ها منطقِ پول‌اند نه نمایش. تست این هم‌ارزی را جدا می‌سنجد.
- **⛔ `chequeActiveSql()` تنها جایی است که «چکِ در جریان» تعریف می‌شود** (مثل `categoryScopeSql()`). هر کوئری‌ای که چکِ باز می‌خواهد باید از همان رد شود: `financialEvents()`, `my-assets.php`, `cheques.php`, `dashboard.php`, `includes/sidebar.php`.
  - دلیلش این است که `is_settled = 0` به‌تنهایی **غلط** است: چکِ برگشتی و خرج‌شده هم پاس نشده‌اند ولی در جریان نیستند. بدترین حالتش چکِ برگشتی است — چون برایش یک ردیف `debts` ساخته می‌شود، اگر همچنان «در جریان» شمرده شود همان پول **دو بار** در دارایی و در آینده‌ی مالی می‌آید. همان دوبار شمردنی که درباره‌ی چکِ پاس‌شده هشدار داده شده.
  - alias می‌گیرد (`chequeActiveSql('ch')`) چون `cheques.php` جدول را alias می‌کند.
- **«فعال» یعنی در جریان، «بایگانی» یعنی هر چیزِ دیگر** — نه «پاس‌شده». چکِ برگشتی و خرج‌شده کارشان تمام است و در فهرستِ فعال ماندنشان هم صفحه را شلوغ می‌کرد هم جمعِ بالای صفحه را غلط.
- **چکِ دریافتیِ برگشت‌خورده خودکار به طلب تبدیل می‌شود**، با `debts.cheque_id` به‌عنوان پیوند. بدون آن پیوند، برگرداندنِ وضعیتِ چک یک طلبِ یتیم به جا می‌گذاشت. برگرداندن، طلب را پس می‌گیرد — **مگر کاربر رویش پرداختی ثبت کرده باشد**، که آن‌وقت کارِ خودِ کاربر از بین می‌رفت.
- **هیچ کنترلِ تازه‌ای به کارت اضافه نشد:** خودِ نشانِ وضعیت دکمه شد. صفحه‌ای که ده‌ها چک دارد با یک منوی جدا کنارِ هر کارت شلوغ می‌شد؛ حالا همان نشانی که کاربر برای *خواندن* وضعیت نگاهش می‌کند، جای *عوض کردنش* هم هست. مودالِ «انتخاب حساب» که از قبل بود، حالا انتخابگرِ سرنوشت هم هست: حسابِ واریز فقط برای «پاس شد»، نامِ گیرنده فقط برای «خرج شد».
- تست در `tests/test_cheque_status.php` است و با دو جهش سنجیده شده: برگرداندنِ `chequeActiveSql()` به `is_settled = 0` تنها (که دقیقاً باگِ دوبار شمردن است) و نادیده گرفتنِ alias.

## ثبت از پیامک بانک — و پیشنهادِ عنوان‌های قبلی

- **پارسر در مرورگر است و هیچ چیزی به سرور نمی‌رود.** PWA و TWA اصلاً به SMS
  دسترسی ندارند، پس کاربر پیامک را می‌چسباند؛ همین محدودیت به سودِ ماست، چون
  متنِ خامِ پیامکِ بانک روی سیم نمی‌رود. **اگر روزی وسوسه شدید این را به یک
  اندپوینت بدهید، دلیلِ نبودنش همین است.**
- `window.parseBankSms()` در `assets/js/app.js` است، **بیرون از
  `DOMContentLoaded` و کاملاً خالص** — هیچ DOM ای نمی‌خواند و نمی‌نویسد. علتش
  آزمودنی بودن است: `tests/test_sms_parse.php` همان فایلِ واقعی را در node
  بارگذاری می‌کند (نه یک کپی)، دقیقاً مثل `test_jalali_parity.php`.
- **⛔ خرابیِ پارسر بی‌صداست.** اگر عددِ «مانده» را به‌جای مبلغ بردارد یا ریال را
  تومان بخواند، هیچ خطایی نمی‌دهد؛ فرم پر می‌شود، کاربر ثبت می‌کند، و ماه بعد
  گزارش غلط است. چهار محافظ دارد و **هر چهار در تست با آزمون جهش سنجیده شده‌اند
  (۱۰ جهش، همه گرفته شد):**
  1. **بدون کلمه‌ی جهت (برداشت/واریز) اصلاً تراکنش نیست.** پیامکِ «موجودی کارت
     شما … ۱۲٬۰۰۰٬۰۰۰ ریال» هم یک عددِ درشت دارد؛ بدون این شرط همان مانده
     به‌عنوان مبلغ پر می‌شد.
  2. **هیچ عددی «به‌طور پیش‌فرض» مبلغ نیست:** باید دست‌کم یکی از سه نشانه را
     داشته باشد — جداکننده‌ی سه‌رقمی، واحدِ پول کنارش، یا نزدیکی به کلمه‌ی
     جهت/«مبلغ». شماره‌ی حسابِ ده‌رقمی و کدِ پیگیری هیچ‌کدام را ندارند.
  3. **شماره‌ی کارت با فاصله پاک می‌شود** (`blankCards`، طولِ متن دست‌نخورده
     می‌ماند تا اندیسِ تاریخ معتبر بماند). بدون آن، `6037` در
     «واریز به 6037-9975-****-1234 مبلغ 500000» مبلغ می‌شد: هم ≥ ۱۰۰۰ است هم
     بلافاصله بعد از کلمه‌ی جهت.
  4. **«مانده / موجودی / سقف / اعتبار» عددِ کنارشان رد می‌شود** — و این جدا از
     رتبه‌بندی لازم است: در «واریز حقوق / مانده حساب: ۵۰٬۰۰۰٬۰۰۰ / مبلغ واریزی
     ۴۵٬۰۰۰٬۰۰۰» مانده به کلمه‌ی جهت **نزدیک‌تر** است.
- **⛔ نبودِ واحد یعنی ریال، نه «نمی‌دانم».** فقط «تومان» و «تومن»ِ صریح از
  تقسیم بر ۱۰ معاف‌اند؛ در هر حالتِ دیگر عدد تقسیم می‌شود و `currency` مقدارِ
  `rial_assumed` می‌گیرد تا پیامِ روی صفحه (زرد، نه سبز) بگوید فرض شده.
  - **نسخه‌ی اول عمداً حدس نمی‌زد و عدد را دست‌نخورده می‌گذاشت.** استدلالش درست
    بود (حدسِ غلط یعنی ده‌برابر شدنِ مبلغ) ولی **فرضش غلط بود**: پیامکِ بانکِ
    ایرانی عملاً همیشه ریال است، پس «دست نزدن» هم خودش یک حدس بود — همان حدسِ
    اشتباه، فقط ساکت‌تر. کاربر هر بار باید یک صفر دستی کم می‌کرد.
  - ⚠ بلوبانک **تومان** می‌نویسد و صریح هم می‌نویسد، پس از این قاعده معاف است.
- **⛔ ادبیاتِ هر بانک فرق می‌کند و فهرستِ رسمی کافی نیست.** بلوبانک به‌جای
  «برداشت» می‌نویسد **«پرید»** و به‌جای «واریز» **«نشست»**. پیش از این پارسر
  هیچ کلمه‌ی جهتی پیدا نمی‌کرد و می‌گفت «تراکنش نیست» — یعنی قابلیت برای
  کاربرِ آن بانک **اصلاً کار نمی‌کرد**، بی‌هیچ خطایی.
- **⛔ مبلغ با «فاصله» از کلمه‌ی جهت انتخاب می‌شود، نه با «سمت».** دو راهِ
  ساده‌تر آزموده و رد شدند، و هر دو را **خودِ تست** گرفت نه بازبینیِ چشمی:
  - *فقط بعد از کلمه‌ی جهت* (نسخه‌ی اول): بلوبانک مبلغ را **قبل** می‌نویسد.
  - *قبل و بعد هم‌ارزش*: در «کارمزد ۵,۰۰۰ ریال، واریز ۴۵,۰۰۰,۰۰۰ ریال» کارمزد
    برنده می‌شد (زودتر در متن).
  - *«بعد» همیشه مقدم بر «قبل»*: در «۲۵۰,۰۰۰ تومان پرید. کارمزد ۵,۰۰۰ تومان»
    کارمزد برنده می‌شد.
  فاصله هر سه را درست جواب می‌دهد، چون در همه‌ی این متن‌ها مبلغِ اصلی به
  کلمه‌ی جهت **چسبیده** و کارمزد یک عبارت دورتر است. دو موردِ آینه‌ای در تست
  هست تا هیچ‌کدام از آن سه قاعده دوباره جا نیفتد.
- **تاریخ از همان `jalali-datepicker.js` رد می‌شود**، نه یک تبدیلِ تازه — وگرنه
  می‌شد پیاده‌سازیِ سومِ تقویم، بیرون از پوششِ `test_jalali_parity`. تاریخِ
  ناموجود (۳۱ آبان، ۱۴۰۳/۱۳/۴۵) با رفت‌وبرگشت رد می‌شود، نه اینکه حدس زده شود.
- **⛔ خواندنِ خودکارِ خودِ پیامک ممکن نیست** و این محدودیتِ سکو است، نه انتخاب:
  نه PWA و نه TWA به SMS دسترسی دارند و هیچ API ای هم برایش نیست (Web OTP فقط
  کدِ ورودِ همان دامنه را می‌دهد، نه پیامکِ بانک). نزدیک‌ترین چیز، دکمه‌ی
  «چسباندن و خواندن» است که خودش کلیپ‌بورد را می‌خواند و بلافاصله پارس می‌کند —
  کاربر پیامک را کپی می‌کند و یک تپ.
  - ⚠ دکمه فقط وقتی رندر می‌شود که `navigator.clipboard.readText` باشد **و**
    `isSecureContext` برقرار باشد؛ روی http دکمه می‌آمد و با زدنش هیچ اتفاقی
    نمی‌افتاد. دکمه‌ی بی‌کار از نبودنش بدتر است.
- **حساب فقط با تطبیقِ چهار رقمِ آخرِ کارت عوض می‌شود.** `walletCardTails()`
  فقط همان چهار رقم را به صفحه می‌دهد، نه شماره‌ی کامل — شماره‌ی کارت حساس است
  و در فهرست حساب‌ها هم نشان داده نمی‌شود.
- **پیشنهادِ عنوان‌های قبلی** (`recentTransactionTitles()` + `recentTitlesDatalist()`)
  هیچ جدول و اندپوینتِ تازه‌ای ندارد و از خودِ `transactions` می‌خواند.
  `JOIN` روی `MAX(id)` است نه `GROUP BY` خام، وگرنه دسته و حساب و مبلغ از
  **ردیف‌های مختلف** می‌آمدند. `<datalist>` است نه `<select>` — همان قاعده‌ی
  `peopleDatalist()`: ورودیِ آزاد باید کار کند.
  - ⚠ دسته **بعد از** `setType()` پر می‌شود: عوض کردنِ نوع فهرستِ دسته‌ها را از
    نو می‌سازد و انتخابِ قبلی را می‌برد.
  - مبلغ فقط وقتی پر می‌شود که کاربر هنوز چیزی ننوشته باشد — نوشته‌ی خودش
    همیشه برنده است.

## ⛔ خواندنِ تراکنش‌ها — `includes/tx_query.php`

- **⛔ چرا فایلِ جدا:** `includes/transactions.php` سمتِ **نوشتن** است و
  وب و API هر دو از آن رد می‌شوند، دقیقاً به این دلیل که دو نسخه از
  منطقِ پول دیر یا زود از هم دور می‌افتند. همین برای سمتِ **خواندن** هم
  درست است: `transactions.php` صافی‌ها را می‌سازد و
  `api/export_transactions.php` باید **دقیقاً همان** ردیف‌ها را بدهد.
  با دو نسخه، کاربر چیزی را می‌دید و چیزِ دیگری را دانلود می‌کرد — و
  **کسی فایلِ CSV را با صفحه مقایسه نمی‌کند**، پس آن خرابی بی‌صداست.
- `buildTransactionFilter()` تنها جای صافی‌هاست (دوره، نوع، جست‌وجو،
  حساب) و **مالکیتِ حساب را هم خودش می‌سنجد**: با سنجشِ جداگانه در هر
  مصرف‌کننده، اولین مسیری که یادش برود شناسه‌ی حسابِ کاربرِ دیگری را
  می‌پذیرد. فهرستِ حساب‌ها از بیرون می‌آید تا کوئریِ تازه‌ای اضافه نشود.

### جست‌وجو: چهار جا، نه فقط عنوان

- پیش از این `t.title LIKE` و بس. یعنی کاربری که دنبالِ یادداشتِ «بابت
  تعمیر پنکه» یا مبلغِ دقیقِ یک تراکنش می‌گشت **هیچ نتیجه‌ای نمی‌گرفت** و
  نتیجه می‌گرفت که ثبتش نکرده. حالا عنوان، یادداشت، نامِ دسته‌بندی، و
  **مبلغِ دقیق**.
- **⛔ روی مبلغ تطابقِ دقیق است، نه `LIKE`.** با `LIKE`، تایپِ «۵۰۰» هم
  ۵۰۰ را می‌آورد هم ۵٬۰۰۰ و ۱٬۵۰۰ و ۲۵۰٬۵۰۰ — نتیجه‌ای که کاربر
  نمی‌تواند پیش‌بینی کند و به‌مرور از جست‌وجو ناامید می‌شود. تطابقِ دقیق
  قابلِ توضیح است: «همان عددی که تایپ کردی».
- `txSearchAmount()` از `toLatinDigits()` رد می‌شود (کیبوردِ فارسی
  «۷۷۷٬۰۰۰» می‌دهد و آن با `777000` یکی نیست) و **فقط** رشته‌ای را عدد
  می‌شمارد که سراسر رقم و جداکننده باشد — «قبض ۵۰۰» و «۱۴۰۴/۰۵» عدد
  نیستند.
- **⚠ «طرفِ حساب» اینجا نیست و نمی‌تواند باشد:** جدولِ `transactions`
  اصلاً ستونِ `counterparty_name` ندارد؛ آن مالِ `cheques`/`debts`/
  `trades` است و گردشِ هر شخص جای خودش را دارد (`person.php?name=…`).
- **⛔ `%` و `_` فرار داده می‌شوند** (`ESCAPE '!'`). بدونِ آن، تایپِ `%`
  کلِ جدول را برمی‌گرداند و `a_b` هر سه‌کاراکتری را — نه یک رخنه (مقدار
  bind است) ولی نتیجه‌ای که کاربر نمی‌فهمد از کجا آمده. کاراکترِ فرار
  عمداً `!` است نه `\`، چون بک‌اسلش در رشته‌ی SQL یک لایه‌ی تفسیرِ دیگر
  دارد.
- **⛔ سه پارامترِ جدا با یک مقدار، نه یک `:q`ِ تکراری — و این یک باگِ
  واقعی بود.** `EMULATE_PREPARES = false` است (قاعده‌ی `db.php`) و در
  آماده‌سازیِ بومیِ MySQL یک نامِ تکراری پشتیبانی نمی‌شود:
  `SQLSTATE[HY093] Invalid parameter number`. یعنی صفحه‌ی تراکنش‌ها با
  **هر** جست‌وجویی ۵۰۰ می‌داد. `php -l` نمی‌گیردش؛ **اولین اجرای تست
  گرفتش**.
- **⚠ JOIN دسته‌بندی در کوئریِ شمارش فقط وقتی می‌آید که واقعاً
  جست‌وجویی در کار باشد.** بازدیدِ عادی نباید هزینه‌ی قابلیتی را بدهد که
  از آن استفاده نمی‌کند — گروهِ «فهرستِ کوتاه کلِ تاریخچه را نمی‌خواند»
  در `test_query_budget` سقفِ ردیفِ خوانده‌شده دارد.

### خروجیِ CSV — `api/export_transactions.php`

- **⛔ POST با CSRF، نه لینکِ GET** — همان استدلالِ `export_data.php`:
  لینکِ GET را می‌شود در `<img src>` جای دیگری جاسازی کرد و مرورگرِ
  کاربرِ واردشده خودش صدایش می‌زند. خروجی اینجا می‌تواند کلِ تاریخچه‌ی
  مالیِ او باشد.
- **⛔ BOM اجباری است.** بدونِ `\xEF\xBB\xBF` اکسلِ ویندوز فایلِ UTF-8 را
  با کدپیجِ سیستم می‌خواند و **کلِ متنِ فارسی درهم** درمی‌آید — فایل باز
  می‌شود، خطایی نیست، و فقط بی‌فایده است.
- **⛔ مبلغ عددِ خامِ لاتین است، نه `formatMoney()`.** با جداکننده و
  ارقامِ فارسی اکسل ستون را **متن** می‌گیرد و کاربر نمی‌تواند جمعش
  بزند — یعنی تنها دلیلِ وجودِ این فایل از بین می‌رود.
- تاریخ **هر دو** می‌آید: شمسی برای خواندن، میلادی برای مرتب‌سازی و
  فرمولِ اکسل. هر دو با ارقامِ لاتین.
- ردیف‌ها یکی‌یکی `fetch` و نوشته می‌شوند، نه `fetchAll()`: با تاریخچه‌ی
  ده‌هزارتایی کلِ نتیجه در حافظه‌ی PHP نمی‌نشیند.
- گزیپی که `db.php` روشن می‌کند پیش از فرستادن بسته می‌شود، وگرنه فایل
  دوبار فشرده است و باز نمی‌شود (همان چیزی که یک بار سرِ خروجیِ JSON
  افتاد).
- دکمه **فقط وقتی رندر می‌شود که نتیجه‌ای باشد** (`$totalRows > 0`) —
  دکمه‌ای که فایلِ خالی بدهد همان «دکمه‌ی بی‌کار».
- ⚠ **حدِ این کار، صادقانه:** جداکننده ویرگول است. با BOM، اکسل
  رمزگذاری را درست می‌خواند ولی *جداکنندهٔ فهرست* را از لوکالِ ویندوز
  می‌گیرد؛ روی نصبی که آن `;` باشد ستون‌ها در یک خانه می‌افتند.
  LibreOffice و Google Sheets همیشه درست باز می‌کنند. اگر گزارش شد، یک
  تنظیمِ جداکننده لازم است — نه یک حدسِ تازه.
- تست در `tests/test_tx_search.php` است (۴۷ بررسی) و با **دوازده جهش**
  سنجیده شده، همه گرفته شد.
  - **⚠ نسخه‌ی اولِ بخشِ CSV منطق را در خودِ تست کپی کرده بود** و
    خروجیِ کپی را می‌سنجید — یعنی آزمونی که خودش را می‌سنجد و با خرابیِ
    اندپوینت هم سبز می‌ماند. حالا از HTTP و نشستِ واقعی رد می‌شود.
  - ⚠ و جهشِ «فقط عنوان گشته شود» اگر فقط SQL را بردارد با خطای bind
    گرفته می‌شود، نه رفتاری. جهشِ **درست** پارامترش را هم برمی‌دارد —
    آن‌وقت بررسیِ «یادداشت هم گشته می‌شود» قرمز می‌شود.

## یادآوری سررسید — تنها جایی که اپ بی‌آنکه بازش کنید حرف می‌زند

- **دلیل وجودش:** هسته‌ی این برنامه سررسید است (چک، طلب و بدهی، تراکنش دوره‌ای)، ولی تا پیش از این اپ فقط وقتی خبر می‌داد که کاربر خودش بازش کند — یعنی دقیقاً همان لحظه‌ای که دیگر یادآوری لازم ندارد. چکِ برگشتی عارضه‌ی حقوقی دارد و بدهیِ دیرکرد رابطه‌ی خانوادگی را خراب می‌کند.
- `deploy/reminders.php` (نگهبان `PHP_SAPI`) هر صبح با cron اجرا می‌شود، برای هر کاربرِ ایمیل‌دار از `financialEvents()` خلاصه می‌سازد و با `Mailer` می‌فرستد. بدون آرگومان فقط گزارش می‌دهد؛ `--send` واقعاً می‌فرستد؛ `--user ali` فقط یک نفر (برای آزمودن)؛ `--install-cron` زمان‌بندی می‌کند.
- **⛔ پیش‌فرض عمداً روشن است.** این تنها تنظیمی است که ارزشش در نبودنش دیده نمی‌شود: کاربری که نمی‌داند وجود دارد هرگز روشنش نمی‌کند، و بعد چکش برگشت می‌خورد. ولی فقط برای کسی می‌رود که هم ایمیل ثبت کرده باشد هم سررسیدی داشته باشد، پس هیچ‌کس ایمیلِ بی‌ربط نمی‌گیرد. نبودِ ردیف در `notification_prefs` یعنی «هنوز تصمیم نگرفته» — به همین دلیل جدولِ جداست نه ستون روی `users`، وگرنه نمی‌شد «انتخاب نکرده» را از «خاموش گذاشته» تشخیص داد.
- **`REMINDER_DAYS` تنها مرجعِ بازه‌هاست** (مثل `Auth::SESSION_WINDOWS`). فهرستِ دوم نسازید، وگرنه گزینه‌ای که کاربر می‌بیند هنگام ذخیره بی‌صدا به پیش‌فرض برمی‌گردد.
- **`last_sent_on` نگهبانِ «یک ایمیل در روز» است.** بدون آن، یک اشتباهِ زمان‌بندی (cron دو بار، یا اجرای دستی) به هر کاربر چند ایمیل می‌فرستاد — سریع‌ترین راهِ اینکه کاربر یادآوری را خاموش کند یا آدرس را اسپم علامت بزند.
- **متنِ ایمیل در `reminderEmailBody()` است، نه داخل اسکریپت.** آن اسکریپت با cron اجرا می‌شود و خروجی‌اش را کسی نمی‌بیند، پس اگر متن خراب شود بی‌صدا خراب می‌ماند؛ اینجا بدون فرستادنِ هیچ ایمیلی آزمودنی است. **نسخه‌ی متنی اختیاری نیست:** بعضی کلاینت‌ها HTML نشان نمی‌دهند و فیلترهای اسپم به ایمیلِ فقط-HTML سخت‌گیرترند.
- لینک از `appBaseUrl()` ساخته می‌شود نه `HTTP_HOST` — همان قاعده‌ی لینکِ بازیابیِ رمز.
- **کلید فقط وقتی در پروفایل دیده می‌شود که جدولش آمده باشد**، و اگر `MAIL_METHOD` تنظیم نشده باشد صریح می‌گوید ایمیلی نمی‌رود. کلیدی که کار نمی‌کند بدتر از نبودنش است.
- **Web Push عمداً فعلاً نیست:** سرویسِ پوشِ کروم از ایران پایدار نیست و روی iOS فقط در حالت نصب‌شده کار می‌کند. ایمیل هیچ وابستگیِ بیرونی ندارد.
- تست در `tests/test_reminders.php` است (۳۰ بررسی) و با سه جهش سنجیده شده: خاموش کردنِ پیش‌فرض، خالی کردنِ نسخه‌ی متنی، و برداشتنِ نگهبانِ یک‌ایمیل‌در‌روز.

## بکاپ و بازیابی

- `deploy/backup.sh` می‌گیرد، `deploy/restore.sh` **می‌سنجد و برمی‌گرداند**. تا پیش از این فقط اولی بود — و بکاپی که یک بار بازیابی نشده باشد بکاپ نیست، فرضیه است.
- **سنجش با `--admin` اجرا می‌شود، نه با کاربر اپ.** سنجش یک دیتابیس موقت می‌سازد و `hesab_user` طبق قانون جداسازی فقط `GRANT ... ON hesab_db.*` دارد — یعنی نمی‌تواند، و **این عمدی و درست است**. راه درست `sudo bash deploy/restore.sh --admin` است که از سوکت یونیکس با کاربر مدیر وصل می‌شود و دسترسی اپ را دست‌نخورده می‌گذارد. ⚠ وسوسه‌ی اشتباه: «یک GRANT به hesab_user بدهیم» — همان کاری است که قانون منع می‌کند؛ خودِ اسکریپت هم صریحاً هشدار می‌دهد.
- **جدولِ «گم‌شده» معمولاً خرابی نیست.** اگر بکاپ پیش از آخرین migration گرفته شده باشد، جدول‌های تازه در آن نیستند و سنجش رد می‌شود — درست، ولی گمراه‌کننده. پیام خطا همین را می‌گوید و راهنمایی می‌کند اول یک بکاپ تازه بگیرید.
- **حالت پیش‌فرضِ `restore.sh` بی‌خطر است:** روی یک دیتابیس موقت بازیابی می‌کند، فهرست جدول‌ها و تعداد ردیف‌ها را با دیتابیس زنده می‌سنجد، و آخرش موقت را پاک می‌کند (با `trap`، پس حتی اگر وسط کار بمیرد هم جا نمی‌ماند). به داده‌ی زنده دست نمی‌زند.
- بازیابی واقعی فقط با `--into NAME`. اگر مقصد همان دیتابیس زنده باشد، اول یک بکاپ ایمنی می‌گیرد و بعد تأیید تایپی می‌خواهد؛ اگر بکاپ ایمنی نگرفت، اصلاً جلو نمی‌رود.
- مقایسه‌ی فهرست جدول‌ها با `LC_ALL=C sort` مرتب می‌شود، نه با `ORDER BY` دیتابیس. ترتیبِ collation با ترتیبِ بایتیِ `comm` یکی نیست (`wallet_kinds` در برابر `wallets`) و `comm` با ورودی نامرتب فقط هشدار می‌دهد ولی *ادامه می‌دهد* و نتیجه‌اش غلط است — یعنی می‌توانست بکاپِ ناقص را سالم اعلام کند. یک بار در همین آزمون دقیقاً همین شد.
- آزموده شده روی MariaDB: رفت‌وبرگشت کامل، `CHECKSUM TABLE EXTENDED` هر ۲۴ جدول یکسان. مسیرهای شکست هم آزموده شده‌اند: فایل خرابِ gzip، بکاپ خالی، و بکاپی که یک جدول کم دارد — هر سه با کد خروج ۱ رد می‌شوند.

### ⛔ بکاپِ خارج از سرور — `deploy/backup-offsite.sh`

- **مسئله‌ی واقعی:** `backup.sh` فقط در `/opt/hesab/backups` می‌نوشت، یعنی
  روی **همان دیسکی** که دیتابیس رویش است. خرابیِ دیسک، پاک شدنِ VPS، یا
  نفوذ، دفتر و هر ۱۴ بکاپش را **با هم** می‌برد. `CLAUDE.md` از قبل
  نوشته بود «تنها خرابیِ برگشت‌ناپذیر»، ولی آن قاعده فقط در سطحِ
  **کاربر** اجرا می‌شد (`Notify::generateBackupHint()`)، نه در سطحِ
  **نصب**.
- **⛔ رمز پیش از رفتن، و این ترتیب کلِ نکته است.** دامپ نام، ایمیل،
  شماره‌ی موبایل، هشِ رمز و کلِ تراکنش‌های همه‌ی کاربران را دارد. با
  فایلِ خام **هیچ** مقصدی بی‌خطر نیست؛ با فایلِ رمزشده مقصد **بی‌اهمیت**
  می‌شود. پس اول رمزنگاری، بعد انتخابِ مقصد — نه برعکس.
- **مقصد تلگرام است چون از این سرور اثباتاً می‌رسد.** دو ربات دیگر روی
  همین VPS کار می‌کنند و `api.telegram.org` جواب می‌دهد (سنجیده شد:
  `401` یعنی رسید). گوگل و بک‌بلیز و R2 روی کاغذ بهترند و از این ماشین
  یا نمی‌رسند یا سرِ ثبت‌نام و OAuth و کارتِ بانکی گیر می‌کنند —
  **مقصدی که راه نیفتد، از نبودنش بدتر نیست ولی همان‌قدر بی‌فایده است.**
- **⛔ سه چیز هرگز فرستاده نمی‌شوند و هیچ مسیری برایشان ساخته نشد:**
  `APP_ENCRYPTION_KEY`، عبارتِ رمزِ بکاپ، و فایلِ خام. اگر کلید کنارِ
  بکاپ برود، رمزنگاریِ شماره کارت و شبا بی‌معنا می‌شود. `--check` و
  پایانِ `--send` فقط **یادآوری** می‌کنند که کلید را دستی جایی نگه
  دارید.
- **⚠ حدِ این محافظت، صادقانه:** عبارتِ رمز در `offsite.conf` روی همین
  سرور است، چون رمزگذاریِ خودکار ناچار است کلید را روی همان ماشین داشته
  باشد. پس در برابرِ «سرور از دست رفت» کامل است و در برابرِ «کسی root
  شد» نیست. این نوشته می‌ماند تا کسی ادعای بیشتری نکند. (راهِ کاملِ
  بعدی رمزنگاریِ نامتقارن است — کلیدِ خصوصی اصلاً روی سرور نباشد — ولی
  آن یعنی کاربر باید جای دیگری کلید بسازد، و قابلیتی که راه نیفتد از
  نبودنش بدتر است.)
- **⛔ رفت‌وبرگشت پیش از ارسال سنجیده می‌شود، نه بعدش.** فایلِ رمزشده
  همان‌جا با همان عبارت باز می‌شود و با اصل `cmp` می‌شود؛ اگر یکی نبود
  **اصلاً فرستاده نمی‌شود**. فایلی که باز نمی‌شود نباید در مقصد بنشیند و
  خودش را «بکاپ» جا بزند.
- **⛔ و بعد از ارسال هم فایل پس گرفته و `sha256` اش مقایسه می‌شود.**
  «فرستاده شد» با «سالم رسید» یکی نیست — همان درسی که قاعده‌ی nginx در
  `api/v1` داد. ⚠ سقفِ `getFile` برای ربات ۲۰ مگابایت است و سقفِ
  `sendDocument` پنجاه؛ در فاصله‌ی این دو، اسکریپت صریح می‌گوید
  **سنجش انجام نشد** و همان را در `.offsite-state` هم می‌نویسد. ادعای
  «سنجیده‌شده» برای چیزی که سنجیده نشده، بدترین شکلِ دروغ است.
- **⛔ `-4` اجباری است و یک خرابیِ واقعی روی همین سرور را بست.**
  `api.telegram.org` رکوردِ AAAA دارد و curl **اول** سراغِ IPv6 می‌رود؛
  مسیرِ IPv6 این VPS مرده است، پس نتیجه
  `curl: (35) Recv failure: Connection reset by peer` بود. با `-4` همان
  درخواست `401` گرفت (یعنی رسید و تلگرام جوابش را داد). اندازه‌گیری شد،
  حدس زده نشد. `IPFLAG=` خالی، رفتارِ پیش‌فرضِ curl را برمی‌گرداند.
  - **⛔ و پیامِ خطا بدتر از خودِ خرابی بود:** اسکریپت آن را «توکن
    پذیرفته نشد» می‌نامید، در حالی که **هیچ‌کس توکن را ندیده بود**.
    مالکِ نصب دنبالِ توکنِ تازه می‌گشت و ایراد در شبکه بود — همان
    الگوی «کلید کنسول معتبر نیست»ِ ملی‌پیامک. حالا کدِ خروجیِ curl نگه
    داشته می‌شود (`TG_RC`) و «نرسید» از «رد شد» جداست، چون دو راهِ
    حلِ کاملاً متفاوت دارند.
- **تلاشِ دوباره فقط برای شکستِ اتصال است، نه برای پاسخِ منفیِ تلگرام**
  (`NET_TRIES`، پیش‌فرض ۳، با مکثِ ۲ و ۶ ثانیه). قطعِ گاه‌به‌گاه روی این
  مسیر واقعی است و یک بکاپِ نرفته تا فردا شب جبران نمی‌شود. ⚠ هزینه‌اش
  نوشته می‌ماند: اگر قطع **بعد از** تحویلِ کامل رخ دهد همان فایل دو بار
  در کانال می‌نشیند — بی‌ضرر، برخلافِ بکاپی که اصلاً نرفته.
- **⚠ فیلدِ توکن می‌گوید «نوشته نمی‌شود».** `read -s` از بیرون دقیقاً
  شبیهِ ترمینالِ هنگ‌کرده است: کاربر می‌چسباند، چیزی نمی‌بیند، و نتیجه
  می‌گیرد چسباندن کار نکرده. روی سرورِ واقعی همین شد.
- **توکن روی خطِ فرمان نمی‌رود.** آدرسِ متد (که توکن داخلش است) در یک
  فایلِ `600` می‌نشیند و با `curl --config` خوانده می‌شود؛ `trap` پاکش
  می‌کند. با `-H` یا آدرسِ خام، هر کاربرِ دیگری روی این VPS آن را در
  `ps aux` می‌دید — همان چیزی که `perf-report.sh` را از `mysql -pرمز` و
  `apk-publish.sh` را از هدرِ Authorization دور کرد.
- **⛔ ربات فقط «ارسال پیام» بگیرد، نه «حذف پیام».** اگر توکن از سرور لو
  برود، مهاجم نباید بتواند بکاپ‌های قبلی را هم پاک کند — همان اصلِ
  «کلیدِ فقط-نوشتنی». `--setup` این را صریح می‌گوید.
- **⛔ `--setup` عبارتِ رمز را می‌سازد و بعد آن را از شما پس می‌خواهد.**
  بدونِ تایپِ دوباره، رایج‌ترین حالت این است که کاربر Enter بزند و
  عبارت را هیچ‌جا نگه ندارد — یعنی روزِ حادثه، همه‌ی بکاپ‌ها هست و
  هیچ‌کدام باز نمی‌شود. سه بار که نشود، **هیچ چیزی ذخیره نمی‌شود**.
- **⛔ فایلِ خراب یا مشکوکانه کوچک فرستاده نمی‌شود** (`gzip -t` و کفِ
  ۱ کیلوبایت): بکاپی که باز نشود از نبودنش بدتر است، چون خیالِ آدم را
  راحت می‌کند. ولی **بکاپِ کهنه فرستاده می‌شود** و فقط هشدار می‌گیرد —
  بکاپِ کهنه در بیرون، از هیچ بکاپی در بیرون بهتر است.
- **`.offsite-state` نگهبانِ «یک بار در روز» است**، مثل `last_sent_on`
  در یادآوری‌ها: اجرای دوباره‌ی cron همان فایل را دوباره نمی‌فرستد
  (`--force` برای وقتی است که واقعاً بخواهید). `--check` هم از روی همان
  می‌گوید «آخرین بکاپ هنوز فرستاده نشده» — یعنی خرابیِ زمان‌بندی
  **دیده** می‌شود.
- **⚠ تجزیه‌ی پاسخ به نبودِ فاصله بعد از `:` بند نیست.** تلگرام امروز
  فشرده جواب می‌دهد، ولی با `"ok":true`ِ خشک، یک تغییرِ قالبِ بی‌ضرر
  باعث می‌شد cron هر شب «ارسال نشد» بدهد برای بکاپی که رفته. **این را
  خودِ آزمونِ اسکریپت پیدا کرد، نه بازبینی.**
- **⚠ `MAX_SEND`/`MAX_VERIFY` با متغیر محیطی جایگزین‌شدنی‌اند** — نه
  برای انعطاف، بلکه چون بدون آن این دو شاخه فقط با ساختنِ یک فایلِ
  ۵۰ مگابایتی آزمودنی بودند، یعنی در عمل هرگز آزموده نمی‌شدند (همان
  دلیلِ متغیرهای `perf-report.sh`). `TG_API` هم به همین دلیل هست.
- **دوازده مسیر با یک سرورِ تلگرامِ ساختگی روی localhost اجرا و دیده
  شدند**، نه بازبینی شدند: تنظیمِ کامل روی pty، ارسالِ موفق با سنجش،
  ارسالِ دوباره (رد می‌شود)، `--force`، **بازگرداندنِ واقعی** (فایلِ
  رسیده با فقط عبارتِ رمز باز شد، `gzip -t` سالم، و بایت‌به‌بایت با اصل
  یکی بود)، فایلِ دست‌کاری‌شده در برگشت (کد خروج ۱)، ردِ ارسال از سمتِ
  تلگرام (۱)، توکنِ نامعتبر، `gzip` خراب (۱)، فایلِ خیلی کوچک (۱)، دو
  سقفِ حجم، بکاپِ کهنه، و نصبِ cron.
- ⚠ ساعتش ۴:۱۰ است چون `backup.sh` روی ۳:۳۰ می‌نشیند؛ باید **بعد از**
  ساخته شدنِ فایلِ همان شب اجرا شود، نه قبلش.
- **⛔ `--verify-pass` — عبارتی که یک بار با آن باز نکرده باشید، عبارتِ
  رمز نیست؛ یک رشته است که روی کاغذ نوشته‌اید.** همان استدلالی که
  `restore.sh` را ساخت (بکاپِ بازیابی‌نشده یک فرضیه است)، این بار برای
  **کلیدش**. یک کاراکترِ جاافتاده یا یک فاصله‌ی اضافه هیچ نشانه‌ای ندارد
  تا روزِ حادثه — و آن روز دیگر راهی برای فهمیدنش نیست.
  - **⛔ عبارتِ ذخیره‌شده را چاپ نمی‌کند و رشته‌ای هم با آن مقایسه
    نمی‌کند.** یک نمونه‌ی واقعی با **همان** مسیرِ `--send` رمز می‌شود و
    بعد با چیزی که کاربر تایپ می‌کند باز می‌شود. مقایسه‌ی رشته‌ای «شبیهِ»
    آزمون است ولی آن چیزی را نمی‌سنجد که روزِ حادثه لازم است.
  - ⚠ `gzip -t` و `cmp` در آن **عمداً افزونه‌اند** (gpg خودش احرازِ اصالت
    دارد و با عبارتِ غلط اصلاً باز نمی‌کند)، پس جهشِ برداشتنشان زنده
    می‌ماند. نوشته شد تا کسی «پس این خط لازم نیست» نخواند.
  - **جهشِ واقعیِ این قابلیت «به‌جای عبارتِ تایپ‌شده، همان ذخیره‌شده را
    بسنج» است** — آزمونِ همیشه‌سبز. اجرا شد و با ورودیِ غلط «درست است»
    داد، یعنی بررسی پوچ نیست.
  - ⚠ چرا فلگ شد و یک دستورِ چندخطی در چت نماند: پیستِ چندخطی در اپِ SSH
    موبایل به هم ریخت و خطوط قاطی شدند. ابزاری که فقط با پیستِ بی‌نقص کار
    کند، همان «قابلیتی که راه نمی‌افتد» است.

## آمار استفاده — جای «تراکنش همه کاربران»

- **⛔ صفحه‌ی `admin/all-transactions.php` حذف شد.** مدیر همه‌ی تراکنش‌های همه‌ی
  کاربران را با عنوان و مبلغ و طرفِ حساب می‌دید — هم بی‌فایده بود (با فهرستِ
  خرید و قرضِ بقیه هیچ تصمیمی گرفته نمی‌شود) هم بدترین نقضِ حریم خصوصی در کلِ
  اپ، و همان تنها استثنای قاعده‌ی «جداسازی کاربران» بود. حالا استثنایی نمانده.
- جایش `admin/insights.php` + `includes/admin_insights.php` آمد که **عمداً فقط
  شمارش است، نه محتوا**: چند تراکنش، آخرین فعالیت کِی، چند کاربر هر بخش را
  استفاده کرده‌اند. نه عنوان، نه مبلغ، نه طرفِ حساب.
- **⛔ هیچ ردیابیِ تازه‌ای اضافه نشد و هیچ جدولِ لاگی ساخته نمی‌شود.** همه‌ی
  اعداد از داده‌ای که از قبل هست ساخته می‌شوند. اگر روزی وسوسه شدید «رویداد»
  ثبت کنید، بدانید آن یعنی ساختنِ یک ردِ رفتاری از کاربران که تا امروز وجود
  نداشته — و آن‌وقت ادعاهای `privacy.php` هم دیگر درست نیستند.
- **قیفِ شروع اولین کارتِ صفحه است، عمداً.** «چند نفر ثبت‌نام کردند» بی‌معناست
  اگر ندانیم چند نفرشان اولین تراکنش را ثبت کردند. اگر عددِ دوم خیلی کمتر از
  اولی باشد، مسئله‌ی اپ «امکانات» نیست — کاربر در همان دقیقه‌ی اول گیر می‌کند.
- «آخرین فعالیت» از تاریخِ ثبتِ رکوردها می‌آید، **نه از لاگِ ورود**: کسی که اپ
  را باز می‌کند ولی چیزی ثبت نمی‌کند از دیدِ محصول فعال نیست، و ورودها را هم
  نمی‌خواهیم ثبت کنیم.

### ⛔ «چه زمانی از اپ استفاده کرده» — و جدولی که فقط یکی‌اش خوانده می‌شد

- **خرابیِ واقعی، و مالکِ نصب گزارشش کرد:** «آخرین استفاده» فقط از
  `transactions` خوانده می‌شد. روی همین دیتابیس سنجیده شد: کاربرِ `admin`
  سه چک، سه طلب و یک یادآور داشت — همه در یک روزِ مشخص — و صفحه
  درباره‌اش می‌نوشت **«هرگز شروع نکرده»** و «هنوز شروع نکرده». نه
  خطایی، نه نشانه‌ای؛ فقط یک عددِ غلط که تصمیمِ محصولی رویش سوار بود.
  - **و برای همین اپ بدترین شکلِ ممکن:** چک و طلب و بدهی همان چیزی‌اند
    که اپ را از رقبای خارجی جدا می‌کنند، پس دقیقاً کاربری که بیشترین
    استفاده را از تفاوتِ اپ می‌کرد **نامرئی** می‌شد — و قیفِ شروع همان
    افتی را نشان می‌داد که وجود نداشت.
- **⛔ `ACTIVITY_TABLES` تنها جای این تصمیم است** و **فهرست بسته است**،
  برخلافِ `userDataTables()`. آنجا فهرست باید **کامل** باشد (خروجی و
  حذف) پس کشف از دیتابیس درست است؛ اینجا باید **گزینشی** باشد و
  دیتابیس نمی‌داند کدام ردیف کارِ کاربر است:
  - `wallets` را `createUserAccount()` و `banks`/`asset_types` را
    `seedUserDefaults()` هنگامِ ثبت‌نام می‌سازند. با کشفِ خودکار، هر
    کاربرِ تازه‌ای در همان **ثانیه‌ی اول** «فعال» می‌شد و قیف دقیقاً
    همان چیزی را پنهان می‌کرد که برای دیدنش ساخته شده. اندازه‌گیری شد:
    هر ۱۷ کاربرِ ساخته‌شده از مسیرِ اپ، ردیفِ `wallets` شان هم‌ثانیه با
    `users.created_at` است.
  - `notifications` و `net_worth_snapshots` و `reminder_occurrences` را
    **برنامه** می‌سازد، نه کاربر؛ `payments` را هدیه‌ی مدیر هم می‌سازد؛
    و `api_tokens`/`password_resets`/`trusted_devices`/`sms_codes`
    اعتبارنامه‌اند.
  - **⛔ و هر پنج فهرستِ کمکیِ «فهرست‌های من» هم بیرون‌اند، نه فقط آن
    دوتایی که seed می‌شوند.** نسخه‌ی اول `categories`، `people` و
    `wallet_kinds` را جزوِ «رکورد» می‌شمرد در حالی که `banks` و
    `asset_types` — که دقیقاً هم‌جنسشان‌اند و در همان `references.php`
    مدیریت می‌شوند — بیرون بودند. **مالکِ نصب خرابی‌اش را گزارش کرد:**
    کاربری با **یک** تراکنش «۷ رکورد» می‌گرفت، چون شش دسته‌بندیِ شخصی
    ساخته بود؛ و خودِ او با ~۷ تراکنش «۳۵». تنظیمِ فرم رکوردِ دفتر
    نیست، و شمردنش هم قیفِ شروع را باد می‌کرد هم «کم‌فعال» را پنهان.
  - **⛔ و عددِ دومِ روی صفحه حالا مابه‌التفاوت است، نه جمعِ کل.**
    `records` خودش تراکنش‌ها را هم دارد، پس «۱ تراکنش · ۷ رکورد» را
    مالکِ نصب «۷ تراکنش» خواند — و حق داشت. دو عددی که کنارِ هم
    می‌نشینند نباید هم‌پوشانی داشته باشند؛ حالا «۱ تراکنش · ۶ رکوردِ
    دیگر» است.
  - `NON_ACTIVITY_TABLES` هر کدام را با **دلیلش** نگه می‌دارد، و
    `tests/test_admin_stats.php` می‌سنجد که هر جدولِ `user_id`دار در
    **یکی** از دو فهرست باشد. بدونِ آن، جدولِ فردا بی‌صدا بیرونِ آمار
    می‌ماند — همان قاعده‌ی `MIGRATIONS` و `EXPECT` و `BUDGET`.
- **⛔ فاصله‌ی روز را دیتابیس حساب می‌کند (`DATEDIFF`)، نه PHP.**
  `new DateTimeImmutable('today')` امروزِ PHP است و `created_at` را
  دیتابیس نوشته؛ در پنجره‌ی بامدادی این دو **دو روزِ متفاوت** می‌گفتند
  و عددِ «چند روز پیش» یکی می‌پرید — همان چیزی که بخشِ «منطقه‌ی زمانی»
  برایش نوشته شد.
  - ⚠ و `diff()->days` **قدرِ مطلق** می‌دهد: یک `created_at`ِ جلوتر از
    امروز (پرشِ ساعتِ سرور) «۳ روز پیش» خوانده می‌شد — عددی که هیچ‌کس
    به آن شک نمی‌کند چون شکلش کاملاً عادی است. حالا علامت‌دار است و
    منفی به صفر («امروز») بریده می‌شود.
- **⛔ `ACTIVE_DAYS` تنها مرجعِ آستانه است.** عددِ ۱۴ در **سه** جا
  سخت‌کد بود (شرطِ `userActivity()`، برچسبِ پله‌ی چهارمِ قیف، و سرآیندِ
  «بیش از ۱۴ روز…» در صفحه) و عوض کردنِ یکی، دو تای دیگر را بی‌صدا
  دروغ‌گو می‌کرد.
- **⛔ قیفِ شروع هم روی «رکورد» شمرده می‌شود، نه تراکنش** — وگرنه همان
  دروغ، این بار در عددی که مالکِ نصب رویش تصمیم می‌گیرد. ⚠ پله‌ی چهارم
  زیرمجموعه‌ی پله‌ی دوم می‌ماند (`active` بودن `days_since` می‌خواهد و
  آن فقط با `records ≥ 1` وجود دارد)، پس `pcKeep` در `funnelVerdict()`
  هرگز از ۱۰۰ رد نمی‌شود.
- **⛔ «حساب و کیف پول» در فهرستِ پذیرش دروغ می‌گفت.** حسابِ پیش‌فرض را
  خودِ برنامه می‌سازد، پس آن ردیف روی هر نصبی نزدیکِ ۱۰۰٪ می‌ایستاد و
  **بالای** فهرستِ «کدام بخش‌ها استفاده می‌شوند» می‌نشست — یعنی
  پراستفاده‌ترین قابلیتِ اپ یک چیزِ دست‌نخورده نشان داده می‌شد. حالا
  کمینه‌ی ۲ دارد و برچسبش «حساب دوم (به‌جز پیش‌فرض)» است. «یادآور» هم
  اضافه شد؛ اصلاً در فهرست نبود.
- **⚠ هزینه‌اش نوشته می‌ماند:** `admin/insights.php` از **۲۹ به ۳۰**
  کوئری رفت و آن یک کوئری دقیقاً ردیفِ تازه‌ی «یادآور» است (با
  برداشتنش دوباره ۲۹ شد). خودِ `userActivity()` همچنان **یک** کوئری
  است — و برخلافِ نسخه‌ی قبلی که دو زیرکوئریِ همبسته **به‌ازای هر
  کاربر** می‌زد، با تعدادِ کاربر رشد نمی‌کند. اندازه‌گیری روی همین
  دیتابیس (۴۰٬۰۰۳ تراکنش، کمترینِ پنج اجرا): **۱۴٫۳ میلی‌ثانیه**، و
  در مقایسه‌ی مستقیمِ SQL هر دو شکل ۱۳٫۲ میلی‌ثانیه.
- دو تست و هر دو لازم: **قاعده ۳۹** در `test_api_contract.php` *شکل* را
  می‌سنجد (یک مرجع بودنِ فهرست، `DATEDIFF` به‌جای PHP، و نبودِ عددِ
  سخت‌کد در تابع و در صفحه) و `tests/test_admin_stats.php` *رفتار* را
  (۳۸ بررسی). با **هشت جهش** روی رفتار و **شش جهش** روی شکل سنجیده شد،
  همه گرفته شد.
  - ⚠ و سه جهشِ اول **اصلاً اعمال نشده بودند**: الگوی جایگزینی در پایتون
    `\$` داشت و آن در پایتون یک بک‌اسلشِ واقعی است، پس هیچ تطبیقی رخ
    نمی‌داد و هر سه «زنده» به نظر می‌رسیدند. **ابزارِ جهش هم می‌تواند
    دروغ بگوید** — همان درسِ `php -r` و `set_exception_handler`. با
    `assert` روی وجودِ الگو گرفته شد.
  - ⚠ نسخه‌ی اولِ قاعده ۳۹ فقط `const ACTIVE_DAYS` را می‌پذیرفت و آن یک
    **هشدارِ الکی** بود: `define()` دقیقاً همان کار را می‌کند. قاعده
    باید چیزی را ببندد که خراب است، نه چیزی را که فقط شکلش فرق دارد.
- **⚠ و یک باگِ کوچکِ همین صفحه که با قاعده‌ی خودِ راهنما پیدا شد:**
  نوارِ «کدام بخش‌ها استفاده می‌شوند» یک چراغِ سه‌مرحله‌ای است
  (≥۵۰٪ سبز، ≥۲۰٪ کهربایی، کمتر قرمز) ولی پله‌ی وسط `var(--gold)`
  داشت — و پالتِ دومِ `style.css` آن را به **آبی** بازتعریف می‌کند،
  پس روی آن پالت پله‌ی میانی هیچ هشداری نمی‌رساند. `--warn-ink` شد،
  همان‌طور که در `funnelVerdict()` هم نوشته شده بود.
- ⚠ رشدِ ماهانه روی تاریخِ **میلادی** گروه‌بندی می‌شود و بعد در PHP به ماهِ شمسی
  می‌رود، نه برعکس: ماهِ شمسی وسطِ ماهِ میلادی شروع می‌شود، پس `GROUP BY
  YEAR/MONTH` با برچسبِ شمسی کنارِ هم دروغ می‌گفتند.
- جدولی که هنوز با migration نیامده باشد در `featureAdoption()` رد می‌شود، نه
  اینکه صفحه را بشکند — نصب‌های عقب‌مانده هم باید این صفحه را ببینند.

### ⛔ `funnelVerdict()` — عددِ خام هیچ تصمیمی نمی‌سازد

- صفحه از اول اعدادِ درستی نشان می‌داد، ولی مالکِ نصب باید خودش نگاه می‌کرد،
  نسبت می‌گرفت و نتیجه می‌گرفت. عملاً یعنی هیچ‌وقت. حالا زیرِ میله‌ها یک حکم
  هست — و **قدمِ بعدی هم گفته می‌شود، نه فقط تشخیص**: «۳۰٪ افت» یک واقعیت
  است؛ «کاربر در دقیقه‌ی اول گیر می‌کند، اول فرمِ ثبت را ساده کن» یک تصمیم.
- **⛔ ترتیبِ دروازه‌ها خودش منطق است، نه سلیقه:** اول نمونه‌ی کوچک، بعد
  بالای قیف، بعد میانه، بعد ماندگاری. تا کاربر اولین تراکنش را ثبت نکرده،
  حرف زدن از ماندگاری بی‌معناست.
- **⛔ زیرِ ۵ کاربر هیچ تشخیصی داده نمی‌شود، حتی اگر عددها فاجعه باشند.** با
  ۴ کاربر «۲۵٪» یعنی یک نفر، و تصمیم گرفتن از روی یک نفر بدتر از تصمیم
  نگرفتن است. حکمِ آن حالت هم عمداً هیچ درصدی نمی‌گوید.
- **⛔ درصدِ هر پله نسبت به پله‌ی قبل است، نه نسبت به کل.** با مخرجِ «همه‌ی
  کاربران»، قیفی که ۵۰٪ فعال‌سازی و ۵۰٪ عبور دارد «۲۵٪» خوانده می‌شود و
  سالم را خراب اعلام می‌کند — یعنی مالکِ نصب دنبالِ مشکلی می‌گردد که وجود
  ندارد.
- ⚠ متنِ حکم با `h()` در HTML چاپ می‌شود، نه مارک‌داون: ستاره‌ی تأکید عیناً
  روی صفحه دیده می‌شود. یک بار همین شد و تست حالا می‌گیردش.
- **⚠ `--gold` دیگر طلایی نیست.** پالتِ دومِ `style.css` آن را به **آبی**
  (`#2563eb`) بازتعریف می‌کند، پس نوارِ «هشدار» با آن دقیقاً مثل نوارِ
  «اطلاع» دیده می‌شد. رنگِ هشدار `--warn-ink` است — همانی که بقیه‌ی اپ به کار
  می‌برد. هر جای دیگری هم که «زرد/کهربایی» می‌خواهید، از `--gold` استفاده
  نکنید.
- تست در `tests/test_funnel_verdict.php` است (۲۹ بررسی، بدون نیاز به دیتابیس)
  و با پنج جهش سنجیده شده: برداشتنِ نگهبانِ نمونه‌ی کوچک، هر دو مخرجِ غلط،
  جابه‌جا کردنِ ترتیبِ دروازه‌ها، و برگشتِ ستاره‌ی مارک‌داون.

## جمله‌های بینشِ صفحه‌ی خانه

- **دلیل وجودش:** اپ همه‌ی عددها را داشت ولی هیچ‌وقت **حرف نمی‌زد**. کاربر
  باید خودش گزارش‌ها را باز می‌کرد و مقایسه می‌کرد. `financialHighlights()`
  حداکثر **سه** جمله می‌سازد و هر کدام یک لینکِ اقدام دارد — خبری که به
  اقدام نرسد دفعه‌ی بعد خوانده نمی‌شود.
- **⛔ خطرِ اصلی دروغ گفتن است، نه نبودن.** یک عددِ غلط را کاربر با نگاه به
  تراکنش‌هایش می‌گیرد، ولی یک **جمله**ی غلط را باور می‌کند — و اولین جمله‌ای
  که بی‌معنا باشد کلِ کارت را برای همیشه بی‌اعتبار می‌کند. پس:
  - کاربری که ماهِ قبل هیچ خرجی نداشته «۱۰۰٪ بیشتر» نمی‌گیرد.
  - رشدِ ۵۰۰ به ۲۰۰۰ تومان «۳۰۰٪ رشد» اعلام نمی‌شود (کفِ مبلغ ۱۰۰٬۰۰۰).
  - نوسانِ زیرِ ۲۵٪ اصلاً خبر نیست.
  - کاربرِ تازه **هیچ** جمله‌ای نمی‌گیرد، نه یک کارتِ خالی.
- ترتیب معنا دارد: سررسیدِ عقب‌افتاده اول است، چون تنها چیزی است که همین
  حالا هزینه دارد.
- ⚠ سقفِ سه جمله **دو نگهبان** دارد (`count($out) < 3` و `array_slice`)، پس
  برداشتنِ هرکدام به‌تنهایی دیده نمی‌شود. برای اینکه معلوم شود تست پوچ نیست،
  جهشِ «هر دو را بردار» اجرا شد و گرفته شد.
- تست در `tests/test_highlights.php` است (۱۴ بررسی) و با پنج جهش سنجیده شده.

## کارت‌های حساب روی خانه — پین‌شده و اسلایدی

- **⛔ یک اسلایدر است، نه یک نوارِ دوم.** اسلاید اول همان «مانده این ماه»
  است — همان جا، همان اندازه، همان چیزی که همیشه بوده — و کارت‌های حساب
  اسلایدهای بعدی‌اند. نسخه‌ی اول یک نوارِ جدا **زیرِ** نوارِ ماه بود و
  همان غلط بود: هر چیزی که پایین‌تر است (جمله‌های بینش، فهرست تراکنش‌ها)
  ۱۲۰ پیکسل پایین می‌رفت، یعنی پین کردنِ یک حساب کلِ صفحه‌ی خانه را
  عوض می‌کرد. حالا هیچ چیزی جابه‌جا نمی‌شود. فقط حساب‌هایی که کاربر
  خودش با آیکونِ پین در `wallets.php` انتخاب کرده.
- **⛔ هیچ عرض و ارتفاعِ ثابتی در کار نیست و نباید باشد.** عرضِ هر اسلاید
  `flex: 0 0 100%` است (یعنی به تعریف، دقیقاً عرضِ نوارِ ماه) و ارتفاعش
  را `align-items: stretch`ِ پیش‌فرضِ فلکس از بلندترین اسلاید می‌گیرد.
  با عددِ ثابت، اولین باری که محتوای نوار عوض شود کارت‌ها **بی‌صدا** از
  قدِ آن می‌افتند. اندازه‌گیری شد: هر چهار اسلاید ۳۴۷×۱۷۷.۴ روی موبایل و
  ۶۸۸×۱۷۷.۴ روی دسکتاپ.
- **⛔ `margin-bottom` نوار داخلِ اسلایدر صفر می‌شود.** فقط برای فاصله
  نیست: حاشیه‌ی یک آیتمِ فلکس در `stretch` از ارتفاعِ جعبه‌اش کم می‌شود،
  پس بدونِ آن نوارِ ماه ۱۲ پیکسل **کوتاه‌تر** از کارت‌های کنارش می‌شد.
- **⛔ صفحه‌ی خانه‌ی کسی که چیزی پین نکرده باید مو نزند.** با فهرستِ خالی
  نقطه‌ها اصلاً رندر نمی‌شوند و فاصله‌ی زیرِ نوار دقیقاً همان ۱۲ پیکسلِ
  همیشگی می‌ماند (`padding-bottom: 26px` منهای `margin-bottom: -14px`).
  اندازه‌گیری شد، حدس زده نشد.
- **نقطه‌ها اختیاری نیستند.** با اسلایدِ تمام‌عرض، «سرک کشیدنِ» کارتِ بعدی
  — تنها نشانه‌ی کشیدنی بودن در نسخه‌ی نواری — از بین می‌رود. نقطه‌ها
  جایگزینِ همان‌اند و با یک اسلاید رندر نمی‌شوند (نشانگرِ تک‌نقطه‌ای چیزی
  نمی‌گوید).
- ⚠ **گامِ اسلاید در `app.js` از فاصله‌ی واقعیِ دو اسلایدِ اول خوانده
  می‌شود، نه از `عرض + عددِ gap`.** با عددِ ثابت، اولین باری که `gap` در
  CSS عوض شود نقطه‌ها بی‌صدا از اسلاید عقب می‌افتند.
- ⚠ **در راست‌به‌چپ `scrollLeft` از صفر شروع می‌شود و منفی می‌رود**، پس
  هم‌گام کردنِ نقطه‌ها قدرِ مطلق لازم دارد؛ و پرشِ نقطه با
  `scrollIntoView({ inline: 'start', block: 'nearest' })` است نه با نوشتنِ
  `scrollLeft`: علامتِ آن عدد بین موتورها یکی نیست و محاسبه‌ی دستی‌اش
  روی یکی از آن‌ها بی‌صدا برعکس می‌شد. `block: 'nearest'` هم لازم است،
  وگرنه صفحه عمودی هم می‌پرد.
- **⛔ `pinnedWallets()` تنها جای این تصمیم است** (مثل `chequeActiveSql()`)
  و روی `walletBalances()` سوار است، نه یک کوئریِ تازه: موجودی شش منبع
  دارد و کوئریِ دوم دیر یا زود عددِ متفاوتی می‌گفت — آن‌وقت عددِ خانه با
  عددِ صفحه‌ی حساب‌ها فرق می‌کرد و کاربر به هیچ‌کدام اعتماد نمی‌کرد.
- **⛔ `wallets.pinned` ستونِ دیتابیس است، نه `localStorage`.** فیلترِ
  اقلامِ صفحه‌ی دارایی (`daftar_asset_off`) عمداً در مرورگر می‌ماند چون
  یک **نگاه** است؛ ولی «این حساب را روی خانه می‌خواهم» تصمیمی ماندگار
  درباره‌ی خودِ حساب است — مثل `sort_order` و `color`. در مرورگر یعنی
  گوشی و لپ‌تاپ دو خانه‌ی متفاوت.
- **⛔ پیش‌فرض ۰:** هیچ نصبِ موجودی بعد از `git pull` خانه‌اش عوض نمی‌شود،
  و اگر چیزی پین نباشد نوار **اصلاً رندر نمی‌شود** — نه یک نوارِ خالی.
- **⛔ حسابِ غیرفعال هرگز نمی‌آید، حتی اگر پین باشد.** کاربری که حسابی را
  می‌بندد انتظار ندارد موجودی‌اش هنوز روی خانه باشد؛ و پینِ قبلی‌اش
  نباید آن را زنده نگه دارد. دکمه‌ی پین هم برای حسابِ غیرفعال اصلاً
  رندر نمی‌شود — کلیدی که کار نمی‌کند بدتر از نبودنش است.
- **⛔ سقفِ `PINNED_WALLET_MAX` (۸) در **دو** جا اعمال می‌شود.** اگر فقط
  `pinnedWallets()` می‌بُرد، کاربر ۱۵ تا پین می‌کرد، ۸ تا می‌دید، و
  هیچ‌جا نمی‌فهمید بقیه کجا رفتند. اندپوینت ۴۲۲ می‌دهد با پیامِ روشن.
- **⛔ کارتِ خانه همان `.bank-card`ِ صفحه‌ی حساب‌هاست، نه طرحِ دوم.**
  `.home-card` فقط سه چیز را عوض می‌کند: `aspect-ratio: auto` (نسبتِ
  واقعیِ کارت بانکی ۱.۵۸۶ است و روی دسکتاپ **۴۳۴ پیکسل** ارتفاع می‌داد،
  در برابر ۱۷۷ِ نوارِ ماه)، گردی و padding از خودِ نوار، و چیپِ کوچک‌تر.
  با دو طرح، کاربر بسته به اینکه کارت را از خانه باز کرده یا از فهرست
  حساب‌ها دو چیزِ متفاوت می‌دید.
- **⛔ تپ روی کارت همان نمای کارت را باز می‌کند** — شماره‌ی حساب و شبا با
  دکمه‌ی «کپی»، و سه دکمه‌ی «ویرایش / تعدیل موجودی / تراکنش‌های این
  حساب». برای همین `#bankCardModal` و `#adjustWalletModal` از `wallets.php`
  به **`includes/wallet_card_modals.php`** منتقل شدند و هر دو صفحه از
  همان‌جا رد می‌شوند؛ با کپی کردنشان، ردیفِ تازه‌ای مثل «شبا» به یکی
  اضافه می‌شد و به آن یکی نه.
  - ⚠ انتخابگرِ `app.js` هم از `.wallet-row.js-show-card` به
    `.js-show-card` باز شد. با انتخابگرِ بسته، کارتِ خانه تپ می‌شد و
    **هیچ اتفاقی نمی‌افتاد** — بی‌هیچ خطایی.
  - ⚠ شیتِ ویرایشِ حساب فقط در `wallets.php` است، پس روی خانه همان دکمه
    برچسبِ «ویرایش در حساب‌ها» می‌گیرد و کاربر را به آن صفحه می‌برد.
    دکمه‌ای که زده شود و کاری نکند از نبودنش بدتر است.
  - ⚠ نگاشتِ فیلدهای کارتِ خانه **دقیقاً** همانِ مودال است (بالا نامِ
    بانک، پایین نامِ حساب). برعکسش روی حسابی که نامش با بانکش یکی است
    هر دو خط را یک کلمه می‌کرد.
- **⚠ شماره‌ی کارت حالا روی خودِ کارتِ خانه دیده می‌شود.** این خواسته‌ی
  صریحِ مالکِ نصب است و جای قاعده‌ی قبلی («این ستون‌ها فقط داخلِ نمای
  کارت باز می‌شوند») را گرفت. هزینه‌اش واقعی است و نوشته می‌ماند: صفحه‌ی
  خانه همان صفحه‌ای است که کاربر جلوی دیگران هم بازش می‌کند. شماره‌ی
  **حساب** و **شبا** عمداً روی کارت نیامدند و فقط داخلِ نما باز می‌شوند —
  آن‌ها در یک نگاه از دور خوانده نمی‌شوند ولی در یک عکس می‌مانند.
- ⚠ تپ روی پین صفحه را **تازه نمی‌کند** — برخلافِ بیشترِ تغییرهای این اپ.
  هیچ عددی روی همان صفحه عوض نمی‌شود (نوارِ خانه جای دیگری است)، پس
  تازه‌سازی فقط جای کاربر را در فهرست گم می‌کرد.
- ⚠ `data-stop="1"` روی دکمه لازم است: کلِ ردیف `js-show-card` است و
  بدونش تپِ پین نمای کارت را هم باز می‌کرد.
- ⚠ `.ltr-num` روی **خودِ عدد** است نه روی ظرفش (ظرف واحدِ «تومان» را هم
  دارد) — همان قاعده‌ی `.money-card` و `.balance-ribbon`.

### ⛔ چهار باگِ چیدمانی که فقط اندازه‌گیری پیدایشان کرد

- **`scroll-snap-align: center` نوار را از وسط شروع می‌کرد.** با
  `scroll-snap-type: mandatory`، مرورگر همان هنگامِ چیدمان اسلایدِ **دوم**
  را وسط می‌آورد و اسلایدِ اول نیمه بیرونِ صفحه می‌ماند — یعنی کاربر
  نوارِ ماه را اصلاً نمی‌دید. `start` درستش کرد.
- **و بعدش `start` هم `padding` را خنثی می‌کرد:** اسنپ لبه‌ی اسلاید را به
  لبه‌ی **جعبه‌ی padding** می‌چسباند، پس مرورگر خودش ۱۴ پیکسل اسکرول
  می‌کرد و اسلاید ۱۴px بیرون‌تر از بقیه‌ی کارت‌های صفحه می‌ایستاد.
  `scroll-padding-inline: 14px` نیمه‌ی دومِ همین رفع است.
- **⛔ `overflow-x: auto` مقدارِ `overflow-y` را هم به `auto` می‌برد، پس
  سایه‌ی زیرِ کارت بریده می‌شد** — نه محو، بلکه با یک لبه‌ی صافِ دیدنی
  کنارِ کارت. با نمونه‌برداریِ پیکسل دیده شد، نه با نگاه: ۸ پیکسل زیرِ
  کارت هنوز `#D5D3E9` بود و ۱۲ پیکسل زیرش ناگهان `#F6F7FB`ِ زمینه.
  رفعش `padding` است تا جعبه‌ی برش از سایه بزرگ‌تر شود — **به‌علاوه‌ی
  حاشیه‌ی منفیِ قرینه**، وگرنه عرضِ اسلاید (که `100%`ِ جعبه‌ی محتواست)
  همان‌قدر کم می‌شد و دیگر «دقیقاً به اندازه‌ی نوارِ ماه» نبود. اندازه‌ی
  padding هم اندازه‌گیری شده است: با ۱۶ پیکسل لبه هنوز دیده می‌شد، با ۲۶
  نه.
- **⛔ `gap` باید از padding افقی کمتر نباشد.** با `gap: 12px` و
  `padding-inline: 14px`، لبه‌ی اسلایدِ بعدی داخلِ همان ۱۴ پیکسل «سرک
  می‌کشید»: یک نوارِ باریکِ رنگی کنارِ صفحه که شبیهِ خرابیِ رندر دیده
  می‌شود نه شبیهِ نشانه‌ی کشیدنی بودن — آن نشانه حالا کارِ نقطه‌هاست.
  با `gap: 28px` لبه‌ی همسایه دقیقاً روی لبه‌ی جعبه‌ی برش می‌ایستد
  (اندازه‌گیری شد: `right = -14`).
- هر چهار در اسکرین‌شات، با `scrollLeft` و با نمونه‌برداریِ پیکسل دیده
  شدند، نه در بازبینی.
- **قاعده ۲۳ در `test_api_contract.php`** سه ستونِ این چیدمان را می‌سنجد
  (`flex: 0 0 100%`، صفر بودنِ `margin-bottom` نوار، و لازم بودنِ
  `scroll-padding-inline`) و با سه جهش سنجیده شده، هر سه گرفته شد.
- **قاعده ۲۴** هم شکلِ دیگری از همین خطر را می‌گیرد: هر عنصرِ
  `.js-show-card` باید همه‌ی `data-*`ی را داشته باشد که هندلرِ `app.js`
  می‌خواند. فهرستِ کلیدها **از خودِ `app.js` کشف می‌شود** (پنجره‌ی بینِ
  `querySelectorAll('.js-show-card')` و `openModal('bankCardModal')`)، نه
  از یک آرایه‌ی دستی — وگرنه کلیدِ یازدهمی که فردا به هندلر اضافه شود
  بی‌صدا بیرونِ پوشش می‌ماند. خرابی‌اش هم بی‌صداست: مودال باز می‌شود و
  همان یک خط خالی است. با سه جهش سنجیده شده (برداشتنِ یک کلید، عوض کردنِ
  نامش، و افزودنِ کلیدِ تازه به هندلر) و هر سه گرفته شد.
  - ⚠ دو اشتباهِ خودِ تست در همان اجرای اول، و هر دو درسِ تکراری‌اند:
    (الف) بلوک‌های PHP باید **پیش از** تجزیه خالی شوند، وگرنه `?>`ِ هر
    `<?= h(...) ?>` یک `>`ِ واقعی است و پنجره‌ی «تا اولین `>`» سرِ اولین
    مقدار بریده می‌شد — تست روی فایلِ **سالم** ۴۰ هشدارِ الکی داد، همان
    چیزی که سرِ کامنت‌های CSS در قاعده ۲۰ هم شد.
    (ب) توضیحِ همان خط، تگِ پایانِ PHP را در متنش داشت و **کلِ فایلِ تست
    را بست** — دقیقاً همان دامی که قاعده ۱۵ برایش نوشته شده. `php -l`
    گرفتش، ولی نکته این است که خودِ قاعده هم نویسنده‌اش را نجات نداد.
- تست در `tests/test_wallet_pin.php` است (۱۹ بررسی) و با پنج جهش سنجیده
  شده: برداشتنِ شرطِ `is_active`، برداشتنِ سقفِ نمایش، عوض کردنِ خودِ
  سقف، برداشتنِ بررسیِ مالکیت در اندپوینت، و برداشتنِ سقفِ سمتِ سرور.
  - ⚠ شرطِ `user_id` روی خودِ `UPDATE` عمداً افزونه است (کوئریِ
    `SELECT` بالایش مالکیت را سنجیده) و جهشش زنده می‌ماند. نوشته شده تا
    کسی «پس این لازم نیست» نخواند.

## ⛔ «دقیقه‌ی اول» — عددی که اپ بدونش از روز اول دروغ می‌گوید

- **خرابیِ واقعی، و کاملاً بی‌صدا:** حسابِ پیش‌فرضِ هر کاربرِ تازه با
  `initial_balance = 0` ساخته می‌شود (`createUserAccount()`) و هیچ‌جای اپ
  به او نمی‌گفت موجودیِ واقعی‌اش را وارد کند. پس با **اولین هزینه**،
  «مجموع حساب‌ها» منفی می‌شد و `safeToSpend()` تا ابد منفی می‌ماند —
  یعنی تنها عددی که به کارِ کاربر می‌آید از روزِ اول غلط بود. نه خطایی،
  نه هشداری؛ فقط کاربر نتیجه می‌گرفت اپ خراب است.
  - این محتمل‌ترین مکانیزمِ همان افتی است که «قیفِ شروع» در
    `admin/insights.php` برای **دیدنش** ساخته شد. آنجا تشخیص است، اینجا درمان.
- **⛔ `openingBalanceHint()` تنها جای این تصمیم است** (مثل
  `categoryScopeSql()`). خانه و `api/adjust_wallet.php` هر دو از همان رد
  می‌شوند؛ با دو نسخه، کارت پس از جواب دادنِ کاربر سرِ جایش می‌ماند یا
  برعکس، زودتر از موعد می‌رود.
- **⛔ دو شرط لازم است و هیچ‌کدام به‌تنهایی کافی نیست:**
  ۱. `users.balance_setup_at` هنوز `NULL` باشد. **بدونِ این نشانه، کاربری
  که واقعاً موجودی‌اش صفر است تا ابد همان کارت را می‌دید** — و کارتی که
  نمی‌شود از دستش خلاص شد، همان «هشدارِ همیشگی» است که آدم را عادت
  می‌دهد هشدارها را نادیده بگیرد.
  ۲. هیچ حسابی موجودی اولیه نداشته باشد؛ کسی که از فرمِ ساختِ حساب عدد
  وارد کرده نباید دوباره پرسیده شود.
- **کارت از همان `api/adjust_wallet.php` با `mode=set` می‌گذرد**، نه یک
  اندپوینتِ تازه: منطقِ «تفاوت تا رسیدن به عددِ واقعی» و بررسیِ مالکیت
  آنجاست و نسخه‌ی دومش دیر یا زود از آن دور می‌افتاد.
- **⛔ `markBalanceSetup()` حتی وقتی هیچ عددی عوض نمی‌شود هم صدا زده
  می‌شود** (`$delta === 0`). کاربری که موجودی واقعی‌اش دقیقاً همان عددِ
  فعلی است، **پاسخ داده** — نادیده گرفتنش یعنی کارت برای همان کسی که
  کارِ درست را کرده برمی‌گردد.
- ورودی روی **حسابِ پیش‌فرض** است (`defaultWalletId()`) و اگر کاربر بیش
  از یک حساب داشته باشد یک لینک به `wallets.php` هم می‌آید. جای کارت
  **بالای همه چیز** است، نه پایین‌تر: حرفش این است که عددهای زیرش هنوز
  درست نیستند.
- **⚠ رنگش آبیِ تعامل است نه کهربایی:** این یک **دعوت** است نه هشدار. با
  رنگِ هشدار، اولین چیزی که کاربرِ تازه می‌بیند یک اخطار می‌شد.

## یادآوری پشتیبان — تنها خرابیِ برگشت‌ناپذیر

- `backup.php` از قبل بود و کار می‌کرد، ولی **هیچ‌کس بی‌یادآوری بکاپ
  نمی‌گیرد**. دفتری که کاربر سه سال نگه داشته، اگر یک بار از دست برود
  رابطه تمام است — و برخلافِ بقیه‌ی خرابی‌های این اپ، این یکی برگشت
  ندارد.
- ماشین‌آلاتش از قبل ساخته شده بود: `Notify` با `dedup_key`. هیچ جدول و
  هیچ cron ای اضافه نشد — فقط `users.last_backup_at` که
  `api/export_data.php` می‌نویسد و `Notify::generateBackupHint()` می‌خواند.
- **⛔ `dedup_key` ماهانه است نه روزانه.** بدونش همان یک جمله هر بازدیدِ
  صفحه تکرار می‌شد و فهرستِ اعلان در یک هفته غیرقابل خواندن — یعنی
  قابلیت خودش را می‌کشت، بی‌هیچ خطایی.
- **⛔ زیرِ `BACKUP_MIN_ROWS` (۲۵ تراکنش) اصلاً نمی‌رود.** کاربری که سه
  تراکنش دارد چیزی برای از دست دادن ندارد؛ اعلانِ بی‌ربط کلِ مرکزِ اعلان
  را برای همیشه بی‌اعتبار می‌کند — همان استدلالِ «کاربرِ تازه هیچ جمله‌ای
  نمی‌گیرد» در `financialHighlights()`.
- **`BACKUP_AFTER_DAYS` یک ماه است و تنها مرجع.** کوتاه‌تر یعنی کاربری که
  ماهی یک بار بکاپ می‌گیرد — رفتارِ کاملاً درست — باز هم یادآوری می‌گیرد.
- **⛔ نشانه پیش از فرستادنِ بدنه نوشته می‌شود.** بعد از `echo` هر کوئریِ
  ناموفقی بی‌صدا گم می‌شود (خروجی رفته و هدرها بسته‌اند)، و آن‌وقت کسی که
  همین حالا بکاپ گرفته باز هم یادآوری می‌گرفت.
- **خطِ «آخرین پشتیبان» در `backup.php` اختیاری نیست:** بدونش کاربر اعلان
  می‌گیرد، می‌آید اینجا، و هیچ راهی ندارد بفهمد اعلان درست گفته یا نه.
- ⚠ رنگِ نشانِ اعلان `--warn-ink` است نه `--out`: پشتیبان نگرفتن خطر است
  ولی سررسیدِ امروز نیست؛ با قرمز، هم‌وزنِ چکِ برگشتی دیده می‌شد.
- تست در `tests/test_onboarding.php` است (۲۸ بررسی، هر دو بخش) و با **ده
  جهش** سنجیده شده.
  - ⚠ **دو جهش اول زنده ماندند و هر دو یعنی تست ناقص بود، نه کد امن:**
    (الف) بررسیِ «صدا زدنِ دوباره تاریخ را عوض نمی‌کند» دو فراخوان را در
    یک ثانیه اجرا می‌کرد، پس `NOW()` عینِ همان مقدار را می‌داد و برداشتنِ
    کاملِ شرطِ `IS NULL` هم سبز می‌ماند؛ حالا تاریخ عمداً به گذشته برده
    می‌شود. (ب) مرزِ بازه با `BACKUP_AFTER_DAYS - 1` نوشته شده بود، یعنی
    **تست مقدارِ خودش را از کدِ زیرِ آزمون می‌گرفت** و با عوض شدنِ ثابت
    مرزش هم جابه‌جا می‌شد؛ حالا عددِ ثابت است و خودِ مقدارِ ثابت هم
    جداگانه سنجیده می‌شود.

### ⛔ همان درسِ حاشیه‌ی امن، این بار در حالت شب

- `.start-card` گرادیان و نوارِ رنگی داشت و در حالت روز بی‌عیب بود، ولی
  در **شب** هر دو ناپدید می‌شدند: `html[data-theme="dark"] .card`
  (خطوطِ ~۱۷۵۵ و ~۲۳۲۰) با وزنِ بیشتر `background` و `border-color` را
  بازنویسی می‌کند. **داشتنِ مقدار در فایل کافی نیست — قاعده‌ای که برنده
  می‌شود باید داشته باشدش**، دقیقاً همان چیزی که یک بار سرِ
  `env(safe-area-inset-*)` گفته شد.
- به همین شکل `padding` و `font-size` روی `.amount-input` با `!important`
  نوشته شده‌اند، پس قاعده‌ی تازه بدونِ `!important` **بی‌صدا** بی‌اثر
  می‌ماند.
- **هر سه با اندازه‌گیری در کرومیوم پیدا شدند، نه با نگاه**
  (`backgroundImage: none`، `borderInlineStartColor: rgb(38,44,71)`،
  `paddingTop: 14px`). «در مرورگر درست به نظر می‌رسد» اینجا مدرک نیست.

## لایه‌ها و اسکرول

- هر لایه‌ی `.modal-overlay` / `.sheet-overlay` / `.more-sheet-overlay` که باز شود، `MutationObserver` در `app.js` بدنه را قفل می‌کند. بدون این، کشیدن انگشت روی شیت، صفحه‌ی زیرش را جابه‌جا می‌کرد و کاربر جای خودش را گم می‌کرد.
- **⛔ قفل هیچ ویژگیِ چیدمانی‌ای را عوض نمی‌کند، و این کلِ نکته است.**
  فقط کلاسِ `is-locked` روی `<html>` می‌نشیند که `overflow: hidden`
  می‌دهد، به‌علاوه‌ی یک شنونده‌ی `touchmove` که فقط وقتی جلوی لمس را
  می‌گیرد که هیچ نیای اسکرول‌شونده‌ای در مسیرش نباشد (`passive: false`
  لازم است، وگرنه `preventDefault` نادیده می‌ماند).
  - **دو نسخه‌ی قبلی هر دو غلط بودند و درسش مهم است.** نسخه‌ی اول بدنه
    را `position: fixed` با `top: -Y` می‌کرد — راهِ رایج، ولی روی آیفون
    نوارِ پایینِ شناور را بالا می‌آورد. نسخه‌ی دوم خواست با کشیدنِ
    `min-height` بدنه جبرانش کند و **کار نکرد**، چون فرضش غلط بود:
    عنصرِ `position: fixed` نسبت به **پنجره** جای می‌گیرد، نه نسبت به
    جعبه‌ی `body` — پس بلند و کوتاه کردنِ بدنه اصلاً به آن ربطی ندارد.
  - درسِ کلی‌ترش: وقتی دو بار جبران‌سازی جواب نداد، مسئله را از بین
    ببرید نه اینکه جبرانِ سوم بنویسید. حالا هیچ چیزی روی `body` عوض
    نمی‌شود، پس هیچ چیزی هم نمی‌تواند تکان بخورد — و چون صفحه جابه‌جا
    نمی‌شود، بازگرداندنِ `scrollY` و پرشِ هنگام بستن هم از بین رفت.
  - ⚠ **در مرورگرِ دسکتاپ هیچ‌کدام از این‌ها بازتولید نمی‌شوند** (آنجا
    `env(...)` صفر است و نوارِ مرورگر جمع نمی‌شود)، پس «در کروم درست
    است» اینجا مدرک نیست.

## ⛔ منوی کناری باید اسکرول شود — وگرنه گزینه‌ها ناپدید می‌شوند

- `.sidebar` ارتفاعش به `100vh` قفل است ولی فهرستش با هر بخشِ تازه
  بلندتر شد. روی حسابِ مدیر محتوای منو به **۱۲۰۱ پیکسل** رسیده بود،
  یعنی روی **هر** پنجره‌ی دسکتاپی (حتی ۱۰۰۰ پیکسل ارتفاع) گزینه‌های
  آخر — «کاربران»، «ورود و پیامک»، «آمار استفاده» — **دست‌نیافتنی**
  بودند: نه اسکرولی، نه بریدگی‌ای، نه خطایی. فقط نبودند.
- **⛔ `overflow-y: auto` به‌تنهایی هیچ کاری نمی‌کند.** فرزندِ فلکس
  پیش‌فرض `min-height: auto` دارد و حاضر نیست کوتاه‌تر از محتوایش شود،
  پس `flex: 1` هرگز فشرده نمی‌شود و `overflow` هرگز فعال نمی‌شود. در
  اندازه‌گیری دیده شد: `clientHeight` و `scrollHeight` هر دو ۱۰۳۱.
  **`min-height: 0` نیمه‌ی دومِ همین رفع است، نه یک احتیاط.**
- `.sidebar-brand` و `.sidebar-footer` هر دو `flex-shrink: 0` دارند تا
  «خروج از حساب» همیشه دمِ دست بماند و عنوان له نشود.
- `overscroll-behavior: contain` هم لازم است، وگرنه رسیدن به ته منو
  اسکرول را به خودِ صفحه زنجیر می‌کند.
- ⚠ **روی موبایل اصلاً دیده نمی‌شود** (آنجا `.sidebar` با `display:none`
  کنار می‌رود)، پس «روی گوشی درست است» اینجا مدرک نیست — قرینه‌ی همان
  درسِ حاشیه‌ی امن، که آنجا فقط روی گوشی دیده می‌شد.
- قاعده ۲۰ در `test_api_contract.php` هر دو نیمه را می‌سنجد و با سه جهش
  سنجیده شده.
  - ⚠ نکته‌ی خودِ تست: کامنت‌های CSS باید **پیش از** تجزیه حذف شوند،
    وگرنه الگوی «هر چیزی تا `{`» بلوکِ توضیحِ بالای قاعده را هم جزوِ
    انتخابگر می‌گیرد و تست روی فایلِ **سالم** هم قرمز می‌شود. اولین
    اجرا دقیقاً همین بود.

## ⛔ ثبت تراکنش باید روی دسکتاپ هم راه داشته باشد

- شیتِ ثبت تراکنش فقط با `#addTxBtn` باز می‌شد و آن دکمه داخلِ
  `.bottom-nav` بود — که بالای ۹۰۰ پیکسل `display: none !important`
  می‌شود. یعنی **روی کامپیوتر هیچ راهی برای ثبت تراکنش نبود**: هسته‌ی
  اپ، کاملاً از کار افتاده، بی‌هیچ خطایی. روی گوشی بی‌عیب بود، پس
  هیچ تستی و هیچ نگاهی آن را نمی‌گرفت.
- **⚠ و بدتر: راهش «وجود داشت» ولی کار نمی‌کرد.** اولین قلمِ نوارِ
  کناری «ثبت تراکنش» با آیکونِ + بود، ولی یک `<a href="index.php">` —
  شکلش دقیقاً دکمه‌ی ثبت بود و کارش فقط رفتن به خانه. بدترین حالت،
  چون کاربر می‌زند و فکر می‌کند اپ خراب است. حالا `<button>` است.
- **کلاسِ `.js-add-tx`** تنها بندِ این کار است، نه یک شناسه: دکمه‌ی +
  نوارِ پایین (موبایل) و قلمِ نوارِ کناری (دسکتاپ) هر دو همان را دارند.
  ورودیِ سومی که فردا اضافه شود هم بدون دست زدن به `app.js` کار می‌کند.
- **⛔ روی دسکتاپ شیت وسط باز می‌شود، نه چسبیده به لبه.** `.sheet` یک
  الگوی موبایلی است (تمام‌عرض، چسبیده به کف، گردیِ فقط بالا) و روی
  عرضِ ۱۴۰۰ پیکسل می‌شد یک نوارِ درازِ لبه‌ی پایین که کسی آن را
  «پنجره‌ی ثبت تراکنش» نمی‌خواند. تبدیل کاملاً در CSS است، پس هیچ
  صفحه‌ای دو نسخه‌ی HTML لازم ندارد.
- **قاعده‌ی کلی: هر قابلیتی که تنها راهش داخلِ `.bottom-nav` باشد، روی
  دسکتاپ وجود ندارد.** هر بار که چیزی به آن نوار اضافه می‌کنید، بپرسید
  روی عرضِ بزرگ از کجا در دسترس است.
- **⚠ و همین قاعده به *متن* هم مربوط است، نه فقط به کد.** متنِ خالیِ
  صفحه‌ی خانه می‌گفت «با دکمه + **پایین صفحه** اولین تراکنش را ثبت
  کنید» — یعنی تنها راهنماییِ کاربرِ خالی‌دفتر، روی دسکتاپ به دکمه‌ای
  اشاره می‌کرد که وجود نداشت. حالا خودِ آنجا **در** است: یک
  `<button class="js-add-tx">`، که روی هر عرضی کار می‌کند و ورودیِ
  سومی هم بدونِ دست زدن به `app.js` اضافه می‌شود.
- نوار پایین `z-index: 260` دارد، بالاتر از شیت (۲۵۰)، تا دکمه‌ی «بیشتر» موقع باز بودن شیت هم قابل زدن بماند — همان دکمه ضامن باز/بسته است. شیت `padding-bottom` به اندازه‌ی نوار دارد تا زیرش پنهان نشود.

### ⛔ «منو می‌رود وسطِ صفحه» — کیبوردِ مجازی، نه CSS

- **گزارشِ کاربر:** «به وقتایی دیدم منو پایین وسط‌های صفحه می‌ره». هیچ
  خطایی نمی‌داد و در مرورگرِ دسکتاپ اصلاً بازتولید نمی‌شد — قرینه‌ی
  همان درسِ حاشیه‌ی امن و منوی کناری.
- **علت:** `.bottom-nav` با `position: fixed` نسبت به viewport
  **چیدمان** جای می‌گیرد. کیبوردِ مجازی آن را کوچک **نمی‌کند**: فقط
  viewport دیداری کوچک می‌شود و صفحه اسکرول می‌شود تا فیلد دیده شود.
  پس کفِ viewport چیدمان جایی **وسطِ صفحه‌ی دیداری** می‌افتد و نوار
  دقیقاً همان‌جا روی محتوا شناور می‌ماند. روی iOS همیشه این‌طور بوده و
  از کروم ۱۰۸ اندروید هم پیش‌فرضش `resizes-visual` است، پس هر دو سکو.
- **⛔ رفعش «جابه‌جا کردنِ نوار» نیست، برداشتنش است** (`html.kb-open`).
  جبران‌سازی با `visualViewport.offsetTop` یعنی نوار با هر ضربه‌ی
  اسکرول تکان می‌خورد — همان درسِ قفلِ اسکرول: وقتی جبران‌سازی جواب
  نمی‌دهد، صورت‌مسئله را بردارید. ضمناً کسی که دارد تایپ می‌کند به
  ناوبری نیاز ندارد و فضای بیشتری هم به دست می‌آورد.
- **⛔ `focusin` و `visualViewport` هر دو لازم‌اند و کارشان فرق می‌کند:**
  - `focusin` **فوراً** پنهان می‌کند. کیبورد چند صد میلی‌ثانیه بالا
    می‌آید و در همان فاصله نوار وسطِ صفحه دیده می‌شد — یعنی دقیقاً
    همان باگ. مرورگرِ بدونِ `visualViewport` هم فقط همین را دارد.
  - `visualViewport` بعدش **تصحیح** می‌کند (`reconcile`, ۴۰۰ms و روی
    هر `resize`): با کیبوردِ سخت‌افزاری (آیپد، دسکتاپ) هیچ چیزی کوچک
    نمی‌شود، پس نوار برمی‌گردد. کفِ تشخیص ۱۲۰ پیکسل است — جمع شدنِ
    نوارِ آدرسِ مرورگر چند ده پیکسل بیشتر نیست و نباید «کیبورد» خوانده
    شود.
- **⛔ `window.kbNeedsKeyboard()` تنها جای صافیِ نوعِ فیلد است** و عمداً
  **بیرون از `DOMContentLoaded`** — همان قاعده‌ی `parseBankSms()` و
  `smsAutoOk()`، تا در node آزمودنی باشد.
  - ⚠ چک‌باکس، رادیو، دکمه و `type="color"` فوکوس می‌گیرند **بدونِ**
    کیبورد. بدونِ این صافی، تپ روی هر کلیدِ `.switch` نوار را ناپدید
    می‌کرد — یعنی رفعِ ما خودش یک خرابیِ تازه می‌ساخت.
  - ⚠ `focusout` یک تیک تأخیر دارد: هنگام جابه‌جا شدنِ فوکوس بینِ دو
    فیلد، `focusout` زودتر از `focusin`ِ بعدی می‌آید و بدونِ تأخیر نوار
    یک لحظه برمی‌گشت. اندازه‌گیری شد: صفر بار.
- **اندازه‌گیری شد، حدس زده نشد** (کرومیوم ۳۹۰×۸۴۴، با یک
  `visualViewport`ِ ساختگی که ارتفاعش را خودمان کم و زیاد می‌کنیم، پس
  همان تصمیمی که روی گوشی گرفته می‌شود واقعاً اجرا می‌شود): پایه
  `top=768`؛ فوکوس → پنهان؛ کیبوردِ باز بعد از reconcile → پنهان؛
  کیبورد بسته → `top=768`؛ کیبوردِ سخت‌افزاری → برمی‌گردد؛ پرشِ بین دو
  فیلد → صفر بار لغو.
- **دو تست و هر دو لازم:** قاعده ۳۳ در `test_api_contract.php` *شکل* را
  می‌سنجد (قاعده‌ی CSS، `focusin`، `visualViewport`، آمدنِ صافی از
  `kbNeedsKeyboard`، و بیرون بودنش از `DOMContentLoaded`) و
  `tests/test_keyboard_nav.php` *رفتار* را (۱۶ بررسی، با node روی خودِ
  `app.js`).
  - ⚠ **نسخه‌ی اولِ قاعده ۳۳ کافی نبود و جهش نشانش داد:** صافیِ نوع
    داخلِ خودِ `keyboardAwareNav()` بود و بررسی فقط دنبالِ رشته‌های
    `checkbox`/`color` می‌گشت؛ جهشِ «همیشه true» **زنده ماند**، چون
    فهرست سرِ جایش می‌ماند و فقط دیگر خوانده نمی‌شد. تابع بیرون برده
    شد و رفتارش با node سنجیده شد. **جهشِ زنده‌مانده یعنی تست ناقص
    است، نه اینکه کد امن است.**
  - با شش جهش سنجیده شد، همه گرفته شد: برداشتنِ قاعده‌ی CSS، برداشتنِ
    `focusin`، «همیشه true» کردنِ صافی، برداشتنِ تصحیحِ
    `visualViewport`، برداشتنِ چک‌باکس از فهرستِ استثنا، و کپیِ محلیِ
    صافی به‌جای `kbNeedsKeyboard`.

## ⛔ نیم‌فاصله جای شکستن است — و ستونِ جدول را له می‌کند

- **خرابیِ واقعی، و فقط روی دسکتاپ:** در `admin/users.php` ستونِ
  «عملیات» روی **۱۱۵ پیکسل** بسته می‌شد، چهار دکمه زیرِ هم می‌افتادند و
  ارتفاعِ هر ردیف از ۶۱ به **۲۱۱** پیکسل می‌رفت — با برچسب‌هایی که نصفه
  دیده می‌شدند («غیرفعال‌ساز»). صفحه از دور شبیهِ خرابیِ رندر بود.
- **⛔ علتِ اصلی نیم‌فاصله است.** مرورگر `U+200C` را **جای شکستن**
  می‌شناسد، پس «غیرفعال‌سازی» می‌تواند به «غیرفعال‌» و «سازی» بشکند.
  یعنی *کمترین عرضِ لازمِ* آن دکمه به یک تکه‌ی کوچک می‌رسد و جدولِ
  خودکار با خیالِ راحت ستون را می‌فشارد. در انگلیسی این اتفاق نمی‌افتد
  و به همین دلیل در هیچ الگوی آماده‌ای هم دیده نمی‌شود.
- **⛔ رفع سه تکه دارد و هر سه لازم است — با برداشتنِ هرکدام ستون دوباره
  له می‌شود:**
  1. `white-space: nowrap` روی `.btn` و `.delete-btn` — برچسبِ یک کنترل
     هرگز وسطِ خودش نمی‌شکند.
  2. `.table-actions { flex-wrap: nowrap }` **فقط در بلوکِ دسکتاپ** —
     تا وقتی ردیفِ دکمه‌ها `wrap` باشد، کمترین عرضِ لازمِ کلِ ستون فقط
     **یک** دکمه است و بند ۳ بی‌اثر می‌ماند. زیرِ ۹۰۱ پیکسل جدول کارتی
     می‌شود و آنجا شکستن دقیقاً درست است.
  3. `.actions-cell { width: 1% }` — یعنی «کمترین عرضِ لازم»، پس فشار
     روی ستون‌هایی می‌افتد که متنشان واقعاً می‌تواند بشکند (نام، ایمیل).
- **⛔ و یک تکه‌ی چهارم که خودِ صفحه لازم داشت:** `.page-content` عرضش
  `720px` است — عرضِ **خواندن**، که برای همه‌ی صفحه‌های این اپ درست است
  ولی یک جدولِ هفت‌ستونه را در نصفِ دسکتاپ می‌فشارد در حالی که ۴۵۰
  پیکسل کنارش خالی است. `$pageWide = true` پیش از `header.php` آن را به
  `1120px` باز می‌کند. **پیش‌فرض باریک است**، پس هیچ صفحه‌ای با آمدنِ
  این قابلیت عوض نشد؛ فقط `admin/users.php` آن را می‌گذارد.
- **⚠ «روی گوشی درست است» اینجا مدرک نبود** — قرینه‌ی همان درسِ منوی
  کناری. جدول زیرِ ۹۰۱ پیکسل کارتی می‌شود، پس این خرابی روی موبایل اصلاً
  وجود نداشت و فقط روی دسکتاپ دیده می‌شد.
- **همه‌ی ۲۶ صفحه روی پنج عرض (۳۹۰ تا ۱۹۲۰) در کرومیوم سنجیده شدند**، نه
  با نگاه: هر برچسبِ کنترل با `Range` و مقایسه‌ی `top`ِ مستطیل‌ها، و هر
  عنصری که از ظرفِ اسکرول‌ناپذیرش بیرون بزند. نتیجه: صفر مورد.
  - ⚠ **دو سنجه‌ی اولِ خودم غلط بودند و هر دو روی صفحه‌ی سالم هشدار
    دادند:** (الف) «ارتفاعِ دکمه بیش از یک خط» — دکمه‌ای که کنارِ یک
    فیلد در ردیفِ فلکس کشیده شده بلندتر است بی‌آنکه متنش شکسته باشد
    (۱۱ هشدارِ الکی)؛ (ب) «تعدادِ مستطیل‌های `Range` بیش از یک» —
    «۱ روز» دو رانِ دوجهته دارد (رقمِ فارسی + واژه) و روی **یک خط** دو
    مستطیل می‌سازد (۱۲ هشدارِ الکی). خطِ واقعی فقط با `top`ِ متفاوت
    شناخته می‌شود.
  - ⚠ و به همین دلیل `white-space: nowrap`ی که برای `.stay-chip` نوشته
    بودم **پس گرفته شد**: با سنجه‌ی درست معلوم شد آن چیپ‌ها حتی روی
    ۳۲۰ پیکسل و با کارتِ باز هم نمی‌شکنند. رفعی که چیزی را رفع نمی‌کند،
    فقط دیفِ بزرگ‌تر است.
- **قاعده ۳۱ در `test_api_contract.php`** هر چهار تکه را نگه می‌دارد و با
  پنج جهش سنجیده شد؛ دو تای مهمش در مرورگر هم بازتولید شدند (برداشتنِ
  `nowrap` → ۲۲ برچسبِ شکسته، برداشتنِ `flex-wrap` → ارتفاعِ ردیف ۱۹۱).
  - ⚠ نسخه‌ی اولِ خودِ قاعده روی فایلِ **سالم** قرمز شد: فایل **دو**
    بلوکِ `@media (min-width: 901px)` دارد و بررسی فقط اولی را می‌گرفت.
    حالا همه‌ی بلوک‌ها با هم سنجیده می‌شوند.
- کارهای مدیریتی در شیت ابزارها زیر یک دکمه‌ی «مدیریت» جمع‌اند که زیرشیت `#adminSheet` را باز می‌کند؛ بقیه‌ی شیت برای مدیر و کاربر عادی یک شکل است.

## حساب‌ها: هیچ پولی بی‌حساب نمی‌ماند

- برنامه برای هر کاربر یک حساب پیش‌فرض می‌سازد به نام **«کیف پول»** (قبلاً «کیف پول نقدی» بود؛ `migration_money_links` تغییر نامش می‌دهد). این همان حسابی است که همه‌ی تراکنش‌های قبلِ راه‌اندازیِ حساب‌ها به آن نسبت داده شدند و پیش‌فرضِ فرم ثبت تراکنش هم هست (`sort_order = 0`).
- `defaultWalletId()` آن را پیدا می‌کند (`kind='cash'` با کمترین `sort_order`؛ اگر نبود، اولین حساب فعال). `resolveWalletId()` نقطه‌ی واحد است: ورودی کاربر را می‌سنجد و اگر نفرستاده یا مال او نیست، حساب پیش‌فرض را برمی‌گرداند. **هر جا پولی جابه‌جا می‌شود از همین رد شوید** — وگرنه مبلغ در هیچ حسابی نمی‌نشیند و کاربر بعداً نمی‌فهمد کجا رفت.
- `walletBalances()` تنها مرجع موجودی است. علاوه بر تراکنش و انتقال و معامله، حالا این‌ها را هم جمع می‌کند:
  - **چک پاس‌شده** (`cheques.settle_wallet_id`): دریافتی مثبت، صادره منفی.
  - **پرداخت طلب/بدهی** (`debt_payments.wallet_id`): وصول طلب مثبت، پرداخت بدهی منفی.
- **هیچ‌کدام از این‌ها ردیف `transactions` نمی‌سازند.** وصول یک طلب درآمد نیست و اگر تراکنش می‌شد، گزارش درآمد/هزینه‌ی آن ماه الکی بالا می‌رفت. تست جدایی در `tests/test_money_links.php` است.
- **همه‌ی جابه‌جایی پولِ طلب و بدهی از `debt_payments` می‌گذرد**، چه پرداخت جزئی باشد چه تسویه‌ی کامل. زدن تیک «تسویه شد» یک ردیف برای *باقیمانده* می‌سازد و با `is_settlement = 1` علامتش می‌زند؛ برداشتن تیک دقیقاً همان‌ها را پس می‌گیرد و پرداخت‌های دستی کاربر دست‌نخورده می‌مانند.
- **تعدیل موجودی** (`api/adjust_wallet.php`) روی `initial_balance` می‌نشیند، نه به شکل تراکنش. موجودی هر حساب محاسبه‌شده است و وقتی با پول واقعی نمی‌خواند باید بشود دستی برابرش کرد؛ ولی یک تعدیل نه درآمد است نه هزینه، پس نباید در گزارش‌ها بیاید. سه حالت دارد: افزودن / کم کردن / «موجودی واقعی این‌قدر است» (تفاوت خودکار حساب می‌شود).
- صفحه‌ی تراکنش‌ها فیلتر `?wallet=ID` دارد و نمای کارتِ هر حساب دو دکمه: «تعدیل موجودی» و «تراکنش‌های این حساب». بدون این دو، موجودی منفی دیده می‌شد ولی هیچ راهی نبود بفهمی از کجا آمده.
- **رنگ در نمودارها معنا دارد، تزئین نیست — ولی تک‌رنگ هم نیست.** `chartPalette($tone, $mode)` یک چرخه‌ی ثابت از هشت هیوی متمایز دارد و فقط *نقطه‌ی شروع* را عوض می‌کند: هزینه از قرمز شروع می‌شود، درآمد و دارایی از سبز. نسخه‌ی اول همه‌ی قاچ‌ها را هم‌خانواده کرده بود (همه سبز یا همه نارنجی) و نمودار خوانده نمی‌شد.
  - ترتیب چرخه دلخواه نیست: با `scripts/validate_palette.js` مهارت dataviz سنجیده شده و هر جفتِ کنارِ هم — در هر دو حالت روشن و شب، و حتی جفتِ اولی/آخری که در دونات به هم می‌رسند — برای چشم عادی و انواع کوررنگی اختلاف کافی دارد. **اگر رنگی را عوض کردید، دوباره اعتبارسنجی کنید.**
  - پله‌های حالت شب همان هشت هیو هستند ولی برای زمینه‌ی تیره دوباره چیده شده‌اند؛ برگرداندن رنگ روشن روی زمینه‌ی تیره کار نمی‌کند (بنفش گم می‌شد، زرد می‌زد توی چشم).
  - نقطه و نوار رنگی در HTML با دو متغیر `--dot-l` و `--dot-d` رنگ می‌گیرند و CSS انتخاب می‌کند؛ نمودارها رنگشان را در جاوااسکریپت می‌گیرند، پس با `registerThemedChart()` ثبت می‌شوند تا با عوض شدن حالت شب دوباره رنگ بگیرند.
  - `registerThemedChart` و `onThemeChange` عمداً **بیرون از `DOMContentLoaded`** در `app.js` تعریف شده‌اند: اسکریپت درون‌صفحه‌ای شنونده‌اش را هنگام تجزیه‌ی HTML ثبت می‌کند، یعنی زودتر از شنونده‌ی `app.js` که با `defer` می‌آید — یک بار همین ترتیب صفحه‌ی گزارش را با «registerThemedChart is not defined» شکست.

## نمای دارایی‌ها: چه چیزی در جمع بیاید

- **بخش معاملات یک ردیفِ جمع‌شده است، نه یکی به‌ازای هر کالا.** «مجموع دارایی‌های بخش معاملات» جمعِ `open_cost` همه‌ی معامله‌های بازِ کاربر است با یک کلید. دلیلش این است که این صفحه نمای *ترکیبِ* دارایی است؛ اسمِ تک‌تکِ معامله‌ها (گوشی، سکه، …) فهرست را شلوغ و درصدها را خرد می‌کرد، در حالی که جای دیدن و فروششان `trades.php` است. دارایی‌های ثبت‌شده عمداً جدا جدا (به تفکیک نوع) می‌مانند، چون خودشان ترکیبِ دارایی‌اند نه یک بخش.
- برچسب کنار نام برداشته شد: دو ردیفِ جمع‌شده خودشان اسمشان را می‌گویند و برچسبِ تکراری فقط عرض می‌گرفت و نام را با «…» می‌برید. در همین صفحه `white-space` نام هم `normal` است تا نامِ بلند به خط دوم برود؛ در گزارش دسته‌بندی که ده‌ها ردیف دارد همچنان `nowrap` است.
- **چک و طلب/بدهی هم هر کدام یک قلمِ خالص‌اند.** «خالص چک‌های در جریان» = دریافتی منهای صادره، و «خالص طلب و بدهی» = باقیمانده‌ی طلب منهای باقیمانده‌ی بدهی. هر دو می‌توانند منفی باشند و آن‌وقت از جمع کل کم می‌کنند.
  - **فقط موارد تسویه‌نشده شمرده می‌شوند، و این قابل مذاکره نیست.** چکِ پاس‌شده و پرداختِ انجام‌شده‌ی بدهی از قبل در `walletBalances()` نشسته‌اند؛ آوردنشان اینجا یعنی دوبار شمردنِ همان پول.
  - قلمِ منفی در نمودار دونات نمی‌آید (قاچِ منفی معنا ندارد) ولی در جمع کل هست. درصد هم فقط وقتی نشان داده می‌شود که هم جمع کل مثبت باشد هم خودِ قلم — وگرنه «۰٪» گمراه‌کننده بود.
- هر قلمِ صفحه‌ی دارایی — دارایی ثبت‌شده، **مجموع دارایی‌های بخش معاملات**، **خالص چک‌ها**، **خالص طلب و بدهی**، و **مجموع حساب‌ها** — یک کلید روشن/خاموش دارد. خاموش کردنش چیزی را پاک نمی‌کند؛ فقط از جمع کل، درصدها و نمودار بیرونش می‌گذارد، تا بشود پرسید «دارایی‌ام بدون طلا چقدر است؟» یا «بدون پولِ توی حساب‌ها چقدر؟».
- انتخاب در `localStorage` همان مرورگر می‌ماند (`daftar_asset_off`)؛ چیزی در دیتابیس ذخیره نمی‌شود چون این یک فیلترِ نگاه است، نه داده.
- جمع و درصدها **در جاوااسکریپت** دوباره حساب می‌شوند، پس PHP فقط حالت «همه روشن» را رندر می‌کند.

## زبان طراحی گزینه‌های دوحالته

- هر گزینه‌ی روشن/خاموشِ اپ یک `<label class="switch">` است با همان سه تکه: `input` پنهان، `.switch-track`، `.switch-knob`. قبلاً هر صفحه چک‌باکس خام مرورگر را نشان می‌داد و هر مرورگر شکل خودش را می‌کشید.
- راست‌به‌چپ: خاموش یعنی دستگیره سمت راست (`inset-inline-start`)، روشن یعنی `translateX(-18px)` به چپ. نسخه‌ی کوچک‌تر برای کنار ردیف‌های فهرست: `.switch-sm`.
- تیک «تسویه شد» / «پاس شد» روی کارت‌های طلب و چک عمداً کلید نیست بلکه چک‌مارک است (`.debt-check`) — کارِ «انجام شد» با تنظیمِ روشن/خاموش فرق دارد — ولی از همان رنگ (`--in`) و همان گردی استفاده می‌کند تا یک خانواده به نظر برسند.

## ⛔ متنِ فارسی راست‌چین، عدد چپ‌چین

- صفحه راست‌به‌چپ است و برچسب‌ها باید بمانند، ولی عدد ذاتاً چپ‌به‌راست
  است. مبلغی که در جریانِ راست‌به‌چپ رها شود سه چیز را خراب می‌کند و
  هیچ‌کدام خطا نمی‌دهد: جداکننده و علامتِ منفی سرِ جای اشتباه می‌نشینند،
  ارقامِ هم‌مرتبه در ستون‌های زیرِ هم تراز نمی‌شوند (پس چشم نمی‌تواند
  «۱٬۲۰۰٬۰۰۰» را از «۱۲۰٬۰۰۰» در یک نگاه جدا کند — و این دقیقاً کارِ یک
  دفترِ مالی است)، و واحدِ پول گاهی این‌طرف عدد می‌افتد و گاهی آن‌طرف.
- **قاعده یک جا تعریف می‌شود** (`.ltr-num` و فهرستِ کلاس‌های هم‌گروهش در
  `style.css`) و همه‌ی کلاس‌های عددی به همان یک قاعده می‌پیوندند.
  **کلاسِ عددیِ تازه = یک نامِ تازه در همان فهرست**، نه یک
  `direction: ltr` تازه جای دیگر — وگرنه نسخه‌ی دومی می‌شود که دیر یا
  زود از این عقب می‌افتد.
- ⚠ فقط روی خودِ **مقدار** است، نه روی ظرفش: `.money-card` و
  `.balance-ribbon` برچسبِ فارسی هم دارند و چپ‌چین کردنشان چیدمانِ
  برچسب را خراب می‌کند.

### ⛔ `<select>` جهت را باید روی خودش داشته باشد، نه از ارث

- **خرابیِ واقعی و فقط روی گوشی:** `<html dir="rtl">` هست و در کرومیوم
  `direction` روی هم `select` و هم `option` مقدارِ `rtl` **محاسبه
  می‌شود** (اندازه‌گیری شد)، پس روی دسکتاپ همه چیز درست بود. ولی
  پاپ‌آپِ بازشده‌ی `<select>` روی iOS **کرومِ خودِ سیستم است، نه جعبه‌ای
  که ما رندر می‌کنیم**: وب‌کیت متنِ هر گزینه را به یک منوی بومی می‌دهد و
  آن منو زنجیره‌ی ارث را نمی‌پیماید. نتیجه: فهرستِ دسته‌بندی‌ها
  **چپ‌چین** دیده می‌شد، وسطِ یک اپِ کاملاً راست‌به‌چپ.
- **در اسکرین‌شاتِ گوشی دیده شد و در مرورگرِ دسکتاپ اصلاً بازتولید
  نمی‌شود** — قرینه‌ی همان درسِ `env(safe-area-inset-*)` و منوی کناری:
  «در کروم درست است» اینجا مدرک نیست.
- رفعش دو خط روی خودِ عنصر است: `select, select option { direction: rtl;
  text-align: right }`.
  - **⚠ `text-align: right` عمداً فیزیکی است، نه `start`.** مقدارِ منطقی
    نسبت به **جهتِ پایه‌ی همان رندرکننده** حل می‌شود و همان چیزی است که
    اینجا غلط از آب درمی‌آید؛ مقدارِ فیزیکی دوباره حل نمی‌شود.
  - ⚠ روی کرومیوم این دو خط هیچ چیزی را عوض نمی‌کنند (هندسه‌ی فیلد پیش
    و پس یکی بود: ۳۷۵×۴۵) — یعنی هزینه‌ی صفر، و فقط بستنِ همان یک درز.
- **⚠ حدِ این رفع، صادقانه:** اگر منوی بومیِ iOS جهتش را از **زبانِ خودِ
  گوشی** بگیرد (نه از محتوای صفحه)، هیچ CSS ای به آن نمی‌رسد. تنها راهِ
  بعدی جایگزینیِ `<select>` با یک انتخابگرِ خودمان است — همان کاری که
  یک بار با چک‌باکسِ خامِ مرورگر شد (`.switch`) — ولی آن یعنی دست زدن به
  ۴۱ `<select>` و به `setType()`، `resetSelect()`، `syncPersonPicker()` و
  `smsAutoOk()` که همه به وجودِ خودِ `<select>` بندند. **پس اول این
  کمترین رفع، و فقط اگر روی گوشی جواب نداد آن راه.**
- ⚠ همه‌ی ۴۱ `<select>`ِ اپ محتوای فارسی دارند، پس استثنایی لازم نشد.
  تنها موردِ مرزی `.map-select` در `data.php` است که نامِ ستون‌های فایلِ
  کاربر را نشان می‌دهد (ممکن است لاتین باشد) — و آنجا هم راست‌چین شدن
  با بقیه‌ی صفحه می‌خواند و bidi خودِ واژه‌ی لاتین را درست می‌چیند.

#### ⛔ و آن رفع جواب نداد — حدِ واقعیِ CSS اینجاست

- روی گوشی همان چپ‌چینی ماند، و این بار با **اندازه‌گیریِ پیکسلیِ
  اسکرین‌شات** ثابت شد نه با نگاه: لبه‌ی **چپ** همه‌ی ردیف‌ها روی یک
  عددِ ثابت قفل بود و لبه‌ی راست ناهموار، و تیکِ ✓ هم سمتِ چپ نشسته
  بود. پس منوی بومیِ iOS جهتش را از **زبانِ خودِ گوشی** می‌گیرد، نه از
  محتوای صفحه — همان حدی که بالا نوشته شده بود.
- **⛔ و عرضش هم از همان جنس است.** منو فضای خالیِ زیادی کنارِ متن
  می‌گذارد؛ آن عرض را هم خودِ سیستم تعیین می‌کند و **هیچ CSS ای — روی
  `select`، روی `option`، یا روی ظرفش — به آن نمی‌رسد.** نه `width`، نه
  `padding`، نه `font-size`. اگر روزی کسی خواست «فقط کمی باریک‌ترش کن»،
  جوابش این است: از این سمت اصلاً اهرمی وجود ندارد.
- آن دو خط CSS **حذف نشدند**: هزینه‌شان صفر است (هندسه‌ی فیلد پیش و پس
  یکی بود) و جهتِ خودِ کنترلِ بسته را می‌بندند.
- **⛔ راهِ دومِ نوشته‌شده در بالا (جعبه‌ی خودمان به‌جای منوی بومی) ساخته
  شد و به خواستِ مالکِ نصب پس گرفته شد** (کامیت `c8a4384` و برگشتش).
  کار می‌کرد و در کرومیوم سنجیده شده بود — لبه‌ی راستِ هر پنج ردیف روی
  یک عدد و لبه‌ی چپ ناهموار — ولی یک شیتِ تمام‌عرضِ کفِ صفحه بود و
  مالکِ نصب آن را نپسندید. **اگر روزی دوباره سراغش رفتید، شکلش باید
  یک پاپ‌آپِ باریکِ چسبیده به خودِ فیلد باشد، نه شیتِ کف** — همان چیزی
  که این بار خواسته شد و آن نسخه نداشت. تاریخچه‌اش در گیت هست.

#### ⛔ و آن جعبه هم پس گرفته شد — منوی بومی برگشت

- `optionPicker()` ساخته شد، در کرومیوم اندازه‌گیری شد و کار می‌کرد
  (پایین‌تر مشخصاتش مانده)، ولی **مالکِ نصب پسش گرفت**: «الان خیلی
  شلوغ شده، فرم‌ها رو برگردون به حالت اپل، همون پاپ‌آپ اول». این
  **دومین** باری است که جعبه‌ی خودمان پس گرفته می‌شود — بارِ اول شیتِ
  کفِ صفحه بود (کامیت `c8a4384`).
- **⚠ هزینه‌اش واقعی است و نوشته می‌ماند:** منوی بومیِ iOS دوباره
  چپ‌چین دیده می‌شود و عرضش را هم سیستم تعیین می‌کند — دقیقاً همان
  حدی که بالاتر نوشته شده. **اگر روزی دوباره وسوسه شدید، بدانید که دو
  شکلِ متفاوت امتحان و رد شده‌اند.**
- دو خطِ CSS ای که جهتِ خودِ **کنترلِ بسته** را می‌بندد
  (`select, select option { direction: rtl; text-align: right }`) حذف
  **نشد**: هزینه‌اش صفر است و همان یک درز را می‌بندد.
- **قاعده ۳۲ هم حذف شد، نه اینکه خالی نگه داشته شود.** قاعده‌ای که
  موضوعش رفته و همیشه سبز می‌ماند، همان «سنجشی که روی خرابی سبز
  می‌شود» است. جایش یک بررسیِ کوچک نشست: آن دو خطِ CSS بماند و
  `opt-pop` برنگردد.
  - ⚠ و برداشتنش یک **کوپلینگِ پنهان** را لو داد: قاعده ۳۳ متغیرِ
    `$jsSrc` را از قاعده ۳۲ قرض می‌گرفت، پس با رفتنِ آن، هر پنج
    بررسیِ قاعده ۳۳ روی فایلِ **سالم** قرمز شد. حالا هر قاعده منبعِ
    خودش را می‌خواند.

#### شکلِ آن جعبه، برای روزی که لازم شد — `optionPicker()`

- شکلش دقیقاً همان چیزی است که بالا نوشته شده بود: **پاپ‌آپِ باریکِ
  چسبیده به فیلد**. لبه‌ی راستش با لبه‌ی راستِ فیلد تراز می‌شود، اگر
  زیرِ فیلد جا نبود می‌رود بالایش، و در هر حال داخلِ پنجره می‌ماند.
- **⛔ `width: max-content` تنها چیزی است که شکایتِ اصلی را جواب
  می‌دهد.** جعبه دقیقاً به اندازه‌ی بلندترین گزینه می‌شود، پس فضای
  خالیِ کنارِ متن صفر است. اندازه‌گیری شد: روی فیلدِ ۳۶۲ پیکسلیِ موبایل
  جعبه **۱۵۴** پیکسل درآمد و بلندترین متن **۱۰۰**؛ روی دسکتاپ همان ۱۵۴
  در برابر فیلدِ ۶۸۸.
- **⛔ `<select>` دست‌نخورده می‌ماند و این کلِ نکته است.** مقدار،
  `change`، اعتبارسنجیِ فرم، و `setType()` / `resetSelect()` /
  `syncPersonPicker()` / `smsAutoOk()` همه به وجودِ خودش بندند — ما فقط
  جلوی باز شدنِ منوی بومی را می‌گیریم و `selectedIndex` را می‌نویسیم.
  پس هیچ‌کدام از آن ۴۱ `<select>` عوض نشد.
- فهرست **هر بار از نو** ساخته می‌شود: `setType()` فهرستِ دسته‌ها را از
  نو می‌سازد و یک نسخه‌ی کش‌شده بی‌صدا کهنه می‌ماند.
- `change` فقط وقتی داده می‌شود که مقدار واقعاً عوض شده باشد، و نامِ
  گزینه با `textContent` نوشته می‌شود نه `innerHTML` (نامِ دسته و حساب
  را خودِ کاربر نوشته).
- **⚠ سه اشتباه که فقط اندازه‌گیری پیدایشان کرد، نه بازبینی:**
  1. **`inset-inline-start: 0` در CSS جعبه را به لبه‌ی *پنجره* می‌چسباند،
     نه به فیلد.** در راست‌به‌چپ آن به `right` نگاشت می‌شود و کنارِ
     `left`ی که جاوااسکریپت می‌نویسد جعبه را بیش‌ازحد مقید می‌کند، پس
     `left` **بی‌صدا** نادیده می‌ماند. روی عرضِ ۱۴۰۰ لبه‌ی راستِ فیلد
     ۱۰۳۷ بود و جعبه روی ۱۳۸۵ نشست. رفعش `left: 0` در CSS است.
  2. **`scrollIntoView` برای رساندنِ گزینه‌ی انتخاب‌شده یک رویدادِ
     `scroll` می‌دهد** و شنونده‌ی «با اسکرول ببند» جعبه را **در همان
     لحظه‌ی باز شدن** می‌بست — آن هم فقط وقتی که فهرست از جعبه بلندتر
     بود، یعنی یک باگِ گاه‌به‌گاه. حالا اسکرولِ داخلِ خودِ لایه استثناست.
  3. **قفلِ اسکرول باید دستی گرفته شود.** `watchOverlays()` فهرستِ
     لایه‌ها را هنگام بارگذاری **یک بار** می‌گیرد و این لایه آن موقع
     وجود ندارد.
- **⚠ `text-align: right` باز هم فیزیکی است، نه `start`** — همان دلیلِ
  بالا، این بار داخلِ جعبه‌ی خودمان.
- اندازه‌گیری شد (کرومیوم، موبایل و دسکتاپ): لبه‌ی **راستِ** هر پنج ردیف
  روی یک عدد قفل است (۳۳۷ / ۱۳۴۶) و لبه‌ی چپ ناهموار (۲۳۷ تا ۲۷۸) —
  یعنی دقیقاً برعکسِ چیزی که منوی بومی می‌داد.
- **قاعده ۳۲ در `test_api_contract.php`** چهار ستونِ این کار را پین
  می‌کند (`width: max-content`، `text-align: right`ِ فیزیکی، خواندنِ جای
  فیلد از `sel.getBoundingClientRect()`، و `left` به‌جای
  `inset-inline-start`) و با **نُه جهش** سنجیده شد.
  - ⚠ و یکی از آن‌ها اول **زنده ماند**: بررسیِ «جای فیلد را می‌خواند»
    دنبالِ هر `getBoundingClientRect`ی می‌گشت، ولی خودِ جعبه هم یک بار
    اندازه گرفته می‌شود — پس با گذاشتنِ جای ثابت هم سبز می‌ماند. حالا
    `sel.` را هم می‌خواهد. **جهشِ زنده‌مانده اول یعنی تست ناقص است، نه
    اینکه کد امن است.**
  - ⚠ و یک هشدارِ الکی روی فایلِ **سالم**: لوک‌اِهدِ
    `innerHTML\s*=\s*(?!'')` عقب می‌نشیند و همان `innerHTML = ''`ِ
    درست را هم می‌گیرد. حالا شمرده می‌شود.

## میله‌ی پیشرفت همیشه از چپ پر می‌شود

- دو خانواده میله در اپ هست و هر دو `direction: ltr` روی *ترک* دارند: `.budget-bar-track` (بودجه، پس‌انداز، پرداخت طلب و بدهی) و `.cat-breakdown-bar-track` (گزارش دسته‌بندی، دارایی).
- **صفحه راست‌به‌چپ است ولی میله‌ی پیشرفت نباید باشد.** بدون آن یک خط، بچه‌ی بلاکیِ داخل ترک از لبه‌ی *راست* شروع می‌کرد و «۳۵٪ پرداخت‌شده» به شکل ۳۵٪ سمت راست دیده می‌شد — خلافِ چیزی که چشم از نوار پیشرفت انتظار دارد، و در کنارِ درصدی که همان‌جا نوشته شده گیج‌کننده بود.
- گرادیان‌های `linear-gradient(90deg, …)` روی خودِ میله فیزیکی‌اند و با جهت صفحه عوض نمی‌شوند، پس دست نخوردند.
- `.switch-track` و `.seg` عمداً بیرون این قاعده‌اند: آن‌ها کنترل‌اند نه نمایش پیشرفت، و جهتشان باید با صفحه بخواند.
- **میله‌ی تازه‌ای که اضافه می‌کنید هم باید همین را داشته باشد.** سنجشش هم آسان است: عرضِ پرشده باید کمتر از ۱۰۰٪ باشد و لبه‌ی چپش با لبه‌ی چپِ ترک یکی — با ۱۰۰٪ اصلاً جهت معلوم نمی‌شود.

## سرعت

- **همه‌ی صفحه‌ها `Cache-Control: no-store, private` می‌گیرند** (در `Auth::initSession()`). بدون این، سافاری روی آیفون نسخه‌ی کش‌شده را نشان می‌داد: کاربر چیزی را حذف می‌کرد، صفحه تازه می‌شد و باز همان عدد قدیمی بود. این عمداً bfcache را هم خاموش می‌کند — رندر سمت سرور ۴ تا ۸ میلی‌ثانیه است، پس ارزشش را دارد.
- **Chart.js در مخزن است** (`assets/js/chart.umd.js`، MIT، نسخه‌ی ۴.۴.۰). قبلاً از jsDelivr می‌آمد و روی اینترنت داخلی همان یک درخواست گاهی چند ثانیه طول می‌کشید یا اصلاً نمی‌رسید و نمودار خالی می‌ماند. حالا از خود سرور می‌آید با gzip (۲۰۵ KB → ۷۰ KB) و کش یک‌ساله‌ی immutable.
- **CSS و JS مستقیم از وب‌سرور می‌آیند و هیچ فایل ثابتی از PHP رد نمی‌شود** (`assetUrls()`). این یک باگ واقعی را بست: نسخه‌ی قبلی همه را از یک اسکریپت PHP (`assets/serve.php`) تحویل می‌داد، پس هر بارگذاری صفحه چهار پروسه‌ی PHP-FPM می‌گرفت (HTML + CSS + JS + chart.js) از `pm.max_children = 5` — و کاربر می‌دید سایت وسط کار چند ثانیه قفل می‌کند. حالا هر بارگذاری **یک** پروسه می‌خواهد. آن اسکریپت و کلید `ASSET_DELIVERY` حذف شدند؛ اگر روزی وسوسه شدید دوباره از PHP تحویلشان بدهید، دلیلش همین است.
  - **دام مسیر فونت:** `@font-face` مسیر نسبی دارد و نسبت به آدرسِ خودِ CSS حل می‌شود، پس `../fonts/…` درست است (`assets/css/` → `assets/fonts/`). کامنت کهنه‌ی بالای همان قاعده در `style.css` عکسِ این را می‌گفت و با حذف `serve.php` اصلاح شد.
- **pool پی‌اچ‌پی از `ondemand`/۵ به `dynamic`/۱۲ رفت** با دو پروسه‌ی همیشه گرم (`deploy/vps-setup.sh`). روی نصب موجود: `sudo bash deploy/tune.sh --apply` — فقط `pool.d/hesab.conf` را می‌نویسد و php-fpm را `reload` می‌کند (نه `restart`)، و اگر تست پیکربندی رد شود خودش برمی‌گرداند.
- **`assets/manifest.php` هدر کش و ETag دارد**؛ بدون آن مرورگر با هر ناوبری دوباره می‌گرفتش و یک پروسه‌ی PHP دیگر می‌خورد.
- **اسکریپت‌ها با `defer` در `<head>`اند**، نه ته `<body>`: دانلودشان موازی با خواندن HTML شروع می‌شود ولی اجرا مثل قبل بعد از ساخته‌شدن صفحه است. فونت هم `preload` دارد چون داخل CSS تعریف شده و مرورگر دیر می‌فهمید لازمش دارد.
- **داشبورد از ۸ کوئری به ۳ رسید:** جمع روزانه‌ی کل تاریخچه یک بار خوانده می‌شود و آمار امروز/هفته/ماه/سال/بازه‌ی دلخواه و شمارش‌ها همه در PHP از رویش ساخته می‌شوند.
- **پرسش‌های ساختار دیتابیس یک کوئری‌اند، نه پنج تا.** `schemaMap()` یک بار در هر درخواست کل نقشه‌ی جدول/ستون را می‌خواند و `tableExists()` / `tableHasColumn()` / `tradesTablesExist()` از حافظه جواب می‌گیرند. نسخه‌ی قبلی برای هر پرسش یک کوئری جدا به `information_schema` می‌زد و فقط داشبورد پنج‌تایش را می‌زد. عمداً بین درخواست‌ها کش نمی‌شود تا بعد از اجرای یک migration تازه، همان درخواست بعدی ساختار واقعی را ببیند.
- **`activeWallets()` فهرست حساب‌ها را در همان درخواست کش می‌کند.** شیت ثبت تراکنش در فوتر هر صفحه است و همان فهرستی را می‌خواست که خودِ صفحه هم گرفته بود — دو بار پشت سر هم یک کوئری.
- شمارش واقعی بعد از این دو: خانه ۷ کوئری، داشبورد ۱۵، حساب‌ها ۱۰، چک‌ها ۱۲، معاملات ۷. رندر سمت سرور ۴ تا ۸ میلی‌ثانیه و HTML گزیپ‌شده حدود ۸ کیلوبایت.

### ⛔ دورِ سومِ کندی — چهار خزشِ بی‌صدا که فقط اندازه‌گیری پیدا کرد

با اضافه شدنِ قابلیت‌ها، شمارشِ بالا **سه برابر** شده بود: خانه ۷ → **۳۱**،
حساب‌ها ۱۰ → **۲۰**، معاملات ۷ → **۲۰**، و `due.php` روی **۵۴** — بیشترین
صفحه‌ی کلِ اپ. هیچ‌کدام یک تغییرِ بد نبودند؛ **حاصلِ جمعشان** بود، و همین
چیزی است که در بازبینیِ هیچ کامیتی دیده نمی‌شود. با لاگِ کوئریِ MariaDB
روی صفحه‌های واقعی گرفته شد، نه با خواندنِ کد.

- **`getSetting()` در هر درخواست کش می‌شود.** `planReadOnly()` در نوارِ
  کناری و شیتِ «بیشتر» **هفت** بار صدا زده می‌شود (هر نشانِ قفل یک بار) و
  هر کدام `plan_enforced` را از نو می‌خواند — یعنی **شش کوئریِ تکراری در
  هر صفحه‌ی اپ**، و هر نشانِ قفلِ فردا یکی بیشتر.
  - `null` یعنی «ردیف نیست» و با «نخوانده‌ایم» فرق دارد، پس با
    `array_key_exists` سنجیده می‌شود؛ و مقدارِ `$default` **کش نمی‌شود**،
    وگرنه `getSetting('x','0')` و `getSetting('x','1')` روی کلیدِ نبوده یک
    جواب می‌دادند.
  - **⛔ `forgetSetting()` تنها مسیرِ حذف است.** `DELETE FROM app_settings`
    دستی، کش را نمی‌بیند و مقدارِ برداشته‌شده تا آخرِ درخواست زنده می‌ماند.
    سه تستِ `plans`/`signup`/`sms_login` دقیقاً همین را گرفتند.
- **`budgetStatuses()` یک کوئری برای هر بازه می‌زند، نه برای هر بودجه.**
  حلقه‌ی قبلی یک `SUM` جدا داشت، پس کاربر با ده بودجه ده کوئریِ اضافه
  می‌داد — و این تابع هم در صفحه‌ی بودجه صدا زده می‌شود هم در «پول قابل
  خرج» روی خانه. گروه‌بندی روی **بازه** است نه «همه با هم»، چون هفتگی و
  ماهانه و دلخواه بازه‌های متفاوتی دارند. برابریِ عددی با نسخه‌ی قبلی روی
  سه بازه‌ی متفاوت (و دسته‌ای بدونِ هیچ تراکنش) سنجیده شد.
- **`Schedule::materializeAll()` دو کوئریِ گروهی می‌زند، نه دو تا در هر
  قانون.** نسخه‌ی قبلی برای هر قانون `materialize()` را صدا می‌زد و آن هم
  بی‌قید یک `COUNT` و یک `UPDATE` می‌زد — حتی وقتی هیچ کارِ تازه‌ای نبود،
  که حالتِ **همیشگی** است. ⚠ سوییپِ `overdue` بدونِ `reminder_id` هم
  دقیقاً همان کار را می‌کند و پیش‌پویش فقط سرعت است: `materialize()` باید
  به‌تنهایی هم درست بماند (از `close()` هم صدا زده می‌شود)، پس **جهشِ
  «دروازه را بردار» عمداً زنده می‌ماند**.
- **`syncScheduleRules()` ردیف‌ها را جمع می‌کند و با یک دستور می‌نویسد.**
  به‌ازای هر چک، هر بدهی و هر تراکنشِ دوره‌ای یک `INSERT … ON DUPLICATE
  KEY` جدا می‌زد — و این تابع در **هر** بارگذاری `due.php` اجرا می‌شود.
  یعنی کاربری با ۵۰ چک و ۵۰ بدهی صد کوئریِ **نوشتن** در هر بار باز کردنِ
  آن صفحه. ⚠ `$flush()` باید **پیش از** سوییپِ «قانونِ بی‌منبع را ببند»
  صدا شود؛ جهشِ برداشتنش تست را شکست.

نتیجه‌ی اندازه‌گیری‌شده: خانه ۳۱ → **۲۴**، `due.php` ۵۴ → **۳۱**، حساب‌ها
۲۰ → **۱۴**، معاملات ۲۰ → **۱۳**، داشبورد ۲۳ → **۱۷**. و مهم‌تر از عددِ
امروز: سه تا از این چهار مورد **با تعدادِ رکوردهای کاربر خطی رشد می‌کردند**.

### ⛔ موجودیِ حساب کش نمی‌شود — یک بار امتحان شد و پس داده شد

- `walletBalances()` سنگین‌ترین کوئریِ اپ است (شش منبعِ پول با LEFT JOIN و
  تجمیع) و روی صفحه‌ی خانه **دو بار** اجرا می‌شد: یک بار از
  `financialHighlights()` → `safeToSpend()` → `totalBalance()` و یک بار از
  `pinnedWallets()`.
- **کشِ درخواستی رویش گذاشته شد و پنج مجموعه‌ی تست همان‌جا قرمز شدند:** هر
  مسیری که پول می‌نویسد و بعد موجودی می‌خواند، عددِ **پیش از** تغییر را
  می‌گرفت — نه خطایی، نه استثنایی، فقط یک عددِ غلط در پاسخ. **حقیقتِ پول
  در حافظه نمی‌ماند.**
- راهِ درست **پاس دادن** است، نه کش کردن: `totalBalance()` و
  `pinnedWallets()` پارامترِ `$rows` گرفتند و `safeToSpend()` /
  `financialHighlights()` پارامترِ `$walletRows`؛ `index.php` یک بار
  می‌خواند و به هر دو مسیر می‌دهد. تازگی را فراخواننده تضمین می‌کند، پس
  چیزی نمی‌تواند کهنه بماند.
- **قاعده ۲۹ در `test_api_contract.php`** برگشتِ کش را ممنوع می‌کند و
  ماندنِ آن چهار پارامتر را هم می‌سنجد — وگرنه کسی که کش را برمی‌دارد
  جایگزینی ندارد و دوباره وسوسه می‌شود. با دو جهش سنجیده شد.
  - ⚠ نسخه‌ی اولِ خودِ قاعده روی فایلِ **سالم** قرمز شد: بدنه‌ی تابع را با
    «تا اولین `}`» می‌برید و هر `if`ِ داخلی یکی از آن‌ها دارد، پس تا نصفِ
    فایل جلو می‌رفت و `static`ِ تابعِ **دیگری** را پیدا می‌کرد. حالا تا
    **تعریفِ تابعِ بعدی** بریده می‌شود.
### ⛔ `tests/test_query_budget.php` — خزشِ سرعت باید خودش را نشان بدهد

- **دلیل وجودش:** هیچ‌کدام از آن چهار خزشِ بالا یک کامیتِ بد نبودند؛
  **حاصلِ جمعشان** بودند، و آن در بازبینیِ هیچ کامیتی دیده نمی‌شود. تا
  امروز تنها راهِ دیدنشان این بود که کسی بگوید «کند شده» و بعد لاگِ
  کوئریِ MariaDB روی سرور روشن شود. حالا ۲۷ صفحه سقفِ ثبت‌شده دارند و
  اولین کامیتی که از سقف رد شود همان‌جا قرمز می‌شود.
- **⛔ فهرستِ `BUDGET` بسته است** (مثل `MIGRATIONS` و `EXPECT`): هر
  `*.php` تازه‌ای در ریشه یا `admin/` که در `BUDGET` یا `NO_BUDGET`
  نباشد تست را می‌شکند، و هر نامِ کهنه‌ای هم. بدونِ آن، صفحه‌ی فردا
  بی‌صدا بیرونِ پوشش می‌ماند و تست به‌مرور بی‌ارزش می‌شد.
- **⛔ سنجشِ «رشد با حجمِ داده» مهم‌تر از خودِ سقف است.** سه تا از آن چهار
  خزش **خطی با تعدادِ رکوردهای کاربر** بزرگ می‌شدند، یعنی روی دیتابیسِ
  توسعه بی‌ضرر به نظر می‌رسیدند. تست همان کاربر را با دو حجمِ داده
  (۲ و ۲۰ برابر) می‌سنجد و اختلافِ بیش از ۳ کوئری را شکست می‌گیرد.
- **⛔ بودجه‌ی گشاد هم شکست است.** اگر صفحه‌ای ۸ کوئری زیرِ سقفش باشد،
  تست می‌گوید سقف را پایین بیاورید — وگرنه سقف‌ها به‌مرور آن‌قدر گشاد
  می‌شوند که دیگر چیزی را نمی‌گیرند. **همین بررسی سه سقفِ دست‌ودل‌بازِ
  خودم را گرفت.**
- ⚠ **گرم کردن پیش از سنجش اجباری است:** `Notify::generateFor()`،
  `recordNetWorthSnapshot()` و `processRecurringTransactions()` کارِ
  «یک بار در روز» می‌کنند، پس اولین بازدید کوئریِ بیشتری دارد و بدونِ
  گرم کردن، عددها به ترتیبِ اجرا بند می‌شدند. کمترینِ سه اجرا گرفته
  می‌شود، نه میانگین.
- ⚠ **شمارشِ کوئری کافی نیست، «چند ردیف خوانده شد» هم لازم است.** جهشِ
  «پنجره‌ی `RECENT_TITLE_SCAN` را بردار» با شمارشِ کوئری **زنده ماند**
  (تعداد عوض نمی‌شود، فقط جدول کامل خوانده می‌شود). گروهِ آخر با
  `Handler_read_*` می‌سنجدش.
  - ⚠ و نسخه‌ی اولِ همان گروه **هم مرزش و هم عمقِ داده‌اش** را از
    `RECENT_TITLE_SCAN` می‌گرفت، یعنی جهش مرزِ خودِ تست را هم جابه‌جا
    می‌کرد و باز هم سبز می‌ماند — **همان دامی که یک بار سرِ
    `BACKUP_AFTER_DAYS` افتادیم**. حالا هر دو عددِ ثابت‌اند و خودِ
    مقدارِ `RECENT_TITLE_SCAN` جداگانه سنجیده می‌شود.
  - ⚠ `Innodb_rows_read` روی این نصبِ MariaDB **وجود ندارد** و بی‌صدا
    `false` می‌داد (یعنی همیشه صفر). جمعِ `Handler_read_next/rnd_next/
    key/first` گرفته می‌شود.
- با پنج جهش سنجیده شد: N+1 در `budgetStatuses()`، برداشتنِ پنجره‌ی
  عنوان‌ها، درجِ تک‌ردیفی در `syncScheduleRules()`، برداشتنِ کشِ
  `getSetting()`، و بزرگ کردنِ خودِ پنجره.

### ایندکس‌ها: `migration_indexes2.sql` — فقط دو تا، و هر دو سنجیده

- **⛔ ایندکسِ حدسی هزینه‌ی نوشتن را بالا می‌برد بی‌آنکه چیزی را تندتر
  کند.** بیشترِ جدول‌های تازه از قبل ایندکسِ درست داشتند؛ فقط دو کوئری
  واقعاً پویشِ کاملِ جدول می‌کردند: شمارشِ اعلانِ نخوانده در
  `includes/header.php` (روی **هر صفحه**) و «چکِ در جریان» از
  `chequeActiveSql()`. هر دو با `EXPLAIN` پیش و پس سنجیده شدند:
  `ALL` → `ref` + `Using index`.
- **⚠ سنجشِ تک‌کاربره دروغ می‌گوید.** روی جدولی که همه‌ی ردیف‌هایش مالِ
  یک کاربر است، اپتیمایزر هر ایندکسِ `user_id` را کنار می‌گذارد و پویشِ
  کامل واقعاً ارزان‌تر است — یعنی سنجش می‌گوید «ایندکس بی‌فایده است».
  با **دو** کاربرِ انبوه سنجیده شد.
- **ایندکسِ قدیمی حذف نشد** حتی جایی که پیشوندِ ایندکسِ تازه است:
  `DROP INDEX` روی نصبی که آن را ندارد یک شاخه‌ی تازه‌ی شکست می‌سازد، و
  هزینه‌ی نوشتنِ یک ایندکسِ اضافه روی جدولِ چندصد ردیفی ناچیز است.
- **سومین کوئریِ داغ ایندکس نمی‌خواست، کوئریِ بهتر می‌خواست:**
  `recentTransactionTitles()` روی کلِ تاریخچه `GROUP BY` می‌زد. با
  پنجره‌ی `RECENT_TITLE_SCAN` (۴۰۰ ردیفِ آخر) از ۵.۵ به ۱.۱ میلی‌ثانیه
  رسید — و آن تابع در فوترِ **هر صفحه** اجرا می‌شود.
- شاهدش در `migrate.sh` از نوعِ تازه‌ی `جدول:ایندکس` است؛ شاهدِ «وجود
  جدول» اینجا همیشه درست می‌بود و `--verify` همان دروغی را می‌گفت که
  برای گرفتنش ساخته شده.

### ⛔ دورِ سومِ ایندکس — کندی‌ای که **تعدادِ کوئری نشانش نمی‌داد**

- **مسئله‌ی واقعی، و از زیرِ همه‌ی بررسی‌های بالا رد شده بود:** تعدادِ
  کوئریِ هیچ صفحه‌ای عوض نمی‌شد، فقط هر کدامشان با بزرگ شدنِ تاریخچه‌ی
  کاربر کندتر می‌شدند. روی دیتابیسِ توسعه (چند صد تراکنش) کاملاً
  نامرئی است؛ `test_query_budget` هم فقط ۶۰ تراکنش می‌سازد.
- **پیدا شدنش با اندازه‌گیری بود، نه با خواندنِ کد:** یک دیتابیسِ
  **دوکاربره با ۲۰٬۰۰۰ تراکنش برای هر کاربر** ساخته شد، همه‌ی ۱۰۲
  کوئریِ متمایزِ اپ با لاگِ کوئریِ MariaDB جمع و `EXPLAIN` شدند.
- **سه ایندکس در `migration_indexes3.sql`، و هر سه سنجیده:**
  1. `idx_user_created (user_id, created_at)` — فهرستِ «آخرین
     تراکنش‌ها» روی خانه `ORDER BY created_at DESC LIMIT 8` است و
     **هیچ ایندکسی `created_at` نداشت**. یعنی برای نشان دادنِ **۸**
     ردیف، کلِ تاریخچه خوانده و در حافظه مرتب می‌شد (`filesort`).
     همین ایندکس زیرکوئریِ «آخرین فعالیت» در `admin/users.php` را هم
     می‌پوشاند.
  2. `idx_user_date_created (user_id, transaction_date, created_at,
     type, amount)` — یک ایندکس برای **دو** کار: مرتب‌سازیِ دوکلیدیِ
     صفحه‌ی تراکنش‌ها، و پوششیِ جمعِ روزانه‌ی داشبورد. نسخه‌ی اول دو
     ایندکسِ جدا بود و با یکی کردنشان همان عددها درآمد.
  3. `idx_user_wallet_sum (user_id, wallet_id, type, amount)` —
     `walletBalances()`، سنگین‌ترین کوئریِ اپ. `idx_user_wallet_date`
     ستون‌های گروه‌بندی را داشت ولی `type`/`amount` را نه، پس برای هر
     ردیف به جدول برمی‌گشت.
- **نتیجه، روی همان ۲۰٬۰۰۰ تراکنش (کمترینِ پنج اجرا، سمتِ سرور):**

  | صفحه | پیش | پس |
  |---|---|---|
  | `index.php` | ۵۵٫۷ ms | **۱۹٫۹ ms** |
  | `transactions.php` | ۲۶٫۲ ms | **۶٫۸ ms** |
  | `dashboard.php` | ۳۵٫۴ ms | **۱۹٫۳ ms** |

- **⚠ هزینه‌اش نوشته می‌ماند:** با هر سه ایندکس، درجِ یک تراکنش از
  ۰٫۰۶۵۷ به ۰٫۰۷۱۳ میلی‌ثانیه رفت (+۸٫۵٪). اندازه‌گیری شد، حدس زده
  نشد — و چون کاربر روزی چند تراکنش ثبت می‌کند و صفحه‌ها را ده‌ها بار
  باز می‌کند، معامله یک‌طرفه است.
- **⛔ `idx_user_wallet_sum` عمداً در `schema.sql` **نیست**.** ستونِ
  `transactions.wallet_id` آنجا وجود ندارد و `migration_repair.sql`
  بعداً اضافه‌اش می‌کند؛ نوشتنش در همان `CREATE TABLE` نصبِ **تازه** را
  با «Unknown column 'wallet_id'» می‌خواباند — روی نصبِ **موجود**
  کاملاً نامرئی. **همان دامِ ستون‌های `icon`/`color`، و باز هم
  `test_migration_chain` گرفتش نه بازبینیِ چشمی.** به همین دلیل شاهدِ
  خودِ migration هم روی **ستون** است نه جدول.
- **گروهِ تازه‌ی `test_query_budget`: «فهرستِ کوتاه کلِ تاریخچه را
  نمی‌خواند».** با `Handler_read_*` روی همان fixture ۱۲۰۰تایی سنجیده
  می‌شود: `index.php` و `transactions.php` با ایندکس **۳۰۱۰** و
  **۲۶۳۳** ردیف می‌خوانند، بدونِ آن **۴۱۴۲** و **۳۷۸۲**. جهش (برداشتنِ
  هر دو ایندکس) هر دو بررسی را قرمز کرد.
  - ⚠ **جدایی باریک است و دلیلش نوشته می‌ماند:** کفِ هر دو عدد کارِ
    صفحه‌های دیگر است — یک صفحه‌ی بی‌فهرست (`privacy.php`) روی همین
    fixture **۱۳۷۹** ردیف می‌خواند، و `walletBalances()` هم عمداً کلِ
    تاریخچه را جمع می‌زند. پس سقف نزدیک به عددِ سالم بسته شد: خطا به
    سمتِ «قرمزِ الکی» بهتر از «سبزِ دروغین» است، چون اولی دیده می‌شود.
  - ⚠ و **شکلِ هر سه ایندکس جدا پین می‌شود**، نه فقط وجودشان: اگر
    `type`/`amount` از انتهای دو تای آخر بیفتد، پوششی بودن بی‌صدا از
    بین می‌رود و عددِ ردیف‌ها هنوز زیرِ سقف می‌ماند.

### ⛔ `app_settings` جای تنظیماتِ **نصب** است، نه یک ردیف به‌ازای هر کاربر

- **خرابیِ واقعی، و دو لایه داشت:** `seedUserDefaults()` نشانه‌ی
  «پیش‌فرض‌های این کاربر ساخته شد» را با کلیدِ `seeded_user_42` در
  `app_settings` می‌نوشت — یعنی یک ردیف **به‌ازای هر کاربر**، برای
  همیشه، در جدولی که باید چند ده تنظیمِ نصب باشد.
- **و بدتر: آن ردیف‌ها هرگز پاک نمی‌شدند.** `deleteUserAccount()`
  جدول‌ها را از روی ستونِ `user_id` پیدا می‌کند (`userDataTables()`) و
  `app_settings` چنین ستونی ندارد، پس کاربر حذف می‌شد و نشانه‌اش
  می‌ماند. روی دیتابیسِ توسعه شمرده شد: **۱۴۷ ردیف، که فقط ۴ تای آن‌ها
  به کاربرِ موجودی می‌خورد** — ۱۴۳ تا یتیمِ کاربرانِ حذف‌شده. همان
  «حذفی که چیزی جا می‌گذارد» که `userDataTables()` برای نبودنش نوشته شد.
- **رفع: `users.defaults_seeded_at`** (`migration_seed_flag`). migration
  نشانه‌های موجود را **دقیق** منتقل می‌کند و بعد ردیف‌ها را پاک می‌کند.
  - **⛔ خودِ نشانه حذف نشد و نباید بشود:** بدونش کاربری که بانک‌های
    پیش‌فرض را **عمداً** پاک کرده، با بازدیدِ بعدی همه را پس می‌گیرد —
    همان استدلالِ `more_categories_seeded`.
  - ⚠ تطبیقِ نقل **عددی** است نه رشته‌ای (شناسه از نامِ کلید بیرون
    کشیده و با `id` مقایسه می‌شود)، پس هیچ مقایسه‌ی ستون‌به‌ستونِ متنی
    و هیچ «Illegal mix of collations» ای ممکن نیست.
  - ⚠ `\_` در `LIKE` عمدی است: `_` خودش وایلدکارتِ تک‌کاراکتری است.
  - ⚠ کد اگر ستون نیامده باشد همان رفتارِ قبلی را دارد، پس نصبِ
    migration‌نخورده نه می‌شکند نه پیش‌فرض‌های پاک‌شده را پس می‌دهد.

### `getSetting()` کلِ جدول را یک بار می‌خواند، نه یک کوئری برای هر کلید

- کشِ per-key از قبل بود (و همان شش کوئریِ تکراریِ `plan_enforced` را
  بست)، ولی هر کلیدِ **متفاوت** هنوز یک رفت‌وبرگشتِ جدا داشت. یک صفحه
  سه تا پنج کلید می‌خواند.
- اندازه‌گیری: کلِ جدول ۰٫۱۷۴ میلی‌ثانیه در برابر ۰٫۱۱۹ برای **یک**
  کلید — یعنی از کلیدِ دوم به بعد مجانی است. نتیجه روی صفحه‌ها:
  `admin/access.php` از **۲۳ به ۱۱** کوئری، `pro.php` و `profile.php` و
  `admin/billing.php` هر کدام ۳ تا کمتر، `privacy.php` ۲ تا.
- **⛔ و این تا پیش از پاک شدنِ آن ۱۴۷ ردیف عاقلانه نبود.** اگر روزی
  کسی دوباره کلیدِ per-user در `app_settings` بنویسد، همین خط همان
  لحظه غلط می‌شود.
- ⚠ «همه را خوانده‌ایم» جدا از خودِ نگاشت نگه داشته می‌شود، وگرنه
  **کلیدِ نبوده** هر بار کلِ جدول را دوباره می‌خواند — همان کوئریِ
  تکراری، فقط گران‌تر.
- ⚠ معنای `null` («ردیف نیست») و کش نشدنِ `$default` هر دو دست‌نخورده
  ماندند: چون *همه‌ی* ردیف‌ها خوانده شده‌اند، «در نگاشت نیست» دقیقاً
  همان «ردیفش وجود ندارد» است.
- ⚠ و بودجه‌ی آن شش صفحه در `test_query_budget` پایین آورده شد. خودِ
  تست مجبورش کرد («بودجه‌ی گشاد هم شکست است») — وگرنه عددِ کهنه اجازه
  می‌داد همان مقدار دوباره خزش کند.

- **هر تنظیمی که ظاهرِ منو را عوض می‌کند هم باید بلافاصله دیده شود.** کلید «بخش معاملات» در پروفایل بعد از ذخیره صفحه را تازه می‌کند، چون شیت «بیشتر» و نوار کناری با حالت قبلی رندر شده‌اند؛ بی‌تازه‌سازی، دکمه‌ی معاملات تا ناوبری بعدی سر جایش می‌ماند و کاربر فکر می‌کند کلید کار نکرد. همین قاعده برای افزودن/حذف در «فهرست‌های من» هم هست.
- **هر تغییر پول باید بلافاصله دیده شود.** حذف تراکنش/طلب/چک فقط ردیف را برنمی‌دارد؛ صفحه هم تازه می‌شود، چون جمع‌ها و موجودی بالای صفحه از روی همان رکورد ساخته شده‌اند و بی‌تازه‌سازی کاربر فکر می‌کند حذف انجام نشده.

### `deploy/perf-report.sh` — «کند است» یک علامت است، نه یک علت

- اجرا: `sudo bash deploy/perf-report.sh` — و برای سنجش صفحه‌های داخلی `--login نام‌کاربری` (رمز پرسیده می‌شود و روی صفحه نمی‌آید). فقط می‌خواند: هیچ فایلی نمی‌نویسد، هیچ سرویسی را `reload`/`restart` نمی‌کند.
- **دلیل وجودش:** «کند است» می‌تواند چهار علتِ کاملاً جدا داشته باشد — PHP کند (opcache، کوئری)، پروسه‌های کم (صف شدن)، فایل‌های بزرگ/بی‌کش، یا شبکه. راهِ هر کدام فرق می‌کند و حدس زدن بینشان هزینه‌ی چند روز کار اشتباه دارد. اسکریپت هر چهار را جدا اندازه می‌گیرد.
- **مهم‌ترین عددش «زمان پاسخ از روی خودِ سرور» است** (بخش ۸): `time_starttransfer` با curl از روی خودِ ماشین، پس شبکه‌ی بین گوشی و سرور در آن نیست. کوچک بودنِ این عدد در کنارِ کندیِ محسوس روی گوشی یعنی مسئله شبکه است و **هیچ بهینه‌سازیِ کدی حلش نمی‌کند**. کمترینِ سه نمونه گرفته می‌شود، نه میانگین، تا یک نوسان اتفاقی داوری را خراب نکند.
- **opcache را باید در SAPI ای سنجید که سایت با آن اجرا می‌شود (fpm)، نه در خط فرمان** — این دو `php.ini` جدا دارند. اسکریپت با `-c` همان `php.ini` و با `PHP_INI_SCAN_DIR` همان `conf.d`ی fpm می‌سنجد.
  - و **اگر نتوانست بسنجد، ادعا نمی‌کند خاموش است**: `extension_loaded()` جدا بررسی می‌شود. نسخه‌ی اول اگر باینریِ نسخه‌دار (`php8.3`) نبود، `ini_get` رشته‌ی خالی می‌داد و اسکریپت «opcache خاموش است» اعلام می‌کرد — یعنی بزرگ‌ترین یافته‌ی گزارش می‌توانست کاملاً ساختگی باشد.
- **رمز دیتابیس از راه `--defaults-extra-file` می‌رود، نه `mysql -pرمز`** — که هر کاربر دیگری روی همین سرور در `ps aux` می‌بیند. روی این VPS سرویس‌های دیگری هم هستند، پس این تفاوت واقعی است. فایل با `chmod 600` ساخته و با یک `trap` مشترک پاک می‌شود (دو `trap EXIT` جدا همدیگر را می‌کشند).
- مسیرها با متغیر محیطی قابل جایگزینی‌اند (`APP_DIR`, `PHP_ROOT`, `SITE_FILE`, `DOMAIN`, `SCHEME`) — نه برای انعطاف، بلکه چون بدون آن هیچ‌کدام از این شاخه‌ها **آزمودنی نبودند**. همه‌ی شاخه‌ها (opcache روشن/خاموش/نامعلوم، pool کم‌ظرفیت، صفحه‌ی کند) با ساختنِ همان وضعیت واقعاً اجرا و دیده شده‌اند.
- تلاش ناموفقِ `--login` مثل هر تلاش دیگری در سدِ `LoginThrottle` شمرده می‌شود؛ پیام خطا خودش راهِ باز کردن قفل را می‌گوید.

## بخش معاملات (خرید و فروش)

- دفتری **جدا از درآمد/هزینه** برای خرید و فروش (`trades.php`). معامله هرگز ردیف `transactions` نمی‌سازد — سودش فقط در صفحه‌ی خودش است و گزارش‌های درآمد/هزینه دست‌نخورده می‌مانند. تست جدایی در `tests/test_trades.php` است.
- جدول‌ها: `trades` (خرید؛ qty اعشاری برای طلا/ارز) و `trade_sales` (فروش‌ها؛ فروش جزئی ممکن است). ساخته‌شده با `migration_trades.sql`.
- **پیش‌فرض خاموش؛ هر کاربر برای خودش** روشن می‌کند (ستون `users.trades_enabled`، کلید در پروفایل، اندپوینت `api/toggle_trades.php`). خاموش کردن داده را پاک نمی‌کند.
- منطق سود فقط در `tradesWithProgress()` است: بهای واحد = (خرید + هزینه‌ی جانبی) / تعداد؛ سود قطعی = جمع فروش‌ها − بهای واحد × فروخته‌شده. هزینه‌ی جانبی به نسبت فروخته‌شده اثر می‌گذارد.
- **سود هر فروش به حسابداری می‌رود** (`syncTradeProfitTransactions`): به ازای هر فروش، یک تراکنش با دسته‌ی «سود معاملات»/«زیان معاملات» برای *سهم سود* ساخته می‌شود (نه کل مبلغ). تراکنش سود عمداً **بدون حساب (wallet)** است چون جابه‌جایی پول را خودِ فروش در `walletBalances` حساب می‌کند — وگرنه دوبار جمع می‌شد. پیوند در `trade_sales.profit_tx_id` است (`migration_trades2`)؛ ویرایش/حذف فروش یا خرید، تراکنش سود را هم به‌روز/حذف می‌کند.
- **دارایی و معامله دو روی یک سکه‌اند:** صفحه‌ی `my-assets.php` نمای کلی همه‌ی دارایی‌هاست (دارایی‌های ثبت‌شده + کالای معاملاتیِ نفروخته) با نمودار و درصد — ولی **هیچ اقدام خرید/فروشی آنجا نیست**. در `trades.php` سه زبانه هست: موجودی / دارایی / سوابق معاملات. زبانه‌ی «دارایی» همان رکوردهای `assets` را می‌آورد و `api/sell_asset.php` فروششان را ثبت می‌کند: مقدار از `assets` کم می‌شود و همان مقدار به‌صورت یک معامله‌ی بسته‌شده در `trades` می‌نشیند (بهای خرید از `unit_price` همان دارایی). آن معامله عمداً `buy_wallet_id` ندارد، چون پولِ خرید قبلاً و جای دیگری خرج شده.
- **کم و زیاد کردن مقدار دارایی فقط در `my-assets.php`؛ خرید و فروش فقط در `trades.php`.** این مرز عمدی است — اگر بشکند، یک قلم از دو جا قابل تغییر می‌شود و اعداد از هم می‌پاشند.
- تنها نقطه‌ی تماس با بقیه‌ی اپ `walletBalances()` است: اگر معامله به حسابی وصل باشد، خرید کم و فروش اضافه می‌شود (با JOIN، نه ردیف تراکنش). `side_costs` عمداً روی موجودی اثر ندارد چون معلوم نیست از کجا پرداخت شده.
- **حسابِ فروش پیش‌فرض «کیف پول» است، نه «بدون حساب».** فرم فروش دیگر گزینه‌ی «بدون واریز» ندارد: پول فروش باید جایی بنشیند، وگرنه سود در حسابداری می‌آمد ولی هیچ حسابی زیاد نمی‌شد. کاربر می‌تواند همان فروش را ویرایش کند و به حساب واقعی ببرد. سمت سرور از `resolveWalletId()` می‌گذرد؛ سمت مرورگر `resetSelect()` فهرست را به گزینه‌ی پیش‌فرضِ HTML برمی‌گرداند (مقداردهی دستی به `'0'` انتخاب را خالی می‌گذاشت).
- ورودی تعداد با `sanitizeQty()` خوانده می‌شود (ارقام و ممیز فارسی را می‌فهمد) — `sanitizeAmount` نقطه‌ی اعشار را می‌اندازد و برای تعداد به کار نمی‌آید.
- فروش بیش از مانده با `SELECT ... FOR UPDATE` در `api/sell_trade.php` بسته شده.

## ورود با ایمیل و بازیابی رمز

- **تصویر صفحه‌ی ورود عکس پروفایل همان کاربر است**، اگر دستگاه نام کاربری‌اش را به خاطر داشته باشد (`Auth::getRememberedUsername()`). اگر عکسی ثبت نکرده یا دستگاه کسی را به خاطر ندارد، آیکن برنامه در **همان قاب گرد** می‌نشیند — شکل قاب در هر دو حالت یکی است.

- ایمیل هنگام **ثبت‌نام و ساخت کاربر الزامی است** (`setup.php`, `admin/users.php`, `api/update_profile.php`, `profile.php`). ویرایش کاربر توسط مدیر عمداً الزامی نیست تا حساب‌های قدیمیِ بدون ایمیل قفل نشوند.
- ورود با **نام کاربری یا ایمیل** انجام می‌شود (`Auth::attemptLogin()`). پیام خطای «رمز غلط» و «حساب وجود ندارد» یکسان است تا وجود حساب لو نرود.
- توکن بازیابی الگوی selector + validator دارد (مثل `trusted_devices`)؛ اعتبار ۶۰ دقیقه، حداکثر ۳ درخواست برای هر کاربر و ۱۰ برای هر IP.
- **همه‌ی زمان‌ها در دیتابیس ساخته و مقایسه می‌شوند** (`DATE_ADD(NOW(), ...)`, `expires_at <= NOW()`). یک بار که PHP زمان را می‌ساخت و MySQL مقایسه می‌کرد، لینک بازیابی بلافاصله «منقضی» می‌شد چون `Auth::initSession()` منطقه‌ی زمانی PHP را عوض می‌کرد. تست رگرسیونش در `tests/test_password_reset.php` است.
- لینک ایمیل از `APP_URL` ساخته می‌شود، نه از `$_SERVER['HTTP_HOST']` (`appBaseUrl()`) — وگرنه با Host header جعلی می‌شد لینک بازیابی را به سایت مهاجم برد.
- بدون تنظیم `MAIL_METHOD`، صفحه‌ی فراموشی رمز صادقانه می‌گوید در دسترس نیست به‌جای اینکه وانمود کند ایمیل فرستاده شد.
- **ایمیل هرگز با ورودی خالی پاک نمی‌شود.** تنها مسیر نوشتن ایمیل تابع `saveUserEmail()` در `functions.php` است (پنل مدیر، `api/update_profile.php` و `deploy/user-admin.php` همه از همان استفاده می‌کنند). رشته‌ی خالی یعنی «دست نزن»؛ پاک کردن فقط با `$allowClear = true` صریح ممکن است. دلیلش یک باگ واقعی است: فرم ویرایش کاربر یکی است و مقدار فعلی ایمیل را جاوااسکریپت از `data-email` پر می‌کند؛ هر بار که آن اسکریپت اجرا نمی‌شد، یک ذخیره‌ی ساده ایمیل ثبت‌شده را بی‌سروصدا NULL می‌کرد و کاربر فقط می‌دید «ایمیل نمی‌مونه». تست رگرسیونش در `tests/test_user_email.php` است.
- وجود ستون `email` را با `usersHaveEmailColumn()` بپرسید، نه با کوئری دستیِ `information_schema` — نتیجه در همان درخواست کش می‌شود.
- **در پشتی همیشه هست:** `php deploy/user-admin.php --reset ali` مستقیم با دیتابیس کار می‌کند و به ایمیل وابسته نیست. فقط از CLI اجرا می‌شود (`PHP_SAPI !== 'cli'` → 404).

## اهداف پس‌انداز: حساب، و انتخابگرِ رنگ

- **⛔ `savings_goals.wallet_id` ارجاعی است، نه انتقالِ پول** (`migration_goal_wallet`).
  `savings_entries` هیچ‌وقت در `walletBalances()` نیامده و **نباید بیاید**:
  اگر امروز بیاید، موجودیِ هر کاربری که تا امروز پس‌انداز ثبت کرده بی‌صدا
  کم می‌شود، بی‌آنکه چیزی خرج کرده باشد. پس‌انداز یک «پاکتِ کنارگذاشته»
  روی پولِ موجود است، نه یک خرج. فایده‌اش جواب دادن به یک سؤالِ واقعی
  است: «آن ۵ میلیونی که برای سفر کنار گذاشته‌ام کجاست؟»
- `ON DELETE SET NULL`: حذفِ حساب هدف را با خودش نمی‌برد.
- حسابِ ورودی در برابر مالکیتِ خودِ کاربر سنجیده می‌شود، وگرنه `NULL`.
  **عمداً از `resolveWalletId()` رد نمی‌شود:** آن تابع برای پولِ واقعی
  است و ورودیِ نامعتبر را به حسابِ پیش‌فرض می‌برد تا پول گم نشود؛ اینجا
  پولی جابه‌جا نمی‌شود و «مشخص نشده» یک انتخابِ درست است.

### ⛔ `input[type="color"]` نباید از قاعده‌ی مشترکِ فیلدها padding بگیرد

- مرورگر نمونه‌ی رنگ را داخلِ **جعبه‌ی محتوا** می‌کشد. با `padding: 11px`
  از ۲۷ پیکسل ارتفاع فقط **۵ پیکسل** برای خودِ رنگ می‌ماند: یک خطِ نازک
  وسطِ یک کادرِ سفید. کاربر می‌دید «کادر رنگ خالی است» ولی رنگ **ذخیره و
  اعمال می‌شد** — خرابیِ کاملاً بی‌صدا، در هر دو صفحه‌ی پس‌انداز و
  حساب‌ها. اندازه‌گیری شد (۲۰۳×۲۷ با ۲۲ پیکسل padding)، حدس زده نشد.
- ارتفاعش ۴۴px است تا در `.form-row` کنارِ فیلدِ مبلغ هم‌تراز بنشیند.
- **و پیش‌نمایشِ زنده کنارش لازم بود:** تا امروز کاربر رنگ را انتخاب
  می‌کرد و تا **بعد از ذخیره** نمی‌دید چه شکلی می‌شود.

## طرحِ اشتراک — مدلِ کامل، اجرای خاموش

### دسترسی رایگان توسط مدیر

- **⛔ `planExtendUntil()` تنها جای حسابِ تاریخ است** و `approvePayment()`
  هم از همان رد می‌شود. با نسخه‌ی دوم، هدیه‌ی مدیر و تأییدِ پرداخت دیر یا
  زود دو جور حساب می‌کردند و تفاوتش فقط در عددِ روزهای کاربر دیده می‌شد.
- **`PLAN_GRANT_PERIODS` عمداً از `PLAN_PERIODS` جداست:** آن یکی فهرستِ
  **فروش** است (ضریبِ تخفیف دارد)، این یکی فهرستِ **هدیه**. یکی کردنشان
  یعنی «۲ ماهه» یک گزینه‌ی فروشِ بی‌قیمت هم می‌ساخت.
- **مادام‌العمر یک تاریخِ دور است، نه کلیدِ بولین** (`PLAN_FOREVER_DATE`)
  — همان استدلالی که `pro_until` را از اول تاریخ کرد. با ستونِ بولینِ
  جدا، هر کوئریِ `pro_until >= CURDATE()` باید هم عوض می‌شد و آن‌که
  نمی‌شد، کاربرِ مادام‌العمر را منقضی نشان می‌داد.
  - `days_left` برای مادام‌العمر `null` می‌شود، وگرنه صفحه «۲٬۹۱۷٬۳۰۹ روز
    باقی مانده — تا ۹۳۷۸/۱۰/۱۱» می‌نوشت و کاربر آن را باگ می‌خواند.
- **⛔ و یک خرابیِ واقعی که فقط آزمونِ جهش پیدا کرد:**
  `DATE_ADD('9999-12-31', INTERVAL 1 MONTH)` از بازه‌ی `DATE` بیرون
  می‌زند و MySQL **NULL** برمی‌گرداند، نه خطا. ستون هم nullable است، پس
  هیچ استثنایی پرتاب نمی‌شد: مدیری که به کاربرِ مادام‌العمر اشتباهاً «یک
  ماهه» هم می‌داد، دسترسیِ او را **کاملاً پاک می‌کرد** و پیامِ «فعال شد»
  هم می‌گرفت. حالا `planExtendUntil()` با یک `CASE` دستِ اشتراکِ
  بی‌پایان را نمی‌زند.
  - ⚠ و اول به شکلِ یک **تستِ سبزِ دروغین** ظاهر شد: بررسیِ «دوره‌ی
    نامعتبر» بعد از هدیه‌ی مادام‌العمر اجرا می‌شد، پس به دلیلِ همین سرریز
    رد می‌شد و نه به دلیلِ خودِ اعتبارسنجی — یعنی با برداشتنِ کاملِ
    اعتبارسنجی هم سبز می‌ماند. **ترتیبِ تست خودش می‌تواند تست را کور
    کند.**
- **هر هدیه یک ردیف در `payments` می‌گذارد** (`admin_grant`، مبلغ صفر) —
  نه برای پول، برای **رد**: شش ماه بعد باید معلوم باشد چرا این کاربر Pro
  است و چه کسی داده. «پس گرفتن» هم تاریخچه را پاک نمی‌کند، فقط یک ردیفِ
  `admin_revoke` اضافه می‌کند.
- **⚠ `action` از خودِ دکمه می‌آید، نه از یک `<input hidden>` + جاوااسکریپت.**
  وسوسه‌ی اول `this.form.action.value = 'revoke_pro'` بود که **بی‌صدا کار
  نمی‌کند**: `form.action` خودِ آدرسِ فرم است نه فیلدی به نامِ action، پس
  آن انتساب بی‌اثر می‌ماند و دکمه‌ی «پس گرفتن» **دسترسی می‌داد**.
- تست در `tests/test_plans.php` است (۵۹ بررسی) و با شش جهشِ تازه سنجیده
  شده، از جمله همان سرریزِ تاریخ.

- **⛔ امروز هیچ قابلیتی محدود نمی‌شود، و این عمدی است.** نصب‌هایی که الان کار
  می‌کنند همه چیز را دارند؛ اگر یک به‌روزرسانی ناگهان چیزی را از کاربرِ فعلی
  بگیرد، بدترین کاری است که می‌شود کرد. `planEnforced()` پیش‌فرض خاموش است و فقط
  مالکِ نصب روشنش می‌کند. یعنی این لایه فعلاً فقط **می‌شمارد و ثبت می‌کند**.
- **`planAllows()` تنها جای این تصمیم است.** هر گیتِ تازه‌ای باید از همان رد شود،
  وگرنه روشن کردنِ اجرا بعضی جاها اثر می‌کند و بعضی جاها نه. فهرستِ Pro-only:
  `trades`, `cheques`, `debts`, `recurring`, `api`, `reminders`.
- **⛔ هسته‌ی ثبتِ پول هرگز پولی نمی‌شود** — تراکنش، حساب، دسته‌بندی، بودجه،
  پس‌انداز، دارایی، گزارش. اگر کاربرِ رایگان نتواند حتی خرجش را ثبت کند، اپ
  برایش بی‌فایده است و اصلاً به خریدن نمی‌رسد. تست هر دو نیمه را می‌سنجد.
  - ⚠ نکته‌ای که بعداً از روی داده سنجیدنی است: چک و طلب و بدهی و تراکنشِ
    دوره‌ای بیشترین تفاوتِ این اپ با اپ‌های خارجی‌اند، پس بستنشان یعنی کاربرِ
    رایگان دقیقاً همان چیزی را نمی‌بیند که قرار بود قانعش کند. اگر «قیفِ شروع»
    در `admin/insights.php` افت کرد، اول همین فهرست را نگاه کنید.
- **⛔ قابلیتِ بسته پنهان نمی‌شود و محتوایش هم پنهان نمی‌شود — فقط درِ ورودیِ
  رکوردِ تازه بسته می‌شود.** پایین‌تر در «فقط خواندنی، نه قفلِ کامل» توضیح
  داده شده. منو سرِ جایش می‌ماند با یک نشانِ قفل، و صفحه کامل باز می‌شود.
- **⛔ گیتِ صفحه بدونِ گیتِ اندپوینت فقط تزئین است.** اندپوینت‌های **ساختِ**
  این چهار بخش `apiRequirePlan()` دارند و کدِ **۴۰۲** برمی‌گردانند («پرداخت
  لازم است»، نه «اجازه نداری») با کلیدِ `need_pro`. بدون آن، کسی که آدرسِ
  اندپوینت را بداند قفل را با یک درخواست دور می‌زد — با curl آزموده شد.
  - ⚠ `api/toggle_trades.php` عمداً بیرون است: کلیدِ روشن/خاموشِ خودِ بخش در
    پروفایل است و باید برای کاربرِ رایگان هم کار کند، وگرنه نمی‌تواند خاموشش کند.
- **⛔ انقضا با تاریخ است (`users.pro_until`)، نه با کلیدِ روشن/خاموش.** با
  بولین، اولین باری که کسی تمدید نکند باید یک کارِ زمان‌بندی‌شده خاموشش کند — و
  آن کار روزی از کار می‌افتد و همه بی‌سروصدا Pro می‌مانند. با تاریخ، انقضا خودش
  اتفاق می‌افتد و هیچ cron ای لازم نیست.
- **⛔ تمدید از بیشترِ «امروز» و «انقضای فعلی» شروع می‌شود**
  (`GREATEST(COALESCE(pro_until, CURDATE()), CURDATE())`). اگر همیشه از امروز
  حساب می‌شد، کسی که یک ماه زودتر تمدید کند روزهای باقی‌مانده‌اش را از دست
  می‌داد — یعنی تنبیهِ کاربرِ خوش‌حساب، و او فقط می‌بیند عددِ روزها کمتر از
  انتظارش است.
- همه‌ی حسابِ تاریخ در **دیتابیس** انجام می‌شود نه در PHP — همان درسی که لینکِ
  بازیابیِ رمز داد (منطقه‌ی زمانیِ PHP و MySQL یکی نبود و لینک بلافاصله «منقضی»
  می‌شد).
- **کارت‌به‌کارت با تأیید دستی، نه درگاه.** درگاه پرداخت ایرانی نماد اعتماد و
  شخصیت حقوقی می‌خواهد؛ تا آن روز این کار می‌کند و هیچ وابستگیِ بیرونی ندارد.
  `payments.method` رشته است تا وقتی درگاه آمد مقدارِ تازه اضافه شود بی‌آنکه
  ساختار عوض شود.
- **اعلامِ پرداخت فقط یک ادعاست** تا وقتی مدیر تأییدش کند؛ هیچ چیزی روی
  `pro_until` نمی‌نشیند. یک پرداختِ در انتظار در هر زمان مجاز است، وگرنه صفحه‌ی
  مدیر پر از تکراری می‌شود.
- **پرداخت‌ها حذف نمی‌شوند**، فقط وضعیتشان عوض می‌شود — تاریخچه‌ی مالی بین شما و
  کاربر باید بماند، حتی پرداختِ ردشده.
- اگر قیمت یا شماره‌ی کارت تنظیم نشده باشد، `pro.php` صریح می‌گوید هنوز راه
  نیفتاده — فرمی که کار نمی‌کند از نبودنش بدتر است.
- تست در `tests/test_plans.php` است (۳۰ بررسی) و با شش جهش سنجیده شده: تمدید از
  امروز (خوردنِ روزهای باقی‌مانده)، پیش‌فرضِ روشنِ اجرا، بستنِ هسته‌ی اپ، تأییدِ
  دوباره‌ی یک پرداخت، نادیده گرفتنِ ضریبِ تخفیف، و نادیده گرفتنِ انقضا.

### ⛔ فقط خواندنی، نه قفلِ کامل — وقتی اشتراک تمام می‌شود

- **نسخه‌ی اول کلِ صفحه را با یک صفحه‌ی قفل عوض می‌کرد** (`requirePlanOrLock()`)،
  یعنی کاربری که اشتراکش تمام می‌شد چک‌ها و طلب‌هایی را که ماه‌ها ثبت کرده بود
  **نمی‌دید**. آن بدترین کارِ ممکن است: کسی که دفترش را پس گرفته‌اند برنمی‌گردد
  تمدید کند، و حق هم دارد. **داده‌ی کاربر گروگان گرفته نمی‌شود.**
- **⛔ `planReadOnly()` تنها جای این تصمیم برای صفحه‌هاست** (مثل
  `categoryScopeSql()`): هم نوارِ توضیح از همان می‌آید، هم پنهان شدنِ دکمه‌های
  «افزودن»، هم نشانِ قفلِ کنارِ منو. با دو تابعِ جدا، دیر یا زود صفحه‌ای دکمه‌اش
  را نشان می‌داد که زدنش ۴۰۲ می‌گرفت.
- **⛔ «تازه» یعنی واقعاً تازه.** بسته: ثبتِ چکِ تازه، طلب/بدهیِ تازه، خریدِ
  تازه، قانونِ دوره‌ایِ تازه. **باز:** دیدن، ویرایش، حذف، و بستنِ چرخه‌ی رکوردِ
  موجود — پاس/برگشتِ چک، پرداختِ قسط، تسویه، فروشِ معامله‌ی باز.
  - دلیلش این است که آن اتفاق‌ها در دنیای واقعی **می‌افتند** چه ما اجازه بدهیم
    چه ندهیم. اگر ثبتشان بسته باشد دفتر روی واقعیتِ ماه‌ها پیش یخ می‌زند — و
    **دفترِ غلط از دفترِ نداشته بدتر است**، چون کاربر بعدش به هیچ عددی اعتماد
    نمی‌کند.
- **`api/save_recurring.php` تنها اندپوینتی است که هم می‌سازد هم ویرایش
  می‌کند**، پس گیتش **بعد از** خواندنِ شناسه است: `if ($id === 0)`. با گیتِ
  بالای فایل، کاربرِ منقضی نمی‌توانست مبلغِ قبضِ همیشگی‌اش را هم درست کند.
- ⚠ رنگِ نوار کهربایی است نه قرمز (`--warn-wash`/`--warn-ink`): چیزی خراب نشده
  و داده سرِ جایش است؛ با قرمز، کاربر فکر می‌کرد اطلاعاتش رفته.
- **قاعده ۲۶ در `test_api_contract.php` فهرست را دو طرفه و بسته نگه می‌دارد**،
  چون هر دو خرابی بی‌صدایند: گیت روی اندپوینتی که نباید (کاربر نمی‌تواند چکِ
  خودش را «پاس شد» کند)، و نبودِ گیت روی اندپوینتِ ساخت (قفل با یک درخواستِ
  مستقیم دور می‌خورد). همان‌جا هم بازگشتِ `requirePlanOrLock()` به هر صفحه‌ای
  ممنوع می‌شود.
- تستِ رفتاری در `tests/test_plan_readonly.php` است (۳۱ بررسی، با HTTP و
  نشستِ واقعی) و با **پنج جهش** سنجیده شده: رندرِ بی‌قیدِ دکمه‌ی ثبت، برگشتِ
  گیت به `update_cheque`، برداشتنِ گیت از `add_cheque`، بی‌قید شدنِ گیتِ
  `save_recurring`، و `planReadOnly()`ِ همیشه‌نادرست. قاعده ۲۶ خودش با چهار
  جهشِ دیگر سنجیده شد.

### ⛔ کد تخفیف — و سقفِ «n نفر اول»

- **دو کار با یک جدول، و تفاوتشان فقط در درصد است** (`discount_codes`،
  `migration_discount_codes`):
  - **۱۰۰٪** → دسترسی کامل و **رایگان** برای `months` ماه (۰ =
    مادام‌العمر). کاربر کد را وارد می‌کند و **همان لحظه** Pro می‌شود؛ نه
    پرداختی، نه انتظارِ تأییدِ مدیر. این همان چیزی است که «۱۰۰ نفر اول
    که نصب می‌کنند» می‌خواهد.
  - **کمتر از ۱۰۰** → تخفیف روی قیمتِ خرید. کد همراهِ اعلامِ پرداخت ثبت
    می‌شود و مدیر مبلغِ تخفیف‌خورده را کنارِ نامِ کد می‌بیند.
- **⛔ `used_count` یک ستون است، نه شمارشِ ردیف‌های `payments`.** «۱۰ نفر
  اول» یعنی این عدد باید **دقیق** باشد حتی وقتی دو نفر هم‌زمان کد را
  می‌زنند. با شمردنِ `payments` داخلِ تراکنش، اسنپ‌شاتِ REPEATABLE READ
  می‌توانست ردیفِ تازه‌ی نفرِ دیگر را نبیند و **هر دو** رد شوند — یعنی
  سقف بی‌صدا می‌شکست. با شمارنده روی خودِ ردیف، `SELECT … FOR UPDATE`
  روی همان کد کافی است.
  - **شرطِ سقف روی خودِ `UPDATE` هم هست** (`consumeDiscountUse()`). این
    افزونه است (فراخواننده از قبل سنجیده) ولی آخرین سد همین است: مسیرِ
    تازه‌ای که فردا بدونِ سنجش صدایش بزند، از دیتابیس رد نمی‌شود.
- **⛔ ردِ پرداخت ظرفیت را پس می‌دهد** (`releaseDiscountUse()` در
  `rejectPayment()`). بدونش کدی با ۱۰ ظرفیت با ۱۰ پرداختِ **ردشده**
  تمام می‌شد، بی‌آنکه حتی یک نفر چیزی گرفته باشد — و مالکِ نصب فقط
  می‌دید کد «تمام شده» است بی‌آنکه بفهمد چرا. به همین دلیل کدِ پرداخت
  **پیش از** عوض کردنِ وضعیت خوانده می‌شود.
- **⛔ کدِ تخفیفِ خرید به‌تنهایی دسترسی نمی‌دهد.** اگر نگهبانِ
  `discountIsFree()` از `redeemDiscountCode()` بیفتد، کدِ «۲۰٪ تخفیف»
  به کدِ «رایگان» تبدیل می‌شود — بدترین شکلِ خرابی، چون هیچ خطایی
  نمی‌دهد و کاربر هم شکایتی نمی‌کند. تست دقیقاً همین را می‌سنجد.
- **⛔ `normalizeDiscountCode()` تنها جایی است که کد شکل می‌گیرد** (مثل
  `SmsLogin::normalizePhone()`). ذخیره و جست‌وجو هر دو از همان رد
  می‌شوند، وگرنه مدیر `nowruz` ثبت می‌کند، کاربر `NOWRUZ ` می‌زند و «کد
  پیدا نشد» می‌گیرد — و هر دو طرف مطمئن‌اند درست تایپ کرده‌اند. ارقامِ
  فارسی هم لاتین می‌شوند: کیبوردِ فارسی «۱۰۰» می‌دهد و آن با `100` یکی
  نیست.
- **⛔ `discountFinalPrice()` تنها جای حسابِ قیمت است** و صفحه‌ی خرید و
  `submitPayment()` هر دو از همان می‌خوانند. به همین دلیل کد با یک POST
  و رفت‌وبرگشتِ سرور اعمال می‌شود، نه با حساب در جاوااسکریپت: نسخه‌ی دومِ
  این حساب یعنی کاربر یک مبلغ می‌دید و مبلغِ دیگری ثبت می‌شد.
  - فیلدِ `discount_code` در فرم فقط **حمل** می‌کند؛ سرور دوباره و
    **زیرِ قفل** می‌سنجدش. بین دیدنِ صفحه و زدنِ دکمه ممکن است ظرفیت
    تمام شده باشد، و پیلودِ مرورگر هم دست‌کاری‌شدنی است.
  - کدِ داخلِ نشست هم **هر بار از نو** سنجیده می‌شود: اگر مدیر بین دو
    بازدید خاموشش کرده باشد، کاربر نباید تخفیفی ببیند که وجود ندارد و
    هنگام ثبت رد شود.
- **⛔ جعبه‌ی کد بیرون از شرطِ «قیمت و کارت تنظیم شده» است.** «۱۰۰ نفر
  اول رایگان‌اند» باید **روزِ اول** کار کند — همان روزی که هنوز هیچ
  شماره کارتی وارد نشده و فروشی در کار نیست. با گذاشتنش داخلِ آن شرط،
  کد دقیقاً وقتی از کار می‌افتاد که بیشترین کاربرد را داشت.
  - ولی اگر **هیچ کدِ فعالی** نباشد اصلاً رندر نمی‌شود
    (`hasUsableDiscountCode()`): فیلدی که هر چه در آن بزنی «نامعتبر»
    می‌گوید، همان «دکمه‌ای که کار نمی‌کند».
- **کد حذف نمی‌شود، فقط خاموش.** ردیف‌های `payments` به آن اشاره دارند —
  همان قاعده‌ی «پرداخت‌ها حذف نمی‌شوند». و **ویرایشِ یک کد `used_count`
  را صفر نمی‌کند**، وگرنه مدیری که فقط توضیحش را عوض می‌کند ظرفیت را
  بی‌سروصدا از نو باز می‌کرد.
- **⚠ `payments.discount_code` یک رشته است، نه کلیدِ خارجی.** تاریخچه
  باید بگوید کاربر «کدام کد» را زده، حتی اگر مدیر بعداً آن کد را عوض
  کند — همان استدلالی که `counterparty_name` را نگه داشت. و **هیچ‌جا
  ستون‌به‌ستون با `discount_codes.code` مقایسه نمی‌شود** (نه JOIN، نه
  `IN`): دو ستون هر دو «قابلیتِ تبدیلِ ۲» دارند و روی نصبی با collation
  متفاوت «Illegal mix of collations» می‌داد. کد همیشه پارامترِ bind‌شده است.
- **الفبای ساختِ خودکار `O`/`0` و `I`/`1` ندارد:** کد را کاربر از روی یک
  بنر **تایپ** می‌کند و این دو جفت رایج‌ترین اشتباهِ تایپ‌اند — نتیجه‌اش
  «کد نامعتبر» برای کسی که کارِ درست را کرده.
- **`jalaliStringToGregorian()` برای تاریخِ انقضا** به `functions.php`
  اضافه شد: تقویمِ تازه‌ای نیست، فقط رشته را به سه عدد می‌شکند و
  `jalaliToGregorian()` را صدا می‌زند، و درستی را با **رفت‌وبرگشت**
  می‌سنجد (۳۱ آبان هم ۱ تا ۱۲ است هم ۱ تا ۳۱، ولی وجود ندارد).
- ⚠ نوارِ ظرفیت از همان `.budget-bar-track` است، نه یک نوارِ تازه — آن
  کلاس `direction: ltr` دارد و بدونش در صفحه‌ی راست‌به‌چپ از راست پر
  می‌شد. و با سقفِ «بی‌نهایت» **اصلاً رندر نمی‌شود**: نوارِ پیشرفت بدون
  مخرج چیزی نمی‌گوید.
- ⚠ `.code-input` عمداً به فهرستِ `.ltr-num` **نپیوسته**: آن فهرست برای
  عدد است (`tabular-nums`)، و اینجا به `text-transform: uppercase` نیاز
  است که برای عدد بی‌معناست.
- تست در `tests/test_discount_codes.php` است (۷۱ بررسی) و با **نُه
  جهش** سنجیده شده، همه گرفته شد: برداشتنِ شرطِ سقف از `UPDATE`،
  برداشتنِ پس دادنِ ظرفیت، نادیده گرفتنِ انقضا، نادیده گرفتنِ خاموش
  بودن، برداشتنِ «یک کاربر یک بار»، نادیده گرفتنِ درصد، دسترسیِ رایگان
  دادنِ کدِ تخفیف، صفر کردنِ شمارنده هنگام ویرایش، و بزرگ نکردنِ حروف
  در نرمال‌سازی.

## ثبت‌نام خودسرویس — پشتِ کلیدِ مدیر

- **⛔ پیش‌فرض خاموش، و این قابل مذاکره نیست.** هر نصبی که تا امروز بالا آمده یک
  دفترِ خصوصی است که مدیرش خودش کاربرها را می‌سازد. روشن شدنِ خودکارِ ثبت‌نام با
  یک `git pull` — در ساکت‌ترین حالتِ ممکن — آن دفتر را به روی اینترنت باز
  می‌کرد. کلید در `admin/users.php` است (`app_settings.allow_signup`).
- **کلیدِ خاموش یعنی صفحه‌ی `register.php` ۴۰۴ می‌دهد، نه «غیرفعال است».** پیام
  «غیرفعال» به یک اسکنر می‌گوید اینجا اپی هست که ثبت‌نام دارد و فقط خاموش است.
  لینکِ صفحه‌ی ورود هم فقط وقتی روشن باشد رندر می‌شود.
- **⛔ `createUserAccount()` و `validateNewUser()` تنها مسیرِ ساختِ کاربرند** و هم
  ثبت‌نامِ خودسرویس از آن‌ها رد می‌شود هم پنل مدیر. پیش از این پنل مدیر نسخه‌ی
  خودش را داشت؛ با دو نسخه شدن، قاعده‌ای مثل «کاربر تازه باید کیف پول داشته
  باشد» از یکی می‌افتاد و آن‌وقت اولین تراکنشِ آن کاربر در هیچ حسابی نمی‌نشست.
- **ایمیل در ثبت‌نام الزامی است، برخلافِ ویرایشِ کاربر در پنل مدیر.** کسی که خودش
  ثبت‌نام می‌کند مدیری ندارد که رمزش را برایش عوض کند؛ بدون ایمیل، اولین
  فراموشیِ رمز یعنی از دست رفتنِ حساب.
  - ⚠ **این قاعده بعداً جایگزین شد، نه حذف:** حالا «دست‌کم یک راهِ بازگشت»
    است — ایمیل **یا** شماره‌ی موبایلِ تأییدشده. استدلالِ بالا دست‌نخورده
    می‌ماند و فقط شماره هم به‌عنوان راهِ بازگشت پذیرفته شد. جزئیات در
    «ثبت‌نام با شماره موبایل» پایین‌تر.
  - ⚠ شرطِ صریحِ «خالی» فقط برای **پیام** است، نه برای رد کردن: `filter_var`
    رشته‌ی خالی را هم رد می‌کند. برداشتنِ آن شرط تست را نمی‌شکست تا وقتی که
    تست خودِ **متنِ پیام** را نسنجید — حالا می‌سنجد.
- سدِ ثبت‌نامِ انبوه از همان جدولِ `login_attempts` استفاده می‌کند با کلیدِ ثابتِ
  `__signup__`، پس با شمارشِ ورودِ ناموفق قاطی نمی‌شود و جدولِ تازه‌ای لازم ندارد.
- تست در `tests/test_signup.php` است (۲۸ بررسی) و **صفحه را با HTTP هم می‌سنجد**،
  نه فقط تابع را: «تنظیم خاموش است» با «صفحه واقعاً بسته است» یکی نیست — همان
  درسی که قاعده‌ی nginx در `api/v1` داد. با پنج جهش سنجیده شده، از جمله روشن
  کردنِ پیش‌فرض و برداشتنِ کیف پولِ پیش‌فرض.

## ورود با کد پیامکی — پشتِ دو کلیدِ جدا

- **⛔ پیش‌فرض خاموش، و دو شرطِ جدا لازم است:** کلیدِ مدیر
  (`app_settings.allow_sms_login`، در `admin/users.php`) **و**
  `SMS_METHOD` در `config.php`. هیچ‌کدام بی‌دیگری کار نمی‌کند.
  دلیلش این است که این قابلیت یک **راهِ ورودِ دومِ کاملِ** به هر حسابی
  است؛ روشن شدنش با یک `git pull` یعنی دری که مالکِ نصب نمی‌داند وجود
  دارد. `SmsLogin::available()` هر سه شرط (کلید، جدول، پنل) را با هم
  می‌سنجد و صفحه‌ها فقط همان را می‌پرسند.
- **کلیدِ خاموش یعنی `sms-login.php` ۴۰۴ می‌دهد**، نه «غیرفعال است» —
  همان قاعده‌ی `register.php`. لینکش در صفحه‌ی ورود هم فقط وقتی رندر
  می‌شود که واقعاً کار کند، وگرنه لینک به بن‌بست می‌برد.
- **چهار محافظ در `SmsLogin`، و هیچ‌کدام اختیاری نیست:**
  1. **⛔ سقفِ تلاش روی خودِ کد** (`sms_codes.attempts`، سقف ۵). کدِ
     ۵ رقمی ۱۰۰٬۰۰۰ حالت دارد؛ بدون این، مهاجمی که شماره را می‌داند یک
     کد می‌گیرد و آزادانه حدس می‌زند — یعنی ورودِ پیامکی از ورودِ با
     رمز **ضعیف‌تر** می‌شد. مهم‌ترین محافظِ کلِ قابلیت.
  2. **یک‌بارمصرف بودن** (`used_at`): وگرنه هر کسی که متنِ پیامک را
     ببیند تا آخرِ پنجره‌ی ۳ دقیقه‌ای وارد می‌شود.
  3. **سقفِ درخواست** (۳ در ساعت برای هر شماره، ۱۰ برای هر IP، و ۶۰
     ثانیه فاصله). اینجا مسئله فقط امنیت نیست، **پول** است: هر پیامک
     هزینه دارد و یک اندپوینتِ بی‌سقف اعتبارِ پنل را در چند دقیقه خالی
     می‌کند. این سقف **پیش از** پیدا کردنِ کاربر سنجیده می‌شود، وگرنه
     شماره‌های تصادفی از آن رد می‌شدند.
  4. **پیامِ یکسان** برای شماره‌ی موجود و ناموجود، وگرنه این صفحه یک
     ابزارِ آماده برای فهمیدنِ «چه کسی کاربرِ این سایت است» می‌شود. برای
     شماره‌ی ناموجود هیچ ردیفی هم ساخته نمی‌شود.
- **کدِ خام هرگز ذخیره نمی‌شود، فقط `sha256`** — همان الگوی `api_tokens`
  و `password_resets`. کدی که کوتاه است و در بکاپ خام بماند، هر کسی که
  یک بار به دیتابیس برسد می‌تواند در همان چند دقیقه با آن وارد شود.
- **کدهای بازِ قبلیِ همان شماره سوزانده می‌شوند.** دو کدِ همزمان معتبر
  یعنی دو برابر شدنِ شانسِ حدس، بی‌هیچ سودی.
- **⛔ `SmsLogin::normalizePhone()` تنها جایی است که شماره معنا می‌گیرد**
  (مثل `categoryScopeSql()`). `+98`، `0098`، `98…`، `9…`، فاصله، خط‌تیره
  و ارقامِ فارسی همه به `09xxxxxxxxx` می‌رسند. اگر ثبتِ شماره و ورود با
  شماره از دو نرمال‌سازی رد شوند، تطبیق **بی‌صدا** شکست می‌خورد: کد
  فرستاده می‌شود، کاربر می‌زندش، و «پیدا نشد». نوشتنِ شماره هم فقط از
  `saveUserPhone()` می‌گذرد که خودش از همان تابع رد می‌شود.
- **`Auth::establishSession()` از `attemptLogin()` جدا شد** — به همان
  دلیلی که `verifyCredentials()` جدا شده بود: با آمدنِ ورودِ پیامکی باید
  بار دوم نوشته می‌شد و دو نسخه از «ورود» دیر یا زود از هم دور می‌افتند.
  کلیدِ جاافتاده در یکی (مثلاً `session_minutes`) یعنی مهلتِ نشست برای آن
  مسیر بی‌صدا به پیش‌فرض برمی‌گردد.
- سدِ عمومیِ حدس از همان `LoginThrottle` می‌آید، با کلیدِ **شماره** نه
  نام کاربری: با نام کاربری، مهاجم می‌توانست با درخواستِ کد، ورودِ **با
  رمزِ** همان حساب را هم قفل کند. راهِ باز کردنش هم همان همیشگی است
  (`deploy/user-admin.php --unlock`).
- **⛔ ملی‌پیامک دو نوع حساب دارد و مسیرشان یکی نیست** — و نبودِ این
  تفکیک همان چیزی بود که پیامک را نمی‌فرستاد:
  - **وب‌سرویسِ کلاسیک** (`rest.payamak-panel.com/.../BaseServiceNumber`):
    نام کاربری + رمز عبورِ پنل.
  - **کنسولِ جدید** (`console.melipayamak.com/api/send/shared/{key}`):
    کلیدِ وب‌سرویس داخلِ خودِ آدرس.
  - **⛔ و «APIKey» ملی‌پیامک مالِ اولی است، نه دومی.** خودِ پنل زیرِ آن
    مقدار نوشته: «کافی است مقدار فوق را **به جای [رمز عبور] در پارامتر
    Password** به متدها ارسال نمایید». یعنی APIKey یک **رمز** است، نه
    کلیدِ کنسول — و چون شکلش UUID است، طبیعی‌ترین کارِ ممکن این بود که
    در فیلدِ «کلید وب‌سرویس» گذاشته شود. نتیجه‌اش روی سرورِ واقعی
    **HTTP 400 «کلید کنسول معتبر نیست»** بود: کنسول درخواست را خواند و
    فهمید و فقط مقدارِ کلید را رد کرد. `smsTestSend()` بعدش مسیرِ دوم را
    آزمود و همان جواب داد — یعنی از دیدِ کاربر «اپ انتخابم را نادیده
    می‌گیرد»، در حالی که داشت مسیرِ درست را پیدا می‌کرد.
  - به همین دلیل برچسبِ فرم دیگر «حساب جدید/قدیمی» نیست بلکه
    «وب‌سرویس — نام کاربری + رمز عبور **یا APIKey**» در برابر «کنسول
    جدید»، و همان جمله‌ی خودِ ملی‌پیامک زیرِ فیلدِ رمز نوشته شده. نامِ
    «حسابِ قدیمی» خودش گمراه‌کننده بود: حسابی که همین امروز APIKey
    می‌دهد، قدیمی نیست.
  - **⛔ `Sms::meliMode()` تنها جایی است که این تصمیم گرفته می‌شود** و
    یک تنظیمِ **صریح** روی فرم است (`sms_meli_mode`)، نه حدس. نسخه‌ی
    اول از «خالی بودنِ نام کاربری» حدس می‌زد و همان یک خطِ نادیدنی کلِ
    خرابی را ساخت: مالکِ یک حسابِ **قدیمی** که فقط فیلدِ کلید را پر
    کرده بود، بی‌آنکه بداند به کنسول فرستاده می‌شد و جواب می‌گرفت
    «کلید کنسول معتبر نیست» — پیامی که **درست است** و هیچ نمی‌گوید
    مشکل می‌تواند اصلاً نوعِ حساب باشد، نه خودِ کلید. او دنبالِ کلیدِ
    تازه می‌گشت در حالی که اصلاً کلیدی ندارد.
  - نبودنِ مقدار یعنی نصبِ قدیمی و همان حدسِ قبلی زده می‌شود، وگرنه
    نصب‌هایی که امروز کار می‌کنند بی‌صدا مسیرشان عوض می‌شد.
  - **هر روش فقط فیلدهای خودش را نشان می‌دهد.** پیش از این هر چهار
    فیلد با هم دیده می‌شدند و راهنما می‌گفت «رمز را در فیلدِ کلید
    بگذارید» — یک فیلد با دو معنا، که تضمینِ همین اشتباه است. (سازگاری
    حفظ شد: رمزِ ذخیره‌شده در فیلدِ کلید هنوز خوانده می‌شود.)
  - **`meliError()` خطای خام را با «حالا چه کار کنم» می‌دهد** و
    می‌گوید از **کدام مسیر** رفته. خطای خام لازم است ولی کافی نیست.
- **کارتِ اتصال مخصوصِ همان پنل است، نه یک فهرستِ درهمِ همه‌ی فیلدها.**
  با فهرستِ درهم، کاربرِ ملی‌پیامک نمی‌فهمد «شماره خط» به او ربط دارد یا
  نه و کاربرِ کاوه‌نگار سه فیلدِ بی‌ربط می‌بیند.
  - ⚠ کارتِ انتخابِ سرویس باید **پیش از** کارت‌های اتصال رندر شود:
    اسکریپتِ تعویض به همان `select` بسته است و اگر بعدش بیاید،
    `getElementById` تهی برمی‌گرداند و تعویض **بی‌صدا** کار نمی‌کند.
    یک بار همین شد و فقط در مرورگر دیده شد.
  - ⚠ `.card` یک `display` دارد و با ویژگیِ `hidden` هم‌وزن است، پس
    کارتِ «پنهان» دیده می‌شد. `.sms-conn[hidden]` صریح لازم است.
- **⛔ در هر کارتِ اتصال فقط **یک** دکمه هست.** نسخه‌ی میانی دو دکمه
  داشت («ذخیره» و «ذخیره و ارسال آزمایشی») و همان هم گیج‌کننده بود:
  کنارِ هم شبیه دو جای تست خوانده می‌شدند و کاربر اولی را می‌زد و
  منتظرِ پیامک می‌ماند. حالا تصمیم از **پر بودنِ خودِ فیلدِ شماره‌ی
  آزمایشی** می‌آید — خالی یعنی فقط ذخیره، پر یعنی ذخیره و بعد آزمایش —
  و برچسبِ دکمه با جاوااسکریپت همان را می‌گوید. بدونِ جاوااسکریپت هم
  درست کار می‌کند، چون تصمیم را سرور از روی همان فیلد می‌گیرد.
- **⛔ «ارسال آزمایشی» داخلِ همان فرمِ اتصال است، نه فرمِ جدا.** پیش از
  این دو فرم بود: کاربر «نوع حساب» را عوض می‌کرد، دکمه‌ی تستِ فرمِ دوم
  را می‌زد، و تست با مقدارِ **قبلی** می‌رفت — چون آن فرم فیلدهای این
  یکی را نداشت. نتیجه همان خطای همیشگی بود و کاربر نتیجه می‌گرفت که
  گزینه‌ی تازه هم جواب نداد. حالا یک فرم است با دو دکمه («ذخیره» و
  «ذخیره و ارسال آزمایشی») و تست دقیقاً با همان چیزی می‌رود که روی
  صفحه دیده می‌شود.
- **⛔ `smsTestSend()` اگر یک مسیرِ ملی‌پیامک رد شد، مسیرِ دیگر را هم
  می‌آزماید** و اگر جواب داد همان را ذخیره می‌کند. دلیلش این است که
  «نوع حساب» چیزی است که خودِ صاحبِ حساب هم اغلب نمی‌داند؛ به‌جای
  پرسیدن، امتحان می‌کنیم. ⚠ مسیرِ دوم فقط وقتی آزموده می‌شود که
  اعتبارنامه‌اش پر باشد، و اگر آن هم شکست خورد **حالتِ اول برمی‌گردد** —
  وگرنه یک تستِ ناموفق تنظیماتِ کاربر را بی‌اجازه عوض کرده بود.
- **خطِ «وضعیتِ ذخیره‌شده» بالای کارت** می‌گوید همین حالا چه چیزی ذخیره
  است و چه چیزی کم دارد. بدونِ آن، کاربر گزینه را عوض می‌کند، ذخیره
  نمی‌زند، و نتیجه‌ی مقدارِ قبلی را می‌بیند بی‌آنکه بفهمد چرا.
- **دکمه‌ی «ارسال آزمایشی» خطای خامِ پنل را نشان می‌دهد.** بدونِ آن،
  «پیامک ارسال نشد» یعنی حدس زدن بینِ کلیدِ غلط، الگوی تأییدنشده، و
  اعتبارِ تمام‌شده — سه چیزِ کاملاً جدا با سه راهِ حلِ جدا.
- **متنِ پیامک (`sms_text`) فقط در حالتِ متنِ آزاد استفاده می‌شود**؛ با
  الگو بی‌اثر است چون متن را خودِ الگو دارد. `{code}` و `{ttl}` جایگزین
  می‌شوند.
- **⛔ تنظیمِ پنل از صفحه‌ی مدیر هم ممکن است، ولی `config.php` برنده
  است.** `smsSetting()` اول ثابتِ `config.php` را می‌بیند و فقط اگر
  خالی بود سراغِ `app_settings` می‌رود. دو دلیل: کسی که فقط می‌خواهد
  پنلِ کاوه‌نگارش را وصل کند نباید مجبور باشد با SSH یک فایل PHP را
  ویرایش کند (وگرنه این قابلیت عملاً وصل نمی‌شود)، ولی هم‌زمان کسی که
  به پنل مدیر دسترسی پیدا کند نباید بتواند پیامک‌ها را به پنلِ خودش
  ببرد.
  - ⚠ کلیدِ ذخیره‌شده در `app_settings` **در بکاپِ دیتابیس هم می‌آید**.
    فرم همین را صریح می‌گوید و راهِ جایگزین (`config.php`) را نشان
    می‌دهد.
  - فرم مقدارِ فعلیِ کلید و رمز را **نشان نمی‌دهد**، فقط «ثبت شده».
    خالی گذاشتنشان یعنی «دست نزن» (قاعده‌ی `saveUserEmail`)؛ پاک کردن
    با تایپِ یک خط تیره.
  - `Sms::missingFor()` می‌گوید هر پنل چه چیزی کم دارد، و همان زیرِ
    منوی انتخابِ پنل نوشته می‌شود — «آماده است» یا «کلید API وارد نشده
    است». کلیدی که کار نمی‌کند بدتر از نبودنش است.
- **⛔ برای کدِ ورود، «الگو» بر متنِ آزاد مقدم است** (`Sms::sendCode()`).
  پنل‌های ایرانی برای پیامکِ خدماتی یک الگو (کاوه‌نگار: `verify/lookup`،
  ملی‌پیامک: «خدمات پایه»/`bodyId`، sms.ir: `templateId`) دارند که از قبل
  تأیید می‌شود: ارزان‌تر است، **به خطِ اختصاصی نیاز ندارد**، و شبانه‌روزی
  می‌رود — در حالی که پیامکِ متن‌آزاد ممکن است اصلاً تحویل نشود.
  - `sms_pattern` خالی یعنی همان متنِ آزادِ قبلی؛ پس نصب‌های موجود
    دست‌نخورده می‌مانند.
  - ⚠ در حالتِ الگو **فقط خودِ کد** به‌عنوان پارامتر می‌رود، نه متنِ کامل —
    متن را خودِ الگو دارد. برای sms.ir نامِ پارامتر باید `CODE` باشد.
  - ⚠ ملی‌پیامک در این مسیر `from` نمی‌خواهد و پاسخش `RetStatus = 1` است،
    نه شناسه‌ی پیام؛ سنجیدنِ اشتباهش یعنی «فرستاده شد» گفتن به پیامکی که
    نرفته.
- **`includes/sms.php` راه‌اندازِ پنل است** و کاوه‌نگار، sms.ir و
  ملی‌پیامک را می‌شناسد، به‌علاوه‌ی `log` برای توسعه که هیچ‌جا نمی‌فرستد و
  کدها را در `var/sms.log` می‌نویسد. **روی سرورِ واقعی `log` یعنی کدهای
  ورود در یک فایلِ متنی روی دیسک.**
  - **۲۰۰ گرفتن از پنل کافی نیست:** هر سه پنل وضعیت را داخلِ بدنه
    می‌گذارند، پس بدنه هم خوانده می‌شود. وگرنه «فرستاده شد» گفته می‌شد
    برای پیامکی که پنل رد کرده.
  - هم `curl` هم `stream` پشتیبانی می‌شوند: pool این اپ
    `exec`/`shell_exec` را غیرفعال کرده (قانون جداسازی سرور) پس curlِ
    خط‌فرمانی ممکن نیست، و افزونه‌ی curl روی هر نصبی نیست. اگر هیچ‌کدام
    نبود صریح خطا می‌دهد — سکوت یعنی کدی که نرفته ولی «موفق» شده.
- **شماره اختیاری است، برخلافِ ایمیل.** کاربری که ورودِ پیامکی نمی‌خواهد
  نباید مجبور شود شماره بدهد. فیلدش در پروفایل فقط وقتی دیده می‌شود که
  قابلیت واقعاً در دسترس باشد، و خالی گذاشتنش شماره‌ی قبلی را **پاک
  نمی‌کند** (همان قاعده‌ی `saveUserEmail`).
  - **⛔ و همین یک بن‌بستِ مرغ و تخم‌مرغ می‌سازد که هیچ خطایی ندارد:**
    `requestCode()` کاربر را با `WHERE phone = :p` پیدا می‌کند، ولی آن
    فیلد فقط بعد از راه افتادنِ پنل ظاهر می‌شود. پس روزِ اولی که پیامک
    کار می‌کند، **هیچ‌کس شماره ندارد** و اولین تلاشِ ورود کاربری پیدا
    نمی‌کند — و صفحه‌ی ورود عمداً همان «کد فرستاده شد» را می‌گوید تا
    فهرستِ کاربران لو نرود. یعنی همه چیز سبز است و هیچ پیامکی نمی‌رود.
  - تنها جایی که این حقیقت گفتنی است **پنلِ مدیر** است (که از قبل فهرستِ
    کاربران را می‌بیند و چیزِ تازه‌ای لو نمی‌دهد): `SmsLogin::usersWithPhone()`
    و هشدارِ صریحِ «هنوز هیچ کاربری شماره ثبت نکرده است» در `admin/access.php`.
  - **⛔ و برای تشخیصِ تک‌موردی:** `php deploy/user-admin.php --sms-check 09123456789`.
    `requestCode()` پنج دروازه دارد و **سه‌تایشان عمداً ساکت‌اند** (شماره‌ی
    ثبت‌نشده، حسابِ غیرفعال، شماره‌ی موجود — همه یک پیام می‌دهند)، پس از
    بیرون هیچ راهی برای فهمیدنِ اینکه کدام‌یک جلو را گرفته وجود ندارد.
    این فرمان هر پنج را به ترتیب می‌سنجد و همان‌جا می‌ایستد که اپ می‌ایستد.
    ⚠ `resendWait()` و `countRecent()` را با Reflection از خودِ `SmsLogin`
    صدا می‌زند، نه با کوئریِ دوباره‌نوشته — وگرنه ابزار چیزی را می‌سنجید
    که اپ نمی‌سنجد و جوابش بی‌ارزش بود. هر پنج شاخه روی دیتابیسِ واقعی
    اجرا و دیده شده‌اند.
  - **⛔ و از وقتی «ثبت‌نام با شماره» آمد، این حلقه راهِ سومی هم دارد:**
    اگر هر دو کلیدِ مدیر روشن باشند، اولین کسی که شماره‌اش را در صفحه‌ی
    ورود می‌زند حسابش همان‌جا ساخته می‌شود و شماره از اول ثبت است. پایین‌تر
    در «ثبت‌نام با شماره موبایل».
  - **⛔ و راهِ بیرون آمدن از آن حلقه:**
    `php deploy/user-admin.php --set-phone ali 09123456789` — همان در پشتی
    که `--set-email` برای بازیابیِ رمز است. روزِ اولی که پنل راه می‌افتد،
    فیلدِ پروفایل تازه ظاهر شده و ممکن است مالکِ نصب خودش هم بیرون از حساب
    مانده باشد. ⚠ از همان `saveUserPhone()` می‌گذرد، نه یک `UPDATE` دستی:
    نرمال‌سازی و بررسیِ تکراری باید یکی بماند، وگرنه شماره‌ای ذخیره می‌شود
    که `requestCode()` پیدایش نمی‌کند و خرابی **بی‌صداست**.
- **Web Push و ورود با OTP روی api/v1 عمداً نیامدند.** آن لایه توکنِ
  ۹۰ روزه دارد و اضافه کردنِ یک مسیرِ ورودِ دومِ بدون‌رمز به آن، سطحِ حمله
  را بی‌دلیل بزرگ می‌کند.
- تست در `tests/test_sms_login.php` است (۳۷ بررسی) و با **ده جهش**
  سنجیده شده — همه گرفته شدند: پیش‌فرضِ روشن، برداشتنِ سقفِ تلاشِ کد،
  یک‌بارمصرف نبودنِ کد، پیامِ متفاوت برای شماره‌ی ناموجود، فرستادنِ کد
  برای حسابِ غیرفعال، برداشتنِ سقفِ ساعتی، نسوزاندنِ کدهای قبلی، خرابیِ
  نرمال‌سازی، ذخیره‌ی کدِ خام، و نادیده گرفتنِ انقضا.
  - ⚠ نکته‌ی مهمِ خودِ تست: سنجشِ سقفِ تلاشِ کد باید سابقه‌ی
    `LoginThrottle` را بین تلاش‌ها پاک کند، وگرنه سدِ **عمومی** (که سقفش
    کمتر است) اول می‌گیرد و تست حتی با برداشتنِ سقفِ کد هم سبز می‌ماند —
    یعنی چیزی را می‌سنجید که فکر می‌کردیم.

## ثبت‌نام با شماره موبایل — و ورودِ همیشگی با پیامک به‌عنوان قابلیتِ Pro

- **یک صفحه، سه مرحله.** `sms-login.php` هم ورود است هم ثبت‌نام:
  شماره → کد → **رمز** (و مرحله‌ی سوم فقط برای شماره‌ی تازه است).
  صفحه‌ی دومی ساخته نشد چون **از روی شماره نمی‌شود (و نباید بشود)
  فهمید کدام حالت است**: هر پیامِ متفاوتی یعنی هر کسی می‌تواند فهرستِ
  کاربرانِ سایت را بسازد.

### ⛔ رمز هنگامِ ثبت‌نام با شماره اجباری شد — و نشانه‌ی تأیید در نشست

- **نسخه‌ی اول رمز را اختیاری می‌گذاشت** (کدِ پیامکی احراز کرده بود) و
  نتیجه‌اش حسابی بود که **تنها درش پیامک است**. روی دستگاهِ دوم، یا
  وقتی کوکیِ «این دستگاه را به خاطر بسپار» پاک شود، چنین کاربری هیچ
  راهِ دیگری ندارد — در حالی که ورودِ پیامکیِ **همیشگی** قرار است
  قابلیتِ Pro باشد. یعنی حساب‌هایی می‌ساختیم که یا باید برای همیشه از
  گیت معاف بمانند یا کاربرشان بیرونِ دفترِ خودش قفل شود. رمز هنگامِ
  ثبت‌نام هر دو را از بین می‌برد.
- **⛔ `validateNewUser()` حالا در همه‌ی مسیرها رمز می‌خواهد.** تنها
  جای این قاعده همان است، پس هیچ مسیرِ تازه‌ای نمی‌تواند دوباره حسابِ
  بی‌رمز بسازد.
- **⛔ `phoneAuthComplete()` دیگر حساب نمی‌سازد.** فقط «شماره تأیید شد»
  را برمی‌گرداند (`need_password`)؛ ساختن کارِ `phoneSignupComplete()`
  است. اهمیتش این است که کاربری که وسطِ کار منصرف می‌شود **هیچ ردیفی**
  به جا نمی‌گذارد — نه یک حسابِ بی‌رمزِ نیمه‌کاره.
- **⛔ نتیجه‌ی تأیید در نشست می‌ماند، نه در فرم** (`phoneVerifyRemember()`
  / `phoneVerifyOk()` / `phoneVerifyBurn()`). بقیه‌ی این صفحه شماره را
  در فیلدِ پنهان حمل می‌کند و آنجا بی‌خطر است چون خودِ **کد** احراز
  می‌کند؛ ولی از مرحله‌ی سوم به بعد کد سوخته و تنها چیزِ باقی‌مانده
  همان ادعاست. ادعایی که از مرورگر بیاید یعنی **هر کسی بدونِ هیچ
  پیامکی حساب می‌سازد** — با curl، در یک درخواست.
  - نشانه شماره را هم نگه می‌دارد و با `hash_equals` سنجیده می‌شود:
    وگرنه تأییدِ شماره‌ی خودت، مجوزِ ساختِ حساب برای شماره‌ی **دیگری**
    می‌شد.
  - **⛔ فقط بعد از ساختِ موفقِ حساب سوزانده می‌شود، نه در هر شکستی.**
    اگر رمزِ کوتاه هم نشانه را می‌سوزاند، یک اشتباهِ تایپی کاربر را
    مجبور می‌کرد پیامکِ تازه بگیرد — هزینه‌ی واقعی، هم برای او هم برای
    اعتبارِ پنل.
  - ⚠ مقایسه‌ی زمانش در PHP است و استثنای قاعده‌ی «زمان را در دیتابیس
    بسنج» نیست: هر دو طرف `time()` اند (اپکِ یونیکس، مستقل از منطقه‌ی
    زمانی). آن قاعده برای مقایسه‌ی مقدارِ PHP با `NOW()` است.
  - `restart` در پاسخ یعنی «برگرد به مرحله‌ی شماره». بدونش کاربر روی
    فرمِ رمز می‌ماند و هر بار همان خطا را می‌گیرد بی‌آنکه بفهمد باید
    کدِ تازه بگیرد.
- **⛔ «این دستگاه را به خاطر بسپار» تیک‌خورده می‌آید** (هر سه فرمِ ورود)
  و برچسبش می‌گوید چه کار می‌کند، نه فقط اسمش را. **این همان چیزی است
  که «دفعه‌ی بعد هیچ چیزی نمی‌خواهد» را می‌سازد** — و جایگزینِ عمدیِ
  «ورود فقط با زدنِ شماره». ورودِ بی‌احراز اصلاً ساخته نشد: هر کسی که
  شماره‌ی کسی را بداند به دفتر، شماره کارت و شبای او می‌رسید و
  می‌توانست پاکش کند، **بی‌هیچ ردی**.
- **⚠ حساب‌های بی‌رمزِ موجود دست‌نخورده‌اند و همه‌ی نگهبان‌هایشان می‌مانند**:
  `password_hash` هنوز nullable است، `userHasPassword()` سرِ جایش، سه
  فرمِ «رمز فعلی» برایشان باز، و استثنای `no_other_door` هم. آن‌ها
  **میراث‌اند، نه چیزی که از امروز ساخته شود** — و تست دقیقاً به همین
  شکل می‌سنجدشان: یکی را دستی می‌سازد، چون دیگر از هیچ مسیرِ اپ
  در‌نمی‌آید.
- **⛔ `phoneSignupEnabled()` تنها جای این تصمیم است** و **هر دو** کلیدِ
  مدیر را می‌خواهد: `allow_signup` و `allow_sms_login`. کلیدِ سومی
  عمداً ساخته نشد — مالکِ نصب باید بتواند کلِ قابلیتِ پیامک را با **یک**
  کلید بخواباند، حتی برای کاربرِ Pro، و آن کلید همان `allow_sms_login`
  است. با کلیدِ سوم، خاموش کردنِ آن یکی مسیرِ ثبت‌نام را باز می‌گذاشت و
  مالکِ نصب فکر می‌کرد پیامک خاموش است.
- **⛔ `SmsLogin::loginAllowedFor()` تنها جای گیتِ ورودِ پیامکی است**
  (مثل `chequeActiveSql()`). سه دروازه، به همین ترتیب:
  1. **کلیدِ مدیر** — بالاتر از هر پرداختی. خاموش یعنی خاموش.
  2. **طرح** — از `planAllows(…, 'sms_login')` رد می‌شود، تنها جای
     تصمیم‌های طرح. `planEnforced()` پیش‌فرض خاموش است، پس **امروز هیچ
     نصبی چیزی از دست نمی‌دهد**.
  3. **⛔ استثنای «حسابِ بی‌رمز»** — امروز فقط برای حساب‌های **میراثی**
     (ثبت‌نام از این به بعد رمز می‌خواهد)، و این نوشته می‌ماند چون
     وسوسه‌ی برداشتنش واقعی است. کسی که با شماره ثبت‌نام کرده و رمزی نگذاشته،
     پیامک **تنها درِ** اوست؛ بستنش پاداشِ نخریدن نیست، گروگان گرفتنِ
     دفترِ خودش است — همان چیزی که «فقط خواندنی، نه قفلِ کامل» برای
     نفیِ آن نوشته شد. و کاربری که نتواند وارد شود اصلاً به خریدن هم
     نمی‌رسد.
     - ⚠ بله، یعنی «رمز نگذار تا پیامکِ رایگان داشته باشی». هزینه‌اش
       کوچک است (چنین کاربری هیچ راهِ دیگری ندارد و سقفِ سه پیامک در
       ساعت سرِ جایش است) و هزینه‌ی حالتِ دیگر برگشت‌ناپذیر. **به‌محضِ
       گذاشتنِ رمز، گیت برایش فعال می‌شود** و تست همین را جدا می‌سنجد.
- **⛔ گیت **بعد از** تأییدِ کد اعمال می‌شود، نه پیش از فرستادنِ آن.**
  پیش از تأیید، هر پیامِ متفاوتی («این شماره ثبت شده ولی Pro نیست»)
  همان لو دادنِ فهرستِ کاربران است. بعد از تأیید، آن آدم مالکیتِ شماره
  را ثابت کرده و دیگر چیزی برای لو رفتن نمانده. هزینه‌اش یک پیامک است
  که سقفِ ساعتی محدودش می‌کند — و در عمل نزدیک به صفر، چون کاربرِ
  رمزدار با رمز وارد می‌شود.
- **⛔ ثبت‌نام هرگز پولی نمی‌شود.** اگر `sms_login` گیتِ ساختِ حساب هم
  می‌شد، کاربرِ تازه باید **پیش از داشتنِ حساب** پول می‌داد. تست هر دو
  نیمه را می‌سنجد.
- **⛔ `password_hash` می‌تواند `NULL` باشد** و آن **تنها** معنای
  «این حساب رمز ندارد» است (`migration_phone_signup`). ⚠ از وقتی رمز
  هنگامِ ثبت‌نام اجباری شد، هیچ مسیرِ تازه‌ای به این حالت نمی‌رسد؛ ستون
  برای حساب‌های میراثی nullable مانده است. ستونِ بولینِ
  جدا (`has_password`) ساخته نشد: دو منبعِ حقیقت دیر یا زود از هم دور
  می‌افتند. پرسشش فقط از `userHasPassword()` می‌گذرد.
  - ⚠ و یک تصحیح که آزمونِ جهش داد: `password_verify($p, null)` روی
    PHP 8 خطای کشنده **نمی‌دهد**؛ `null` را رشته‌ی خالی می‌گیرد،
    `false` برمی‌گرداند و فقط `Deprecated` می‌نویسد. پس نگهبانِ
    `Auth::verifyCredentials()` رفتار را عوض نمی‌کند و **جهشش زنده
    ماند** تا وقتی تست خودِ آن هشدار را نسنجید. دو دلیل که می‌ماند:
    هشدارِ همیشگی در لاگ (که آدم را عادت می‌دهد هشدارها را نادیده
    بگیرد)، و اینکه در PHP 9 همین `TypeError` می‌شود.
- **⛔ «ایمیل الزامی است» جایش را به «دست‌کم یک راهِ بازگشت» داد** و این
  جایگزینی است نه حذف. استدلالِ قبلی («کسی که خودش ثبت‌نام می‌کند مدیری
  ندارد که رمزش را عوض کند») هنوز درست است — ولی **شماره‌ی تأییدشده
  دقیقاً همان کار را می‌کند**. پس شرط شد: ایمیل **یا** شماره. حسابِ
  بی‌هر‌دو از روزِ اول یک بن‌بست است و ساخته نمی‌شود.
  - همین قاعده در `api/update_profile.php` هم هست، وگرنه کاربری که با
    شماره ثبت‌نام کرده پروفایلش را هم نمی‌توانست ذخیره کند.
  - متنِ خطای مسیرِ بی‌شماره **عیناً** همان متنِ قبلی ماند، پس کاربرِ
    آن مسیر تفاوتی حس نمی‌کند و تستِ متن‌سنجِ `test_signup` هم سرِ
    جایش است.
- **نام کاربری خودِ شماره است** (`usernameFromPhone()`): خواسته‌ی صریح
  «شماره مثل نام کاربری عمل کند» بود، و نامِ تصادفی چیزی است که کاربر
  نه یادش می‌ماند نه با آن وارد می‌شود. `09xxxxxxxxx` از
  `usernameRuleError()` رد می‌شود، پس ویرایشِ بعدیِ پروفایل بن‌بست
  نمی‌سازد (همان درسِ `ali.k`). برخوردِ تکراری پسوند می‌گیرد.
  - **⚠ نامِ نمایشی عمداً شماره نیست** بلکه «کاربر + چهار رقمِ آخر»:
    نامِ نمایشی بالای هر صفحه دیده می‌شود و کاربر گاهی صفحه را جلوی
    دیگران باز می‌کند — همان استدلالی که شماره‌ی حساب و شبا را از رویِ
    کارتِ خانه برداشت.
- **شماره یک شناسه‌ی ورودِ کامل است**، مثل ایمیل: `verifyCredentials()`
  روی `phone` هم تطبیق می‌دهد و **پیش از مقایسه نرمال می‌کند**، وگرنه
  `+98912…` و `0912…` دو چیزِ متفاوت بودند و خرابی بی‌صدا (همان پیامِ
  «نام کاربری یا رمز عبور اشتباه است»).
- **⛔ سه فرمِ «رمز فعلی» برای حسابِ بی‌رمز بن‌بست بودند** و هر سه باز
  شدند: ویرایشِ پروفایل، تعیینِ رمز، و حذفِ حساب. هیچ‌کدام دری باز
  نمی‌کند — کاربر همین حالا واردِ حسابِ خودش است؛ تنها چیزی که عوض
  می‌شود این است که «رمزِ قبلی» ای که وجود ندارد پرسیده نمی‌شود.
  - حذفِ حساب برای این کاربر **دو** سد دارد نه سه (نشستِ خودش، و تایپِ
    دقیقِ عبارتِ تأیید). جایگزینش «اول رمز بگذار» بود که یعنی گروگان
    گرفتنِ داده.
  - کارتِ «تعیین رمز عبور» در پروفایل **باز** رندر می‌شود نه جمع‌شده:
    تنها کاری است که آن کاربر واقعاً باید انجام بدهد.
- **`sms_codes.user_id` هم nullable شد**: کدِ ثبت‌نام پیش از وجود
  داشتنِ کاربر می‌رود. ⚠ سقفِ درخواست روی ستونِ `phone` شمرده می‌شود نه
  `user_id`، پس این هیچ سدی را باز نمی‌کند.
- **`deploy/user-admin.php --sms-check` هم به‌روز شد و این اختیاری
  نبود:** گامِ ۳ آن می‌گفت «هیچ کاربری با این شماره نیست، علتِ کد
  نیامدن همین است» — که با باز بودنِ ثبت‌نام **دروغ** است. سنجشی که روی
  سیستمِ سالم تشخیصِ غلط می‌دهد، آینه‌ی همان سنجشی است که روی خرابی سبز
  می‌شود. گامِ ۵ هم اضافه شد (گیتِ Pro)، چون «کد می‌رسد ولی وارد
  نمی‌شوم» از بقیه گیج‌کننده‌تر است. هر سه شاخه روی دیتابیسِ واقعی اجرا
  و دیده شدند.
- **قاعده ۲۸ در `test_api_contract.php`** دو تصمیم را بسته نگه می‌دارد:
  فقط `SmsLogin::loginAllowedFor()` گیتِ `'sms_login'` را می‌خواند، و
  هر فایلی که `password_verify()` دارد باید در همان فایل حسابِ بی‌رمز
  را هم بسنجد (همان شکلِ قاعده ۱۴، تا مسیرِ پنجمیِ فردا جا نماند). با
  دو جهش سنجیده شد.
  - ⚠ و نسخه‌ی اولش روی فایلِ **سالم** قرمز شد: رشته‌های متنی را هم دور
    می‌ریخت، پس نگهبانِ واقعی (`$user['password_hash'] === null`) به
    `$user[] === null` تبدیل می‌شد. فقط **کامنت‌ها** باید حذف شوند.
- تست در `tests/test_phone_signup.php` است (۱۰۱ بررسی، نیمی با HTTP و
  نشستِ واقعی) و با **۲۴ جهش** سنجیده شده، همه گرفته شد. شش جهشِ تازه:
  همیشه‌درستِ `phoneVerifyOk()`، نسوزاندنِ نشانه، نسنجیدنِ شماره‌ی داخلِ
  نشانه، برگشتِ «رمز اختیاری»، نادیده گرفتنِ مهلتِ نشانه، و ساختنِ
  دوباره‌ی حساب در `phoneAuthComplete()`.
  - ⚠ «نشانه سوزانده نشد» با یک بررسیِ ساده گرفته **نمی‌شد**: فراخوانیِ
    دوم همچنان `ok=false` برمی‌گرداند (شماره تکراری است) ولی یک ردیفِ
    کاربرِ **یتیم** با نامِ `…‎.2` به جا می‌گذارد. تست شمارشِ ردیف‌ها را
    هم می‌سنجد، وگرنه نشتی بی‌صدا می‌ماند.
  - ⚠ مهلتِ نشانه با ساختنِ **دستیِ** نشانه در نشست سنجیده می‌شود؛ راهِ
    دیگرش پانزده دقیقه صبر کردن بود.
  - ⚠ `test_sms_login.php` حالا `allow_signup` را صریح **خاموش** می‌کند.
    آن فایل دنیای «فقط ورود» را می‌سنجد و وضعیتِ اطرافش را به شانس
    سپرده بود؛ با روشن ماندنِ ثبت‌نام از اجرای قبلی، بررسیِ «برای
    شماره‌ی ناموجود کدی ساخته نمی‌شود» درست شکست می‌خورد.
  - ⚠ همه‌ی درخواست‌های سرورِ آزمایشی از `127.0.0.1` می‌آیند، پس سقفِ
    «۱۰ درخواست در ساعت برای هر IP» بعد از چند اجرا تست را
    **وابسته به تعدادِ اجراهای قبلی** می‌کرد. یک بار همین شد و پنج
    بررسی بی‌دلیل قرمز شدند؛ حالا پیش از هر ثبت‌نامِ HTTP پاک می‌شود.
    خودِ آن سقف در `test_sms_login.php` سنجیده می‌شود.

## بکاپ: `backup.php` و پسوندِ `.sthesab`

- **⛔ صفحه‌ی جدا دارد چون بن‌بست می‌ساخت.** پیش از این دکمه‌ی خروجی یک
  فرمِ POST مستقیم به `api/export_data.php` بود. مسیرِ موفق مشکلی
  نداشت (مرورگر فایل را می‌گیرد و صفحه عوض نمی‌شود)، ولی هر **شکستی**
  کاربر را در یک صفحه‌ی سفید با یک خطِ JSON رها می‌کرد — بی‌منو،
  بی‌دکمه، و در اپِ نصب‌شده حتی بی‌دکمه‌ی بازگشتِ مرورگر.
- دو لایه جلویش را می‌گیرد و **هر دو لازم‌اند**: `backup.php` دکمه‌ی
  «بازگشت» دارد که همیشه سرِ جایش است (چه بکاپ بگیرد چه پشیمان شود)، و
  خودِ اندپوینت هم با `exportFail()` در هر شکستی به همان‌جا برمی‌گردد.
  - ⚠ استثناء: **بدونِ ورود همچنان ۴۰۱ می‌دهد، نه هدایت.** قاعده‌ی
    `api/` این است و `test_api_auth` هر ۵۹ اندپوینت را با هم می‌سنجد.
    کاربرِ واقعی هم به آنجا نمی‌رسد چون `backup.php` خودش
    `requireLogin()` دارد.
  - ⚠ `Csrf::isJsonRequest()` **خصوصی** است و از بیرون صدا زده نمی‌شود؛
    `exportFail()` همان شرط را خودش دارد (هم `X-Requested-With` هم
    `Accept`). یک بار فراموش شد و اندپوینت ۵۰۰ داد.
- **`backupFileName()` تنها جای ساختنِ نامِ فایل است**: فقط تاریخِ شمسیِ
  همان روز و پسوندِ `.sthesab` — `1405-06-12.sthesab`. کوتاه است تا در
  فهرستِ دانلودِ گوشی کامل خوانده شود، و نام روی صفحه هم **پیش از**
  زدنِ دکمه نشان داده می‌شود تا کاربر بداند دنبالِ چه بگردد.
  - درونش JSON است ولی پسوندِ `.json` یعنی مرورگر و پیام‌رسان سعی
    می‌کنند بازش کنند و کاربر یک دیوارِ متن می‌بیند. `Content-Type` هم
    به همین دلیل `application/octet-stream` شد.
  - ⚠ ارقام **لاتین**اند و `/` به `-` می‌رود: نامِ حاوی ارقامِ فارسی در
    سرآیندِ `filename=` غیرمجاز است و مرورگر بی‌سروصدا کنارش می‌گذارد.

## خروجیِ کاملِ داده و حذفِ حساب

- **دلیل وجودشان:** کاربری که حس کند داده‌اش گروگان است پول نمی‌دهد. «هر وقت
  خواستی همه‌اش را ببر و هر وقت خواستی پاکش کن» تنها چیزی است که این حس را از
  بین می‌برد — برای فروش پیش‌نیاز است نه تزئین. هر دو در پروفایل، بخش «داده‌ی من».
- **⛔ فهرستِ جدول‌ها از خودِ دیتابیس کشف می‌شود** (`userDataTables()`: هر جدولی
  که ستون `user_id` دارد)، نه از یک آرایه‌ی دستی. فهرستِ دستی دیر یا زود عقب
  می‌افتد و اینجا عقب افتادنش دو خرابیِ بی‌صدا می‌سازد: خروجی‌ای که کامل نیست، و
  حذفی که چیزی جا می‌گذارد.
- خروجی JSON است، ستون‌های رمزشده در آن **باز** می‌شوند (فایلی که نمی‌شود خواند
  خروجی نیست)، و `password_hash` و جدول‌های اعتبارنامه (`api_tokens`,
  `password_resets`, `trusted_devices`) در آن نمی‌آیند — آن‌ها داده‌ی کاربر
  نیستند و فقط سطحِ حمله را بزرگ می‌کنند.
  - ⚠ نامِ فایل با **ارقام لاتین** ساخته می‌شود. نامِ حاوی ارقامِ فارسی در سرآیندِ
    `filename=` غیرمجاز است و مرورگر بی‌سروصدا کنارش می‌گذارد و فایل را
    «download» ذخیره می‌کند. با کروم دیده شد.
  - ⚠ بافرِ gzip که `db.php` روشن می‌کند باید پیش از ارسالِ فایل بسته شود، وگرنه
    چیزی که ذخیره می‌شود دوبار فشرده است و باز نمی‌شود.
- حذف سه سد دارد: رمز فعلی، تایپِ عبارتِ «حذف حساب»، و اینکه آخرین مدیر نتواند
  خودش را حذف کند (`countOtherActiveAdmins()` که به همین مناسبت از
  `admin/users.php` به `functions.php` منتقل شد — قاعده‌ی دامنه است نه جزئیاتِ
  یک صفحه).
- **⛔ و یک سدِ چهارم داخلِ خودِ `deleteUserAccount()`:** پیش از `commit` شمرده
  می‌شود که از هر جدول بیش از سهمِ همین کاربر حذف نشده باشد؛ اگر شد، rollback.
  - **دلیلش یک خرابیِ واقعی است.** هنگام آزمونِ جهش، نسخه‌ی جهش‌یافته‌ی این تابع
    (بدونِ شرطِ `user_id`) روی دیتابیسِ **توسعه** اجرا شد و **کامیت کرد**. تست
    بعدش خرابی را دید، ولی داده از قبل رفته بود. یک تستِ خوب جلوی حذفِ
    کامیت‌شده را نمی‌گیرد؛ خودِ کد باید بگیرد. با اجرای همان جهش روی یک
    دیتابیسِ موقت سنجیده شد: حالا رد می‌شود و چیزی از دست نمی‌رود.
  - **⛔ جهش‌سنجیِ `tests/test_user_data.php` را روی دیتابیسِ دارای داده اجرا
    نکنید.** همین هشدار بالای خودِ فایل هم هست.
- ترتیبِ حذف از روی کلیدهای خارجی حدس زده نمی‌شود بلکه **چند دور** تلاش می‌شود؛
  دوری که هیچ پیشرفتی نداشته باشد یعنی واقعاً گیر کرده و با خطا برمی‌گردد.
  ساختنِ فهرستِ ترتیب دستی همان «فهرستِ دوم» می‌شد.
- `privacy.php` عمداً کوتاه و بی‌تعارف است و **هر ادعایش باید در کد قابل نشان
  دادن باشد**. اگر روزی یکی از آن‌ها عوض شد، این صفحه هم باید همان روز عوض شود.
  وضعیتِ رمزنگاری را از `Crypto::available()` می‌خواند، نه از یک جمله‌ی ثابت.
- **نسخه از `var/version.txt` خوانده می‌شود** که `deploy.sh` هنگام استقرار
  می‌نویسد. از `git` خوانده نمی‌شود چون pool این اپ `exec`/`shell_exec` را
  غیرفعال کرده (قانون جداسازی سرور).
- تست در `tests/test_user_data.php` است (۱۹ بررسی) و با چهار جهش سنجیده شده:
  فهرستِ دستیِ ناقص، ماندنِ هشِ رمز در خروجی، حذف بدونِ شرطِ `user_id`، و آمدنِ
  جدول‌های اعتبارنامه در خروجی.

### ⛔ صفحه‌ی حریم خصوصی از پیکربندی می‌خواند، نه از متنِ خودش

- **خرابیِ واقعی، و دقیقاً روی صفحه‌ای که کلِ ارزشش راست بودن است:** آن
  صفحه یک جمله‌ی ثابت داشت — «هیچ چیزی به سرویس بیرونی فرستاده نمی‌شود»
  — و روزی که دامنه پشتِ کلادفلر رفت، همان جمله **دروغ** شد: TLS روی
  لبه‌ی آن‌ها باز می‌شود، پس رمز و شماره کارتِ کاربر از دیدشان رد
  می‌شود. نه خطایی، نه تستِ قرمزی، و `git pull` هم عوضش نمی‌کرد چون
  جمله ثابت بود.
- **⛔ `outboundDataFlows()` تنها جای این تصمیم است** (مثل
  `categoryScopeSql()`) و از خودِ پیکربندی می‌خواند: CDN، روشِ ایمیل، و
  پنلِ پیامک. فهرستِ **خالی** یعنی واقعاً هیچ چیزی بیرون نمی‌رود — و
  همان جمله‌ی مطلق فقط در آن حالت گفته می‌شود.
- **⛔ `cdnInFront()` فقط می‌تواند «بله» بگوید، نه «نه».** ثابتِ
  `APP_BEHIND_CDN` روشنش می‌کند ولی هیچ‌وقت خاموشش نمی‌کند، و یک
  `false`ِ صریح هم سنجشِ سرآیند را ساکت نمی‌کند. علتش نامتقارن بودنِ
  هزینه‌ی اشتباه است: «CDN هست» گفتن وقتی نیست فقط افشای بیش از حد است
  (بی‌ضرر)، ولی عکسش یعنی همان دروغ. **مهم‌ترین بررسیِ تست هم همین
  است**، چون مالکِ نصبی که خطِ نمونه را دست‌نخورده کپی کرده دقیقاً در
  آن حالت می‌افتد.
- **⛔ هیچ سرآیندِ آی‌پی‌ای خوانده نمی‌شود** — فقط `CF-Ray` و `CDN-Loop`،
  که نشانی از هیچ‌کس نیستند و هیچ تصمیمِ اعتمادی رویشان سوار نمی‌شود
  (قاعده ۲۷). جعلشان فقط باعث می‌شود همان بازدیدکننده متنِ
  محافظه‌کارانه‌تری ببیند.
- **⛔ `SMS_METHOD = 'log'` در فهرست نمی‌آید:** آن حالت هیچ‌جا نمی‌فرستد
  (فقط `var/sms.log`)، پس ادعای «به پنلِ بیرونی می‌رود» درباره‌اش دروغ
  است — همان‌قدر دروغ که جمله‌ی قبلی بود، فقط در جهتِ عکس.
- ⚠ **مصرف‌کننده باید `includes/sms.php` را لود کند**، وگرنه
  `class_exists('Sms')` نادرست می‌شود و قلمِ پیامک **بی‌صدا** از فهرست
  می‌افتد. قاعده ۳۰ همین را هم می‌سنجد. (به همین دلیل `functions.php`
  خودش آن فایل را نمی‌آورد: بقیه‌ی صفحه‌ها نباید کوئریِ اضافه بدهند.)
- `deploy/nginx-realip.sh` بعد از فلیپِ موفق یادآوری می‌کند که آن یک خط
  در `config.php` هم گذاشته شود — همان‌جایی که آدم به کلادفلر فکر
  می‌کند، نه شش ماه بعد.
- دو تست و هر دو لازم: **قاعده ۳۰** در `test_api_contract.php` *شکل* را
  می‌سنجد (صفحه از تابع بخواند، ادعای مطلق فقط داخلِ شاخه‌ی خالی، هر سه
  منبع پرسیده شود، مصرف‌کننده `sms.php` را لود کند) و
  `tests/test_privacy.php` *رفتار* را (۱۸ بررسی). با **هشت جهش** سنجیده
  شد، همه گرفته شد.

### ⛔ رمزنگاریِ شماره کارت، شماره حساب و شبا

- **پیش‌فرض خاموش، و این عمدی است.** بدون `APP_ENCRYPTION_KEY` همه چیز دقیقاً
  مثل قبل کار می‌کند (خام). روشن کردنش: `php deploy/encrypt-cards.php --setup`
  که کلید می‌سازد، در `config/config.php` می‌نویسد، و از کانفیگ قبلی بکاپ
  می‌گیرد. بعدش `--apply` ردیف‌های موجود را مهاجرت می‌دهد.
- **⛔ کلید گم شود، داده رفته است.** بکاپِ دیتابیس **بدونِ کلید قابل بازیابی
  نیست** — کلید در `config/config.php` است که در دامپ نمی‌آید. این عمدی است:
  اگر کلید کنارِ دیتابیس بماند، رمزنگاری بی‌معناست. `restore.sh` بعد از بازیابی
  اگر مقدارِ رمزشده ببیند صریحاً هشدار می‌دهد.
- **`Crypto` (`includes/crypto.php`) تنها جای این کار است.** قالب:
  `enc:v1:` + base64(نانس ‖ متنِ رمزشده) با `sodium_crypto_secretbox`
  (XSalsa20-Poly1305، احراز اصالت‌دار).
  - **رمزگشاییِ چیزی که رمز نشده، خودِ همان است.** ردیف‌های قدیمی خام‌اند و باید
    بخوانند؛ بدون این، روشن کردنِ رمزنگاری همه‌ی شماره‌کارت‌های موجود را
    ناپدید می‌کرد.
  - شکستِ رمزگشایی `null` می‌دهد نه رشته‌ی آشغال — آشغال می‌رفت داخل فرم و
    کاربر ذخیره‌اش می‌کرد.
  - نانس تصادفی است: با نانسِ ثابت، دو حسابِ هم‌شماره مقدارِ ذخیره‌شده‌ی یکسان
    می‌گرفتند و از روی خودِ دیتابیس می‌شد فهمید کدام کاربران یک کارت دارند.
- **جاهایی که باید از `Crypto` رد شوند:** نوشتن در `api/save_wallet.php`،
  خواندن در `walletBalances()` و `walletCardTails()`. هر کوئریِ تازه‌ای که این
  ستون‌ها را بیاورد هم باید `Crypto::decryptRow()` بخورد، وگرنه کاربر رشته‌ی
  `enc:v1:…` را داخل فرمِ خودش می‌بیند و با ذخیره کردنش شماره‌اش را نابود
  می‌کند. ⚠ در `walletCardTails()` رمزگشایی باید **پیش از** برداشتنِ ارقام
  باشد، وگرنه «از پیامک بانک» حساب را با چهار رقمِ تصادفی اشتباه می‌گیرد.
- **ستون‌ها با `migration_wallet_encrypt` به ۲۵۵ پهن می‌شوند.** بدون آن، روی
  پیکربندیِ غیر-strict مقدار **بی‌صدا بریده** می‌شد و رمزگشایی برای همیشه شکست
  می‌خورد. شاهدِ این migration در `migrate.sh` از نوع تازه‌ی
  `جدول.ستون>=طول` است؛ شاهدِ «وجود ستون» اینجا همیشه درست می‌بود و
  `--verify` دقیقاً همان دروغی را می‌گفت که برای گرفتنش ساخته شده.
- `deploy/encrypt-cards.php` سه محافظ دارد و هیچ‌کدام اختیاری نیست: هر ردیف
  **پیش از نوشتن** رفت‌وبرگشت آزموده می‌شود، همه در یک تراکنش‌اند، و بعد از
  نوشتن همه‌ی مقدارها دوباره خوانده و با اصل مقایسه می‌شوند. `--decrypt-all`
  راهِ برگشت است.
- تست در `tests/test_crypto.php` است (۱۹ بررسی) و با پنج جهش سنجیده شده: نانسِ
  ثابت، `null` کردنِ مقدارِ خام، لایه‌ی دومِ رمز، نادیده گرفتنِ شکستِ رمزگشایی،
  و بی‌اثر کردنِ `decryptRow`.

### ⛔ تگِ پایانِ PHP داخل کامنت

- نوشتنِ آن تگ داخل یک کامنتِ `//` هم PHP را **می‌بندد** و بقیه‌ی فایل HTML
  حساب می‌شود. یک بار در `deploy/encrypt-cards.php` همین شد (در جمله‌ای که
  توضیح می‌داد «پیش از تگِ پایانِ PHP اضافه می‌شود») و خطای «Unclosed {» داد که
  ۵۰ خط بالاتر را نشان می‌داد. **اینجا شانس آوردیم؛** در فایلی که آکولادهایش
  بالانس باشد هیچ خطایی نمی‌دهد و فقط نصفِ فایل بی‌صدا اجرا نمی‌شود — یا سورس
  به مرورگر می‌رود. قاعده ۱۵ در `test_api_contract.php` این را می‌سنجد.

### ⛔ ریدایرکت و لینکِ داخلی همیشه مطلق — وگرنه ۴۰۴ِ خودِ nginx

- `Auth::requireLogin()` می‌نوشت `header('Location: login.php')`. مرورگر مسیرِ
  نسبی را نسبت به **آدرسِ همان درخواست** حل می‌کند، پس روی `/admin/users.php`
  می‌شد `/admin/login.php` که وجود ندارد. آن‌وقت `try_files $uri =404` (که از
  راهِ `include snippets/fastcgi-php.conf` داخلِ قاعده‌ی `\.php$` است) درخواست
  را **اصلاً به PHP نمی‌رساند**: کاربر صفحه‌ی ۴۰۴ِ خودِ nginx را می‌دید —
  بی‌قالب، بی‌منو، و **بدون هیچ ردی در لاگ PHP**.
  - **بدترین حالتش این بود:** مدیر در `admin/users.php` تنظیمات را ذخیره
    می‌کرد، نشستش همان لحظه منقضی شده بود، و به‌جای صفحه‌ی ورود یک ۴۰۴ِ خام
    می‌گرفت — بی‌آنکه بفهمد ذخیره شد یا نه. با nginx واقعی بازتولید و سنجیده
    شد: پیش از اصلاح `/admin/login.php` با کد ۴۰۴، پس از آن `/login.php` با ۲۰۰.
  - همه‌ی صفحه‌های ریشه درست کار می‌کردند، پس **«روی گوشی/داشبورد درست است»
    اینجا مدرک نبود**؛ فقط `/admin/` می‌شکست.
- **قاعده: فایلِ مشترک هرگز مسیرِ نسبی ننویسد.** هر چیزی در `includes/` و
  `assets/js/` روی صفحه‌هایی با عمقِ مختلف اجرا می‌شود، پس مسیرِ نسبی آنجا
  «گاهی درست» است و همین بدترین حالت است. مطلق بنویسید: `APP_BASE_PATH` در
  PHP و `window.APP_BASE` در جاوااسکریپت (`app.js` برای `logout.php` همین را
  نداشت).
- قاعده ۱۶ در `test_api_contract.php` هر دو نیمه را می‌سنجد: مسیرِ نسبی در
  فایلِ مشترک، و لینک یا ریدایرکتی که فایلش روی دیسک نیست (مثل صفحه‌ای که حذف
  شده ولی لینکش در منو مانده). با سه جهش سنجیده شده و هر سه گرفته شد.
  - ⚠ نسخه‌ی اولِ خودِ تست مسیرِ نسبیِ فایلِ مشترک را «نسبت به ریشه» حل می‌کرد
    و آن‌وقت `login.php` سبز می‌شد — یعنی تست نسبت به همان باگی که برایش نوشته
    شده بود **کور** بود. جهشِ اول همین را نشان داد.

### ⛔ رمز که عوض شد، هر دسترسیِ قبلی باطل می‌شود

- **`revokeAllAccessFor()` تنها جای این کار است** (مثل `categoryScopeSql()`). هر
  چهار مسیرِ نوشتنِ رمز از آن رد می‌شوند: `api/change_password.php`,
  `includes/password_reset.php`, `admin/users.php`, `deploy/user-admin.php`.
- **چرا لازم است:** توکنِ `api/v1` نه به رمز وابسته است نه به نشست — ۹۰ روز زنده
  می‌ماند و رمزِ تازه هیچ اثری رویش ندارد. کاربری که حسابش لو رفته رمز را عوض
  می‌کند، پیام «رمز عوض شد» می‌گیرد، خیالش راحت می‌شود، و مهاجم همچنان با اپ
  موبایل داخل است. **هیچ خطایی، هیچ نشانه‌ای.** پیش از این فقط `trusted_devices`
  باطل می‌شد.
- **⚠ ترتیبِ `require` داخلِ خودِ تابع اهمیت دارد و یک بار شکست:** نسخه‌ی اول اول
  `Auth::revokeAllDevices()` را صدا می‌زد و بعد `api_auth.php` را لود می‌کرد.
  `password_reset.php` تنها `db/mailer/functions` را لود می‌کند، پس آنجا کلاس
  `Auth` وجود نداشت و فراخوانی خطای کشنده می‌داد — که در همان فایل با
  `catch (Throwable)` بلعیده می‌شد و بازیابیِ رمز بی‌صدا **هیچ** دسترسی‌ای را
  باطل نمی‌کرد. `test_password_reset` گرفتش.
- دو تست جدا و هر دو لازم: قاعده ۱۴ در `test_api_contract.php` *شکل* را می‌سنجد
  (هر فایلی که `UPDATE users SET … password_hash` دارد باید تابع را صدا بزند، پس
  مسیرِ پنجمی که فردا اضافه شود جا نمی‌ماند)، و `tests/test_token_revoke.php`
  *رفتار* را: توکنِ واقعی می‌سازد، ثابت می‌کند کار می‌کند، رمز را عوض می‌کند، و
  ثابت می‌کند در یک **پروسه‌ی جدا** دیگر کار نمی‌کند (کشِ ایستای
  `ApiAuth::$current` وگرنه جوابِ کهنه می‌دهد). با سه جهش سنجیده شده، از جمله
  برداشتنِ شرطِ `user_id` که تغییر رمزِ یک نفر همه‌ی کاربران را بیرون می‌انداخت.

### ⛔ تصمیمِ مدیر باید به نشستِ زنده برسد

- **خرابیِ واقعی، و کاملاً بی‌صدا:** نشستِ PHP فقط یک فایل روی دیسک است
  و `Auth::isLoggedIn()` فقط به `$_SESSION` اعتماد می‌کرد. پس مدیر
  کاربری را «غیرفعال» می‌کرد، پیامِ سبز می‌گرفت، و آن کاربر با مرورگرِ
  بازش تا هر وقت خودش خارج نمی‌شد کار می‌کرد — و با «بدون مهلت» (که
  پیش‌فرضِ همه است) یعنی **هرگز**. برداشتنِ نقشِ مدیر هم همین بود.
  `api_auth.php` توکنِ کاربرِ غیرفعال را از قبل رد می‌کرد؛ فقط وب باز
  مانده بود. «کاربر غیرفعال شد» دروغ بود.
- **رفعش همان تیکِ «تمدیدِ `last_seen` هر یک دقیقه» است که از قبل بود**
  (`touchSession()` → `refreshAccountState()`): همان‌جا یک کوئریِ سبک به
  `users` می‌زند و سه چیز می‌سنجد — ردیف هست و `is_active = 1`؛
  `access_revoked_at` بعد از `login_time`ِ نشست نیست؛ و `role` را از
  دیتابیس در نشست تازه می‌کند. یعنی **بدترین حالت شصت ثانیه** فاصله بینِ
  تصمیمِ مدیر و بیرون افتادنِ کاربر است، نه یک کوئری در هر درخواست.
  - ⚠ این سقف یک **مصالحه‌ی عمدی** است و تست گروهِ ۳ آن را ثبت می‌کند:
    اگر کسی «درستش کند» و در هر درخواست بپرسد، تست قرمز می‌شود تا
    تصمیم آگاهانه باشد، نه تصادفی.
- **⛔ `users.access_revoked_at` (`migration_access_revoke`) همان چیزی است
  که «خروج از همه‌ی دستگاه‌ها» و تغییرِ رمز را برای نشستِ وب هم واقعی
  می‌کند.** پیش از این `revokeAllAccessFor()` فقط `trusted_devices` و
  `api_tokens` را می‌برد؛ مرورگری که همان لحظه وارد بود می‌ماند — یعنی
  همان ادعای «رمز که عوض شد، هر دسترسیِ قبلی باطل می‌شود» برای نشستِ
  زنده درست نبود. حالا آن تابع مهر را هم می‌زند و `isLoggedIn()` نشستِ
  قدیمی‌تر از مهر را خالی می‌کند. `NULL` یعنی هرگز، پس نصبِ موجود بعد
  از migration کسی را بیرون نمی‌اندازد.
  - **⛔ مقایسه‌ی زمان در دیتابیس است** (`access_revoked_at >
    FROM_UNIXTIME(:login)`)، نه در PHP — همان درسِ لینکِ بازیابیِ رمز.
  - **⛔ اگر خودِ کاربر ابطال را راه انداخته** (تغییر رمز در پروفایل)،
    نشستِ جاری‌اش هم قدیمی‌تر از مهر است و تا یک دقیقه‌ی بعد بیرون
    می‌افتاد — یعنی تنبیهِ کسی که کارِ درست را کرده. `api/change_password.php`
    بعدش `Auth::renewCurrentSession()` را می‌زند. هر مسیرِ تازه‌ای که
    کاربر برای **خودش** `revokeAllAccessFor()` صدا بزند همین را لازم دارد.
  - ستون نیامده → کوئریِ دوم بدونِ آن ستون (فقط `42S22` این معنا را
    دارد؛ هر خطای دیگری خرابیِ دیتابیس است و کاربر را بیرون نمی‌اندازد،
    مثل `LoginThrottle`). و عمداً **بین درخواست‌ها کش نمی‌شود**: یک
    ایستای «ستون نیست» در FPM تا بازیافتِ کارگر می‌ماند و بعد از
    migration سنجش را بی‌صدا خاموش نگه می‌داشت.
- **پنلِ مدیر:** غیرفعال‌سازی حالا `revokeAllAccessFor()` را هم صدا می‌زند،
  و دکمه‌ی تازه‌ی **«خروج از دستگاه‌ها»** (`revoke_access`) همان کار را
  بی‌آنکه حساب بسته یا رمز عوض شود انجام می‌دهد — برای «گوشی‌ام را گم
  کرده‌ام». روی حسابِ خودِ مدیر رندر نمی‌شود؛ راهش پروفایل است.
- تست در `tests/test_admin_session.php` است (۳۱ بررسی) و با **شش جهش**
  سنجیده شده، همه گرفته شد: برداشتنِ سنجشِ دیتابیس، برداشتنِ مهرِ
  ابطال، برداشتنِ تازه‌سازیِ نقش، سنجش در هر درخواست، `revokeAllAccessFor`
  بدونِ مهر، و غیرفعال‌سازیِ بدونِ ابطال.

### ⛔ صفحه‌ی کاربران باید ابزارِ پشتیبانی باشد، نه فقط فهرست

چهار کارِ روزمره‌ی پشتیبانی یا اصلاً ممکن نبودند یا فقط از راهِ SSH.

- **باز کردنِ قفلِ ورود.** سدِ `LoginThrottle` بین کاربرِ واقعی و مهاجم
  فرق نمی‌گذارد، پس کاربری که رمزش را پنج بار غلط زده تا پایانِ پنجره
  بیرون می‌ماند. تنها راهِ باز کردنش `deploy/user-admin.php --unlock`
  بود — یعنی **مالکِ نصبی که SSH ندارد هیچ کاری نمی‌توانست بکند**.
  - دکمه فقط کنارِ کاربری رندر می‌شود که واقعاً تلاشِ ناموفق دارد
    (`LoginThrottle::failureCounts()`)، وگرنه یک دکمه‌ی همیشه‌روشن بود
    که معلوم نیست کاری می‌کند یا نه.
  - **⚠ یک کوئری برای کلِ فهرست، نه یکی به‌ازای هر ردیف.** با ده‌ها
    کاربر، N+1 همان چیزی است که در «سرعت» بارها گرفته شده.
  - نشانِ کمتر از سقف کهربایی است و نشانِ قفلِ کامل قرمز: با یک رنگ،
    مدیر «۲ تلاش» و «قفل شده» را از هم تشخیص نمی‌داد — و همان تفاوت
    است که تصمیمش را عوض می‌کند.
- **⛔ حذفِ کاربر واقعاً کار نمی‌کرد.** کوئری یک `DELETE FROM users`
  خام بود که روی اولین کلیدِ خارجی می‌خورد و پیام می‌داد «این کاربر
  تراکنش دارد و قابل حذف نیست» — یعنی **هر کاربری که یک بار از اپ
  استفاده کرده باشد حذف‌شدنی نبود**، و دقیقاً همان کاربری است که
  می‌خواهد پاک شود. حالا از `deleteUserAccount()` رد می‌شود: ترتیبِ
  جدول‌ها را خودش پیدا می‌کند، همه را در یک تراکنش می‌برد، و پیش از
  commit می‌شمارد که از هیچ جدولی بیش از سهمِ همین کاربر حذف نشده باشد.
  - ⚠ اینجا عمداً `confirm()` مانده و «لغو» نشده: `Undo` فقط یک ردیف و
    فرزندانِ CASCADE اش را عکس می‌گیرد، و حذفِ کاربر داده‌ی هر جدولی را
    می‌برد — همان دلیلی که حذفِ معامله و حساب هم `confirm()` نگه داشتند.
- **شماره موبایل در فرمِ ویرایش.** بدونش تنها راهِ ثبتِ شماره
  `deploy/user-admin.php --set-phone` بود، و همان بن‌بستِ مرغ و
  تخم‌مرغِ «ورود با کد پیامکی» می‌ماند: روزِ اولی که پنل راه می‌افتد
  هیچ‌کس شماره ندارد، پس هیچ‌کس نمی‌تواند وارد شود. از `saveUserPhone()`
  رد می‌شود (نه `UPDATE` دستی) و خالی یعنی «دست نزن».

#### ⛔ قاعده ۲۵ — قاعده‌ی نام کاربری فقط در `usernameRuleError()`

- **چهار مسیر نام کاربری می‌نوشتند و هر کدام الگوی خودش را داشت:**
  `setup.php`، `includes/signup.php`، `admin/users.php` و
  `api/update_profile.php`. دو تا با هم نمی‌خواندند — سه‌تای اول
  `[a-zA-Z0-9_.]` (نقطه مجاز، بدونِ حداقلِ طول) و چهارمی
  `[A-Za-z0-9_]{3,50}` (نقطه ممنوع).
- **خرابی‌اش یک بن‌بستِ کامل بود:** کاربری با نامِ `ali.k` — که خودِ
  پنل مدیر ساخته — هر بار پروفایلش را ذخیره می‌کرد «نام کاربری معتبر
  نیست» می‌گرفت، **بی‌آنکه نامش را عوض کرده باشد**. یعنی ایمیل و
  شماره‌اش را هم نمی‌توانست ذخیره کند، و خطا درباره‌ی فیلدی بود که دست
  نزده بود — پس هیچ راهی هم برای فهمیدنش نداشت.
- **⛔ قاعده‌ی سازنده برنده شد، نه سخت‌گیرانه‌ترین.** هر نام کاربری‌ای که
  امروز در هر دیتابیسی هست از همان قاعده گذشته؛ حداقلِ طولِ ۳ هرگز
  هنگام ساخت اجرا نشده بود، پس اعمالش فقط کسانی را از حسابِ خودشان
  بیرون می‌کرد که کارِ اشتباهی نکرده‌اند.
- قاعده ۲۵ در `test_api_contract.php` این را می‌سنجد و **همان اجرای
  اول `setup.php` را پیدا کرد** — مسیرِ چهارمی که در بازبینیِ چشمی از
  قلم افتاده بود.
  - ⚠ با توکنایزر است و **فقط وقتی که همان خط `username` داشته باشد**:
    نسخه‌ی اول توصیفِ شکلِ کلیدِ پنلِ پیامک در `includes/sms.php` را هم
    قرمز کرد — هشدارِ الکی روی فایلِ سالم، که از نبودِ تست بدتر است.
- تست‌های رفتاری در `tests/test_signup.php` (قاعده‌ی نام) و
  `tests/test_login_throttle.php` (شمارشِ قفل) هستند.

### ⛔ پشتِ CDN، `REMOTE_ADDR` دیگر کاربر نیست

- **مسئله‌ی واقعی:** `hesab.stland.ir` مستقیم به آی‌پیِ سرور می‌خورد و
  آن آی‌پی از ایران در دسترس نیست — کاربر بدون فیلترشکن اصلاً اپ را باز
  نمی‌کند. تشخیصش قطعی بود: `stland.ir` که **پشت کلادفلر** است بدون
  فیلترشکن باز می‌شود و زیردامنه‌ی اپ نه. پس راهش بردنِ همان زیردامنه
  پشتِ کلادفلر است، نه هیچ تغییری در کد.
- **⛔ ولی روشن کردنِ ابر بدونِ این کار، همه‌ی کاربران را بیرون
  می‌اندازد.** پشتِ هر CDN، `REMOTE_ADDR` آی‌پیِ لبه است و شش جای اپ روی
  همان حساب می‌کنند. بدترینش سدِ حدس رمز است: سقفِ «۲۰ تلاش در ۱۵ دقیقه
  برای هر آی‌پی» یک‌باره برای **همه با هم** شمرده می‌شود و ظرف چند
  دقیقه هیچ‌کس وارد نمی‌شود — با همان پیامی که عمداً هیچ نمی‌گوید چرا.
  بازیابیِ رمز، ثبت‌نام و سقفِ پیامک هم همین‌طور.
- **⛔ جایگزینی در nginx است، نه در PHP** (`deploy/nginx-realip.sh`).
  اگر PHP سرآیندِ `CF-Connecting-IP` را بخواند، باید خودش تصمیم بگیرد
  کِی باورش کند — و آن **یک آسیب‌پذیری است، نه یک میان‌بر**: سرآیند را
  هر کسی می‌فرستد، پس هر چهار سد با یک خطِ curl دور می‌خورند. nginx فقط
  وقتی جایگزین می‌کند که **خودِ اتصال** از یکی از بازه‌های کلادفلر آمده
  باشد؛ آن‌وقت `REMOTE_ADDR` خودش درست است و هیچ‌جای PHP عوض نمی‌شود.
  قاعده ۲۷ در `test_api_contract.php` این را نگه می‌دارد و با جهش
  سنجیده شده.
- **⛔ اسکریپت خودِ رفتار را می‌سنجد، در هر دو جهت** — چون هر کدام یک
  خرابیِ متفاوت را می‌گیرد و هر دو بی‌صدایند:
  ۱. سرآیند از آی‌پیِ **نامعتبر** نباید باور شود (وگرنه قفل‌ها دور
  می‌خورند)؛ ۲. از آی‌پیِ **معتبر** باید باور شود (وگرنه ماژول بارگذاری
  نشده یا نامِ سرآیند غلط است و همان قفلِ دسته‌جمعی برمی‌گردد). برای
  جهتِ دوم، `127.0.0.1` **موقتاً** قابل اعتماد می‌شود و بلافاصله
  برداشته. با nginx واقعیِ همان نسخه اجرا و دیده شد.
- `location = /__realip` فقط از خودِ سرور باز است و آی‌پیِ دیده‌شده را
  برمی‌گرداند — تنها راهِ دیدنِ اینکه سرور واقعاً چه می‌بیند.
  - **⛔ گیتش روی `$realip_remote_addr` است، نه `allow`/`deny` — و این
    اجباری است.** `return` در فازِ **rewrite** اجرا می‌شود و آن **پیش
    از** فازِ **access** است، پس `allow 127.0.0.1; deny all;` کنارِ یک
    `return` هرگز نوبتش نمی‌رسد. نسخه‌ی اول همین را داشت و کامنتِ
    بالایش هم می‌گفت «بیرون بسته است» — در حالی که مسیر برای کلِ
    اینترنت **باز** بود. روی سرور واقعی دیده شد (درخواست از بیرون
    به‌جای ۴۰۳، **۲۰۰** گرفت و آی‌پی را چاپ کرد) و با nginx 1.24
    بازتولید شد؛ همان‌جا هم دیده شد که از loopback ۲۰۰ می‌دهد و از یک
    آدرسِ غیرِ loopback هم ۲۰۰.
    - چیزی که لو می‌رفت **آی‌پیِ خودِ درخواست‌کننده** بود، یعنی عملاً
      هیچ. ولی «نگهبانی که کار نمی‌کند» به‌علاوه‌ی ادعای نوشته‌شده‌ی
      کنارش، دقیقاً همان چیزی است که این پروژه جای دیگری برایش تست
      نوشته.
    - **`$realip_remote_addr` آدرسِ واقعیِ اتصال است، پیش از جایگزینی.**
      پس بازدیدکننده‌ی بیرونی (که از لبه‌ی کلادفلر می‌آید) ۴۰۴ می‌گیرد،
      ولی **هر دو سنجشِ خودِ اسکریپت** — که از `127.0.0.1` وصل می‌شوند و
      سرآیند می‌فرستند — دست‌نخورده کار می‌کنند. با nginx واقعی هر چهار
      حالت سنجیده شد: loopback ۲۰۰، loopback با سرآیندِ جعلی ۲۰۰ (مقدارِ
      جعلی چاپ می‌شود، که همان چیزی است که سنجشِ دوم لازم دارد)، از
      بیرون ۴۰۴، و از بیرون با سرآیندِ جعلی هم ۴۰۴.
    - قاعده ۲۷ در `test_api_contract.php` این را نگه می‌دارد و **روی
      بلوکِ رندرشده** می‌سنجد، نه روی متنِ اسکریپت (دلیلش پایین).
- **⛔ بلوک با `awk -v` به فایلِ سایت منتقل نمی‌شود، با `getline` از یک
  فایلِ موقت می‌رود.** awk مقدارِ `-v` را مثل رشته‌ی برنامه تفسیر می‌کند و
  هر دنباله‌ی escape را **بی‌صدا** عوض می‌کند: `\.` می‌شود `.` و
  `\n` می‌شود یک خطِ واقعی. یعنی چیزی که روی سرور می‌نشست با چیزی که
  `build_block` ساخته بود یکی نبود — و تستی که **سورس** را می‌سنجید،
  چیزی را تأیید می‌کرد که اجرا نمی‌شود.
  - روی سرورِ واقعی با هشدارِ `awk: warning: escape sequence \. treated
    as plain .` وسطِ خروجیِ `--apply` دیده شد. **هر دو تغییر اتفاقاً
    بی‌ضرر بودند** (رجکسِ ضعیف‌تر ولی هنوز درست، و خطِ واقعی به‌جای `\n`
    که nginx همان‌طور برمی‌گرداند) — ولی «اتفاقاً بی‌ضرر» یک خاصیت نیست،
    و هشدارِ همیشگی وسطِ یک اسکریپتِ امنیتی همان چیزی است که آدم را
    عادت می‌دهد هشدارها را نادیده بگیرد.
  - با `getline` انتقال بایت‌به‌بایت است و **آزموده شد**: خروجیِ
    `build_block` با بلوکی که داخلِ فایلِ سایت می‌نشیند `diff` صفر دارد
    (۲۵ خط).
  - قاعده ۲۷ خودِ `awk -v block=` را ممنوع می‌کند. ⚠ نسخه‌ی اولِ آن
    بررسی به‌جای مسیرِ انتقال، «بک‌اسلش نداشتنِ بلوک» را می‌سنجید و
    **روی فایلِ سالم قرمز شد**: آن `\n` نحوِ درستِ nginx است. قاعده باید
    چیزی را ببندد که خراب است، نه چیزی را که فقط شبیهِ آن است.
  - **⛔ و بعد از نارنجی شدنِ ابر، همان آدرس از بیرون ۴۰۴ می‌دهد — که
    نشانه‌ی *سلامت* است، نه خرابی.** نسخه‌ی اولِ اسکریپت دقیقاً همین
    آدرس را به‌عنوان سنجشِ **پس از** فلیپ چاپ می‌کرد: یعنی مالکِ نصب
    سرِ همان لحظه‌ی حساس یک صفحه‌ی خطا می‌دید و نتیجه می‌گرفت کار
    نکرده. **سنجشی که روی سیستمِ سالم قرمز می‌شود، آینه‌ی همان سنجشی
    است که روی خرابی سبز می‌شود** — هر دو دروغ می‌گویند. حالا سنجشِ
    چاپ‌شده با `--resolve` به `127.0.0.1` می‌خورد (همان قاعده‌ی
    همیشگی) و متن صریح می‌گوید خطای بیرونی انتظارِ درست است.
- **⛔ حالتِ SSL/TLS باید «Full (strict)» باشد، نه «Flexible».** با
  Flexible کلادفلر با **http** به سرور وصل می‌شود، بلوکِ `:80` (که
  certbot ساخته) به https برش می‌گرداند، و حلقه‌ی ریدایرکت می‌شود —
  یعنی سایت برای **همه با هم** و دقیقاً در لحظه‌ی نارنجی شدن می‌خوابد.
  گواهیِ اوریجین معتبر است (Let's Encrypt)، پس strict بی‌دردسر کار
  می‌کند. اسکریپت این را کنارِ خودِ دستور چاپ می‌کند.
- **⚠ و همان سنجش دو بار غلط بود، هر دو درسِ تکراری:**
  - نسخه‌ی اول با `http://127.0.0.1/...` می‌زد، پس روی سروری که چند سایت
    دارد به بلوکِ **پیش‌فرض** می‌خورد و ۴۰۴ِ سایتِ دیگری را می‌گرفت —
    همان چیزی که قاعده‌ی «با `--resolve` بسنج، نه با آدرسِ خام» برایش
    نوشته شده بود و رعایت نشد.
  - و بدتر: چون فقط «با مقدارِ جعلی برابر نیست» را می‌سنجید، همان
    صفحه‌ی ۴۰۴ را **موفق** خواند. **سنجشی که روی خرابی سبز می‌شود از
    نبودِ سنجش بدتر است.** حالا پاسخ باید شکلِ آی‌پی داشته باشد، وگرنه
    صریح می‌گوید درخواست به بلوکِ این سایت نخورده. هر دو حالت با nginx
    واقعی و یک بلوکِ `default_server` بازتولید و سنجیده شد.
- **فهرستِ بازه‌ها زنده گرفته می‌شود**، با فهرستِ پشتیبانِ داخلِ اسکریپت
  و **هشدارِ صریح** اگر به آن رسید. فهرستِ کهنه همان قفلِ دسته‌جمعی است،
  فقط دیرتر. فهرستِ **ناقص** از کهنه بدتر است (نیمی از کاربران آی‌پیِ
  لبه می‌گیرند و نیمی نه)، پس یا کاملِ زنده نوشته می‌شود یا کاملِ
  پشتیبان.
- ⚠ **هزینه‌ی کلادفلر واقعی است و باید نوشته بماند:** TLS روی لبه‌ی آن‌ها
  باز می‌شود، یعنی ترافیکِ کاربر — از جمله رمز و شماره کارت — از دیدشان
  رد می‌شود. `privacy.php` باید همان روزی که ابر نارنجی می‌شود بازخوانی
  شود؛ آن صفحه قرار است هر ادعایش در کد نشان‌دادنی باشد.
- فقط `sites-available/hesab` دست می‌خورد و فقط `reload` می‌شود، آن هم
  بعد از تستِ موفق — قانون جداسازیِ سرور دست‌نخورده می‌ماند.

### فایل‌های خط‌فرمانی داخل ریشه‌ی وب

`tests/` و `deploy/` داخل `/opt/hesab/app` هستند، یعنی داخل ریشه‌ی وب. دو لایه جلویشان را می‌گیرد و **هر دو لازم‌اند**:

1. سایت nginx این مسیرها را می‌بندد: `/deploy/`, `/tests/`, `/config/`, `/includes/`, `.git`, فایل‌های نقطه‌دار، و پسوندهای `.sql` / `.md` / `.sh`.
2. **خودِ فایل نگهبان دارد:** `if (PHP_SAPI !== 'cli') { http_response_code(404); exit; }`.

لایه‌ی دوم اختیاری نیست. لایه‌ی اول می‌تواند از دست برود — certbot فایل سایت را بازنویسی می‌کند، یا کسی روی هاست دیگری نصب می‌کند — و آن‌وقت باز کردن `https://…/tests/test_trades.php` در مرورگر، تست را روی دیتابیس **واقعی** اجرا می‌کرد: کاربر و کیف پول و معامله می‌ساخت و پاک می‌کرد، بدون هیچ احراز هویتی. `test_api_contract.php` وجود این نگهبان را روی همه‌ی `tests/test_*.php` و `deploy/*.php` می‌سنجد.

> در متن راهنمای اسکریپت‌ها placeholder به شکل `<...>` ننویسید. کاربر آن را عیناً کپی می‌کند و bash علامت `<` را تغییرمسیر ورودی می‌فهمد و فرمان با `syntax error near unexpected token` می‌میرد. به‌جایش مقدار نمونه‌ی واقعی بنویسید (`--set-email ali ali@gmail.com`).

## API نسخه‌دار برای اپ‌های موبایل (`api/v1`)

مرجع کامل برای کسی که اپ می‌نویسد: **`docs/API.md`**.

- **`api/` و `api/v1/` دو چیز کاملاً جدا هستند.** `api/` لایه‌ی داخلیِ AJAX خودِ سایت است: با نشست و کوکی و CSRF کار می‌کند، آزادانه با صفحه‌ها عوض می‌شود، و چهار تای آن (`day_detail`, `category_transactions`, `savings_history`, `transaction_attachments`) اصلاً HTML برمی‌گردانند نه JSON. **اپ موبایل هرگز نباید به آن‌ها وصل شود.**
- **قاعده‌ی نسخه، دلیلِ وجودِ این جداسازی است:** اپی که روی گوشی کاربر نصب شده را نمی‌شود مجبور به به‌روزرسانی کرد. اگر اپ روی `api/save_wallet.php` می‌نشست، هر تغییر در سایت همان لحظه هر اپ نصب‌شده‌ای را می‌شکست. در v1 **افزودن** مجاز است (کلید تازه، پارامتر اختیاری، اندپوینت تازه) و **تغییر یا حذف** ممنوع؛ هر تغییرِ شکننده → `api/v2/`.
- **احراز هویت با توکن است، نه نشست.** `includes/api_auth.php` با همان الگوی selector/validator ای که `trusted_devices` و `password_resets` دارند. توکن خام هرگز ذخیره نمی‌شود، فقط `sha256`. عمر ۹۰ روز، حداکثر ۱۰ توکن زنده برای هر کاربر.
  - **`Auth::userId()` و `cachedCategories()` در v1 ممنوع‌اند** چون کاربر را از نشست می‌گیرند و درخواست API نشستی ندارد — شناسه صفر می‌شد و کاربر فقط دسته‌های پیش‌فرض را می‌دید. قاعده ۹ در `test_api_contract.php` این را می‌سنجد (با توکنایزر، وگرنه همین توضیح، تست را می‌شکست — یک بار واقعاً شکست).
- **`Auth::verifyCredentials()` از `attemptLogin()` جدا شد.** اولی فقط رمز را می‌سنجد و به نشست دست نمی‌زند؛ دومی همان را صدا می‌زند و بعد نشست می‌سازد. بدون این جداسازی، منطق سدِ حدس رمز باید برای API دوباره نوشته می‌شد و دو نسخه‌اش از هم دور می‌افتادند.
- **منطق نوشتن تراکنش در `includes/transactions.php` است و وب و API هر دو از همان‌جا رد می‌شوند.** پیش از این داخل `api/add_transaction.php` و `api/update_transaction.php` بود؛ با آمدن v1 باید بار دوم نوشته می‌شد و دو نسخه از منطقِ پول دیر یا زود از هم دور می‌افتند. آن توابع هیچ چیز چاپ نمی‌کنند و `exit` نمی‌کنند — فقط نتیجه برمی‌گردانند تا هر لایه پاکت خودش را بسازد.
- **تاریخ همیشه هم میلادی و هم شمسیِ آماده فرستاده می‌شود** (`Api::date()`). اگر اپ خودش تبدیل کند، می‌شود پیاده‌سازی سومِ تقویم شمسی — بیرون از پوشش `test_jalali_parity` — و دیر یا زود یک روز اختلاف پیدا می‌کند.
- **مبلغ همیشه عدد صحیح است** (`Api::money()`). ستون‌ها `BIGINT` اند؛ اگر به `double` برود، مقدارهای بزرگ دقتشان را از دست می‌دهند.
- **⛔ قاعده‌ی nginx هرگز `^~` نمی‌گیرد.** `^~` ارزیابیِ location های regex را متوقف می‌کند و `location ~ \.php$` هم regex است — یعنی nginx فایل PHP را به‌جای اجرا **خام** تحویل می‌دهد. یک بار روی سرور واقعاً همین شد: `/api/v1/ping` سورس کامل `index.php` را برگرداند و API اصلاً کار نکرد. `nginx -t` سبز بود و `reload` هم موفق، پس هیچ ابزاری جلویش را نگرفت. با nginx واقعی A/B شد: با `^~` سورس لو می‌رود، با prefix ساده به PHP می‌رسد. `deploy/nginx-api.sh` حالا بعد از اعمال خودِ `/api/v1/ping` را صدا می‌زند و اگر سورس یا چیزی جز JSON دید، پیکربندی را برمی‌گرداند؛ قاعده ۱۰ در `test_api_contract.php` هم `^~` را ممنوع می‌کند.
  - درسِ کلی‌ترش: **«پیکربندی معتبر است» با «درست کار می‌کند» یکی نیست.** هر اسکریپتی که پیکربندی وب‌سرور را عوض می‌کند باید آخرش خودِ نتیجه را بسنجد، نه فقط `nginx -t` را.
- **دو شکل آدرس، و فقط دومی بی‌نیاز از nginx است.** `/api/v1/transactions` به قاعده‌ی nginx نیاز دارد (`deploy/nginx-api.sh --apply`)؛ `/api/v1/index.php?p=transactions` همیشه کار می‌کند چون آدرس واقعی خودِ `index.php` است و به `location ~ \.php$` می‌خورد. **این دوگانگی عمدی است:** اگر certbot یا کسی فایل سایت را بازنویسی کند، آن قاعده از دست می‌رود و اپ‌های نصب‌شده نباید بخوابند.
  - ⚠ **`/api/v1/index.php/transactions` روی nginx کار نمی‌کند** و اولین نسخه‌ی سند اشتباه ادعا کرده بود که کار می‌کند. `~ \.php$` با آدرسی که به `/transactions` ختم می‌شود تطابق ندارد؛ آن شکل فقط روی سرور توسعه‌ی PHP جواب می‌دهد. با nginx واقعی سنجیده شد: `?p=` می‌رسد، `index.php/…` نمی‌رسد.
- **سنجشِ بعد از `reload` باید صبر داشته باشد.** `systemctl reload nginx` به‌محضِ فرستادنِ سیگنال برمی‌گردد؛ خودِ nginx غیرهمزمان بارگذاری می‌کند و پروسه‌های کارگرِ قدیمی تا صدها میلی‌ثانیه بعد هنوز با پیکربندیِ قبلی جواب می‌دهند. سنجشِ فوری همان پاسخِ قدیمی را می‌گرفت و `nginx-api.sh` پیکربندیِ **درست** را برمی‌گرداند — دو بار روی سرور واقعی همین شد و وقت زیادی گرفت. با nginx واقعی اندازه‌گیری شد: تا **۱۵۰ میلی‌ثانیه** پاسخِ قدیمی می‌آمد. حالا سنجش تا ۱۰ ثانیه تکرار می‌شود و فقط اگر تا آخر درست نشد رول‌بک می‌کند (مگر سورس خام که همان‌جا خطر است).
- **سنجشِ اسکریپت‌ها با `--resolve` باشد، نه `-H "Host: …"`.** روی این سرور چند سایت روی یک nginx هستند. `curl -H "Host: x" https://127.0.0.1` هیچ SNI ای نمی‌فرستد، پس nginx بلوکِ پیش‌فرض را برای دست‌دادن TLS برمی‌دارد و پاسخ ممکن است از سایتِ دیگری باشد — یک بار همین باعث شد `nginx-api.sh` پیکربندیِ **درست** را به‌خیالِ خراب بودن برگرداند.
- تست رفتاری در `tests/test_api_v1.php` است و قربانی را خودش می‌سازد. مهم‌ترین بخشش جداسازی کاربران است و با آزمون جهش سنجیده شده: برداشتن بررسی مالکیت یا فیلترِ `user_id` هر دو تست را می‌شکند.

## اپ اندروید (`mobile/`) — پوسته‌ی TWA روی همان PWA

### ⛔ نامِ بسته `ir.stland.hesabland` — و پنج جایی که باید با هم بخوانند

- **تنها مرجعش `applicationId` در `mobile/app/build.gradle.kts` است**
  (مثل `categoryScopeSql()`)، و `namespace` هم همان مقدار را می‌گیرد.
  پنج جای دیگر فقط **مصرف‌کننده**اند: پوشه‌ی سورسِ جاوا و خطِ `package`
  هر دو فایل، `action` صفحه‌ی تنظیمِ پیامک در manifest، `action=` و
  `package=` در لینکِ `intent://` داخل `profile.php`، و `package_name`
  در `.well-known/assetlinks.json`.
- **هر کدام که جا بماند، خرابی‌اش بی‌صداست و شکلش هم فرق می‌کند:**
  assetlinks عقب‌مانده یعنی اپ **نصب می‌شود ولی با نوار آدرس بالا**؛
  لینکِ `intent://` عقب‌مانده یعنی دکمه‌ی «تنظیم در اپ اندروید» زده
  می‌شود و **هیچ اتفاقی نمی‌افتد** — نه خطایی، نه صفحه‌ای. هیچ‌کدام
  هنگام ساخت دیده نمی‌شوند. قاعده ۱۲ در `test_api_contract.php` هر پنج
  را از روی همان یک خطِ gradle می‌سنجد و با هشت جهش سنجیده شده.
- **⚠ `keyAlias = "daftar"` عمداً با برند عوض نشد.** آن رشته نامِ ما
  نیست، نامِ ورودیِ داخلِ همان فایلِ keystore است که در
  `KEYSTORE_BASE64` نشسته (و در `.github/workflows/android.yml` هم
  همان است). عوض کردنش یعنی `keytool` ورودی را پیدا نمی‌کند و ساخت با
  **«keystore password was incorrect»** می‌ایستد — پیامی که هیچ ربطی
  به علتش ندارد و ساعت‌ها دنبالِ رمز می‌گردید.
- **⚠ و عوض کردنِ نامِ بسته یعنی یک اپِ کاملاً تازه.** برای هر کسی که
  نسخه‌ی قبلی را نصب کرده، نسخه‌ی تازه **کنارِ** آن نصب می‌شود نه
  رویش. تا وقتی `assetlinks.json`ِ **روی سرور** به‌روز نشده، نسخه‌ی
  تازه با نوار آدرس بالا می‌آید — و نسخه‌ی **قدیمیِ نصب‌شده** هم از
  همان لحظه نوار آدرس می‌گیرد، چون اسمش دیگر در آن فایل نیست.

### ⛔ مجوزها — و `LAUNCHER` که جابه‌جا شد و **پس داده شد**

- **خرابیِ واقعی و بی‌صدا:** `POST_NOTIFICATIONS` در manifest **اعلام
  شده بود ولی هیچ‌جا خواسته نمی‌شد**. روی اندروید ۱۳ به بالا یعنی
  کاربر مجوزِ خواندنِ پیامک را می‌داد، پیامک هم خوانده می‌شد،
  `nm.notify()` هم بی‌هیچ خطایی اجرا می‌شد — و **هیچ اعلانی دیده
  نمی‌شد**. از بیرون دقیقاً شبیهِ «قابلیت کار نمی‌کند» بود.
- **رفعش این است که `SmsSetupActivity` هر دو مجوز را در زمانِ اجرا
  بخواهد**، و آن تنها راهِ ممکن است: مجوزِ زمانِ اجرا فقط از یک
  اکتیویتیِ **خودمان** خواسته می‌شود و `LauncherActivity` مالِ
  کتابخانه است. راهِ رسیدن به آن صفحه همان لینکِ `intent://` از
  پروفایل است.
- **⛔ و یک بار `MAIN`/`LAUNCHER` به همان صفحه منتقل شد تا مجوز در
  «اولین اجرا» پرسیده شود — کار می‌کرد و باز هم پس داده شد:**
  - **هزینه‌اش روی مسیرِ پرتکرار می‌نشست.** یک اکتیویتیِ اضافه **پیش
    از** کروم، در **هر** بار باز کردنِ اپ. کاربر گزارش داد «انگار چند
    بار رفرش می‌شود، صفحه سفید می‌شود، بعد می‌آید». مجوز یک بار
    پرسیده می‌شود و اپ هزار بار باز می‌شود؛ گذاشتنِ هزینه‌ی دائمی برای
    سودِ یک‌باره معامله‌ی بدی است.
  - و بدتر از کندی: **اولین چیزی که کاربرِ تازه از یک دفترِ مالی
    می‌دید، یک صفحه‌ی مجوزِ پیامک بود.**
  - «در اجراهای بعدی هیچ view ای نمی‌سازد» جبرانش نکرد — همان پرشِ
    اضافه سرِ جایش می‌ماند. نشانه‌ی `sms_onboarded` هم با همین برگشت
    حذف شد، چون دیگر چیزی نیست که در اولین اجرا نشان داده شود.
  - فیلترِ `VIEW` با `autoVerify` روی `LauncherActivity` **همیشه** سرِ
    جایش بوده و هست — تأییدِ TWA به آن بند است، نه به `LAUNCHER`.
  - **قاعده ۱۲ حالا هر دو سمت را می‌بندد:** `LAUNCHER` باید روی
    `LauncherActivity` باشد و روی `SmsSetupActivity` **نباشد**. با
    بستنِ فقط یک سمت، همین جابه‌جایی دوباره بی‌صدا برمی‌گشت.
  - و دو رشته‌ی `sms_setup_skip` / `sms_setup_first_run` هم برداشته
    شدند: رشته‌ی بی‌مصرف فقط ترجمه‌ی بعدی را گمراه می‌کند.
- **⛔ `android:noHistory` روی آن اکتیویتی ممنوع است**، هرچند برای یک
  صفحه‌ی واسط طبیعی به نظر می‌رسد: با آن، اکتیویتی هنگام بالا آمدنِ
  دیالوگِ مجوز `finish` می‌شود و `onRequestPermissionsResult` **هرگز
  صدا زده نمی‌شود** — یعنی کاربر «اجازه» را می‌زند و اپ همچنان خاموش
  می‌ماند. قاعده ۱۲ همین را هم می‌بندد.
- **⛔ رد کردن هیچ دری را نمی‌بندد.** کلید خاموش می‌ماند، یک پیام داده
  می‌شود، و اپ کامل بالا می‌آید — همان قاعده‌ی «داده‌ی کاربر گروگان
  گرفته نمی‌شود». و در **اولین اجرا** کاربر به تنظیماتِ سیستم پرت
  نمی‌شود: کسی که همین حالا «نه» گفته، فرستادنش به آنجا یعنی نادیده
  گرفتنِ جوابش.
- **`POST_NOTIFICATIONS` فقط وقتی خواسته می‌شود که خودِ قابلیت روشن
  شود.** تنها چیزی که این اپ اعلان می‌کند همان پیامکِ بانک است، پس
  بدونِ آن قابلیت این مجوز واقعاً لازم نیست — و مجوزی که لازم نیست
  نباید خواسته شود. به همین دلیل `READ_SMS` هم اصلاً نیست: اپ صندوقِ
  پیامک را نمی‌خواند، فقط پیامکِ **رسیده** را می‌گیرد.
- **⛔ حالتِ سوم جدا نشان داده می‌شود: «مجوز هست، اعلان‌ها بسته‌اند».**
  سنجشش هم `areNotificationsEnabled()` است نه خودِ مجوز — آن یکی
  «کاربر اعلان‌های اپ را از تنظیماتِ گوشی بسته» را نمی‌دید، و آن هم
  دقیقاً همان خرابیِ بی‌صداست. دکمه‌ی رفعش **فقط در همان حالت** رندر
  می‌شود؛ دکمه‌ای که همیشه باشد و بیشترِ وقت‌ها کاری نکند همان
  «دکمه‌ی بی‌کار از نبودنش بدتر است».
- **⛔ و دو چیز روی `SmsSetupActivity` اجباری‌اند — هر دو با یک کرشِ
  واقعی خریداری شده‌اند:** در همان مدتِ کوتاهی که این صفحه درِ ورودی
  بود، اپ نصب می‌شد و با زدنِ آیکون **بسته می‌شد، بی‌هیچ توضیحی**.
  حالا که درِ ورودی نیست، هر دو **می‌مانند**: خرابیِ اینجا دیگر «اپ باز
  نمی‌شود» نیست ولی همچنان «صفحه‌ی تنظیم بی‌صدا بسته می‌شود» است، و آن
  هم همان خرابیِ بی‌صداست.
  - **۱. `AppCompatActivity`، نه `android.app.Activity`ِ خام.** تمِ این
    اکتیویتی `Theme.AppCompat.NoActionBar` است و همه‌ی `Button` و
    `TextView` ها در کد ساخته می‌شوند، پس سبکشان را از همان تم می‌گیرند —
    یک جفتِ ناهمخوان. تا وقتی این صفحه فقط از لینکِ `intent://` باز
    می‌شد **هیچ‌کس به آن نمی‌رسید**، پس ایراد سال‌ها بی‌صدا ماند و
    دقیقاً همان روزی بیدار شد که `LAUNCHER` رویش نشست.
  - **۲. `onCreate` گاردِ `Throwable` دارد و راهِ فرارش `finish()` است.**
    همه‌ی ویجت‌ها در کد ساخته می‌شوند و هر ناهمخوانیِ تم/اکتیویتی یک
    استثنا می‌دهد؛ بدونِ گارد، کاربری که «تنظیم در اپ اندروید» را
    می‌زند فقط یک صفحه‌ی نیمه‌ساخته یا یک اپِ بسته‌شده می‌بیند. متنِ
    خطا هم **نشان داده می‌شود** — تنها جایی در این پروژه که خطای خام به
    کاربر گفتن درست است، چون جایگزینش سکوتِ کامل است.
  - قاعده ۱۲ هر دو را می‌سنجد، و گاردش دقیقاً روی «`build()` داخلِ try
    است» می‌نشیند، به‌علاوه‌ی «`finish()` **بعد از** همان `catch`».
    ⚠ دو نسخه‌ی اولش **پوچ** بودند: اولی رشته‌ی `catch (Throwable` را
    در کلِ فایل می‌گشت، دومی در کلِ `onCreate` — و هر دو با
    `catch (Throwable ignored)`ِ خودِ همان بلوک سبز می‌ماندند.
    **دو جهش لازم شد تا معلوم شود.**
- workflow هم `POST_NOTIFICATIONS` را در manifestِ **خودِ APK** می‌گردد
  (کنارِ `RECEIVE_SMS` و آن دو کلاس) — همان درسِ «ساخت موفق شد ≠ درست
  کار می‌کند».
- ⚠ **دو اشتباهِ خودِ تست، و هر دو درسِ تکراری:**
  (الف) glob مسیرِ پکیج در قاعده ۱۹ سخت‌کد بود
  (`java/ir/stland/daftar/*.java`)، پس با اولین تغییرِ نام هیچ فایلی
  برنمی‌گرداند و همه‌ی بررسی‌هایش **بی‌صدا** ناپدید می‌شدند و تست سبز
  می‌ماند. حالا بازگشتی می‌گردد و **نبودنِ فایل خودش یک خطاست**.
  (ب) بررسیِ «کد واقعاً مجوز را می‌خواهد» اول کامنت‌ها را حذف نمی‌کرد و
  توضیحاتِ همان فایل نامِ هر دو مجوز را داشتند — پس با برداشتنِ کاملِ
  درخواستِ مجوز هم سبز می‌ماند. **جهشِ زنده‌مانده اول یعنی تست ناقص
  است، نه اینکه کد امن است.**

### ⛔ گرفتنِ خودکارِ پیامک بانک — تنها استثنای «اپ کد ندارد»

- **دو فایلِ بومی، و ناچاری‌اند نه انتخاب:** `BankSmsReceiver.java` و
  `SmsSetupActivity.java`. نه PWA و نه TWA به SMS دسترسی دارند و هیچ API
  وبی هم برایش نیست، پس گرفتنِ خودکار **فقط** از لایه‌ی بومی ممکن است.
  قاعده ۱۲ در `test_api_contract.php` حالا یک فهرستِ **بسته** دارد: فایلِ
  بومیِ سومی بی‌سروصدا اضافه نمی‌شود.
- **⛔ آن دو فایل پیامک را «نمی‌خوانند»، فقط تحویل می‌دهند.** کلِ منطق در
  `window.parseBankSms()` می‌ماند — همان تابعی که با ده جهش سنجیده شده و
  چهار محافظ دارد. پارسرِ دومِ جاوا یعنی دو پیاده‌سازی که دیر یا زود از
  هم دور می‌افتند و خرابی‌اش **بی‌صداست**. قاعده ۱۲ حتی نشانه‌های منطقِ
  دامنه («ریال»، «تومان»، تقویم، تقسیم بر ۱۰) را هم داخلِ همان دو فایل
  ممنوع می‌کند.
- **⛔ متنِ پیامک روی سیم نمی‌رود، و این تنها تضمینِ حریمِ خصوصیِ این
  قابلیت است.** آدرسی که اعلان باز می‌کند متن را در **فرگمنت** می‌گذارد
  (`#sms=…`) نه در query string: مرورگر فرگمنت را هرگز به سرور نمی‌فرستد
  — نه در خطِ درخواست، نه در `Referer`، نه در لاگِ دسترسیِ nginx.
  - تا دیروز این تضمین **رایگان** به دست می‌آمد (وب به SMS دسترسی نداشت،
    پس کاربر خودش می‌چسباند). حالا که خودکار شده باید عمداً نگه داشته
    شود؛ **عوض کردنِ یک `#` به `?` کلِ آن را بی‌صدا می‌برد و قابلیت
    دقیقاً همان‌طور کار می‌کند.** قاعده ۱۹ همین را می‌سنجد و هیچ کلاسِ
    شبکه‌ای هم در لایه‌ی بومی نمی‌گذارد.
- **⚠ `setTimeout(0)` در `app.js` اختیاری نیست.** بلوکِ خواندنِ فرگمنت
  بالاتر از `go.addEventListener('click', …)` است، پس بدونِ آن یک تیک
  تأخیر، `go.click()` وقتی اجرا می‌شد که هنوز شنونده‌ای ثبت نشده بود:
  جعبه باز می‌شد و متن می‌نشست ولی **هیچ چیزی پارس نمی‌شد**. در مرورگر
  واقعاً دیده شد.
- **⚠ کلاسِ باز شدنِ جعبه `open` است، نه `is-open`.** نسخه‌ی اول اشتباه
  نوشته بود و خرابی‌اش بی‌صدا بود: پارس انجام می‌شد ولی جعبه بسته
  می‌ماند، پس کاربر پیامِ «مبلغ پر شد» را نمی‌دید.
- **فرگمنت بلافاصله پاک می‌شود** (`history.replaceState`): با تازه‌سازیِ
  صفحه دوباره اجرا می‌شد (تراکنشِ تکراری) و تا آن موقع متنِ پیامک در
  نوارِ آدرس و تاریخچه‌ی مرورگر می‌ماند.
- **پیش‌فرض خاموش و دو شرطی**: کلیدِ داخلِ اپ **و** مجوزِ اندروید. صفحه‌ی
  تنظیمش «روشن» را فقط وقتی می‌گوید که هر دو برقرار باشند — وگرنه کاربری
  که مجوز را از تنظیماتِ گوشی پس گرفته «روشن است» می‌دید و هیچ اعلانی
  نمی‌گرفت.
- **⚠ آخرین تپ در حالتِ عادی دستِ کاربر است.** اعلان خودکار می‌آید و فرم از
  قبل پر می‌شود، ولی ثبت را خودِ کاربر می‌زند. دلیلش همان چیزی است که بالای
  «ثبت از پیامک بانک» نوشته شده: خرابیِ پارسر **بی‌صداست** و ثبتِ خودکارِ
  یک برداشتِ غلط، دفتر را بی‌آنکه کسی بفهمد خراب می‌کند. (برداشتنِ آن تپ
  فقط با کلیدِ زیر و فقط برای خواندن‌های **قطعی** ممکن است.)

### ⛔ برداشتنِ آن تپِ آخر — `smsAutoOk()`

- **⛔ پیش‌فرض خاموش، و این قابل مذاکره نیست.** روشن شدنش با یک
  به‌روزرسانی یعنی اپ بی‌آنکه کسی خواسته باشد شروع کند به **نوشتن در دفترِ
  مالیِ** کاربر — ساکت‌ترین و بدترین شکلِ تغییرِ رفتار. کلید در پروفایل و
  داخلِ همان کارتِ اندرویدیِ «ثبت خودکار از پیامک» است.
  - انتخابش در `localStorage` همان دستگاه می‌ماند، نه در دیتابیس: این رفتار
    فقط جایی معنا دارد که پیامک واقعاً می‌رسد (اپ اندروید با مجوزِ SMS)، و
    پاک شدنِ داده‌ی سایت **خاموشش** می‌کند نه روشن — یعنی شکستش به سمتِ امن
    است. `window.smsAutoEnabled` عمداً بیرون از `DOMContentLoaded` است تا
    همین «پیش‌فرض خاموش» در node آزمودنی باشد، نه فقط یک ادعای خواندنی.
- **⛔ `window.smsAutoOk()` تنها جای این تصمیم است** (مثل `categoryScopeSql()`)
  و سه شرط دارد که هیچ‌کدام اختیاری نیست:
  1. **واحد پول در متن نوشته شده باشد.** `rial_assumed` یعنی حدس، و حدسِ
     غلط یعنی مبلغِ **ده‌برابر**. این شرط خودبه‌خود قوی‌ترین نشانه‌ی مبلغ را
     هم تضمین می‌کند: عددی که «ریال» یا «تومان» کنارش نوشته شده.
  2. مبلغ مثبت و نوع روشن.
  3. **مقصد حدس نباشد** — یا کارت با حسابی خواند، یا کاربر اصلاً بیش از یک
     حساب ندارد.
- **⚠ شرطِ سوم اول «کارت باید بخواند» بود و آن یعنی این قابلیت برای
  رایج‌ترین حالت هرگز کار نمی‌کرد:** شیتِ ثبت وقتی کاربر **یک** حساب دارد
  اصلاً `<select>`ی نمی‌سازد (`input hidden` می‌گذارد)، پس هیچ تطبیقی رخ
  نمی‌داد. در مرورگر دیده شد، نه در بازبینی. با یک حساب هم ابهامی نیست.
- **⚠ `requestSubmit()` روی اعتبارسنجیِ خودِ مرورگر می‌ایستد، بی‌صدا.**
  فیلدِ `title` در فرم `required` است و مسیرِ دستی خالی رهایش می‌کند تا کاربر
  بنویسد — ولی اینجا کسی نیست که بنویسد. نتیجه: نه ثبتی، نه خطایی، نه
  پیامی. حالا عنوانِ پیش‌فرض («واریز — از پیامک بانک») گذاشته می‌شود **و**
  پیش از ارسال `checkValidity()` سنجیده می‌شود؛ اگر باز هم معتبر نبود،
  «خودکار» ادعا نمی‌کنیم.
- **⛔ `hashchange` هم لازم است.** اگر اپ همین حالا روی همان صفحه باز باشد،
  تپ روی اعلان فقط فرگمنت را عوض می‌کند: ناوبریِ **هم‌سند**، بدون بارگذاری
  دوباره، پس `DOMContentLoaded` اجرا نمی‌شود و پیامک **بی‌صدا نادیده گرفته
  می‌شود**. کاربر اعلان را می‌زند، اپ باز می‌شود، و هیچ اتفاقی نمی‌افتد.
  (این از قبل هم باگ بود، نه فقط برای مسیرِ خودکار.)
- **⛔ نوارِ «لغو» چیزی است که این را از یک ثبتِ پنهان جدا می‌کند.** بعد از
  ثبت، صفحه **باید** تازه شود (جمع‌ها و موجودی سمت سرور رندر شده‌اند)، پس
  یک دکمه‌ی درجا با همان تازه‌سازی از بین می‌رفت. نشانه در `sessionStorage`
  می‌ماند و `.sms-auto-bar` را همان صفحه‌ی تازه می‌سازد — با «لغو» که
  `api/delete_transaction.php` را صدا می‌زند. مهلتش دو دقیقه است، وگرنه
  کاربری که نیم‌ساعت بعد صفحه‌ای باز می‌کند نوارِ یک ثبتِ فراموش‌شده را
  می‌دید.
  - `api/add_transaction.php` حالا `id` هم برمی‌گرداند — یک کلیدِ **افزوده**،
    که بدونش «لغو» باید شناسه را حدس می‌زد.
- **⛔ نگهبانِ تکراری در مرورگر است، نه روی سرور.** پاک شدنِ فرگمنت جلوی
  *تازه‌سازی* را می‌گیرد ولی جلوی **تپِ دومِ همان اعلان** را نه. اثرِ انگشتِ
  پیامک (`window.smsFingerprint`، FNV-1a) با مهلتِ یک شبانه‌روز در
  `localStorage` می‌ماند و **هرگز از دستگاه بیرون نمی‌رود** — همان قاعده‌ای
  که متنِ پیامک را در فرگمنت نگه می‌دارد. اگر روزی وسوسه شدید برای
  «همگام‌سازی بینِ دستگاه‌ها» آن را به سرور بفرستید، دلیلِ نبودنش همین است.
- **وقتی خودکار نمی‌شود، دلیلش گفته می‌شود.** سکوت یعنی کاربر کلید را روشن
  کرده و گاهی کار می‌کند و گاهی نه، بی‌آنکه بفهمد چرا — و آن‌وقت نتیجه
  می‌گیرد کلید خراب است.
- تست در `tests/test_sms_parse.php` است (گروهِ آخر) و با **شش جهش** سنجیده
  شده: برداشتنِ محافظِ واحدِ حدس‌زده، برداشتنِ شرطِ مقصد، ورودیِ `null`
  (خطای کشنده به‌جای «نه»)، نرمال‌سازیِ اثرِ انگشت، بی‌اثر شدنِ ارقام در آن،
  و **روشن کردنِ پیش‌فرض**.
  - ⚠ جهشِ «شرطِ `!r || !r.ok` را بردار» اول **زنده ماند**، چون
    `parseBankSms` در شکست همه‌ی فیلدها را `null` می‌کند و شرط‌های بعدی
    خودشان می‌گرفتندش. تنها جایی که آن شرط واقعاً کار می‌کند ورودیِ `null`
    است — و آنجا نبودش خطای کشنده می‌دهد، یعنی کلِ ثبتِ تراکنش می‌خوابد.
    ورودیِ نشانه‌دارِ `__PROBE_JUNK__` در `sms_js_dump.js` برای همین اضافه
    شد. **جهشِ زنده‌مانده اول یعنی تست ناقص است، نه اینکه کد امن است.**
### ⛔ «هیچ اعلانی نیامد» سه علت دارد و از بیرون یک شکل‌اند

- **مسئله‌ی واقعی:** کاربر مجوز را داد، کلید را روشن کرد، و باز هم پیامک
  خوانده نشد. کد در هر نقطه‌ای که خواندنی بود درست بود — کانال ساخته
  می‌شد، زنجیره‌ی مجوز درست بود، گیرنده در APK بود (workflow با `aapt2`
  می‌سنجدش). یعنی علت جایی بود که **از روی کد دیدنی نیست**، و سه حالت
  کاملاً متفاوت دقیقاً یک نشانه می‌دادند:
  1. پیامک اصلاً به گیرنده **نرسید** — محدودیتِ پس‌زمینه‌ی رام، یا اپی که
     در حالتِ stopped مانده.
  2. رسید ولی از **صافی** نگذشت — متنِ آن بانک کلمه‌ی جهت یا عددِ درشت
     نداشت.
  3. رسید و اعلان ساخته شد ولی **اعلان‌ها بسته‌اند**.
- **⛔ و راهِ حلِ هر سه فرق می‌کند.** حدس زدن بینشان یعنی هر بار یک ساختِ
  کاملِ APK و یک نصب و آزمایشِ روی گوشی — و اگر حدس غلط باشد، هیچ چیزی
  یاد نمی‌گیریم. پس به‌جای حدس، **خرابی صدادار شد**؛ همان کاری که این
  پروژه همه‌جا با «خرابیِ بی‌صدا» می‌کند.
- **خطِ «آخرین پیامک» روی صفحه‌ی تنظیم** هر سه را از هم جدا می‌کند:
  «هنوز هیچ پیامکی نرسیده» (حالت ۱)، «رد شد چون کلمه‌ی جهت نداشت» (۲)، یا
  «شناخته شد و اعلان ساخته شد» (۳ — چون کاربر همان اعلان را ندیده).
  - **⛔ ردْ پیش از هر خروجِ زودهنگام نوشته می‌شود، نه در شاخه‌ی موفق.**
    کلِ ارزشش همان حالتی است که اعلانی ساخته نمی‌شود؛ داخلِ شاخه‌ی موفق
    دقیقاً در خرابی ساکت می‌ماند و صفحه می‌گفت «هیچ پیامکی نرسیده» در
    حالی که رسیده بود.
  - **⛔ متنِ پیامک ذخیره نمی‌شود** — فقط زمان، فرستنده، و نتیجه. همان
    قاعده‌ای که متن را در فرگمنت نگه می‌دارد: نسخه‌ی دائمیِ تازه‌ای از
    پیامکِ بانک روی دیسک نمی‌سازیم، آن هم برای تشخیصی که به متن نیازی
    ندارد.
  - زمان به‌صورت **فاصله** نشان داده می‌شود («۳ دقیقه پیش»)، نه تاریخ:
    تبدیلِ شمسی فقط در توابعِ خودِ پروژه است و پیاده‌سازیِ دومش در جاوا
    ممنوع — ضمن اینکه برای تشخیص هم گویاتر است.
- **دکمه‌ی «آزمایش اعلان»** حالت ۳ را در یک تپ ثابت می‌کند، بی‌آنکه منتظرِ
  پیامکِ بانک بمانیم. **⛔ از همان `postNotification`ِ مسیرِ واقعی
  می‌رود، نه یک سازنده‌ی دومِ اعلان:** با نسخه‌ی دوم، دکمه می‌توانست
  اعلان نشان بدهد در حالی که مسیرِ واقعی خراب است — همان «سنجشی که روی
  خرابی سبز می‌شود»، این بار در دستِ کاربر.
  - ⚠ متنِ نمونه در `strings.xml` است نه در جاوا: قاعده ۱۲ منطقِ دامنه
    (واحدِ پول) را داخلِ فایلِ بومی ممنوع می‌کند و آن ممنوعیت درست است —
    یک رشته‌ی نمایشی پارسر نیست، ولی جایش هم داخلِ کد نیست.
- **`classify()` همان دو شرطِ قبلی است، فقط نام‌دار.** منطقِ تصمیم عوض
  نشد؛ اگر عوض می‌شد، صافیِ گیرنده از `parseBankSms()` دور می‌افتاد —
  همان چیزی که کلِ این فایل برای نفیِ آن نوشته شده.
- قاعده ۱۹ چهار چیز را نگه می‌دارد: نبودِ متن در prefs، ترتیبِ ثبت پیش از
  خروج، نبودِ سازنده‌ی دومِ اعلان، و رفتنِ آزمایش از مسیرِ واقعی. با پنج
  جهش سنجیده شد.
  - ⚠ و **یکی از آن‌ها اول زنده ماند:** بررسیِ ترتیب دنبالِ خودِ
    `PREF_LAST_WHY` می‌گشت، ولی آن اولین بار **بالای فایل به‌عنوان
    ثابت** تعریف شده و همیشه از هر چیزی جلوتر بود — یعنی بررسی پوچ بود.
    حالا جای **نوشتن** (`putString(PREF_LAST_WHY`) سنجیده می‌شود.
    **جهشِ زنده‌مانده اول یعنی تست ناقص است، نه اینکه کد امن است.**

- **⚠ APK بازرسی می‌شود، نه فقط ساخته.** اگر گیرنده یا مجوزش از manifestِ
  نهایی بیفتد، گریدل هیچ خطایی نمی‌دهد و کاربر تازه بعد از اولین برداشتِ
  بانکی می‌فهمد. workflow با `aapt2` داخلِ خودِ APK را می‌گردد — همان درسِ
  «ساخت موفق شد ≠ درست کار می‌کند».
- ⚠ `RECEIVE_SMS` مجوزِ محدودشده‌ی Google Play است، ولی این اپ از راه
  GitHub Release نصب می‌شود نه پلی، پس موضوعیت ندارد. اگر روزی به پلی
  رفت، باید درخواستِ استثنا داد.

### ⛔ پیشنهادِ نصب روی اندروید — و چرا لینکِ GitHub جواب نمی‌دهد

- کسی که آدرسِ سایت را در مرورگرِ گوشی می‌زند، هیچ‌وقت نمی‌فهمید اپی هم
  هست. حالا `androidInstallBanner()` بالای صفحه یک نوار می‌گذارد.
- **⛔ مخزن خصوصی است، پس لینکِ Release به درد نمی‌خورد.** دارایی‌های
  Release یک مخزنِ خصوصی برای کاربرِ ناشناس **۴۰۴** می‌دهند — و
  بدترینش این است که روی مرورگرِ خودمان (که وارد گیت‌هاب است) **کار
  می‌کند**، پس در آزمایش سالم به نظر می‌رسد و فقط برای کاربر خراب است.
  APK باید روی خودِ دامنه بنشیند: `deploy/apk-publish.sh` می‌گذاردش در
  `download/hesabland.apk` (untracked؛ `deploy.sh` هیچ `git clean` ای
  ندارد پس دست‌نخورده می‌ماند).
- **⛔ `androidApkUrl()` تنها جای این تصمیم است** (مثل
  `categoryScopeSql()`): نوار، صفحه‌ی ورود و کارتِ پروفایل هر سه از
  همان می‌پرسند. **فایل نباشد → هیچ‌کدام رندر نمی‌شوند**؛ لینکِ ۴۰۴ از
  نبودِ لینک بدتر است. ثابتِ `ANDROID_APK_URL` در `config.php` بر فایل
  مقدم است (برای میزبانیِ جای دیگر، مثلاً بازار).
- **⛔ سرور نمی‌تواند بفهمد کاربر داخلِ خودِ اپ است.** TWA با کرومِ
  معمولی رندر می‌شود و User-Agent اش هیچ فرقی با مرورگر ندارد. تنها
  نشانه‌ی قابل اتکا `document.referrer` است که در **اولین** ناوبریِ TWA
  با `android-app://` شروع می‌شود — پس همان یک بار در `localStorage`
  ثبت می‌شود (`daftar_has_apk`) و برای همیشه می‌ماند. بدونِ ذخیره،
  کاربری که اپ را نصب کرده از صفحه‌ی **دوم** به بعد داخلِ خودِ اپ
  پیشنهادِ نصب می‌گرفت.
- **⛔ هیچ `position: fixed` ای ندارد.** نوارِ شناور با `.bottom-nav` و
  `env(safe-area-inset-bottom)` درگیر می‌شود و آن دسته خرابی فقط روی
  گوشیِ نصب‌شده دیده می‌شود. این یک کارتِ معمولیِ بالای محتواست.
  `[hidden]` صریح هم دارد (قاعده ۱۸)، وگرنه پیش از تصمیمِ جاوااسکریپت
  یک لحظه دیده می‌شد.
- **روی صفحه‌ی ورود هم هست، نه فقط صفحه‌های داخلی:** کسی که آدرس را تازه
  زده دقیقاً همان‌جا می‌ایستد. `login.php` خودش `header.php` را لود
  نمی‌کند، پس فراخوانی جداست — ولی تابع همان یکی است.
- **کارتِ «اپ اندروید» در پروفایل درِ دوم است:** بدونش نوار یک‌بارمصرف
  بود و کسی که «×» را زد دیگر هیچ راهی به APK نداشت. آن کارت روی هر
  دستگاهی دیده می‌شود (نه فقط اندروید) چون کاربرِ دسکتاپ ممکن است
  بخواهد لینک را برای گوشیِ خودش بفرستد.
- ⚠ رنگش رنگِ **دعوت** است (`--gold-wash`، همان که `.start-card` دارد و
  پالتِ دوم آبی‌اش می‌کند)، نه رنگِ هشدار.
- `apk-publish.sh` فایل را **واقعاً می‌سنجد** (امضای zip + وجودِ
  `AndroidManifest.xml`) و اتمی جابه‌جا می‌کند: فایلِ اشتباه روی گوشی
  فقط «برنامه نصب نشد» می‌دهد و علتش را نمی‌گوید، و نوشتنِ مستقیم به
  کسی که همان لحظه دانلود می‌کند نصفه‌ی فایل را می‌داد.
  - **⛔ `--from-github` همان خصوصی بودنِ مخزن را دور می‌زند، نه اینکه
    نادیده‌اش بگیرد:** با یک توکنِ فقط-خواندنی، خودِ اسکریپت نسخه را
    می‌خواند و فایل را می‌گیرد. بدونِ آن تنها راه، دانلود در مرورگرِ
    واردشده و بعد آپلود روی سرور بود — یعنی هر به‌روزرسانیِ اپ یک کارِ
    دستیِ چندمرحله‌ای.
  - **⛔ توکن روی خطِ فرمان نمی‌رود.** `curl --config` یک فایلِ موقتِ
    `600` می‌خواند و `trap` پاکش می‌کند. با `-H "Authorization: …"` هر
    کاربرِ دیگری روی همین VPS آن را در `ps aux` می‌دید — همان چیزی که
    `perf-report.sh` را از `mysql -pرمز` به `--defaults-extra-file` برد.
  - ⚠ JSON با پوسته تجزیه می‌شود (`jq` روی سرور نیست)، ولی **فقط بخشِ
    `assets`**: متنِ توضیحِ نسخه دستِ آدم است و می‌تواند `{` یا حتی یک
    `"name":"….apk"` داشته باشد. با فایلِ ساختگی آزموده شد.
- تست در `tests/test_android_install.php` است (۲۶ بررسی، بدون نیاز به
  سرور) و با هشت جهش سنجیده شده: برداشتنِ نگهبانِ `android-app://`،
  برداشتنِ گیتِ اندروید، آدرس دادن بدونِ فایل، برداشتنِ قاعده‌ی
  `[hidden]`، شناور کردنِ نوار، بردنِ توکن روی argv، برداشتنِ
  `chmod 600`، و برداشتنِ `trap`.
  - ⚠ خودِ مسیرِ `--from-github` با یک **سرورِ گیت‌هابِ ساختگی** روی
    localhost اجرا و دیده شد (توکنِ غلط، نسخه‌ی ناموجود، نسخه‌ی درست،
    و HTML به‌جای APK) — «اسکریپت نوشته شد» با «کار می‌کند» یکی نیست.

**اپ جز آن دو فایل هیچ کد برنامه‌ای ندارد و نباید داشته باشد.** یک *Trusted Web Activity*
است: پوسته‌ای که به اندروید می‌گوید کروم `hesab.stland.ir` را تمام‌صفحه و
بدون نوار آدرس باز کند. خودِ برنامه همین سایت PHP است. راهنمای کامل در
`mobile/README.md`.

- **اول یک اپ فلاتر ساخته شد و کنار گذاشته شد.** APK اش **۵۱ مگابایت** درآمد
  (موتور گرافیکی فلاتر برای هر سه معماری؛ کدِ ما شاید ۳۰۰ کیلوبایت از آن بود)
  در برابر زیر ۱ مگابایت برای TWA. ولی مسئله‌ی اصلی حجم نبود: آن اپ **۴ صفحه**
  داشت و سایت **همه چیز** — چک، طلب و بدهی، بودجه، پس‌انداز، دارایی، معاملات،
  گزارش‌ها. یعنی دو نسخه از یک برنامه که هر قابلیت باید دو بار نوشته می‌شد.
- **`api/v1` سر جایش ماند و حذف نشد.** اگر روزی اپ بومی لازم شد (اعلان، ویجت،
  کار آفلاینِ جدی) آن لایه با تست‌هایش آماده است.
- **⛔ سه چیز به هم گره خورده‌اند و شکستنشان بی‌صداست:** کلیدِ امضا در
  `KEYSTORE_BASE64`، اثر انگشتِ همان کلید در `.well-known/assetlinks.json`، و
  در دسترس بودنِ آن فایل روی دامنه. اگر یکی بخواند و بقیه نه، اپ **نصب می‌شود
  ولی با نوار آدرس بالا** — همه چیز سبز است و فقط کاربر می‌فهمد.
  - به همین دلیل workflow بعد از ساخت، اثر انگشتِ خودِ APK را با
    `keytool -printcert -jarfile` می‌خواند و با `assetlinks.json` می‌سنجد؛
    نخواند، ساخت را می‌شکند. **«ساخت موفق شد» با «درست کار می‌کند» یکی نیست** —
    همان درسی که قاعده‌ی nginx در `api/v1` داد.
  - امضا با کلیدِ debug اصلاً کار نمی‌کند: CI هر بار یک کلید تازه می‌سازد، پس
    اثر انگشت هر ساخت عوض می‌شود.
  - قاعده‌ی nginx `location ~ /\.(?!well-known)` عمداً `.well-known` را استثنا
    کرده، پس این مسیر از قبل باز است و تنظیم تازه‌ای نمی‌خواهد.
- **آدرس سرور فقط یک جا نوشته شده:** دو `manifestPlaceholders` در
  `mobile/app/build.gradle.kts`. `AndroidManifest.xml` هر دو را از همان‌جا
  می‌خواند. دامنه‌ی تازه یعنی `assetlinks.json` تازه روی همان دامنه.
- **`.github/workflows/android.yml` باید در ریشه‌ی مخزن بماند، نه در
  `mobile/`.** گیت‌هاب فقط `.github/workflows/` ریشه را می‌خواند؛ یک پله
  پایین‌تر **بی‌هیچ خطایی** نادیده گرفته می‌شود و زبانه‌ی Actions خالی می‌ماند.
  - فیلتر `paths` هم لازم است، وگرنه هر اصلاح PHP یک ساخت راه می‌اندازد که
    خروجی‌اش با قبلی مو نمی‌زند.
  - ⚠ `path:` در `upload-artifact` از **ریشه‌ی workspace** حساب می‌شود، نه از
    `working-directory`.
- ساخت روی گیت‌هاب انجام می‌شود چون Android SDK فقط از `dl.google.com` می‌آید
  و آن میزبان در محیط توسعه مسدود است. خروجی هم artifact می‌شود هم Release؛
  **Release برای نصب روی گوشی است**، چون artifact همیشه zip دانلود می‌شود و
  اندروید بدون فایل‌منیجر بازش نمی‌کند.
- **`deploy.sh` کل مخزن را روی سرور می‌ریزد**، پس `mobile/` هم آنجا می‌رود؛
  `vps-setup.sh` مثل `/deploy/` و `/tests/` از وب مسدودش می‌کند. ولی
  `.well-known/` **نباید** مسدود شود — بدون آن تأیید TWA کار نمی‌کند.

---

## پیش از هر push

```bash
bash tests/run.sh
```
کد خروج باید ۰ باشد. (بررسی نحو داخل همین مجموعه هست، پس `php -l` جدا لازم نیست.)

> **درج متغیر کنار گیومه‌ی فارسی:** `"کاربر «$username» فعال شد"` غلط است. PHP بایت‌های ≥ 0x80 را جزء نام متغیر می‌داند، پس نام متغیر `$username»` خوانده می‌شود و پیام وسطش خالی درمی‌آید. همیشه `{$username}` بنویسید. `php -l` این را نمی‌گیرد؛ تست `test_api_contract.php` با توکنایزر می‌گیرد.

## یادآور شخصی و مرکز اعلان

### فرمِ یادآور: چیپ، اقساط، و پیش‌آگاهی

- **⛔ `Schedule::RECUR_PRESETS` تنها مرجعِ دوره‌های تکرار است** (مثل
  `Auth::SESSION_WINDOWS`). هم چیپ‌های فرم از آن رندر می‌شوند هم
  `Schedule::RECUR_TYPES` که `api/save_reminder.php` با آن اعتبارسنجی
  می‌کند. **فهرستِ دوم نسازید** — گزینه‌ای که کاربر می‌بیند آن‌وقت هنگام
  ذخیره بی‌صدا به «یک بار» برمی‌گردد.
- منوی کشویی برای تکرار **جوابگو نبود**: کاربر باید منو را باز می‌کرد،
  «هر چند ماه» را می‌زد، و بعد یک فیلدِ عددی ظاهر می‌شد — سه حرکت برای
  چیزی که یک تپ است. حالا چیپ‌های هفتگی تا سالانه در یک نگاه‌اند و
  «دلخواه» N + واحد را برای بقیه نگه می‌دارد (تأمین اجتماعیِ هر ۴ ماه).
- **⛔ `every_n_days` از `jalaliAddMonths()` رد نمی‌شود** و نباید بشود:
  «هر ۱۵ روز» در هر تقویمی دقیقاً ۱۵ روز است و روزِ لنگر برایش بی‌معناست.
  `Schedule::nextDue()` تنها جای این تصمیم است — `api/save_reminder.php`
  حسابِ محلیِ خودش را داشت و حذف شد، وگرنه «هر ۱۵ روز» اینجا و در
  `due.php` دو تاریخ می‌گفتند.
- **⛔ چند روز قبل خبر بده تا امروز ذخیره می‌شد ولی هیچ‌جا خوانده
  نمی‌شد.** تولیدکننده فقط `remind_date <= today` را می‌گرفت، پس انتخابِ
  «یک هفته قبل» هیچ اثری نداشت و اعلان همیشه روزِ خودِ سررسید می‌آمد —
  تنظیمی که کار نمی‌کند. حالا افق تا بلندترین بازه باز است و بازه در
  `dedup_key` هم می‌آید (کسی که ۷ و ۱ را با هم زده باید هر دو را بگیرد).
  `Notify::REMINDER_STEPS` تنها مرجعِ این بازه‌هاست.
- **⛔ اقساط هیچ ردیفی ذخیره نمی‌کند**: `total_count`/`done_count` روی
  خودِ قانون می‌نشینند (`migration_reminder_plan`) — همان مدلِ مجازیِ
  `debtInstallments()` و به همان دلایل. باقیمانده‌ی تقسیم به **قسطِ
  آخر** می‌رود و تنها جای این حساب `Schedule::installmentAmount()` است.
- **⛔ `amount` و `total_amount` هر دو لازم‌اند.** «۱۲ قسط، ۱۲ میلیون» دو
  معنی دارد و حدس زدنش یعنی عددِ کارت ده‌برابرِ واقعیت شود، بی‌هیچ خطایی.
  پس چیپِ «مبلغ هر قسط / جمع کل» صریح می‌پرسد و خلاصه‌ی زنده (`#rm_plan_note`)
  پیش از ذخیره نشان می‌دهد یعنی چه.
- **⚠ توگل‌های «چند روز قبل» غلط بودند و درسش کلی است:** `.switch` یک
  ردیفِ تمام‌عرض است (`display:flex` با `.switch-text{flex:1}`)، پس چهار
  کلید زیرِ هم می‌افتادند و دستگیره‌ی سفید کنارِ متن شبیهِ یک تکه‌ی
  جامانده دیده می‌شد — و هیچ CSS ای هم برای `.notify-days` وجود نداشت.
  **کلید برای «روشن یا خاموش» است؛ انتخابِ چندتایی از یک مجموعه چیپ
  است** و زبانِ طراحیِ خودِ پروژه از قبل `.stay-chip` را داشت.
- **⛔ کامنتِ پی‌اچ‌پی داخلِ رشته‌ی SQL، ثبتِ یادآور را کشته بود.** دو خط
  توضیحِ `//` داخلِ کوتیشنِ `INSERT` جا مانده بودند و MySQL آن را
  نمی‌شناسد: هر یادآورِ تازه با خطای نحوی رد می‌شد، در حالی که `php -l`
  سبز بود، ویرایش و «انجام شد» کار می‌کردند، و تستِ «۵۰۰ نمی‌دهد» با
  ورودیِ خالی اصلاً به `INSERT` نمی‌رسید (عنوانِ خالی همان اول ۴۲۲
  می‌گیرد). دو تست حالا می‌گیرندش: قاعده ۱۷ در `test_api_contract.php`
  (شکل) و «مسیر واقعیِ یادآور» در `test_api_auth.php` (رفتار، با پیلودِ
  کامل) — همان درسِ لایه‌ی ۳ که یک بار سرِ `add_transaction` گفته شد.
- **⛔ و یک خرابیِ بی‌صدا در خودِ تست پیدا شد:** پاک‌سازیِ کاربرِ تست در
  `test_api_auth.php` سه جدول را **دستی** نام می‌برد. با آمدنِ هر جدولِ
  تازه، یک ردیفِ جامانده کلیدِ خارجی را نگه می‌داشت و ساختِ کاربر شکست
  می‌خورد — و آن‌وقت **کلِ لایه‌ی «با ورود»** با پیامِ «اتصال به دیتابیس
  برقرار نشد» رد می‌شد و مجموعه سبز می‌ماند. حالا از `userDataTables()`
  رد می‌شود (کشف از خودِ دیتابیس، نه فهرستِ دستی) و آن لایه از ۱۲ بررسی
  به ۳۲ برگشت.

### ⛔ `[hidden]` بدونِ قاعده‌ی خودش کار نمی‌کند

- ویژگیِ `hidden` با یک قاعده‌ی **UA** پیاده می‌شود، یعنی ضعیف‌ترین
  قاعده‌ی ممکن. هر `.foo { display: flex }` ساده بر آن غلبه می‌کند و
  عنصری که در HTML `hidden` نوشته شده **دیده می‌شود**، بی‌هیچ خطایی و
  فقط روی مرورگر.
- تا امروز پنج بار زده و چهار موردش با هم پیدا شد: چیپ‌های «مبلغ هر قسط /
  جمع کل»، دکمه‌ی «انصراف از ویرایش»، «بازه‌ی دلخواه» بودجه، و کارهای
  اضافیِ حساب. **بدترینش دکمه‌ی «چسباندن و خواندن» پیامک بانک بود** که
  طبق قاعده‌ی خودش فقط با `navigator.clipboard` و `isSecureContext` باید
  رندر شود: روی http دیده می‌شد و زدنش هیچ کاری نمی‌کرد — دقیقاً همان
  «دکمه‌ی بی‌کار از نبودنش بدتر است» که برایش نوشته شده بود.
- قاعده ۱۸ در `test_api_contract.php` این را می‌سنجد: هر کلاسی که در PHP
  با `hidden` رندر می‌شود و در CSS `display` می‌گیرد، باید `[hidden]`ِ
  خودش را هم داشته باشد.
  - ⚠ فقط **آخرین compound**ِ هر انتخابگر شمرده می‌شود: `.form-group label
    { display: … }` هیچ ربطی به پنهان شدنِ `.form-group` ندارد و نسخه‌ی
    اولِ تست دقیقاً به همین دلیل چهار هشدارِ الکی داد. **هشدارِ الکی از
    نبودِ تست بدتر است.**
  - ⚠ `includes/` هم اسکن می‌شود، وگرنه شیتِ ثبت تراکنش — که در **هر**
    صفحه رندر می‌شود — از قلم می‌افتاد؛ همان‌طور که اولین نسخه افتاد.

- **دلیل وجودش:** تا پیش از این تنها راهِ خبر دادن، ایمیلِ روزانه بود — و
  کسی که ایمیل ثبت نکرده باشد هیچ خبری نمی‌گرفت. اعلانِ داخلِ اپ هیچ
  وابستگیِ بیرونی ندارد (نه پنل، نه اعتبار، نه سرویسی که از ایران در
  دسترس باشد)، پس **همیشه** کار می‌کند و بقیه‌ی کانال‌ها رویش سوارند.
- **⛔ دو جدولِ جدا** (`migration_notifications`): `reminders` چیزی است که
  **کاربر می‌سازد** و `notifications` چیزی است که **برنامه تولید می‌کند**.
  با یک جدول، پاک کردنِ یک اعلانِ خوانده‌شده یادآورِ خودِ کاربر را هم می‌برد.
- **⛔ `dedup_key` قلبِ این قابلیت است.** بدونش هر بازدیدِ صفحه همان
  سررسید را دوباره اعلان می‌کرد و فهرست در دو روز پر از تکراری می‌شد —
  یعنی قابلیت عملاً می‌مرد، بی‌آنکه هیچ خطایی بدهد. یکتایی روی
  `(user_id, dedup_key)` است نه خودِ کلید، وگرنه اولین کاربری که یک
  سررسید داشت جلوی اعلانِ همه‌ی بقیه را می‌گرفت.
  - رویدادهای `financialEvents()` شناسه‌ی ردیف ندارند (قسط و تراکنشِ
    دوره‌ای اصلاً ردیفِ ذخیره‌شده ندارند)، پس کلید از **محتوای خودِ
    رویداد** hash می‌شود.
- **`Notify::generateFor()` هیچ cron ای نمی‌خواهد**: اولین بازدیدِ روز
  کار را انجام می‌دهد (الگوی `processRecurringTransactions`). نگهبانِ
  نشست فقط برای **سرعت** است؛ درستی از `dedup_key` می‌آید.
  - ⚠ و دقیقاً به همین دلیل `api/save_reminder.php` آن نگهبان را باز
    می‌کند: بدونش، یادآوری که کاربر **برای همین امروز** ثبت می‌کرد تا
    فردا هیچ اعلانی نمی‌ساخت و کاربر نتیجه می‌گرفت کار نمی‌کند.
- **⛔ یادآور جدا از «تراکنش دوره‌ای» است.** آن یکی پول جابه‌جا می‌کند و
  در گزارش می‌نشیند؛ یادآور هیچ پولی جابه‌جا نمی‌کند. بیمه‌ی آتش‌سوزی و
  عوارض خودرو مبلغشان را از قبل نمی‌دانی و تراکنشِ خودکارشان **غلط**
  است.
- **یادآورِ تکرارشونده با «انجام شد» بسته نمی‌شود بلکه به دوره‌ی بعد
  می‌رود**، و تاریخِ بعدی از `advanceRecurringDate()` می‌آید نه یک
  محاسبه‌ی تازه — همان تابعی که روی تقویمِ **شمسی** جلو می‌رود و روز را
  به طولِ واقعیِ ماه محدود می‌کند (۳۰ اسفندِ کبیسه → ۲۹ اسفند).
- **اعلانِ پرداخت بعد از `commit` ساخته می‌شود، نه داخلِ تراکنش.** اگر
  داخل می‌بود، خطای اعلان می‌توانست یک پرداختِ تأییدشده را برگرداند —
  یعنی کاربر پول داده و اشتراکش فعال نشده.
- **⛔ Web Push همچنان نیست** و دلیلش عوض نشده: سرویسِ پوشِ کروم از ایران
  پایدار نیست و روی iOS فقط در حالت نصب‌شده کار می‌کند. یک اعلانِ مالی که
  **گاهی** برسد از نبودنش بدتر است. کانال‌های امروز: داخلِ اپ (همیشه)،
  ایمیلِ روزانه، و پیامک برای کدِ ورود.
- تست در `tests/test_reminders_notify.php` است (۲۶ بررسی) و با چهار جهش
  سنجیده شده: برداشتنِ کلیدِ یکتایی، نادیده گرفتنِ `is_done`، برداشتنِ
  شرطِ `user_id` از شمارشِ نخوانده، و اعلان دادنِ یادآورِ آینده.
  - ⚠ نکته‌ی خودِ تست: `toJalali()` **ارقام فارسی** برمی‌گرداند و
    `intval` رویش صفر می‌شود. نسخه‌ی اول همان‌جا افتاد؛ حالا از
    `gregorianToJalali()` خوانده می‌شود.

## هسته‌ی مشترکِ سررسید

- **⛔ ارجاع، نه انتقال.** جدولِ `cheques`، `debts` و `recurring_transactions`
  **صاحبِ پول و تاریخِ خودشان می‌مانند**؛ `reminders` فقط قانونِ تکرار و
  اعلان را نگه می‌دارد و با `source_type` + `source_id` به آن‌ها اشاره
  می‌کند. اگر تاریخ‌ها منتقل می‌شدند، برای هر سررسید **دو ردیفِ حقیقت**
  می‌ماند (پرداختِ جزئی در `debt_payments` و وضعیت در occurrence) و
  ناهماهنگیِ بینشان بی‌صدا بود.
- **تاریخ‌ها میلادی ذخیره و شمسی نمایش داده می‌شوند** — همان قاعده‌ی
  همیشگی. با رشته‌ی شمسی، `ORDER BY` و `BETWEEN` و ایندکس غلط می‌شدند و
  `financialEvents()` و بکاپ و `api/v1` باید بازنویسی می‌شدند.
- **⛔ `jalaliAddMonths()` تنها جای حسابِ تاریخِ بعدی است** و **روزِ لنگر**
  می‌گیرد. تابعِ قدیمی روز را از تاریخِ *فعلی* برمی‌داشت، پس ۳۱ بعد از
  یک بار افتادن در ماهِ ۳۰ روزه **برای همیشه** ۳۰ می‌ماند: ۳۱ شهریور →
  ۳۰ مهر → ۳۰ آبان → … . با لنگرِ جدا، در اولین ماهِ بلندِ بعدی دوباره
  ۳۱ می‌شود.
  - ⚠ کبیسه از **تبدیلِ واقعیِ تقویم** می‌آید (`jalaliMonthLength()`)، نه
    از فرمولِ «هر چهار سال». کبیسه‌ی بعد از ۱۴۰۳ سالِ **۱۴۰۸** است نه
    ۱۴۰۷ — این را خودِ تست با تقویم سنجید و انتظارِ اولیه‌ی من غلط بود.
- **⛔ occurrence ها از قبل انبوه نمی‌شوند.** اگر برای یک قانون سررسیدِ
  **باز** (pending/overdue) وجود داشته باشد، `materialize()` هیچ کاری
  نمی‌کند. بدونِ این نگهبان هر اجرا یک سررسیدِ آینده اضافه می‌کرد و
  کرونِ ساعتی روزی ۲۴ ردیف انبوه می‌ساخت. **با اجرای واقعی پیدا شد، نه
  با نگاه.**
  - ⚠ و تستش باید قانونِ **تکرارشونده** داشته باشد: قانونِ یک‌باره بعد از
    اولین سررسید `nextDue()` ندارد و حلقه خودش می‌ایستد، پس نسبت به این
    نشتی کور است. جهش نشانش داد.
- **سررسیدهای عقب‌افتاده گم نمی‌شوند**: هرکدام ردیفِ جدا با وضعیتِ
  `overdue`، نه یکی به‌جای همه.
- **کرونِ ساعتی `deploy/due-notify.php`** فقط برای کاربرانی کار می‌کند که
  ساعتِ یادآوریشان همان ساعت باشد. ایدمپوتنت بودنش از **کلیدِ یکتای**
  `reminder_notifications` می‌آید، نه از زمان‌بندی.
- **صفحه‌ی چک، طلب و بدهی، و تراکنش دوره‌ای سرِ جایشان‌اند** و دکمه‌های
  خودشان دست‌نخورده است؛ `due.php` فقط همه‌ی سررسیدها را کنارِ هم نشان
  می‌دهد. (خودِ `due.php` بعداً با «آینده مالی»، «تقویم مالی» و
  «یادآورها» یکی شد — پایین‌تر در «سررسیدها: یک مقصد، سه زبانه».)
- **⛔ و `due.php` هیچ پولی ثبت نمی‌کند.** «تسویه» فقط وضعیتِ سررسید را
  می‌بندد و لینکِ ردیف به صفحه‌ی همان بخش می‌رود که منطقِ پولش آنجاست.
  با ثبتِ پول از اینجا، یک پرداخت دو جا نوشته می‌شد.
- تست در `tests/test_schedule.php` است (۴۱ بررسی): مرزهای تقویم (۳۱
  فروردین + ۳ ماه، ۳۰ بهمن در سالِ غیرکبیسه، ۳۰ اسفند + ۱۲ ماه، N=۴ در
  شش تکرار) و رفتارِ اتصال روی دیتابیس. با نُه جهش سنجیده شده.

## سررسیدها: یک مقصد، سه زبانه

- **⛔ پنج قلمِ منو یک سؤال را جواب می‌دادند** — «چه چیزی در راه است؟»:
  «آینده مالی»، «تقویم مالی»، «سررسیدها»، «یادآورها» و «تراکنش دوره‌ای».
  هر کدام وقتی اضافه شد درست بود، ولی حاصلِ جمعشان منویی ۲۲تایی شد که
  کاربر کاملش را نمی‌خواند. **همین‌جا بود که مجبور شدیم منوی کناری را
  اسکرول‌شدنی کنیم — آن رفعِ نشانه بود، این رفعِ علت.** حالا محتوای منو
  ۸۱۰ پیکسل است (پیش از این ۱۲۰۱) و روی حسابِ مدیر هم اسکرول لازم ندارد.
- **⛔ و یک باگِ واقعی را هم بست:** «سررسیدها» و «یادآورها» در
  `sidebar.php` **داخلِ بلوکِ `Auth::isAdmin()`** افتاده بودند — یعنی
  کاربر عادی روی دسکتاپ هیچ راهی به آن‌ها نداشت. نه خطایی، نه بریدگی‌ای؛
  فقط نبودند. (روی موبایل از شیتِ «بیشتر» پیدا می‌شدند، پس **«روی گوشی
  درست است» اینجا هم مدرک نبود** — قرینه‌ی همان درسِ منوی کناری.)
- **`DUE_TABS` و `DUE_FILTERS` در `due.php` تنها مرجع‌اند** (مثل
  `Auth::SESSION_WINDOWS`): هم نوار از آن‌ها رندر می‌شود هم `?t=`/`?f=`
  با آن‌ها سنجیده می‌شود. **فهرستِ دوم نسازید** — `due.php` هر `?t=`
  ناشناخته را بی‌صدا به زبانه‌ی پیش‌فرض می‌برد، پس عوض کردنِ نامِ یک
  زبانه هیچ خطایی نمی‌دهد و فقط لینکِ ایمیل‌ها به جای اشتباه می‌رود.
- **⛔ زبانه‌ها لینک‌اند و هر بار فقط یکی از سرور می‌آید.** با رندر کردنِ
  هر سه و پنهان کردنِ دو تا، هر بارگذاری کوئریِ تقویم و یادآور و سررسید
  را با هم می‌زد — سه برابر کار برای چیزی که کاربر یکی‌اش را می‌بیند. و
  دکمه‌ی بازگشتِ مرورگر هم درست کار می‌کند.
- **⛔ `upcoming.php`، `calendar.php` و `reminders.php` حذف نشدند** بلکه
  هدایت می‌کنند. حذفشان چهار چیز را بی‌صدا می‌شکست: بوک‌مارک، میان‌برِ
  PWA، مقصدِ اعلان‌های ساخته‌شده در دیتابیس، و **ایمیل‌هایی که از قبل
  فرستاده شده‌اند** — `reminderEmailBody()` لینکش را با
  `appBaseUrl() . '/upcoming.php'` می‌ساخت و آن ایمیل‌ها قابل عوض کردن
  نیستند. `calendar.php` ماهِ انتخاب‌شده را هم حفظ می‌کند.
  - ⚠ هر سه `Auth::requireLogin()` دارند، وگرنه کاربرِ واردنشده به‌جای
    صفحه‌ی ورود یک پرشِ اضافه می‌خورد. `test_api_auth` همین را گرفت.
- **⛔ زبانه‌ی فهرست بدونِ جدولِ سررسید هم کار می‌کند.** اگر
  `Schedule::available()` نادرست باشد، همان نمای قدیمیِ `financialEvents()`
  رندر می‌شود (بدونِ دکمه‌های اقدام، که به جدولِ occurrence بندند). بدونِ
  آن، ادغام یک صفحه‌ی **سالم** را روی نصب‌های migration‌نخورده از کار
  می‌انداخت.
- **⛔ «پول قابل خرج» افقِ ثابتِ ۳۰ روزه دارد** و به صافیِ فهرست وابسته
  نیست: با هر صافی می‌پرید و کاربر نمی‌فهمید کدام عدد «قابل خرج» است.
  و جمعِ دریافتی/پرداختیِ جدا **برداشته شد** — نسخه‌ی دومی از همان چیزی
  بود که `safeToSpend()` می‌گوید و دیر یا زود با آن اختلاف پیدا می‌کرد.
- **«تراکنش دوره‌ای» زبانه نشد** چون خودش پول جابه‌جا می‌کند و در گزارش
  می‌نشیند؛ فقط از منوی اصلی برداشته شد و لینکش پایینِ همین صفحه است
  (`.due-more-link`) — وگرنه روی دسکتاپ فقط از شیتِ «بیشتر» پیدا می‌شد.
- **زبانه‌ها زیرخط‌دارند نه قرص** (`.page-tabs`): `.due-filter` و
  `.filter-chip` هر دو قرص‌اند، و با شکلِ مشترک صافیِ داخلِ یک زبانه و
  خودِ زبانه‌ها هم‌وزن دیده می‌شدند.
- قاعده ۲۱ در `test_api_contract.php` هر دو نیمه را می‌سنجد (لینکِ
  `?t=…` به زبانه‌ی موجود، و ماندنِ استاب‌ها) و با چهار جهش سنجیده شده.

## ⛔ «حذف شد — لغو» به‌جای «مطمئنید؟»

- **دلیلش:** مودالِ تأیید هزینه‌اش را از **همه** می‌گیرد تا اشتباهِ
  گاه‌به‌گاهِ یک نفر را بگیرد — و کاربری که صد بار «بله» زده، بارِ صد و
  یکم هم بی‌خواندن می‌زند. یعنی مودال دقیقاً وقتی لازم است کار نمی‌کند.
  حذف انجام می‌شود و راهِ برگشت باز می‌ماند.
- **⛔ حذفِ نرم نیست.** `deleted_at` روی هر جدول یعنی هر کوئریِ موجود
  باید صافیِ تازه بگیرد، و اولین کوئری‌ای که یادش برود ردیف‌های
  حذف‌شده را برمی‌گرداند — بی‌هیچ خطایی. اینجا ردیف **واقعاً** حذف
  می‌شود و عکسش پیش از حذف در نشست می‌ماند؛ «لغو» همان عکس را دوباره
  می‌نشاند. پس هیچ migration ای هم لازم نشد.
- **⛔ `Undo` (`includes/undo.php`) تنها جای این کار است**، و فهرستِ
  فرزندان را از **خودِ دیتابیس** کشف می‌کند (`information_schema`)، نه از
  یک آرایه‌ی دستی — همان قاعده‌ی `userDataTables()`. حذفِ یک بدهی،
  `debt_payments` را هم با CASCADE می‌برد؛ اگر عکس فقط پدر را داشت، «لغو»
  بدهی را برمی‌گرداند و پرداخت‌هایش را **بی‌صدا** جا می‌گذاشت و عددِ
  باقیمانده عوض می‌شد.
- **ردیف‌ها با همان شناسه برمی‌گردند**، وگرنه هر پیوندی که جای دیگری به
  آن‌ها اشاره می‌کند بی‌صدا به هیچ‌جا وصل می‌شد.
- **⛔ `user_id` همیشه از نشست بازنویسی می‌شود، حتی از عکسِ خودمان.** عکس
  در فایلِ نشست روی دیسک است؛ اگر شناسه از آن خوانده شود، یک عکسِ
  دست‌کاری‌شده می‌تواند ردیفی به نامِ کاربرِ دیگری بسازد.
- **⛔ اگر عکس گرفتن ممکن نشد، توکن `null` است و صفحه به همان
  `confirm()` برمی‌گردد.** «لغوِ ناموجود» از تأیید **بدتر** است: کاربر
  روی وعده‌ای حساب می‌کند که وجود ندارد.
- **نُه حذف برگشت‌پذیر شد:** تراکنش، طلب و بدهی، چک، بودجه، هدف پس‌انداز،
  یادآور، انتقال، رکورد پس‌انداز، دارایی.
- **⛔ و این‌ها عمداً `confirm()` نگه داشتند، چون برگشتشان کامل نیست:**
  - **معامله و فروشِ معامله** — `deleteTradeProfitTransactions()` ردیف‌هایی
    از `transactions` را می‌برد که فرزندِ CASCADE نیستند، پس عکس آن‌ها را
    ندارد و گزارشِ درآمد/هزینه بعد از «لغو» بی‌صدا فرق می‌کرد.
  - **قانونِ تراکنش دوره‌ای** — `transactions → recurring_transactions` از
    نوعِ `SET NULL` است، پس پیوندِ تراکنش‌های گذشته با برگشتِ قانون
    بازسازی نمی‌شود.
  - **حساب (کیف پول)** — همان `SET NULL` روی `transactions.wallet_id`.
  - **پیوست و تصویر پروفایل** — فایلشان از دیسک `unlink` می‌شود.
  - **دستگاه‌های مورد اعتماد** — کارِ امنیتی است و تأیید همان چیزی است که
    باید باشد.
- **نوار بعد از تازه‌سازی می‌آید، نه درجا** (`queueUndoBar` +
  `sessionStorage`): جمع‌ها و موجودیِ بالای صفحه سمتِ سرور رندر شده‌اند،
  پس بدونِ تازه‌سازی کاربر «حذف شد» می‌دید ولی عددها عوض نشده بودند —
  همان قاعده‌ی «هر تغییر پول باید بلافاصله دیده شود».
- **یک نوار برای همه** (`.undo-bar`): ثبتِ خودکار از پیامک هم از همین
  رد می‌شود. دو پیاده‌سازیِ جدا یعنی دو رفتار که دیر یا زود از هم دور
  می‌افتند.
- تست در `tests/test_undo.php` است (۲۱ بررسی) و با شش جهش سنجیده شده.
  - ⚠ جهشِ «`user_id` را از عکس بخوان» اول **زنده ماند**: در هر حالتِ
    عادی آن دو یکی‌اند، پس تست تفاوتی نمی‌دید. حالا عکس عمداً
    دست‌کاری می‌شود تا فرق کنند. **جهشِ زنده‌مانده اول یعنی تست ناقص
    است، نه اینکه کد امن است.**
  - ⚠ دو نگهبان **عمداً** افزونه‌اند و جهششان زنده می‌ماند: سوزاندنِ
    توکن (کلیدِ یکتای دیتابیس هم جلوی درجِ دوباره را می‌گیرد) و سنجشِ
    نامِ جدول در `capture()` (که `collect()` هم می‌سنجد). نوشته شده تا
    کسی جهشِ زنده‌مانده را «پس این خط لازم نیست» نخواند.

---

## ⛔ شش سفت‌کاریِ زیرساخت — و هر شش از یک جنسِ خرابی آمدند

هر شش موردِ این بخش یک چیزِ مشترک دارند: **خرابی‌شان هیچ نشانه‌ای
نداشت**. نه خطایی، نه تستِ قرمزی، نه شکایتِ کاربری. سه تایشان همین
حالا روی همین مخزن **زنده** بودند و با اندازه‌گیری پیدا شدند، نه با
بازبینی.

### ⛔ ۱ — «اجرا نشد» با «موفق شد» یکی نیست

- **خرابیِ واقعی، و ریشه‌اش دو لایه بود:** `bash tests/run.sh` ته اجرا
  می‌نوشت «✅ هر ۴۲ مجموعه تست موفق بود» در حالی که **هیچ‌کدام به
  دیتابیس نرسیده بودند**. با خواباندنِ MariaDB بازتولید شد: `EXIT=0`.
- **⛔ علتِ اصلی این است که `die()` با کدِ خروجِ صفر تمام می‌شود.**
  `Database::getConnection()` در شکستِ اتصال `die()` می‌زد، پس حدود
  **۲۵ مجموعه‌ی تست** که دور آن فراخوانی `try/catch` داشتند تا
  آبرومندانه رد شوند، **کدِ مرده** بودند: هیچ استثنایی پرتاب نمی‌شد و
  پروسه همان‌جا با کد ۰ می‌مرد.
  - رفعش این است که روی **خط فرمان** پرتاب شود، نه `die()`. روی وب همان
    پیامِ آبرومند می‌ماند: آنجا کاربر یک صفحه می‌خواهد، نه stack trace.
- **⛔ و لایه‌ی دوم: «رد شد» با «موفق شد» یکی شمرده می‌شد.** حتی بعد از
  زنده شدنِ آن بلوک‌ها، `T::skip()` همه را یک‌کاسه می‌کرد. حالا سه
  حالتِ جدا هست:
  | حالت | یعنی | کدِ خروج |
  |---|---|---|
  | موفق | اجرا شد و درست بود | ۰ |
  | `T::skip` | ابزارِ **اختیاری** نبود (node، grantِ تستِ زنجیره) | ۰ |
  | `T::blocked` | زیرساختِ **لازم** نبود (دیتابیس، `config.php`) | **۲** |
- `run.sh` کدِ ۲ را می‌شناسد و جدا گزارش می‌دهد، ولی **خروجی‌اش
  همچنان ناصفر است** — چون از دیدِ «آیا می‌توانم push کنم؟» مجموعه‌ای
  که اجرا نشده هیچ چیزی را تأیید نکرده.
- ⚠ ۵۸ فراخوانِ `T::skip` در ۳۴ فایل به `T::blocked` تبدیل شدند. معیار
  ساده است: **اگر بدونِ آن هیچ بررسی‌ای اجرا نمی‌شود، blocked است.**
- قاعده ۳۴ در `test_api_contract.php` این را نگه می‌دارد (پرتابِ CLI در
  `db.php`، شناختنِ کد ۲ در `run.sh`، ناصفر بودنِ خروجی، و نبودِ
  `T::skip` با متنِ «اتصال به دیتابیس») و با پنج جهش سنجیده شد.

### ⛔ ۲ — `tests/test_money_invariant.php`: ترازی که هیچ تستی نمی‌سنجید

- **مسئله:** `walletBalances()` از **شش منبع** جمع می‌زند —
  `initial_balance`، تراکنش، انتقال (با کارمزد)، معامله، چکِ پاس‌شده، و
  `debt_payments`. هر کدام تستِ خودش را داشت، ولی **هیچ‌کس نمی‌سنجید که
  جمعشان درست است**. یک نشتیِ بینِ دو منبع از زیرِ همه رد می‌شد.
- **⛔ دفترِ مرجع را خودِ تست نگه می‌دارد، نه SQL.** با کوئریِ دوباره‌نوشته،
  تست فقط کدِ زیرِ آزمون را دوباره پیاده می‌کرد و با همان باگ سبز می‌ماند.
  اینجا یک آرایه‌ی PHP در حینِ درج به‌روز می‌شود و بعد از **هر** عملیات با
  `walletBalances()` و `totalBalance()` مقایسه می‌شود.
- بعد از ویرایش، حذف، `Undo::restore()` و غیرفعال کردنِ یک حساب دوباره
  سنجیده می‌شود — همان جاهایی که یک منبع می‌تواند بی‌صدا جا بماند.
- **⚠ و خودِ تست یک فرضِ غلطِ من را گرفت:** فکر می‌کردم `txUpdate()`
  حسابِ تراکنش را جابه‌جا می‌کند. **نمی‌کند** و این تصمیمِ ثبت‌شده‌ی
  بالای همان تابع است. حالا یک بررسیِ صریح همان رفتار را پین می‌کند، تا
  اگر روزی عوض شد عمدی باشد نه تصادفی.
- ۳۲ بررسی، و با شش جهش سنجیده شد، همه گرفته شد: انداختنِ چکِ پاس‌شده،
  نادیده گرفتنِ کارمزدِ انتقال، شمردنِ حسابِ غیرفعال، برعکس کردنِ علامتِ
  `debt_payments`، انداختنِ فروشِ معامله، و «لغو»ی که چیزی برنمی‌گرداند.

### ⛔ ۳ — خطای PHP باید جایی دیده شود — `app_errors`

- **۶۱ فایل `error_log()` صدا می‌زنند و هیچ صفحه‌ای آن‌ها را نشان
  نمی‌داد.** یعنی هر ۵۰۰ بی‌صدا در لاگِ FPM می‌ماند و مالکِ نصب فقط
  وقتی می‌فهمد که کاربری شکایت کند — و **بیشترِ کاربرها شکایت نمی‌کنند،
  فقط اپ را می‌بندند**.
- `AppErrors::install()` در **`db.php`** نصب می‌شود، نه جای دیگر: تنها
  فایلی است که **هر** مسیر (صفحه، اندپوینت، cron، تست) لود می‌کند. با
  نصب از جای دیگر همیشه مسیری می‌ماند که از قلم بیفتد — و آن دقیقاً
  همان مسیری است که خطایش را کسی نمی‌بیند.
- **⛔ `fingerprint` کلیدِ یکتاست، نه یک ردیف به‌ازای هر رخداد** (همان
  استدلالِ `dedup_key` در `notifications`): خطایی که در حلقه هزار بار
  می‌افتد جدول را غیرقابل خواندن و دیسک را پر می‌کند — یعنی قابلیت
  خودش را می‌کشد. فقط `hits` بالا می‌رود.
- **⛔ و عمداً هیچ ستونی برای `user_id`، آدرس، یا بدنه‌ی درخواست ندارد.**
  این جدول درباره‌ی **کد** است نه رفتارِ کاربر؛ با یک ستونِ `user_id`
  همان «ردِ رفتاریِ تازه» ساخته می‌شد که `admin/insights.php` صریحاً از
  آن پرهیز کرده و `privacy.php` رویش ادعا نوشته. تست این را با
  `SHOW COLUMNS` می‌سنجد، نه با خواندنِ فایلِ migration.
- **`scrub()` پیام را می‌شوید ولی خرابش نمی‌کند:** ایمیل و هر عددِ
  چهاررقمی به بالا ماسک می‌شوند و متنِ تشخیصی می‌ماند. پیامِ خطا اغلب
  مقدارِ واقعی را در خودش دارد.
- `PER_REQUEST_MAX = 5` جلوی پر شدنِ جدول در یک درخواستِ حلقه‌دار را
  می‌گیرد؛ بیشتر از پنج خطا در یک درخواست، اطلاعاتِ تازه‌ای هم نمی‌دهد.
- **⛔ وجودِ جدول در زمانِ **خطا** سنجیده می‌شود، نه هنگامِ نصبِ گیرنده.**
  آنجا هنوز `functions.php` لود نشده و `tableExists()` وجود ندارد؛ پس
  نصب روی دیتابیسِ migration‌نخورده هم بی‌خطر است.
- ⚠ **یک تشخیصِ غلطِ خودم:** اول به نظر می‌رسید گیرنده‌ی استثنا اصلاً
  اجرا نمی‌شود. علتش این بود که `php -r` **هیچ‌وقت** `set_exception_handler`
  را صدا نمی‌زند؛ با یک فایلِ واقعی درست کار می‌کرد. **ابزارِ سنجش هم
  می‌تواند دروغ بگوید.**

### ⛔ ۴ — نشانه‌ی «آخرین اجرا» برای هر cron

- **مسئله‌ی واقعی:** پنج کارِ زمان‌بندی‌شده هست (بکاپ، بکاپِ بیرونی،
  پاک‌سازیِ نشست، ایمیلِ یادآوری، اعلانِ ساعتی) و **هیچ‌کدام هیچ ردی
  نمی‌گذاشتند**. یک cron که بی‌صدا از کار بیفتد — فایلِ `/etc/cron.d`
  پاک شود، اسکریپت جابه‌جا شود، یا مسیرِ PHP عوض شود — تا **روزِ حادثه**
  دیده نمی‌شد. و بدترینش بکاپ است: همان «تنها خرابیِ برگشت‌ناپذیر».
- **⛔ نشانه یک فایلِ ساده با اپکِ یونیکس است** (`var/cron/<job>.beat`)،
  نه یک ردیفِ دیتابیس. دلیلش این است که `backup.sh` و
  `backup-offsite.sh` **بشِ خالص‌اند** و نباید برای یک نشانه به PDO و
  `config.php` وابسته شوند — مخصوصاً مسیرِ بکاپ، که باید حتی وقتی
  دیتابیس خراب است هم کار کند.
- **⛔ `CronHealth::JOBS` فهرستِ بسته است** (مثل `MIGRATIONS` و `EXPECT`):
  تست از روی `--install-cron` های موجود کشف می‌کند و هر کارِ تازه‌ای که
  در فهرست نباشد **تست را می‌شکند**. بدونش، cronِ فردا بی‌صدا بیرونِ
  پوشش می‌ماند.
- **سه حالت، نه دو تا:** `never` (هرگز اجرا نشده — تازه نصب شده یا cron
  اصلاً ثبت نشده)، `ok`، و `stale`. آستانه‌ی کهنگی `(دوره × ۲ + ۱) ساعت`
  است، نه خودِ دوره: یک تأخیرِ عادی نباید هشدارِ الکی بدهد، و **هشدارِ
  الکی آدم را عادت می‌دهد هشدارها را نادیده بگیرد**.
- **⛔ نشانه فقط بعد از اجرای **موفق** زده می‌شود.** با زدنش در ابتدای
  اسکریپت، کارِ شکست‌خورده هم «تازه» دیده می‌شد — یعنی دقیقاً همان
  «سنجشی که روی خرابی سبز می‌شود».
- ⚠ `reminders.php` و `due-notify.php` فقط در حالتِ **واقعی** نشانه
  می‌زنند، نه در اجرای نمایشی؛ وگرنه یک `--dry-run` دستی، cronِ
  خوابیده را سالم نشان می‌داد.
- هر دو کارت (cron و خطاها) در `admin/insights.php` هستند، و دکمه‌ی
  «پاک کردن» خطاها CSRF دارد.
- تست در `tests/test_ops_health.php` است (۲۵ بررسی) و هر دو موردِ ۳ و ۴
  را می‌سنجد.

### ⛔ ۵ — `Content-Security-Policy`

- سایت سه هدرِ امنیتی داشت (`nosniff`, `SAMEORIGIN`, `Referrer-Policy`)
  ولی **هیچ CSP ای نداشت** — یعنی اگر روزی جایی از خروجی فرار داده
  نشود، مرورگر هیچ سدِ دومی ندارد.
- **⚠ حدِ این کار، صادقانه، و نوشته می‌ماند:** سیاست `'unsafe-inline'`
  دارد، چون قالب‌های این اپ ده‌ها `<script>` درون‌صفحه‌ای دارند
  (نمودارها، `registerThemedChart`، اسکریپت‌های هر صفحه). پس **سدِ
  کاملی در برابر XSS نیست**. ولی `object-src 'none'`،
  `base-uri 'self'`، `form-action 'self'` و `frame-ancestors 'self'` سه
  بردارِ واقعی را می‌بندند و هزینه‌شان صفر است.
  - راهِ کاملِ بعدی nonce است، ولی آن یعنی دست زدن به هر اسکریپتِ
    درون‌صفحه‌ای و یک هدرِ پویا به‌ازای هر درخواست — کاری که باید عمدی و
    یک‌جا انجام شود، نه نصفه.
- **سیاست در دو جا نوشته شده و باید یکی بماند:** `vps-setup.sh` برای
  نصبِ تازه و `deploy/nginx-csp.sh` برای نصبِ موجود. قاعده ۳۵ هر دو را
  با هم می‌سنجد — اگر یکی عوض شود و دیگری نه، دو نصب دو رفتار می‌گیرند
  و آن تفاوت فقط وقتی دیده می‌شود که صفحه‌ای روی یکی بشکند.
- **⛔ اسکریپت بعد از reload خودِ رفتار را می‌سنجد** (همان درسِ `^~` در
  `nginx-api.sh`): هم نشستنِ هدر، هم ۲۰۰ ماندنِ `/login.php`. اگر
  هرکدام نشد، برمی‌گرداند. سنجش با `--resolve` است نه `-H "Host:"` —
  روی این سرور چند سایت روی یک nginx هستند.
- انتقالِ بلوک با فایلِ موقت و `getline` است نه `awk -v`: خودِ سیاست
  کوتیشن و `'` دارد و awk دنباله‌های escape را بی‌صدا عوض می‌کند (همان
  چیزی که `nginx-realip.sh` را گرفت).
- ⚠ **قاعده ۳۵ اول ناقص بود و جهش نشانش داد:** بررسیِ «سنجش با
  `--resolve` است» رشته را در کلِ فایل می‌گشت، و **کامنتِ توضیحیِ همان
  بخش خودش آن کلمه را دارد** — پس با برداشتنِ `--resolve` از خودِ
  `curl` هم سبز می‌ماند. حالا کامنت‌ها پیش از بررسی حذف می‌شوند و
  `-H "Host:` هم صریح ممنوع است. **جهشِ زنده‌مانده اول یعنی تست ناقص
  است، نه اینکه کد امن است.**
- روی نصبِ موجود: `sudo bash deploy/nginx-csp.sh --apply`.

### ⛔ ۶ — منطقه‌ی زمانی: یک اختلافِ ۳:۳۰ ساعته‌ی **زنده**

- **خرابیِ واقعی، و با اندازه‌گیری پیدا شد:** `date('Y-m-d H:i:s')` روی
  ۱۱:۳۰ بود و `SELECT NOW()` روی ۰۸:۰۰ — چون PHP روی `APP_TIMEZONE`
  بود و MySQL روی `SYSTEM` (که اینجا UTC است).
- قاعده‌ی پروژه («زمان را در دیتابیس بساز و در دیتابیس مقایسه کن») این
  را تا امروز بی‌خطر نگه داشته بود، ولی **فقط تا مرزِ روز**: هر شب بین
  ۰۰:۰۰ و ۰۳:۳۰ به وقتِ تهران، `today()`ِ PHP و `CURDATE()`ِ دیتابیس دو
  روزِ متفاوت می‌گفتند. یعنی `remind_date <= today`، کلیدِ یکتای
  `snap_date`، `last_sent_on` و نگهبانِ «یک بار در روز»ِ تراکنش‌های
  دوره‌ای در آن پنجره **بی‌صدا** غلط می‌شدند.
- **⛔ `applyAppTimezone()` تنها جای این تصمیم است و در `db.php` صدا
  زده می‌شود، نه در `initSession()`.** پیش از این فقط نشست آن را
  می‌گذاشت، یعنی هر پروسه‌ای که نشست نمی‌سازد — **همه‌ی تست‌های خط
  فرمان و هر cron ای که auth را لود نمی‌کند** — با منطقه‌ی زمانیِ
  سیستم کار می‌کرد. پس کلِ این دسته خطا در تست **اصلاً اجرا نمی‌شد**،
  چون آنجا PHP و MySQL اتفاقاً هر دو UTC بودند.
- **⛔ `Database::syncTimezone()` افستِ عددی می‌فرستد، نه نامِ منطقه.**
  `SET time_zone = 'Asia/Tehran'` فقط وقتی کار می‌کند که جدول‌های
  `mysql.time_zone` بارگذاری شده باشند — که روی بیشترِ نصب‌ها نیستند و
  آن‌وقت این دستور **بی‌صدا** خطا می‌داد.
- ⚠ و عمداً هر بار از `date('P')` خوانده می‌شود نه یک ثابت: افستِ ایران
  ۳:۳۰ است ولی هر کشوری که ساعتِ تابستانی دارد سالی دو بار عوض می‌شود،
  و مقدارِ ثابت شش ماه بعد یک ساعت غلط می‌بود.
- تست در `tests/test_timezone.php` است (۷ بررسی) و مهم‌ترینش این است که
  **با `TZ=UTC` و `-d date.timezone=UTC` در یک پروسه‌ی جدا** اجرا
  می‌شود: یعنی ثابت می‌کند `db.php` واقعاً سیستم را بازنویسی می‌کند، نه
  اینکه اتفاقاً هم‌راستا بوده. هم‌خوانیِ ساعتِ PHP و MySQL هم مستقیم
  سنجیده می‌شود (اختلافِ زیرِ ۹۰ ثانیه) و `date_default_timezone_set`
  با توکنایزر جای دیگری مجاز نیست.

---

## ⛔ قاعده ۳۶ — بک‌تیک داخلِ رشته‌ی دابل‌کوتِ bash

- **خرابیِ واقعی، و روی سرور دیده شد نه در بازبینی.** پیامِ هشدارِ
  `deploy/nginx-csp.sh` برای زیبایی نامِ یک دستورِ CSP را داخلِ بک‌تیک
  گذاشته بود. داخلِ `"…"` بک‌تیک **جانشینیِ فرمان** است، پس bash سعی
  کرد `'unsafe-inline'` را **اجرا** کند: وسطِ خروجیِ `--apply` نوشت
  `line 170: unsafe-inline: command not found` و همان جمله را با یک
  **حفره** چاپ کرد — یعنی دقیقاً آن جمله‌ای که «حدِ صادقانه»ی محافظ را
  می‌گوید، نصفه به مالکِ نصب رسید.
- **⚠ اینجا شانس آوردیم که آن رشته فرمانی نبود.** همان شکل با یک نامِ
  فرمانِ واقعی (یا `$(…)` که هم‌معناست) **اجرا می‌شد** — داخلِ اسکریپتی
  که با `sudo` اجرا می‌شود و پیکربندیِ nginx را می‌نویسد. همان جنسِ
  خطرِ «تگِ پایانِ PHP داخل کامنت».
- **⛔ بررسی با پیمایشِ کاراکتری است نه grep.** بک‌تیک سه جای کاملاً
  بی‌خطر هم دارد و هیچ‌کدام نباید قرمز شوند (هشدارِ الکی روی فایلِ سالم
  از نبودِ تست بدتر است): فرارداده‌شده (`\`` در SQLِ `migrate.sh`)،
  داخلِ رشته‌ی **تک‌کوتی** (`printf` در `nginx-realip.sh`)، و داخلِ
  **کامنت** — که در این مخزن ده‌ها بار تکرار شده. پس وضعیتِ کوتیشن،
  فرار، کامنت و heredoc همه دنبال می‌شوند و وضعیت بینِ خط‌ها هم حمل
  می‌شود (رشته‌ی دابل‌کوت می‌تواند چندخطی باشد).
- هر ۲۰ اسکریپتِ پوسته‌ای (`deploy/*.sh`، `deploy.sh`، `tests/run.sh`)
  سنجیده می‌شوند، و **خالی بودنِ فهرست خودش یک خطاست** — همان درسِ globِ
  سخت‌کدِ قاعده ۱۹ که با اولین تغییرِ نام بی‌صدا ناپدید می‌شد.
- با چهار جهش سنجیده شد: برگرداندنِ خودِ باگ (گرفته شد)، برداشتنِ
  بک‌اسلشِ یک بک‌تیکِ فرارداده‌شده در `migrate.sh` (گرفته شد)، بک‌تیک
  داخلِ تک‌کوتیشن (**سبز ماند**، یعنی هشدارِ الکی نمی‌دهد)، و خراب کردنِ
  glob (گرفته شد).

---

## ⛔ دقیقه‌ی اول، بارِ دوم — فرمِ ثبت تراکنش

هسته‌ی این اپ ثبت یک تراکنش است و کاربر روزی چند بار انجامش می‌دهد.
با اندازه‌گیری در کرومیوم شمرده شد: **شش تپ**. هیچ‌کدام از فیلدها یک
تصمیمِ بد نبودند؛ **حاصلِ جمعشان** بود — همان الگوی «چهار خزشِ بی‌صدا»
در بخشِ سرعت، این بار روی دستِ کاربر به‌جای دیتابیس. حالا **سه تپ**.

### ⛔ ۱ — پیش‌فرضِ نوع «هزینه» شد

- **خرابیِ بی‌صدا بود، نه یک سلیقه.** پیش‌فرض «درآمد» بود؛ روی همین
  دیتابیس شمرده شد: **۲۹٬۲۲۴ هزینه در برابر ۱۰٬۷۷۹ درآمد** (۷۳٪ به
  ۲۷٪). یعنی کاربری که عجله دارد و تاگل را نمی‌بیند، خرجش را به‌عنوان
  **درآمد** ثبت می‌کند — نه خطایی، نه هشداری؛ فقط موجودی بالا می‌رود و
  گزارشِ ماه دروغ می‌گوید.
- **⚠ ترتیبِ دکمه‌ها عمداً دست نخورد.** جابه‌جا کردنشان حافظه‌ی عضلانیِ
  کاربرِ فعلی را می‌شکست بی‌آنکه چیزی به دست بیاید: پیش‌فرض همان کاری را
  می‌کند که لازم است.
- **⛔ `active` روی دکمه و `value` روی فیلدِ پنهان باید همیشه یکی
  باشند** — قاعده ۳۷.
  - **⚠ و این را تستِ رفتاری نمی‌بیند:** `setupTypeToggle()` هنگام
    بارگذاری `activate(initialBtn)` را صدا می‌زند و مقدارِ فیلد را از
    روی دکمه‌ی فعال **بازنویسی** می‌کند، پس ناهماهنگی در مرورگر
    خودبه‌خود ترمیم می‌شود. **جهشش در `test_tap_budget` زنده ماند** و
    همان نشان داد که قاعده‌ی شکل لازم است.
  - ولی بی‌ضرر نیست: اگر `app.js` نرسد (همان حالتی که `js-loading`
    برایش ساخته شد)، فرم با مقدارِ خامِ HTML می‌رود در حالی که کاربر
    دکمه‌ی دیگری را فعال می‌بیند — تراکنش **برعکس** ثبت می‌شود.

### ⛔ ۲ — عنوان دیگر اجباری نیست

- آن یک فیلد بین کاربر و ثبتِ خرجش می‌ایستاد و جوابش را هم اغلب
  نداشت: «۴۵ هزار نان» عنوانِ تازه‌ای لازم ندارد، دسته‌اش خودش می‌گوید.
- **⛔ خودِ اپ یک بار دورش زده بود** و همان مدرکِ اصلی است: مسیرِ «ثبت
  خودکار از پیامک» ناچار شد عنوانِ ساختگی («واریز — از پیامک بانک»)
  بگذارد، وگرنه `requestSubmit()` روی اعتبارسنجیِ مرورگر **بی‌صدا**
  می‌ایستاد. وقتی یک قاعده را برای مسیرِ خودکار دور می‌زنید، آن قاعده
  برای مسیرِ دستی هم اشتباه است.
- **⛔ `fallbackTxTitle()` تنها جای این تصمیم است** و در **سرور** است نه
  مرورگر: هر سه مصرف‌کننده (وب، `api/v1`، پیامک) از `txCreate`/`txUpdate`
  رد می‌شوند. عنوانِ خالی نامِ دسته می‌گیرد و بدونِ دسته «هزینه»/«درآمد».
- **⛔ و **بعد از** `txResolveCategory()` صدا زده می‌شود**، وگرنه نامِ
  دسته‌ی کاربرِ دیگری داخلِ عنوانِ این کاربر می‌نشست. قاعده ۳۸ ترتیبش را
  پین می‌کند.
  - ⚠ و کوئریِ داخلش هم از `categoryScopeSql()` رد می‌شود. آن شرط
    **افزونه** است (فراخواننده از قبل سنجیده) ولی **قاعده ۶ خودش
    گرفتش** — نه بازبینیِ چشمی — و درست هم می‌گفت: اولین مسیری که فردا
    این تابع را بی‌سنجش صدا بزند، بدونِ آن شرط نشت می‌کند.
- `edit_tx_modal.php` هم `required` را از دست داد تا دو رفتار نماند:
  وگرنه کاربری که عنوان را پاک می‌کند از مرورگر «الزامی است» می‌گرفت در
  حالی که سرور قبولش می‌کند.

### ⛔ ۳ — دسته‌بندی: شبکه‌ی چیپ، مرتب بر اساس استفاده‌ی خودِ کاربر

- **⛔ جایگزینِ `<select>` نشد، رویش سوار شد.** مقدار، رویدادِ `change`،
  اعتبارسنجیِ فرم، و `setType()` / `resetSelect()` / `optionPicker()` /
  `smsAutoOk()` همه به وجودِ خودِ `<select>` بندند — همان استدلالی که
  `optionPicker()` را هم به این شکل نگه داشت. چیپ فقط `selectedIndex` را
  می‌نویسد و `change` می‌دهد. پس هیچ مصرف‌کننده‌ای عوض نشد.
- **`<select>` زیرِ شبکه می‌ماند و این عمدی است:** شبکه فقط
  `CATEGORY_GRID_MAX` (۸) چیپِ اول را نشان می‌دهد، و بقیه باید جایی
  در دسترس بمانند — وگرنه کاربری با ۲۰ دسته، دوازده‌تایش را گم می‌کرد.
- **⛔ `categoriesByUse()` تنها جای ترتیب است** و هم شبکه هم خودِ منو از
  آن رد می‌شوند. با دو ترتیب، چیپِ سومِ بالا با گزینه‌ی سومِ منو یکی
  نمی‌بود و کاربر هر بار کلِ فهرست را می‌خواند.
  - مرتب‌سازی **پایدار** است (PHP ≥ ۸٫۰): دسته‌های بی‌استفاده ترتیبِ
    الفباییِ `cachedCategories()` را نگه می‌دارند، وگرنه فهرست بین دو
    بارگذاری بی‌دلیل جابه‌جا می‌شد.
- **⛔ هزینه‌اش یک کوئریِ تازه در **هر** صفحه است و نوشته می‌ماند.**
  شیتِ ثبت در فوترِ هر صفحه است، پس `categoryUseCounts()` هم همان‌جا
  اجرا می‌شود. اندازه‌گیری شد (کاربرِ ۲۰٬۰۰۰ تراکنشی): **۰٫۶۸
  میلی‌ثانیه و ۶۲۸ ردیف** با پنجره‌ی ۲۰۰، در برابر ۰٫۸۴ و ۱۲۲۸ با ۴۰۰.
  روی fixture تست: `index.php` ۲۷ → **۲۸** کوئری و ۳۰۰۹ → **۳۴۸۱**
  ردیف. سقفِ ردیفِ `test_query_budget` به همین دلیل از ۳۵۵۰ به ۳۶۰۰ رفت
  (جهشِ «ایندکس‌ها را بردار» همچنان بالای ۴۶۰۰ می‌دهد، پس هنوز تشخیص
  می‌دهد).
  - **⛔ `CATEGORY_USE_SCAN` نصفِ `RECENT_TITLE_SCAN` است، عمداً:** شبکه
    هشت چیپ نشان می‌دهد، پس ۲۰۰ ردیفِ آخر برای رتبه‌بندیِ هشت‌تای اول
    کافی است و ۴۰۰ فقط هزینه را دو برابر می‌کرد.
  - ⚠ راهِ «رایگان» هم سنجیده و رد شد: می‌شد ترتیب را از همان کوئریِ
    `recentTransactionTitles()` درآورد، ولی آن روی **عنوانِ متمایز**
    گروه می‌شود نه روی تعدادِ استفاده — یعنی «نان»ِ هر روزه و «تعمیر
    پنکه»ی یک‌باره هم‌وزن می‌شدند. اسمش را «بر اساس استفاده» گذاشتن و
    چیزِ دیگری حساب کردن، همان دروغِ بی‌صداست.
- **آیکون از `categoryIconSvg()` می‌آید، نه یک نگاشتِ دومِ جاوااسکریپتی.**
  با نسخه‌ی دوم، اولین آیکونِ تازه (مثل `shield` برای بیمه) عقب می‌ماند و
  همه بی‌صدا `default` می‌شدند.
- **⛔ `JSON_HEX_TAG` اجباری است.** نامِ دسته را خودِ کاربر می‌نویسد و
  داخلِ `<script>` می‌نشیند؛ نسخه‌ی قبلی این پرچم را نداشت، پس دسته‌ای
  به نامِ «‎</script>…» از تگ بیرون می‌زد. نامِ چیپ هم با `textContent`
  نوشته می‌شود نه `innerHTML` (آیکون برعکس: markupِ خودمان است).
- ⚠ **مودالِ ویرایش شبکه نمی‌گیرد** و این عمدی است: ویرایش کمیاب است و
  رکورد از قبل دسته دارد، پس یک ردیفِ چیپِ اضافه فقط مودالِ بلند را
  بلندتر می‌کرد.
- ⚠ `.cat-grid` با `:empty` پنهان می‌شود نه با `[hidden]`: ظرف خالی از
  سرور می‌آید و `app.js` پرش می‌کند، پس هیچ‌جا ویژگیِ `hidden` روی این
  کلاس رندر نمی‌شود و قاعده ۱۸ موضوعیت ندارد.
- ⚠ `white-space: nowrap` روی چیپ لازم است — همان درسِ نیم‌فاصله در
  قاعده ۳۱: «خوراکی‌ها» جای شکستن دارد و چیپ را دو خط می‌کرد.

### ⛔ ۳٫۱ — یک خطِ افقی، و انتخابِ خودِ کاربر

**گزارشِ مالکِ نصب:** «الان خیلی شلوغ شده… دسته‌بندی با آیکون ایده‌ی
خوبی بود به شرطی که امکانِ انتخاب داشته باشه، مثلاً فقط گوشی و لوازم
بیارم که شلوغ نشه، و بهتر خطی کنارِ هم بیان.»

- **⛔ `flex-wrap: nowrap` به‌جای `wrap`.** اندازه‌گیری شد (کرومیوم، عرضِ
  ۸۰۰): با هشت چیپ، `wrap` سه ردیف می‌ساخت و کلِ فرم را پایین می‌برد؛
  حالا `lines: 1` و ارتفاعِ ردیف **۴۲ پیکسل**. سرریز افقی اسکرول
  می‌شود (`scrollWidth 735` در برابر `clientWidth 370`) و از لبه‌ی شیت
  هم بیرون نمی‌زند (`overflowsSheet: false`).
  - ⚠ `overflow-x: auto` مقدارِ `overflow-y` را هم `auto` می‌کند و
    `:active { transform: scale(.96) }` را لبه‌ی برش می‌گرفت — همان درسِ
    سایه‌ی کارت‌های خانه. `padding: 3px` + `margin-inline: -3px`.
  - ⚠ `flex: 0 0 auto` روی چیپ لازم است، وگرنه مرورگر همه را در عرضِ
    موجود فشرده می‌کند و نام‌ها با «…» بریده می‌شوند — یعنی `nowrap`
    بی‌اثر می‌شد.
- **⛔ `categoriesForGrid()` تنها جای «کدام دسته‌ها چیپ می‌گیرند» است**
  (مثل `pinnedWallets()`): اگر کاربر برای آن **نوع** چیزی پین کرده
  باشد دقیقاً همان‌ها، وگرنه پرکاربردترین‌ها. **نبودِ آن fallback یعنی
  هر نصبِ موجودی بعد از `git pull` ردیفِ چیپش خالی می‌شد** و بودجه‌ی
  «سه تپ» بی‌صدا به چهار برمی‌گشت.
- **⛔ پین یک جدولِ جداست (`category_pins`)، نه ستونی روی `categories`.**
  دسته‌ی پیش‌فرضِ برنامه `user_id IS NULL` دارد و **بینِ همه‌ی کاربران
  مشترک** است: یک ستونِ `pinned` روی آن ردیف یعنی پین کردنِ «خوراکی»
  توسطِ یک نفر، آن را روی فرمِ **همه** می‌نشاند — یک نشتیِ بی‌صدا بینِ
  کاربران. (`wallets.pinned` می‌توانست ستون باشد چون هر حساب یک مالک
  دارد.) تست این را روی **صفحه‌ی واقعی** می‌سنجد، نه با کوئریِ
  بازنویسی‌شده — پایین‌تر.
- **⛔ هزینه‌اش صفر است، چون پین با `JOIN` به `cachedCategories()` می‌آید
  نه با یک کوئریِ دوم.** شیتِ ثبت در فوترِ **هر** صفحه رندر می‌شود، پس
  کوئریِ جدا یعنی یکی به بودجه‌ی هر ۲۷ صفحه — همان «خزشِ بی‌صدا» که کلِ
  `test_query_budget` برای گرفتنش نوشته شد. شمارشِ کوئریِ هیچ صفحه‌ای
  عوض نشد.
- **⛔ سقف در دو جا اعمال می‌شود** (`categoriesForGrid()` و خودِ
  اندپوینت، با ۴۲۲) — همان درسِ `PINNED_WALLET_MAX`: اگر فقط هنگام
  نمایش بریده می‌شد، کاربر ۲۰ تا پین می‌کرد، هشت‌تا می‌دید و هیچ‌جا
  نمی‌فهمید بقیه کجا رفتند.
- مدیریتش در **«فهرست‌های من»** است، کنارِ نامِ خودِ دسته — نه یک کارتِ
  هفتم. `references.php` هم از همان `cachedCategories()` می‌خواند (یک
  کوئری کمتر، و مهم‌تر: آیکونِ پینِ آن صفحه از همان چیزی می‌خواند که
  فرمِ ثبت می‌خواند).
- ⚠ تپ روی پین صفحه را **تازه نمی‌کند** — همان استدلالِ پینِ حساب: هیچ
  عددی روی همان صفحه عوض نمی‌شود و تازه‌سازی فقط جای کاربر را در
  فهرستِ بلندِ دسته‌ها گم می‌کرد.
- تست در `tests/test_category_pin.php` است (۲۶ بررسی، نیمی با HTTP و
  نشستِ واقعی) و با **شش جهش** سنجیده شد: برداشتنِ fallback، نادیده
  گرفتنِ انتخابِ کاربر، برداشتنِ سقفِ نمایش، برداشتنِ بررسیِ مالکیت در
  اندپوینت، برداشتنِ سقفِ سمتِ سرور، و برداشتنِ شرطِ `p.user_id`.
  - ⚠ **جهشِ آخر اول زنده ماند** و دلیلش مهم است: تستِ اولیه کوئریِ
    `cachedCategories()` را **در خودش کپی کرده بود** و همان کپی را
    می‌سنجید — یعنی آزمونی که خودش را می‌سنجد و با خرابیِ کد هم سبز
    می‌ماند (همان دامِ بخشِ CSV در `test_tx_search`). حالا `references.php`
    را با دو نشستِ واقعی (کاربر A و B) باز می‌کند و `data-pinned` را
    می‌خواند. **جهشِ زنده‌مانده اول یعنی تست ناقص است، نه اینکه کد امن
    است.**
- ⚠ **حدش، صادقانه:** با هشت چیپ و عرضِ ~۳۷۰ پیکسل، پنج‌تای آخر پشتِ
  اسکرول می‌مانند. برای کاربری که چیزی پین نکرده این یک عقب‌گرد نسبت
  به `wrap` است (آنجا همه دیده می‌شدند) — ولی ترتیب بر اساسِ
  پرکاربردترین است و شکایتِ اصلی **ارتفاع** بود نه پنهان شدن. راهِ
  حلِ واقعی همان پین است.

### ۴ — فوکوسِ مبلغ، همزمان با تپ

- فوکوس از قبل بود ولی فقط داخلِ `setTimeout(…, 120)` (برای انیمیشنِ
  `sheetUp`). **روی iOS کیبوردِ مجازی تنها وقتی بالا می‌آید که
  `focus()` در همان زنجیره‌ی رویدادِ لمس صدا زده شود**؛ داخلِ تایمر آن
  زنجیره بریده است، پس فیلد فوکوس می‌گرفت و کیبورد نمی‌آمد — یعنی یک
  تپِ دیگر روی خودِ فیلد.
- حالا هر دو: یکی همزمان (برای iOS) و یکی بعد از انیمیشن (برای موتوری
  که فوکوس روی عنصرِ تازه‌نمایان را نادیده می‌گیرد).
- **⚠ این استدلال است نه اندازه‌گیری:** اینجا iOS نداریم. آنچه سنجیده
  شد، فوکوس گرفتنِ فیلد در کرومیوم است — و `test_tap_budget` همان را
  پین می‌کند.

### ⛔ ۵ — `tests/test_tap_budget.php`

- قرینه‌ی `test_query_budget`، برای دستِ کاربر: **۲۷ بررسی**، و عدد را
  در **کرومیومِ واقعی** می‌شمارد نه از روی HTML — فهرستِ دسته‌ها و شبکه
  را `app.js` می‌سازد، پس سورسِ صفحه چیزی درباره‌ی «چند تپ» نمی‌گوید.
- **⛔ هر گام شرطی است**: «اگر شرط از قبل برقرار است تپی خرج نکن». پس
  عدد از خودِ DOM درمی‌آید نه از سناریوی سخت‌کد. اندازه‌گیری شد:
  **هزینه ۳ تپ، درآمد ۴** (باز کردن → چیپ → ثبت؛ درآمد یکی بیشتر برای
  تاگل). اختلافِ یک تپ **بهای عمدیِ** پیش‌فرضِ هزینه است و تست خودش
  همان یک را می‌سنجد.
- **⛔ بودجه‌ی گشاد هم شکست است** — همان قاعده: اگر عدد از سقف کمتر
  باشد، تست می‌گوید سقف را پایین بیاورید.
- **⛔ «شبیه‌سازی شد» با «ثبت شد» یکی نیست:** همان مسیر واقعاً `submit`
  می‌زند و تست ردیفِ دیتابیس را می‌سنجد — از جمله اینکه عنوانِ خالی با
  نامِ دسته پر شده.
- **⛔ هیچ وابستگیِ npm ندارد.** کرومیوم را مستقیم با CDP می‌راند
  (node ≥ ۲۲ خودش `WebSocket` و `fetch` دارد). یک `npm install` در این
  مخزن یعنی تستی که روی سرور اجرا نمی‌شود. نبودِ کرومیوم `T::skip` است
  (ابزارِ اختیاری)، نبودِ دیتابیس `T::blocked`.
- **⚠ حدش، صادقانه:** `el.click()` می‌زند نه لمسِ مختصاتی. پس **تعدادِ
  تپِ لازم** را می‌سنجد، نه اینکه دکمه به اندازه‌ی انگشت است یا زیرِ
  کیبورد می‌ماند. آن‌ها را فقط روی گوشی می‌شود دید.
- با پنج جهش سنجیده شد، همه گرفته شد: برگشتِ `required` روی عنوان (۶
  بررسی قرمز)، برگشتِ پیش‌فرض به درآمد (۴)، برداشتنِ شبکه‌ی چیپ (۴)،
  برداشتنِ `fallbackTxTitle` (۲)، و شکستنِ توافقِ `active`/`value` —
  که **زنده ماند** و همان قاعده ۳۷ را ساخت.
- ⚠ **دو باگِ خودِ probe در همان اجرای اول، و هر دو درسِ تکراری:**
  (الف) `document.querySelector('.type-btn[data-type="income"]')` دکمه‌ی
  **مودالِ ویرایش** را می‌زد (که در `index.php` زودتر رندر می‌شود)، پس
  نوع عوض نمی‌شد و probe بی‌صدا عددِ غلط می‌داد — انتخابگر باید به
  `#typeToggle` بسته باشد. (ب) یک بک‌تیک داخلِ کامنتِ یک **template
  literal** رشته را بست و فایل را شکست: همان دامِ قاعده ۳۶، این بار در
  جاوااسکریپت.

### قاعده‌های تازه در `test_api_contract.php`

- **قاعده ۳۷** — دکمه‌ی فعالِ تاگل با مقدارِ ارسالی یکی باشد، و
  پیش‌فرضِ شیتِ ثبت «هزینه» بماند. با سه جهش سنجیده شد.
  - ⚠ نسخه‌ی اول شرطِ ورودش `class="type-toggle"` بود و روی فایلِ
    **سالم** قرمز شد: `wallet_card_modals.php` همان کلاس را برای چیزِ
    دیگری دارد. حالا شرط `data-type="expense"` است.
- **قاعده ۳۸** — شبکه‌ی دسته‌بندی و عنوانِ اختیاری: `textContent` برای
  نام، سقف از `window.CATEGORY_GRID_MAX`، نوشتن روی `<select>` و دادنِ
  `change`، `JSON_HEX_TAG`، نبودِ `required` روی عنوان، و ترتیبِ
  `fallbackTxTitle` بعد از `txResolveCategory`. با شش جهش سنجیده شد.
  - و سه بررسیِ تازه بعد از «یک خطِ افقی»: `flex-wrap: nowrap` روی
    `.cat-grid`، خواندنِ فهرست از `window.CATEGORY_GRID` در `app.js`
    (وگرنه `slice()`ِ محلی انتخابِ کاربر را بی‌صدا نادیده می‌گیرد)، و
    آمدنِ آن فهرست از `categoriesForGrid()` در `add_tx_sheet.php`. هر
    سه با جهش سنجیده شدند.
  - ⚠ **جهشِ «`JSON_HEX_TAG` را بردار» اول زنده ماند**، چون نامش در
    کامنتِ بالای همان خط بود و `strpos` رویِ سورسِ خام پیدایش می‌کرد.
    حالا کامنت‌ها با توکنایزر حذف می‌شوند — همان دامی که قاعده ۳۵ و ۱۹
    هم افتادند. **جهشِ زنده‌مانده اول یعنی تست ناقص است، نه اینکه کد امن
    است.**
