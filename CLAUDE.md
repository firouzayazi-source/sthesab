# راهنمای توسعه — دفتر مالی (پروژه حسابداری)

اپلیکیشن حسابداری شخصی/خانوادگی، چندکاربره، فارسی و راست‌به‌چپ، با تاریخ شمسی.
PHP خام (بدون فریم‌ورک و بدون Composer) + MySQL/MariaDB + PDO.

**پیش‌نیاز اجرا:** PHP ≥ ۸.۰ (تست‌شده تا ۸.۴)، افزونه‌های `pdo_mysql`, `gd`, `zlib`, `mbstring`.

---

## نقشه‌ی پروژه

```
config/       تنظیمات (فقط config.example.php در گیت است؛ config.php محرمانه و ignore شده)
includes/     لایه‌ی مشترک — همه‌ی صفحات از اینجا شروع می‌شوند
api/          ~۵۰ اندپوینت JSON برای فراخوانی‌های AJAX
admin/        صفحات ویژه‌ی مدیر (کاربران، دسته‌بندی‌ها، آمار استفاده)
assets/       style.css، app.js، jalali-datepicker.js، آیکون‌ها، فونت وزیرمتن
mobile/       اپ فلاتر (اندروید و iOS) — مصرف‌کننده‌ی api/v1
*.php (ریشه)  صفحات کاربر
*.sql         schema.sql + ۱۸ فایل migration
```

### فایل‌های کلیدی `includes/`

| فایل | نقش |
|---|---|
| `db.php` | کلاس `Database::getConnection()` — PDO تک‌نمونه‌ای، `ERRMODE_EXCEPTION`، `EMULATE_PREPARES=false`. همچنین فشرده‌سازی gzip خروجی را روشن می‌کند و باید **پیش از هر خروجی** لود شود. |
| `auth.php` | کلاس `Auth` — نشست، ورود، «دستگاه مورد اعتماد» با کوکی، نقش‌ها. متدهای پرکاربرد: `initSession()`, `requireLogin()`, `requireAdmin()`, `userId()`, `isAdmin()`. |
| `csrf.php` | کلاس `Csrf` — `token()`, `field()`, `verifyOrFail()` با `hash_equals`. |
| `mailer.php` | کلاس `Mailer` — ارسال ایمیل با `mail()` یا SMTP (کلاینت SMTP دست‌نویس، چون Composer نداریم). |
| `password_reset.php` | کلاس `PasswordReset` — منطق بازیابی رمز، جدا از صفحه‌ها تا قابل تست باشد. |
| `functions.php` | ~۹۰ تابع کمکی (۱۰۸۸ خط) — بزرگ‌ترین فایل پروژه و مرکز منطق دامنه. |
| `header.php` / `sidebar.php` / `footer.php` | قالب مشترک صفحات. |
| `add_tx_sheet.php` / `edit_tx_modal.php` | فرم‌های مشترک ثبت و ویرایش تراکنش. |

### توابع مهم `functions.php`

- **تاریخ شمسی:** `toJalali()`, `jalaliToGregorian()`, `gregorianToJalali()`, `today()`, `startOfWeek()`, `startOfJalaliMonth()`, `startOfJalaliYear()`, `jalaliMonthLength()`, `jalaliWithWeekday()`
- **ورودی/خروجی:** `postParam()`, `getParam()`, `jsonResponse()`, `h()` (escape)، `sanitizeAmount()`, `isValidDate()`, `toPersianDigits()`, `formatMoney()`
- **دامنه:** `walletBalances()`, `totalBalance()`, `budgetStatuses()`, `savingsGoalsWithProgress()`, `debtRemaining()`, `processRecurringTransactions()`, `financialEvents()`, `safeToSpend()`, `spendingInsights()`, `monthComparison()`
- **تنظیمات:** `getSetting()` / `setSetting()` روی جدول `app_settings`

---

## قواعد کدنویسی (هنگام افزودن قابلیت رعایت شود)

### الگوی استاندارد یک اندپوینت `api/`

هر فایل نویسنده‌ی داده در `api/` دقیقاً همین ترتیب را دارد:

```php
<?php
require_once __DIR__ . '/../includes/auth.php';
require_once __DIR__ . '/../includes/csrf.php';
require_once __DIR__ . '/../includes/functions.php';

Auth::initSession();
header('Content-Type: application/json; charset=utf-8');

if (!Auth::isLoggedIn()) { jsonResponse(['success' => false, 'message' => 'ابتدا وارد شوید.'], 401); }
if ($_SERVER['REQUEST_METHOD'] !== 'POST') { jsonResponse(['success' => false, 'message' => 'درخواست نامعتبر است.'], 405); }
Csrf::verifyOrFail(postParam('csrf_token'));

$userId = Auth::userId();   // همیشه از اینجا، هرگز از ورودی کاربر
```

### قواعدی که نباید شکسته شوند

1. **جداسازی کاربران:** هر کوئری روی داده‌ی کاربر باید `WHERE user_id = :uid` داشته باشد. `user_id` همیشه از `Auth::userId()` می‌آید، هرگز از `$_POST`. **استثنایی هم ندارد:** تنها استثنای قبلی `admin/all-transactions.php` بود که حذف شد (پایین‌تر در «آمار استفاده» توضیح داده شده).
2. **کوئری‌ها:** فقط `prepare()` + پارامتر نام‌دار. اگر ناچار به ساختن پویای `WHERE` شدید، مثل `transactions.php` عمل کنید: قطعه‌های ثابت در آرایه، مقادیر همیشه bind. رشته‌ی ورودی کاربر هرگز داخل SQL درج نشود.
3. **CSRF:** هر اندپوینتی که چیزی می‌نویسد `Csrf::verifyOrFail()` دارد. اندپوینت‌های فقط-خواندنی (`dashboard_stats`, `day_detail`, `category_transactions`, `savings_history`, `transaction_attachments`, `view_attachment`) عمداً ندارند — اگر روزی نویسنده شدند، باید اضافه شود.
4. **خروجی HTML:** هر مقدار متغیر با `h()` یا `htmlspecialchars` فرار داده شود.
5. **مسیرها:** لینک‌ها و آدرس دارایی‌ها با ثابت `APP_BASE_PATH` ساخته شوند تا نصب در زیرپوشه نشکند.
6. **تاریخ:** ذخیره در دیتابیس میلادی (`DATE`)، نمایش شمسی. تبدیل فقط با توابع خود پروژه — مبدأ الگوریتم PHP و `assets/js/jalali-datepicker.js` باید یکی بماند.
7. **پول:** ورودی با `sanitizeAmount()` پاک شود (ارقام فارسی و جداکننده‌ها را می‌فهمد) و با `formatMoney()` نمایش داده شود.

### دیتابیس و migration

- `schema.sql` جداول پایه را می‌سازد: `users`, `categories`, `transactions`, `debts`, `app_settings`. دسته‌بندی‌های پیش‌فرض را فقط وقتی درج می‌کند که جدول `categories` خالی باشد — بدون این شرط، اجرای دوباره روی دیتابیس دارای داده همه را تکراری اضافه می‌کرد.
- بقیه‌ی جداول و ستون‌ها با فایل‌های `migration_*.sql` اضافه شده‌اند (کیف‌پول، بودجه، چک، دارایی، پس‌انداز، تکرارشونده، آیکون دسته‌ها، ایندکس‌ها).
- **ترتیب اجرا الفبایی نیست.** ترتیب درست در آرایه‌ی `MIGRATIONS` داخل `deploy/migrate.sh` ثبت شده — تنها مرجع ترتیب همان است.
- `migration_repair.sql` هم `wallets`/`transfers` را می‌سازد و هم `budgets`/`savings_*`/`debt_payments`/`recurring_transactions` را، به‌شکل idempotent. دو فایل قدیمی‌تر که همین‌ها را غیر-idempotent می‌ساختند (`migration_wallets.sql` و `migration_p1.sql`) حذف شدند؛ اجرای دستی‌شان روی دیتابیس دارای داده خطر داشت و هیچ چیزی که `migration_repair.sql` نسازد نمی‌ساختند.
- `migration_indexes.sql` باید **بعد از** `migration_cheques_assets.sql` بیاید، چون روی جدول `cheques` ایندکس می‌زند. این فایل idempotent نیست (`ADD INDEX` بدون `IF NOT EXISTS`) و اجرای دوباره‌اش خطای «ایندکس تکراری» می‌دهد — `migrate.sh` این خطا را می‌شناسد و به‌عنوان «از قبل اعمال شده» ثبتش می‌کند.
- همه‌ی فایل‌ها به‌جز `migration_indexes.sql` ایدمپوتنت‌اند؛ روی MariaDB 10.11 تست شده که اجرای کامل روی یک دیتابیس دارای داده، داده را تغییر نمی‌دهد.
- **ردیابی migration:** جدول `schema_migrations` (نام فایل، چک‌سام، زمان اجرا) که `deploy/migrate.sh` می‌سازد و نگه می‌دارد.
  - `migrate.sh` بدون آرگومان فقط وضعیت را نشان می‌دهد؛ `--apply` اعمال می‌کند.
  - **جدول ردیابی به‌تنهایی مدرک نیست.** هر migration در `SENTINEL` یک شاهد دارد (جدولی یا ستونی که می‌سازد) و `--verify` ثبت را با ساختار واقعی می‌سنجد. علتش یک خرابی واقعی است: `--baseline` روی دیتابیس ایمپورت‌شده، `migration_p4` را «اجراشده» ثبت کرد در حالی که ستون `users.avatar` وجود نداشت؛ از آن به بعد `--apply` می‌گفت «چیزی برای اجرا نیست» و آپلود تصویر در اپ خطا می‌داد، بی‌آنکه این دو به هم ربط داده شوند. حالا `--baseline` فقط چیزی را علامت می‌زند که شاهدش واقعاً هست، و `--apply` این ناهم‌خوانی را خودش ترمیم می‌کند.
  - migration تازه = یک ردیف تازه در `SENTINEL` هم لازم دارد؛ بدون آن اسکریپت متوقف می‌شود.
  - **شاهدِ migration ای که فقط داده اضافه می‌کند، شکلِ `جدول~ستون=مقدار` دارد**
    (`app_settings~setting_key=more_categories_seeded`). ⚠ و آن شاهد عمداً
    خودِ داده نیست: اگر شاهد «دسته‌ی خودرو هست» می‌بود، کاربری که آن را حذف
    می‌کند باعث می‌شد `--apply` دوباره اجرایش کند و کارِ خودش را پس بزند.
  - `--baseline` برای دیتابیسی است که از قبل کامل است ولی ردیابی ندارد (مثل ایمپورت از هاست). فقط `BASELINE_SET` را «اجراشده» علامت می‌زند — یعنی migration هایی که پیش از راه‌اندازی ردیابی وجود داشتند. هر فایلی که بعداً اضافه شده باشد واقعاً اجرا می‌شود و baseline از رویش نمی‌پرد. (نسخه‌ی اول کل فهرست را علامت می‌زد و همین باعث می‌شد migration تازه بی‌سروصدا جا بماند.)
  - اگر فایل `migration_*.sql` تازه‌ای اضافه کنید و در آرایه‌ی `MIGRATIONS` ثبتش نکنید، اسکریپت با خطا متوقف می‌شود — عمدی است تا بی‌سروصدا نادیده گرفته نشود.
  - چک‌سام هر فایل ذخیره می‌شود؛ اگر migration اجراشده‌ای بعداً تغییر کند هشدار می‌دهد. **فایل اجراشده را ویرایش نکنید — migration تازه بنویسید.**
  - **`schema.sql` از این هشدار معاف است** (`CHECKSUM_EXEMPT`). قاعده‌ی بالا می‌گوید با هر تغییر ساختار باید به‌روزش کرد، پس چک‌سامش عمداً و مرتب عوض می‌شود. پیش از این همین یک هشدار دائمی بعد از هر استقرار می‌ساخت که کاری هم برایش نمی‌شد کرد — و هشدارِ همیشگی بدتر از نبودنش است، چون آدم را عادت می‌دهد هشدارها را نادیده بگیرد و آن‌وقت همین هشدار روی یک migration واقعی هم دیده نمی‌شود. `--apply` چک‌سام ثبت‌شده‌اش را تازه می‌کند تا جدول ردیابی واقعیت را بگوید.
- تغییر ساختار جدید = یک فایل `migration_*.sql` تازه (idempotent بنویسید: `IF NOT EXISTS` / بررسی ستون)، به‌علاوه به‌روزرسانی `schema.sql` برای نصب‌های تازه.

### ⛔ collation: چرا SQL درست، روی سرور می‌میرد

- **متغیرِ کاربری در مقایسه با یک ستون ممنوع است.** `FIND_IN_SET(name, @list)`
  روی سرور با `Illegal mix of collations (utf8mb4_persian_ci, utf8mb4_general_ci)`
  مرد و migration وسطِ کار ایستاد. علتش این است که `@var` همان «قابلیتِ تبدیلِ ۲»
  را دارد که یک ستون دارد، پس هیچ‌کدام به دیگری تبدیل نمی‌شود. **رشته‌ی ثابت
  قابلیتِ ۴ دارد و همیشه به collation ستون تبدیل می‌شود** — پس `name IN ('…','…')`
  بنویسید، نه `FIND_IN_SET` با متغیر.
- **⚠ و چرا محلی دیده نمی‌شود:** `mysql` بدون آرگومان با `latin1_swedish_ci` وصل
  می‌شود و آنجا **تبدیلِ charset** اتفاق می‌افتد، پس خطا نمی‌دهد. ولی
  `migrate.sh` با `--default-character-set=utf8mb4` وصل می‌شود و همان مقایسه
  می‌میرد. هر آزمونِ دستی روی SQL باید **همان آرگومان** را بدهد، وگرنه چیزی را
  می‌آزماید که روی سرور اجرا نمی‌شود.
- **`schema.sql` باید با seedِ خودش بخواند.** یک بار ستون‌های `icon`/`color` به
  `INSERT` اضافه شدند ولی به `CREATE TABLE` نه. روی نصبِ **موجود** نامرئی بود
  (آن ستون‌ها را `migration_category_icons.sql` قبلاً ساخته بود) ولی نصبِ
  **تازه** با «Unknown column 'icon'» بدونِ هیچ دسته‌بندی‌ای بالا می‌آمد.
- **`tests/test_migration_chain.php` هر دوی این‌ها را می‌گیرد** و با جهش سنجیده
  شده. کلِ زنجیره را روی یک دیتابیسِ **تازه‌ی** `utf8mb4_persian_ci` با اتصالِ
  `utf8mb4` اجرا می‌کند، بعد می‌سنجد که دسته‌بندی‌های پیش‌فرض واقعاً ساخته شده‌اند
  («بدون خطا اجرا شد» با «کار کرد» یکی نیست)، و آخر همه را دوباره اجرا می‌کند تا
  ایدمپوتنت بودن سنجیده شود. فهرستِ فایل‌ها را از خودِ آرایه‌ی `MIGRATIONS` در
  `migrate.sh` می‌خواند، پس فهرستِ دومی وجود ندارد که از آن عقب بیفتد.
  - این تست یک دیتابیسِ موقت می‌سازد، پس کاربرِ اپ (که طبق قانونِ جداسازی فقط
    روی `hesab_db` دسترسی دارد) نمی‌تواند اجرایش کند و **رد می‌شود، نه شکست**.
    روی ماشین توسعه با یک grantِ محدود فعال می‌شود:
    `GRANT ALL ON \`hesab\_chain\_%\`.* TO 'کاربرِ‌تست'@'localhost'`

---

## بررسی وضعیت هنگام ایمپورت (۱۴۰۵/۰۶/۰۵)

انجام شد روی کل سورس:

- ✅ `php -l` روی هر ۸۶ فایل PHP — بدون خطای نحوی (با PHP 8.4)
- ✅ اسکن کلیدواژه‌های محرمانه — هیچ رمز، توکن یا کلیدی در سورس نیست؛ فقط `config.example.php`
- ✅ هر ۴۸ اندپوینت `api/` بررسی ورود کاربر دارند
- ✅ ۲۰۴ فراخوانی `prepare()` در برابر ۸ `query()` (بدون پارامتر ورودی) — تزریق SQL دیده نشد
- ✅ **فونت اضافه شد** — `assets/fonts/Vazirmatn.woff2`، نسخه‌ی variable و subset‌شده (۸۵ KB در یک درخواست، به‌جای ۱۵۳ KB در سه). جزئیات در `assets/fonts/README.md`.
- ✅ **ردیابی migration اضافه شد** — جدول `schema_migrations` و `deploy/migrate.sh` (بالا توضیح داده شد).
- ✅ **مجموعه تست اضافه شد** — `bash tests/run.sh` (پایین‌تر توضیح داده شد).

---

## ⛔ قانون سرور — استقلال کامل پروژه

روی VPS مقصد، سرویس‌های دیگری (دو ربات) در حال کارند. **دفتر مالی کاملاً مستقل است.**

هیچ کد، اسکریپت یا دستوری در این مخزن اجازه ندارد:

- به پوشه، فایل، سرویس systemd، یا دیتابیس هیچ سرویس دیگری روی سرور دست بزند
- سرویس مشترکی را `stop` یا `restart` کند (فقط `reload`، آن هم بعد از تست موفق)
- فایل‌های پیکربندی مشترک را تغییر دهد: `nginx.conf`، `php.ini` سراسری، سایت‌های nginx دیگر، pool های دیگر، قوانین فایروال موجود
- کاربر دیتابیسی با دسترسی گسترده‌تر از `hesab.*` بسازد

منابعی که این پروژه **فقط** حق دارد استفاده کند:

| منبع | مقدار |
|---|---|
| پوشه | `/opt/hesab/app` |
| کاربر سیستمی | `hesab` |
| سایت nginx | `/etc/nginx/sites-{available,enabled}/hesab` |
| pool مربوط به PHP | `/etc/php/<نسخه>/fpm/pool.d/hesab.conf` |
| سوکت | `/run/php/php-hesab.sock` |
| دیتابیس / کاربر | `hesab_db` / `'hesab_user'@'localhost'` با `GRANT ... ON hesab_db.*` |
| بکاپ | `/opt/hesab/backups` و `/etc/cron.d/hesab-backup` |

> **دام دسترسی `var/sessions`:** `deploy.sh` عمداً `chown -R root:root .` می‌زند تا PHP نتواند کد را عوض کند. ولی `session.save_path` این pool داخل خودِ پوشه‌ی اپ است (`var/sessions`)، پس همان دستور آن را هم مال root می‌کرد و PHP — که با کاربر `hesab` اجرا می‌شود — دیگر نمی‌توانست فایل نشست بنویسد. **خرابیِ حاصل هیچ ردی در لاگ نداشت:** هر درخواست یک نشستِ خالیِ تازه می‌گرفت، پس توکن CSRF هرگز نمی‌ماند و صفحه‌ی ورود در حلقه‌ی «نشست شما منقضی شده بود» گیر می‌کرد — هیچ‌کس نمی‌توانست وارد شود، و هر بار اجرای `deploy.sh` دوباره خرابش می‌کرد. `deploy.sh` حالا مثل `uploads`، پوشه‌ی `var` را هم برمی‌گرداند و نوشتنی بودنش را با `sudo -u hesab test -w` واقعاً می‌آزماید. قاعده ۸ در `test_api_contract.php` این را می‌سنجد.
>
> **دام دسترسی `config/config.php`:** فایل‌های اپ مال `root:root` هستند ولی PHP با کاربر `hesab` اجرا می‌شود. پس `chmod 640` به‌تنهایی یعنی «هیچ‌کس جز root»، و PHP دیگر کانفیگ را نمی‌خواند — کل سایت ۵۰۰ می‌دهد. یک بار `deploy/mail-setup.sh` همین کار را کرد و اپ خوابید. ترتیب درست: `chown root:hesab` بعد `chmod 640`، و در آخر واقعاً آزمودن با `sudo -u hesab test -r`؛ اگر خوانده نشد، برگشت به `644`. هر اسکریپتی که به دسترسی این فایل دست می‌زند باید همین کار را بکند (`deploy.sh` و `deploy/mail-setup.sh` می‌کنند).

سه لایه‌ای که این قانون را عملاً تضمین می‌کنند:

1. `deploy/vps-setup.sh` تابع `assert_allowed()` دارد؛ هر نوشتن خارج از فهرست بالا اسکریپت را متوقف می‌کند.
2. pool اختصاصی PHP با `open_basedir=/opt/hesab/app:/tmp:/usr/share/php` و `disable_functions` شامل `exec,shell_exec,system,proc_open,popen`.
3. PHP این اپ با کاربر `hesab` اجرا می‌شود، نه `www-data`.

هنگام افزودن هر اسکریپت یا قابلیت مربوط به سرور، همین قانون رعایت شود.

---

## نصب و استقرار

- راه‌اندازی روی VPS: `DEPLOY.md` — شامل قانون جداسازی و انتقال داده از هاست اشتراکی. **تنها راه نصب همین است**؛ پشتیبانی از هاست اشتراکی (Apache) برداشته شد.
- گزارش وضعیت سرور پیش از نصب (فقط خواندنی): `deploy/vps-preflight.sh`
- **تشخیص علت کندی (فقط خواندنی): `deploy/perf-report.sh`** — پایین‌تر توضیح داده شده
- اعمال migration ها با ردیابی: `deploy/migrate.sh`
- نصب روی VPS (پیش‌فرض نمایشی؛ با `--apply` اجرا می‌شود): `deploy/vps-setup.sh`
- به‌روزرسانی روی سرور: `./deploy.sh`
- تنظیم pool پی‌اچ‌پی روی نصب موجود: `deploy/tune.sh` (بدون آرگومان نمایشی، با `--apply` اعمال)
- بکاپ روزانه دیتابیس: `deploy/backup.sh --install-cron`
- **پاک‌سازی نشست‌های منقضی: `deploy/session-clean.sh --install-cron`** — پایین‌تر توضیح داده شده
- **یادآوری روزانه‌ی سررسیدها: `deploy/reminders.php --install-cron`** — پایین‌تر توضیح داده شده
- **سنجش و بازیابی بکاپ: `deploy/restore.sh`**
- افزودن استثنای سرویس‌ورکر به nginx روی نصب موجود: `deploy/nginx-sw.sh` (نمایشی؛ با `--apply` اعمال)
- روشن کردن HTTP/2 روی سایت (سرعت روی شبکه‌ی پرتأخیر): `deploy/nginx-perf.sh` (نمایشی؛ با `--apply` اعمال)
- تنظیم ارسال ایمیل: `deploy/mail-setup.sh`
- مدیریت کاربر از خط فرمان: `deploy/user-admin.php` (از جمله `--unlock ali` برای باز کردن قفل ورود)

`config/config.php` و `uploads/` هرگز وارد گیت نمی‌شوند و در استقرار دست‌نخورده می‌مانند.

---

## حساب بانکی و نمای کارت

- `wallets` علاوه بر نام و بانک، `card_number`، `account_number`، `iban` و `bank_code` هم دارد (`migration_wallet_cards`). این‌ها **حساس‌اند**: در فهرست حساب‌ها نمایش داده نمی‌شوند، فقط در نمای کارت باز می‌شوند، و در فایل بکاپ هم می‌آیند.
- **رنگی که کاربر انتخاب می‌کند همیشه برنده است.** کارت با دو رنگ ساخته می‌شود: رنگِ خودِ حساب و نسخه‌ی تیره‌ترش (`shadeColor()`). نسخه‌ی اول رنگ بانکِ انتخاب‌شده را جای رنگ کاربر می‌گذاشت، پس کاربر رنگ عوض می‌کرد و هیچ اتفاقی نمی‌افتاد.
- **نوع حساب:** نقدی / حساب بانکی / سایر. «کارت بانکی» برداشته شد (عملاً همان حساب بانکی بود) ولی حساب‌های قدیمی با این نوع دست‌نخورده‌اند و گزینه‌اش فقط برای همان‌ها در فرم می‌ماند. «سایر» اسم می‌گیرد: `wallets.kind_label` به‌علاوه‌ی فهرست `wallet_kinds` که برای دفعات بعد می‌ماند (`migration_wallet_kinds`).
- `bankPresets()` فهرست بانک‌ها با رنگ هویتی تقریبی است. **لوگو یا تصویر کارت هیچ بانکی در مخزن نیست** — علامت تجاری‌اند؛ کارت با گرادیان و طرح خودمان ساخته می‌شود. انتخاب بانک فقط رنگ پیش‌فرض را می‌گذارد و کاربر می‌تواند عوضش کند.
- اعتبارسنجی: کارت دقیقاً ۱۶ رقم، شبا دقیقاً ۲۴ رقم بعد از `IR`. ورودی با `digitsOnly()` پاک می‌شود و نمایش با `formatCardNumber()` / `formatIban()`.
- ستون‌ها ممکن است هنوز با migration نیامده باشند، پس `walletBalances()` و `api/save_wallet.php` وجودشان را می‌سنجند و بدون آن‌ها هم کار می‌کنند.

## فهرست‌های من

- `references.php` تنها جای افزودن و حذف فهرست‌های کمکی است: بانک‌های من، بانک‌های طرف مقابل، انواع دارایی، انواع حساب، و **دسته‌بندی درآمد و هزینه**. همه از `api/manage_reference.php` می‌گذرند و چیزی که جایی استفاده شده باشد قابل حذف نیست.
- پیش از این هر کدام ته صفحه‌ی مربوط به خودش قایم بود (بانک‌ها ته `cheques.php`، انواع دارایی ته `my-assets.php`) و کاربر پیدایشان نمی‌کرد. آن صفحه‌ها حالا فقط یک لینک به اینجا دارند. **فهرست تازه‌ای که اضافه می‌کنید هم باید همین‌جا بیاید، نه ته صفحه‌ی دیگری.**
- صفحه دکمه‌ی «بازگشت» دارد (`.page-back` + `.js-page-back`): اگر کاربر از داخل اپ آمده `history.back()` می‌زند، وگرنه به خانه می‌رود. صفحه‌ای که فقط از شیت ابزارها باز می‌شود بدون این راهِ بسته شدن نداشت.

## اشخاص (طرف مقابل)

- جدول `people` (نام + سمتِ آزاد) با `migration_people`. مدیریتش در «فهرست‌های من» است، مثل بقیه‌ی فهرست‌های کمکی.
- **ستون `counterparty_name` عمداً دست‌نخورده ماند و همچنان نامِ ذخیره‌شده است.** این جدول فقط فهرستِ کمکیِ پر کردنِ فرم است، نه جایگزینِ آن ستون. سه دلیل: رکوردهای قدیمی بدون کار کردن، دست نخوردنِ همه‌ی صفحه‌های نمایش، و اینکه حذف یک شخص از فهرست تاریخچه را خراب نکند.
- به همین دلیل، برخلاف بانک و نوع دارایی، **«استفاده شده» مانع حذف شخص نیست.**
- وصل شدن با `<datalist>` است نه `<select>`: ورودی آزاد باید کار کند، چون کاربر ممکن است اسمی را یک بار وارد کند و نخواهد به فهرست اضافه‌اش کند.
- `peopleDatalist()` یک بار در `includes/footer.php` رندر می‌شود تا همه‌ی فرم‌های همان صفحه (افزودن و ویرایش) از یک نسخه استفاده کنند. هر ورودی با `list="peopleList"` وصل می‌شود.
- **انتخابگر شخص** با `personPicker()` ساخته می‌شود: یک `<select>` از اشخاص به‌علاوه‌ی «سایر»، و زیرش همان ورودیِ متنیِ همیشگی که تا زدنِ «سایر» پنهان است. **سمت (همکار / مشتری / …) داخل منو نمی‌آید** بلکه به‌صورت یک نشان کنارِ عنوان دیده می‌شود — داخل منو نامِ بلند را می‌برید و فهرست را شلوغ می‌کرد. رنگش از `.asset-tag` می‌آید نه از رنگِ «تعامل»، چون کنارِ عنوانِ آبی شبیه ادامه‌ی خودِ عنوان دیده می‌شد. چیزی که به سرور می‌رود همچنان `counterparty_name` است، پس هیچ اندپوینتی عوض نشد. فرم ویرایش با `syncPersonPicker()` هماهنگ می‌شود: نامِ داخل فهرست انتخاب می‌شود، نامِ بیرون از فهرست «سایر» با ورودیِ پرشده.
- **گردش حساب هر شخص** در `person.php?name=…` است — همه‌ی چک، طلب و بدهی، و معامله‌های او با خالص‌ها. کلیدش *نام* است نه شناسه، پس رکوردهای قدیمی هم دیده می‌شوند.
- `migration_people` ستون `counterparty_name` را به `trades` و `trade_sales` هم اضافه می‌کند. `save_trade` و `sell_trade` وجودش را با `tableHasColumn()` می‌سنجند، پس پیش از اجرای migration هم نمی‌شکنند.

## دسته‌بندی: پیش‌فرضِ برنامه در برابر شخصیِ کاربر

- `categories.user_id` سه‌حالتی است (`migration_user_categories`):
  - `NULL` → دسته‌ی پیش‌فرضِ برنامه. همه می‌بینندش و فقط مدیر از `admin/categories.php` تغییرش می‌دهد.
  - عدد → دسته‌ی شخصیِ همان کاربر. فقط خودش می‌بیند، اضافه می‌کند و حذف می‌کند.
- **`categoryScopeSql()` / `categoryScopeParams()` تنها جایی‌اند که این معنا تعریف می‌شود.** هر کوئری روی دسته‌ها باید از همان‌ها رد شود — وگرنه یک صفحه دسته‌های شخصی را می‌بیند و صفحه‌ی بغلی نه. جاهایی که رد می‌شوند: `cachedCategories()`, `category-report.php`, `budget.php`, `recurring.php`, `api/add_transaction`, `api/update_transaction`, `api/save_budget`, `api/save_recurring`.
- **پنل مدیر فقط پیش‌فرض‌ها را می‌بیند و دست می‌زند** (`$defaultsOnly` در `admin/categories.php`). اگر دسته‌های شخصیِ کاربران هم آنجا فهرست می‌شدند، مدیر می‌توانست ناخواسته دسته‌ی کسی را حذف کند و تراکنش‌های او بی‌دسته می‌ماند.
- کوئریِ حذف در `api/manage_reference.php` شرط `user_id = :u` دارد، پس دسته‌ی پیش‌فرض (که `NULL` است) اصلاً پیدا نمی‌شود — کاربر عادی به هیچ روشی نمی‌تواند پیش‌فرض‌ها را پاک کند. تست جداسازی در `tests/test_user_categories.php` است.
- **این قاعده یک بار واقعاً شکست:** `data.php` (صفحه‌ی ورود اطلاعات از فایل) دسته‌ها را بدون scope می‌خواند. همان فهرست هم برای تطبیق نامِ ستون استفاده می‌شد و هم با `window.IMPORT_CATEGORIES` به مرورگر می‌رفت — یعنی نامِ دسته‌های شخصیِ همه‌ی کاربران در سورس صفحه دیده می‌شد، و یک ردیف وارد شده می‌توانست `category_id` کاربر دیگری را بگیرد. حالا `test_api_contract.php` قاعده ۶ هر `FROM categories` را جدا می‌سنجد تا این دوباره تکرار نشود.
  - نکته درباره‌ی خودِ آن تست: پنجره‌ی بررسی تا **اولین `;`** بریده می‌شود، نه تعداد ثابتی کاراکتر. نسخه‌ی اول ۶۰۰ کاراکتر می‌گرفت و به کوئریِ بعدی سرریز می‌کرد (`FROM wallets WHERE user_id = :u`) و شرطِ کیف پول را به حساب دسته‌بندی می‌گذاشت. کامنت‌ها هم با توکنایزر حذف می‌شوند، وگرنه توضیحی که *بالای* یک کوئریِ ناایمن نوشته شده تست را سبز می‌کرد. و شرط فقط در `WHERE`/`AND` شمرده می‌شود — `ORDER BY (user_id IS NULL)` صافی نیست.
- **شش دسته‌ی هزینه‌ی جاافتاده با `migration_more_categories` اضافه شدند:**
  خودرو، بیمه، دیون مذهبی، مالیات و عوارض، کارمزد و سود بانکی، جریمه و
  دیرکرد. «خودرو» عمداً از «حمل‌ونقل» جداست (آن یکی تاکسی و مترو است) و
  «جریمه و دیرکرد» از «قسط و وام» (قسط برنامه‌ریزی‌شده است، جریمه نه — و
  دیدنِ جداشان همان چیزی است که رفتار را عوض می‌کند).
  - **⛔ «سرمایه‌گذاری» عمداً نیامد** با اینکه در فهرستِ اپ‌های مشابه زیرِ
    هزینه‌هاست. خریدِ طلا یا سهام هزینه نیست؛ اگر هزینه حسابش کنیم گزارشِ
    هزینه دروغ می‌گوید و «پول قابل خرج» بی‌دلیل کم می‌شود. جای درستش از
    قبل هست: دارایی، معاملات، و خالص دارایی.
  - **⛔ نشانه‌ی `more_categories_seeded` در `app_settings` یعنی این فایل
    دیگر کاری نمی‌کند.** فقط برای ایدمپوتنت بودن نیست (شرطِ `LEFT JOIN`
    خودش آن را می‌دهد) بلکه برای این است که اگر کاربر یکی از این دسته‌ها
    را **عمداً حذف کند**، اجرای بعدی زنده‌اش نکند. بدونِ نشانه، هر deploy
    کارِ کاربر را پس می‌زد.
  - **⛔ جدولِ موقتش با collation صریح ساخته می‌شود.** `JOIN` روی `name`
    بین دو ستون است و هر دو «قابلیتِ تبدیلِ ۲» دارند، پس هیچ‌کدام به
    دیگری تبدیل نمی‌شود — همان چیزی که `FIND_IN_SET(name, @var)` را روی
    سرور کشت. اینجا رشته‌ی ثابتی نیست که خودش تبدیل شود.
  - آیکونِ `shield` برای «بیمه» به `categoryIconMap()` اضافه شد. قرض
    گرفتنِ `health` وسوسه‌کننده بود ولی بیمه‌ی ثالث ربطی به سلامت ندارد و
    دو دسته با یک آیکون در فهرست از هم تشخیص داده نمی‌شوند. **این را
    `test_migration_chain` گرفت**، نه بازبینیِ چشمی: قاعده‌اش این است که
    فقط یک دسته‌ی پیش‌فرض حق دارد آیکونِ `default` داشته باشد.
- «سود معاملات» و «زیان معاملات» عمداً پیش‌فرض (`user_id IS NULL`) ساخته می‌شوند؛ اگر شخصی می‌شدند هر کاربر یک نسخه‌ی تکراری می‌ساخت.
- **راه ورود به گزارش دسته‌بندی از خودِ داشبورد است** (`.breakdown-cta` در `dashboard.php`). پیش از این `category-report.php` فقط از شیت «بیشتر» پیدا می‌شد، در حالی که سؤالِ بعدیِ کاربر بلافاصله بعد از دیدن جمع هزینه‌ی ماه همان «این پول کجا رفت؟» است. دو دکمه‌ی جدا برای هزینه و درآمد، چون دو گزارش جدا هستند؛ هر کدام جمع همان جهت را نشان می‌دهد و با `?type=…&preset=this_month` مستقیم روی همان زبانه باز می‌شود. رنگ دکمه‌ها از `--out`/`--in` می‌آید تا با رنگی که کاربر داخل خودِ گزارش می‌بیند یکی باشد.

## ورود: سدِ حدس رمز

- `LoginThrottle` (`includes/login_throttle.php`) هر تلاش ناموفق را در جدول `login_attempts` ثبت می‌کند (`migration_login_throttle`). پیش از این، صفحه‌ی ورود هیچ سدی نداشت و می‌شد بی‌نهایت رمز امتحان کرد — در حالی که *بازیابی* رمز از قبل محدودیت داشت. یعنی در پشتی محکم بود و در جلویی باز.
- **دو کلید جدا شمرده می‌شوند و هر دو لازم‌اند:** نام کاربری (۵ در ۱۵ دقیقه) و IP (۲۰ در ۱۵ دقیقه). فقط IP کافی نیست چون مهاجم از چند آدرس می‌آید؛ فقط نام کاربری هم کافی نیست چون آن‌وقت می‌شود با تلاش عمدی، حسابِ *یک نفر دیگر* را قفل کرد.
- سقفِ IP عمداً بالاتر است چون پشت NAT (یک خانه، یک دفتر، اینترنت موبایل) چند نفر واقعی ممکن است یک IP داشته باشند.
- **سنجش پیش از `password_verify` است**، نه بعدش: آن تابع عمداً کند است و نباید به مهاجم هدیه شود.
- پنجره کشویی است، نه قفلِ ساعت‌دار — تلاش‌های قدیمی که از پنجره بیرون بروند خودبه‌خود آزاد می‌کنند، پس هیچ کار زمان‌بندی‌شده‌ای لازم نیست. `prune()` هم بعد از هر ورود موفق جدول را کوچک نگه می‌دارد.
- **ورود موفق فقط سابقه‌ی همان نام کاربری را پاک می‌کند، نه سابقه‌ی IP را.** وگرنه مهاجمی که یک حساب معتبر دارد می‌توانست بین هر ۲۰ حدس یک بار وارد شود و شمارنده را صفر کند.
- پیام خطای تلاش ناموفق دست‌نخورده مانده تا وجود حساب لو نرود؛ پیام قفل هم چون روی نامِ ناموجود هم می‌آید، چیزی لو نمی‌دهد.
- اگر migration اجرا نشده باشد، `available()` نادرست برمی‌گردد و ورود دقیقاً مثل قبل کار می‌کند — سد بی‌سروصدا خاموش می‌ماند، نه اینکه صفحه بخوابد.
- **در پشتی:** `php deploy/user-admin.php --unlock ali` (یا `--unlock --all` که شمارنده‌ی IP را هم پاک می‌کند). سد بین کاربر واقعی و مهاجم فرق نمی‌گذارد، پس خودتان هم می‌توانید قفل شوید.
- تست در `tests/test_login_throttle.php` است و مهم‌ترین بررسی‌اش این است که **در حالت قفل، رمزِ درست هم رد شود** — وگرنه سد بی‌فایده است.

## نشست، مهلت، و «۳۰ روز به خاطر بسپار»

- **`logout()` فقط برای خروجِ خواسته‌ی کاربر است** و عمداً اعتماد این دستگاه را هم باطل می‌کند. انقضای مهلت از `expireSession()` رد می‌شود که نشست را خالی می‌کند ولی به `trusted_devices` دست نمی‌زند.
- **این یک بار واقعاً شکسته بود و بی‌سروصدا:** `isLoggedIn()` هنگام انقضای مهلت `logout()` را صدا می‌زد، پس ردیف `trusted_devices` پاک می‌شد و `loginFromTrustedDevice()` بلافاصله بعدش چیزی برای کار کردن نداشت. یعنی همان مهلت یک‌ساعته‌ای که قرار بود «۳۰ روز به خاطر بسپار» از رویش پل بزند، خودِ آن را نابود می‌کرد — کاربر با وجود روشن بودنِ گزینه هر ساعت دوباره رمز می‌خواست، و هیچ خطایی هم دیده نمی‌شد.
- `expireSession()` نشست را `destroy` نمی‌کند بلکه خالی می‌کند و شناسه‌اش را عوض می‌کند، چون `loginFromTrustedDevice()` بلافاصله بعدش می‌خواهد در همان نشست بنویسد.
- تست در `tests/test_session_trust.php` است و هر سه حالت را با هم می‌سنجد — چون درست کردنِ یکی به قیمت دیگری آسان است: انقضا با دستگاه مورد اعتماد (باید بماند)، خروجِ کاربر (باید اعتماد باطل شود)، و انقضا بدون دستگاه مورد اعتماد (باید بیرون بیندازد).

### مهلت «ماندن در حساب» — یک تنظیم، و کشویی

- **`users.session_minutes` تنها مرجع است** (`migration_session_window`). ۰ یعنی
  «بدون مهلت» و **پیش‌فرضِ همه همین است**. ستون قدیمی `session_hours` عمداً حذف
  نشد (بکاپ‌ها و کد قدیمی) ولی هیچ‌جا خوانده نمی‌شود.
- فهرست مقدارهای مجاز فقط در `Auth::SESSION_WINDOWS` است — از ۱ دقیقه تا یک ماه،
  به‌علاوه‌ی «بدون مهلت». پروفایل و `api/update_session_pref` هر دو از همان
  می‌خوانند؛ **فهرست دوم نسازید**، وگرنه گزینه‌ای که کاربر می‌بیند هنگام ذخیره
  بی‌سروصدا به پیش‌فرض برمی‌گردد.
- همین یک عدد **هم** مهلت بی‌فعالیتیِ نشست را تعیین می‌کند **هم** عمر کوکی
  دستگاه مورد اعتماد. پیش از این دو چیز جدا بودند (یک ساعت در برابر ۳۰ روزِ ثابت)
  و کاربر نمی‌فهمید کدام حاکم است.
- **⛔ کشویی بودن یعنی هر سه تکه با هم جلو بروند** (`slideTrustedDevice()`):
  `expires_at` در دیتابیس، `expires` روی کوکی، و `last_used_at`. اگر یکی جا
  بماند، خرابی **بی‌صداست**: نسخه‌ی قبلی فقط سومی را می‌نوشت، پس مهلت ثابت بود و
  کاربری که هر روز اپ را باز می‌کرد هم سرِ سررسیدِ اول رمز می‌خواست.
- نشستِ زنده از `loginFromTrustedDevice()` رد نمی‌شود، پس `isLoggedIn()` هم
  کوکی را تمدید می‌کند — با سقفِ **روزی یک بار** (`trust_slid_at` در نشست)، وگرنه
  هر بارگذاری صفحه یک `UPDATE` می‌زد.
- عوض کردن مهلت در پروفایل فوراً کوکیِ همان دستگاه را هم هم‌تراز می‌کند
  (`refreshTrustForCurrentDevice()`). بدون آن، کاربر «یک ماه» را انتخاب می‌کرد و
  تا ورود بعدی هیچ اثری نمی‌دید — یعنی تنظیمی که کار نمی‌کند.
- «بدون مهلت» کوکی ده‌ساله می‌گیرد (`FOREVER_SECONDS`). اگر PHP فایل نشست را جمع
  کند، همان درخواستِ بعدی از کوکی دوباره وارد می‌شود و کاربر چیزی نمی‌بیند.
- **`logout()` فقط دستگاهِ خودش را باطل می‌کند، نه همه را.** تست این را جدا
  می‌سنجد؛ اگر روزی همه را پاک کند، کاربری که روی گوشی خروج می‌زند لپ‌تاپش را هم
  بیرون انداخته.
- تست در `tests/test_session_trust.php` است (۲۳ بررسی) و با آزمون جهش سنجیده
  شده: برگشت به مهلت ثابت، بی‌اثر کردن «بدون مهلت»، و باطل کردن همه‌ی دستگاه‌ها
  در خروج — هر سه شکست می‌دهند.

### `session.gc_maxlifetime`

- کاربر در پروفایل مهلتش را انتخاب می‌کند (`users.session_minutes`؛ بالاتر توضیح داده شد)، ولی پیش‌فرض `session.gc_maxlifetime` در PHP ۲۴ دقیقه است. `Auth::initSession()` آن را پیش از `session_start()` روی بزرگ‌ترین گزینه‌ی **محدود** (یک ماه) می‌گذارد. گزینه‌ی «بدون مهلت» بلندتر است ولی این عدد را بالا نمی‌برد: جمع شدن فایل نشست آنجا بی‌خطر است، چون همان درخواست بعدی از کوکی دستگاه دوباره وارد می‌شود.
- **⚠ و دقیقاً به همین دلیل، پوشه‌ی نشست خودش پاک نمی‌شود.** سه چیز دست به دست
  هم می‌دهند: `session.save_path` داخل خودِ پروژه است پس کرونِ `sessionclean`
  دبیان سراغش نمی‌آید؛ روی اوبونتو `gc_probability = 0` است پس خودِ PHP هم
  جمعشان نمی‌کند؛ و این خط مهلت را یک ماه می‌کند. با یک کاربر نامرئی است، با
  صدها کاربر ده‌ها هزار فایل در یک پوشه جمع می‌شود و آن‌وقت باز کردنِ خودِ پوشه
  کند می‌شود یا اینود تمام می‌شود. `deploy/session-clean.sh` همین را می‌برد:
  بدون آرگومان فقط گزارش می‌دهد، با `--apply` پاک می‌کند، و با `--install-cron`
  شبانه زمان‌بندی می‌شود.
  - **⚠ بیشترِ آن فایل‌ها اصلاً کاربر ندارند.** `login.php` هم `initSession()`
    می‌زند (برای توکن CSRF)، پس هر ربات و هر بازدیدِ گذری یک فایل جا می‌گذارد.
    روی سرور واقعی این عدد با سه کاربر به **۴۳۱۰ فایل** رسیده بود.
  - به همین دلیل **دو مهلتِ جدا** هست: نشستِ کاربرِ واردشده ۳۵ روز (بیشتر از
    بلندترین مهلتِ محدودِ اپ، وگرنه کاربرِ فعال بیرون می‌افتاد)، و نشستِ
    بی‌کاربر فقط ۲ روز. نسخه‌ی اول همه را ۳۵ روز نگه می‌داشت — آن استدلال برای
    فایلی که کاربری ندارد بی‌معنا بود و یعنی انبار کردنِ آشغال به مدت پنج هفته.
  - تشخیص با وجودِ رشته‌ی `user_id|i:` در فایل است، یعنی به قالبِ سریال‌سازیِ
    پیش‌فرضِ PHP بند است. **اگر آن قالب عوض شود، هیچ نشستی «کاربردار» دیده
    نمی‌شود و اسکریپت همه را بعد از دو روز پاک می‌کند** — یعنی بیرون انداختنِ
    همه‌ی کاربران، بی‌هیچ خطایی. محافظش این است: اگر پوشه پر باشد ولی در هیچ
    فایلی نشانه پیدا نشود، دست نگه می‌دارد و توضیح می‌دهد.
  - محافظ دوم روی مسیر است: اگر `SESSION_DIR` به `var/sessions` ختم نشود امتناع
    می‌کند. بدون آن یک متغیر محیطیِ اشتباه کافی بود تا جای دیگری پاک شود.
- **روی اوبونتو این تنظیم عملاً بی‌اثر است و مشکلی هم نیست:** آنجا `session.gc_probability = 0` است (PHP خودش جمع نمی‌کند) و `sessionclean` دبیان فقط `save_path`ِ داخل `php.ini` را پاک می‌کند، نه مسیر اختصاصیِ این pool را. پس این خط برای نصب روی هاست اشتراکی است که GC خودِ PHP فعال است.
- روی هیچ سرویس دیگری اثر ندارد: pool این اپ `session.save_path` خودش را دارد (`hesab.conf`) و `ini_set` فقط همان درخواست را عوض می‌کند، نه `php.ini` سراسری.
- **عارضه‌ی جدی‌ترش توکن CSRF بود.** توکن داخل همان فایل نشست است، پس با جمع شدن نشست از بین می‌رفت و فرمی که روی گوشی ساعت‌ها باز مانده بود، با submit شدن به `Csrf::verifyOrFail` می‌خورد — که آن موقع فقط `die()` با یک رشته‌ی خام می‌زد: صفحه‌ی کاملاً سفید، بدون قالب و بدون هیچ دکمه‌ای. بن‌بست کامل.
  - `verifyOrFail` حالا صفحه‌ی «نشست شما منقضی شد» را رندر می‌کند: قالب کامل، حالت شب، و دو راه خروج (خانه/ورود و «بازگشت»). مثل `offline.html` عمداً بدون وابستگی به `style.css` است.
  - **`login.php` عمداً `verifyOrFail` صدا نمی‌زند** بلکه `Csrf::validate()` را خودش می‌سنجد و در صورت کهنه بودن، فرم را با توکن تازه و پیام «نشست شما منقضی شده بود» دوباره نشان می‌دهد. صفحه‌ی ورود بیشتر از هر صفحه‌ی دیگری باز می‌ماند و بن‌بست شدنش دقیقاً بدترین حالت است. امنیت کم نمی‌شود: توکن نامعتبر همچنان وارد نمی‌کند.
  - `test_api_contract.php` قاعده ۷ هر سه را می‌سنجد.
  - **`isJsonRequest()` باید `X-Requested-With` را هم بپذیرد.** همه‌ی `fetch` های `app.js` این سرآیند را می‌فرستند ولی `Accept: application/json` نمی‌فرستند؛ بدون آن شرط، ردِ CSRF به یک فراخوان AJAX صفحه‌ی HTML می‌داد، `res.json()` می‌شکست و کاربر فقط «خطا در ارتباط با سرور» می‌دید.

## ورودِ اطلاعات از فایل: پیلود مرورگر داده است، نه دستور

- مرحله‌ی ثبت در `data.php` روی JSON ای کار می‌کند که خودِ مرورگر ساخته. یک بار `category_id` و `wallet_id` را همان‌طور که آمده بودند می‌نوشت — یعنی هر کاربری می‌توانست پیلود را دست‌کاری کند و تراکنشش را به دسته یا حسابِ **کاربر دیگری** بچسباند. با curl آزموده و تأیید شد.
- حالا هر دو در برابر مالکیتِ خودِ کاربر سنجیده می‌شوند: دسته باید از `categoryScopeSql()` رد شود *و* جهتش (درآمد/هزینه) بخواند، وگرنه `NULL` می‌شود؛ حساب باید مالِ خودش باشد وگرنه حساب پیش‌فرض می‌گیرد (همان قاعده‌ی `resolveWalletId()`، تا پول در هیچ‌کجا گم نشود).
- تست رگرسیونش در `tests/test_api_auth.php` است و **قربانی را خودش می‌سازد**؛ تکیه بر داده‌ی موجود یعنی روی دیتابیس خالی بی‌سروصدا رد می‌شود و چیزی را نگه نمی‌دارد.

## `tests/test_dead_code.php` — چیزی که `php -l` نمی‌بیند

- **متغیرِ استفاده‌شده ولی هرگز مقدارنگرفته.** همان باگی که دو بار زد (`categoryScopeParams($userId)` در `add_transaction` و `update_transaction`). با توکنایزر کار می‌کند و چهار حالت را جدا می‌شناسد تا هشدار الکی ندهد: تخصیصِ تجزیه‌ای (`[$a,$b] = …`)، ویژگیِ ایستا (`Mailer::$lastError`)، `&$ref` (که توکنش در PHP 8.1 عوض شده)، و پارامترهای خروجیِ ارجاعی (`preg_match(…, $m)`).
- **نکته‌ی کلیدی:** نسخه‌ی اول هر `(` را «پارامتر تابع» فرض می‌کرد و محافظه‌کارانه می‌پذیرفت — یعنی دقیقاً همان باگی که تست برایش نوشته شده بود را نمی‌گرفت. `isParamList()` حالا *تعریف* را از *فراخوانی* جدا می‌کند. با آزمون جهش سنجیده شده.
- **تجزیه‌ای در `foreach` هم تخصیص است:** `foreach ($rows as [$a, $b])`. پیش‌پویشِ تجزیه‌ای فقط دنبالِ `=` بعد از براکت بود، ولی اینجا `)` می‌آید — پس `$b` «بدون مقدار» دیده می‌شد. **هشدارِ الکی از نبودِ تست بدتر است**، چون آدم را وادار می‌کند کدِ بدتر بنویسد تا تست سبز شود (اینجا وسوسه این بود که `foreach` را به `$f[0]`/`$f[1]` بشکنیم). حالا اگر توکنِ *قبلِ* براکت `as` باشد هم تخصیص شمرده می‌شود. با دو جهش سنجیده شده: متغیرِ تعریف‌نشده‌ی معمولی، و متغیرِ تعریف‌نشده داخلِ همان حلقه‌ی تجزیه‌ای — هر دو گرفته می‌شوند.
- کد مرده (تابع یا اندپوینتِ بی‌فراخوان) فقط **گزارش** می‌شود، نه شکست.

## اندپوینت‌ها: چرا تستِ «بدون ورود» کافی نیست

- `tests/test_api_auth.php` سه لایه دارد و هر سه لازم‌اند:
  ۱. بدون ورود، هیچ اندپوینتی داده ندهد.
  ۲. **با ورود**، هیچ اندپوینتی ۵۰۰ ندهد.
  ۳. **مسیر واقعیِ پول** — ثبت و ویرایش یک تراکنشِ دسته‌دار با پیلود کامل.
- لایه‌ی ۳ از یک باگ واقعی درآمد: `api/add_transaction.php` و `api/update_transaction.php` تابع `categoryScopeParams($userId)` را صدا می‌زدند در حالی که `$userId` در آن دو فایل هرگز تعریف نشده بود. نتیجه، خطای کشنده‌ی PHP و پاسخ ۵۰۰ با بدنه‌ی خالی بود؛ در مرورگر فقط «خطا در ارتباط با سرور» دیده می‌شد. **ثبت و ویرایش تراکنشِ دسته‌دار — یعنی هسته‌ی اپ — کاملاً از کار افتاده بود.**
- `php -l` متغیر تعریف‌نشده را نمی‌گیرد و تست قرارداد فقط شکل کد را می‌بیند.
- **لایه‌ی ۲ هم به‌تنهایی کافی نبود:** خطِ باگ‌دار داخل `if ($categoryIdValue !== null)` بود، پس با ورودیِ خالی اصلاً اجرا نمی‌شد و تست سبز می‌ماند. هر اندپوینتی که شاخه‌ی شرطی دارد، دست‌کم یک بار باید با **پیلود کاملِ واقعی** صدا زده شود.

## نصب روی گوشی (PWA)

- `assets/manifest.php` عمداً PHP است نه JSON ایستا، چون `start_url` و `scope` باید مسیر مطلق درست را بدانند. آیکون ۵۱۲ یک بار هم با `purpose: maskable` می‌آید تا اندروید داخل کادر سفید نگذاردش.
- **`sw.js` هیچ HTML ای کش نمی‌کند و این قابل مذاکره نیست.** همه‌ی صفحه‌ها عمداً `no-store` می‌گیرند (پایین‌تر در «سرعت» توضیح داده شده)؛ اگر سرویس‌ورکر HTML را کش کند همان باگِ سافاری برمی‌گردد — این بار بدتر، چون کشِ سرویس‌ورکر با تازه‌سازی صفحه هم پاک نمی‌شود. `test_api_contract.php` این را می‌سنجد (قاعده ۶).
- درخواست‌های ناوبری شاخه‌ی جدا دارند: همیشه از شبکه، و اگر شبکه نبود `offline.html`. کش فقط برای دارایی‌های ثابتِ داخل `assets/` است، آن هم به شکل «اول کش، بعد تازه‌سازی در پس‌زمینه».
- `offline.html` عمداً HTML ایستا با استایل درون‌خطی است — وقتی نشان داده می‌شود که هیچ چیزی از سرور نمی‌آید، پس نباید به `style.css` وابسته باشد.
- `sw.js` در ریشه‌ی اپ است چون دامنه‌ی کنترل یک سرویس‌ورکر نمی‌تواند بالاتر از پوشه‌ی خودش برود. مسیرهای داخلش نسبی حل می‌شوند، پس نصب در زیرپوشه هم کار می‌کند.
- **دام کش:** سایتِ nginx به همه‌ی `.js` کشِ «یک سال، immutable» می‌دهد. برای فایل‌های نسخه‌دار درست است ولی `sw.js` نسخه ندارد — با آن هدر، نسخه‌ی تازه ممکن است هرگز نرسد. `vps-setup.sh` استثنای `location = /sw.js` را دارد؛ روی نصب موجود با `sudo bash deploy/nginx-sw.sh --apply` اضافه می‌شود.
- **دارایی کش‌شده در پس‌زمینه دوباره گرفته نمی‌شود، و این عمدی است.** نسخه‌ی اول «stale-while-revalidate» بود و نتیجه‌اش **بدتر از نداشتنِ سرویس‌ورکر** شد: در هر ناوبری ۶ فایل (فونت، `style.css`، `app.js`، `chart.js`، آیکون — حدود ۲۲۰ کیلوبایت) دوباره از شبکه گرفته می‌شد با اینکه همه کش بودند، و روی دیتای موبایل با خودِ صفحه سرِ پهنای باند دعوا می‌کرد. اندازه‌گیری شد: **۶ درخواست در هر جابه‌جایی → ۰**. تازه‌سازی لازم نیست چون آدرس این فایل‌ها نسخه دارد (`?v=…`). قاعده ۶ در `test_api_contract.php` این را می‌سنجد.
- **کشِ دقیق خالی بودن نباید صفحه را بی‌جان کند.** بعد از هر deploy آدرس `app.js` عوض می‌شود (`?v=`)، پس کش خطا می‌خورد و فایل باید از شبکه بیاید. اگر همان یک درخواست روی اینترنت موبایل گیر می‌کرد، صفحه **کامل و خوش‌ظاهر** بالا می‌آمد — HTML و CSS رسیده بودند — ولی `app.js` اجرا نشده بود: صفحه اسکرول می‌شد و هیچ دکمه‌ای کار نمی‌کرد، بی‌هیچ نشانه‌ای از خرابی. بستن و باز کردن اپ درستش می‌کرد، چون بار دوم فایل می‌رسید. حالا سه لایه هست: کشِ دقیق → شبکه با مهلت `NET_TIMEOUT` → **نسخه‌ی قبلیِ همان فایل** (`cache.match(req, { ignoreSearch: true })`). کدِ یک نسخه عقب‌تر بی‌نهایت بهتر از صفحه‌ی بی‌جان است و دفعه‌ی بعد خودبه‌خود به‌روز می‌شود. با A/B روی سرورِ خوابیده سنجیده شد: پیش از اصلاح `504` با بدنه‌ی خالی، پس از آن `200` با ۱۵۵ کیلوبایتِ واقعی.
- **نسخه‌های قدیمی هر فایل پاک می‌شوند** (`dropOtherVersions`). بدون آن، هر deploy یک `?v=` تازه به کش اضافه می‌کرد و قبلی برای همیشه می‌ماند — کش روی گوشی کاربر بی‌پایان بزرگ می‌شد. در آزمون، بعد از دو deploy سه نسخه‌ی `app.js` کنار هم بودند؛ حالا همیشه یکی می‌ماند.
- **`<html class="js-loading">`** در `header.php` گذاشته می‌شود و اولین کارِ `app.js` برداشتنِ آن است. تا وقتی هست یعنی اسکریپت نرسیده؛ استایلش با تأخیر ۹۰۰ میلی‌ثانیه دکمه‌ها را کم‌رنگ می‌کند. بارگذاری عادی چند ده میلی‌ثانیه است و هرگز این حالت را نمی‌بیند. دلیلش همان باگ بالاست: سکوتِ کامل بدترین حالت بود — کاربر نمی‌فهمید صفحه هنوز آماده نیست یا خراب است.
- **`viewport-fit=cover` بدون آن، همه‌ی حاشیه‌های امن صفر می‌شوند.** `style.css` در پنج جا از `env(safe-area-inset-*)` استفاده می‌کند، ولی iOS این مقادیر را **فقط** وقتی می‌دهد که متای viewport شامل `viewport-fit=cover` باشد. نبودش هیچ خطایی نمی‌دهد — فقط نوار پایین زیر خطِ خانه می‌رود و عنوان صفحه زیر ناچ/Dynamic Island. مدت‌ها همین‌طور بود و در اسکرین‌شات کاربر دیده می‌شد. قاعده ۱۱ در `test_api_contract.php` این را می‌سنجد، و چون هر صفحه‌ای که سرآیند خودش را دارد (`login`, `setup`, `forgot-password`, `reset-password`) هم باید داشته باشد، همه‌شان سنجیده می‌شوند.
- **نوار بالا `env(safe-area-inset-top)` را در `padding` حساب می‌کند.** `apple-mobile-web-app-status-bar-style=black-translucent` عمدی است (نوار وضعیت هم‌رنگ اپ)، ولی همان باعث می‌شود وب‌ویو تا بالای صفحه کشیده شود؛ بدون این padding، عنوان زیر ناچ می‌رود.
- **⛔ داشتنِ `env(...)` در CSS کافی نیست — قاعده‌ای که *برنده می‌شود* باید داشته باشدش.** قاعده ۱۱ فقط وجودِ `viewport-fit=cover` را می‌سنجد، یعنی اینکه iOS اصلاً این مقادیر را بدهد؛ دو خرابیِ واقعی از زیرش رد شدند چون مقدارها داده می‌شدند و `env(...)` هم در فایل بود، ولی یک قاعده‌ی **بعدی** رویش می‌نوشت:
  - `.topbar` قاعده‌ی پایه `calc(13px + env(safe-area-inset-top))` داشت، ولی `@media (max-width:900px)` شورتهندِ `padding: 12px 14px` را بعدتر می‌نوشت و حاشیه‌ی بالا را کاملاً پاک می‌کرد.
  - `.page-content` فاصله‌ی کف ۹۶px ثابت داشت، در حالی که نوارِ شناور از کفِ پنجره `14 + env(bottom) + 62` بالا می‌ایستد — در مرورگر ۷۶px (کافی) ولی روی آیفون ۱۱۰px.
  - **هر دو در مرورگر بی‌عیب بودند** (چون `env(...)` آنجا صفر است) و فقط روی گوشیِ نصب‌شده دیده می‌شدند. قاعده ۱۳ در `test_api_contract.php` همین را می‌سنجد: آخرین قاعده‌ای که این فاصله را تعیین می‌کند باید `env(...)` داشته باشد.
- **مودال‌ها هم حاشیه‌ی امن می‌خواهند، نه فقط نوارها.** شیت‌ها از اول
  `env(safe-area-inset-bottom)` داشتند ولی `.modal-overlay` فقط `padding: 16px`
  بود. با `max-height: 88vh` و `align-items: center`، مودالِ بلند (مثل ویرایش چک
  بعد از اضافه شدنِ انتخابگر شخص) لبه‌ی بالایش روی **۵۱px** می‌نشست در حالی که
  ناچ تا **۵۹px** می‌آید — یعنی سرآیند و دکمه‌ی بستن زیرِ ناچ. اندازه‌گیری شد،
  حدس زده نشد.
  - `max-height` حالا `100%` است نه `88vh`: درصد روی جعبه‌ی محتوای همان لایه حل
    می‌شود که حاشیه‌ی امن و padding از آن کم شده، پس هیچ عددِ جادویی لازم نیست.
    `vh` روی iOS کلِ صفحه را می‌شمارد و نوار مرورگر را هم نادیده می‌گیرد.
  - `.modal-overlay` حالا `overflow-y: auto` هم دارد: با `align-items: center`
    تنها، بالای یک مودالِ بلندتر از لایه بیرون می‌افتد و **قابل اسکرول هم نیست**.
  - قاعده ۱۳ در `test_api_contract.php` حالا `.modal-overlay` را هم می‌سنجد.
- **نوار بالا روی موبایل هم چسبنده است، نه فقط دسکتاپ.** در مرورگر نوارِ خودِ سافاری بالای صفحه است و محتوا هرگز زیر ساعت نمی‌رود؛ در حالت نصب‌شده هیچ نواری نیست، پس با اولین اسکرول کارت‌ها می‌رفتند زیر ساعت و باتری و اپ اصلاً سرآیند نداشت.
- **آزمودنش هم واقعی است، نه حدسی:** کروم با `Emulation.setSafeAreaInsetsOverride` (از راه CDP) حاشیه‌ی امن را **واقعاً** تنظیم می‌کند، پس همان `calc()`ی که در `style.css` نوشته شده اجرا می‌شود. بازنویسیِ دستیِ قاعده‌ها با عددِ ثابت این را نمی‌سنجد — فقط طرحِ خودتان را دوباره می‌خواند. مقدارهای iPhone 15 Pro: بالا ۵۹px، پایین ۳۴px.
- با هر تغییرِ فهرست پیش‌کش، `VERSION` در `sw.js` بالا برود؛ نسخه‌های قدیمی هنگام `activate` پاک می‌شوند. (برای تغییرِ *منطق* بالا بردنش لازم نیست و حتی بد است: پاک شدنِ کش یعنی نسخه‌ی قبلیِ فایل‌ها — همان تورِ نجات — از دست می‌رود.)

## وام و قسط — برنامه‌ی مجازی، نه ردیفِ ذخیره‌شده

- رایج‌ترین بدهیِ خانوارِ ایرانی وامِ بانکی یا قرضِ صندوق با اقساطِ ماهانه است، ولی مدل فقط یک مبلغ و **یک** سررسید داشت. کاربر یا ۳۶ بدهیِ جدا می‌ساخت (دفترش غیرقابل خواندن می‌شد) یا کلِ وام را با سررسیدِ آخر ثبت می‌کرد — و آن‌وقت «آینده مالی» و «پول قابل خرج» قسطِ ماهِ بعد را **اصلاً نمی‌دیدند** و عددِ قابل خرج تا سه سال خوش‌بین می‌ماند.
- سه ستون روی `debts` (`migration_installments`): `installment_count`, `installment_every`, `first_installment_date`.
- **⛔ هیچ ردیفِ قسطی ذخیره نمی‌شود.** برنامه مثل تراکنش‌های دوره‌ای *مجازی* است و در `debtInstallments()` ساخته می‌شود. سه دلیل: ۳۶ ردیف به‌ازای هر وام جدول را سریع بزرگ می‌کند؛ پرداختِ واقعی از قبل در `debt_payments` هست و ردیفِ قسط نسخه‌ی دومی از همان حقیقت می‌شد؛ و عوض کردنِ تعداد اقساط یعنی بازسازیِ همه‌ی ردیف‌های پرداخت‌نشده.
- **باقیمانده‌ی تقسیم به قسطِ آخر می‌رود، نه پخش‌شده.** ۱۰ میلیون در ۳ قسط می‌شود ۳٬۳۳۳٬۳۳۳ + ۳٬۳۳۳٬۳۳۳ + ۳٬۳۳۳٬۳۳۴. وگرنه جمعِ اقساط با مبلغِ وام یکی نمی‌شد و کاربر یک بدهیِ چندریالیِ **ابدی** پیدا می‌کرد که هرگز تسویه نمی‌شود.
- **«کدام قسط پرداخت شده» از جمعِ پرداختی می‌آید، نه از تاریخ.** کاربر ممکن است دو قسط را یک‌جا بدهد یا زودتر بپردازد؛ پول پول است و ترتیبش اهمیتی ندارد.
- **⛔ بدهیِ قسطی نباید هم به‌صورت اقساط بیاید هم به‌صورت یک مبلغِ درشت.** `financialEvents()` شناسه‌های قسطی را از کوئریِ ساده کنار می‌گذارد؛ بدون آن، وامی که سررسیدِ آخرش داخلِ بازه است **دو بار** شمرده می‌شد. تست دقیقاً همان حالت را می‌سازد.
- ⚠ مبلغِ کمتر از تعدادِ اقساط، قسط‌های **صفر** می‌ساخت (۱ تومان در ۲ قسط) — هم در کارت «۰ تومان» نشان می‌داد هم تعهدِ صفر وارد آینده‌ی مالی می‌کرد. حالا در آن حالت بدهی مثل بدهیِ ساده رفتار می‌کند. **این را خودِ تست پیدا کرد، نه بازبینیِ چشمی.**
- سقفِ ۴۸۰ قسط (۴۰ سال ماهانه) در `api/add_debt.php` جلوی ساختنِ ده‌هزار رویداد را می‌گیرد.
- روی صفحه: یک **کلید** «قسطی است» که سه فیلد را باز می‌کند (اکثرِ ثبت‌ها ساده‌اند و نباید سه فیلدِ اضافه ببینند)، یک **خط** روی کارت («قسط ۳ از ۱۲ — بعدی ۱٬۰۰۰٬۰۰۰ در …»)، و مبلغِ قسط بعدی به‌عنوان پیش‌فرضِ فرمِ پرداخت.
- **تاریخچه‌ی پرداخت از قبل در `debt_payments` بود ولی هیچ‌جا رندر نمی‌شد**: کاربر می‌دید «۳ میلیون پرداخت شده» ولی نمی‌توانست بفهمد کِی و از کدام حساب. حالا در یک `<details>` جمع‌شونده زیر نوار پیشرفت است. ⚠ ستون `payment_date` است نه `paid_at`.
- تست در `tests/test_installments.php` است (۲۶ بررسی) و با سه جهش سنجیده شده: گم شدنِ باقیمانده، دوبار شمردنِ بدهیِ قسطی، و حساب کردنِ «پرداخت‌شده» از تاریخ به‌جای جمعِ پرداختی.

## ارزشِ روز و روندِ خالص دارایی

- **`assets.unit_price` بهای واحد هنگام ثبت است، نه ارزش.** صفحه‌ی دارایی همان را جمع می‌زد و «ارزش کل دارایی‌ها» می‌نامیدش. در اقتصادِ تورمی این حرفِ بی‌معنایی است: ارزشِ سکه‌ای که پارسال خریده شده هیچ ربطی به قیمتِ پارسال ندارد، و طلا و دلار ابزارِ اصلیِ پس‌اندازِ کاربر ایرانی‌اند.
- `asset_types.current_price` + `price_updated_at` (`migration_asset_prices`). ارزش با `COALESCE(at.current_price, a.unit_price)` حساب می‌شود — **اگر نرخ وارد نشده باشد همان بهای خرید می‌ماند، نه صفر** (رفتارِ قبلی حفظ می‌شود).
- **`assetSummaryRows()` تنها جای این محاسبه است** و هم صفحه هم عکسِ روزانه از آن می‌خوانند. `total_value` (به نرخِ روز) و `total_cost` (بهای تمام‌شده) جدا برمی‌گردند تا «سود/زیانِ محقق‌نشده» گفتنی باشد — بدون آن کاربر فقط یک عددِ بزرگ‌تر می‌بیند و نمی‌داند چقدرش رشدِ قیمت است و چقدرش پولی که گذاشته.
- **نرخ دستی است و از هیچ سرویسِ بیرونی نمی‌آید** — همان دلیلی که Chart.js را محلی کرد: اینترنتِ داخلی و تحریم. سرویسی که نصفِ روزها در دسترس نباشد، از نبودنش بدتر است.
- **ورودِ نرخ روی خودِ چیپِ نوعِ دارایی در «فهرست‌های من» است**، نه یک بخشِ تازه. آن صفحه از قبل شش فهرست دارد؛ کارتِ هفتمی به نام «نرخ‌ها» همان شلوغی‌ای بود که صفحه برای حذفش ساخته شد.
- **`net_worth_snapshots` اجزا را ذخیره می‌کند، نه جمع.** صفحه‌ی دارایی برای هر قلم یک کلیدِ روشن/خاموش دارد؛ با ذخیره‌ی جمع، روندِ «بدون طلا» یا «بدون حساب‌ها» ساختنی نبود.
  - نوشتن **بدون cron** است: اولین بازدیدِ روز ردیف را می‌سازد (همان الگوی `processRecurringTransactions`). `INSERT IGNORE` با کلیدِ `(user_id, snap_date)` یعنی بازدیدِ دوم چیزی اضافه **و بازنویسی** نمی‌کند — با `REPLACE` هر بارگذاری عددِ اول روز را خراب می‌کرد.
  - `recordNetWorthSnapshot()` عمداً هیچ استثنایی پرتاب نمی‌کند: کارِ جانبی است و نباید بارگذاری صفحه را بشکند.
  - ⚠ نامِ سطل‌ها باید دقیقاً با `kind` های `$portfolio` بخواند (`wallets`, `trade`, `cheques`, `debts`)؛ مفرد نوشتنشان همه را جزوِ «دارایی» می‌برد. جمعِ کل درست می‌ماند ولی اجزا غلط — و **بی‌صدا**، چون عددِ روی صفحه فرق نمی‌کند.
- سرآیندِ صفحه «خالص دارایی» شد نه «ارزش کل دارایی‌ها»، چون بدهی از آن کم می‌شود.
- اسپارک‌لاین عمداً بی‌محور و بی‌عدد است: اینجا شکلِ روند مهم است، مقدارِ دقیق را همان عددِ بزرگِ بالای صفحه می‌گوید. رنگش از روندِ کلی می‌آید (رشد `--in`، افت `--out`) و با **`onThemeChange`** دوباره رنگ می‌گیرد — نه `registerThemedChart` که آرایه‌ی رنگِ قاچ‌ها را می‌گیرد و برای این کار نیست.
- تست در `tests/test_insights.php` است و با سه جهش سنجیده شده: برگشت به بهای خرید، `REPLACE` به‌جای `INSERT IGNORE`، و برعکس شدنِ ترتیبِ تاریخچه.

## چرخه‌ی چک: چهار سرنوشت، نه دو تا

- چک تفاوت‌سازترین چیزِ این اپ برای بازار ایران است، ولی مدلش فقط `is_settled` صفر یا یک بود. در واقعیت چکِ دریافتی اغلب **خرج می‌شود** (به نفر سوم واگذار) و گاهی **برگشت می‌خورد**. کاربر ناچار بود چکِ خرج‌شده را «پاس شد» بزند — که پول را به اشتباه به حسابش اضافه می‌کرد — یا حذفش کند و تاریخچه را از دست بدهد.
- `cheques.status` چهار حالت دارد: `pending` / `cleared` / `bounced` / `endorsed` (`migration_cheque_status`).
- **⛔ `is_settled` عمداً نماند بلکه مقدارِ مشتق شد: `is_settled = 1 ⇔ status = 'cleared'`.** حذفش یعنی بازنویسیِ یک‌جای `walletBalances()` و همه‌ی تست‌ها — و آن‌ها منطقِ پول‌اند نه نمایش. تست این هم‌ارزی را جدا می‌سنجد.
- **⛔ `chequeActiveSql()` تنها جایی است که «چکِ در جریان» تعریف می‌شود** (مثل `categoryScopeSql()`). هر کوئری‌ای که چکِ باز می‌خواهد باید از همان رد شود: `financialEvents()`, `my-assets.php`, `cheques.php`, `dashboard.php`, `includes/sidebar.php`.
  - دلیلش این است که `is_settled = 0` به‌تنهایی **غلط** است: چکِ برگشتی و خرج‌شده هم پاس نشده‌اند ولی در جریان نیستند. بدترین حالتش چکِ برگشتی است — چون برایش یک ردیف `debts` ساخته می‌شود، اگر همچنان «در جریان» شمرده شود همان پول **دو بار** در دارایی و در آینده‌ی مالی می‌آید. همان دوبار شمردنی که درباره‌ی چکِ پاس‌شده هشدار داده شده.
  - alias می‌گیرد (`chequeActiveSql('ch')`) چون `cheques.php` جدول را alias می‌کند.
- **«فعال» یعنی در جریان، «بایگانی» یعنی هر چیزِ دیگر** — نه «پاس‌شده». چکِ برگشتی و خرج‌شده کارشان تمام است و در فهرستِ فعال ماندنشان هم صفحه را شلوغ می‌کرد هم جمعِ بالای صفحه را غلط.
- **چکِ دریافتیِ برگشت‌خورده خودکار به طلب تبدیل می‌شود**، با `debts.cheque_id` به‌عنوان پیوند. بدون آن پیوند، برگرداندنِ وضعیتِ چک یک طلبِ یتیم به جا می‌گذاشت. برگرداندن، طلب را پس می‌گیرد — **مگر کاربر رویش پرداختی ثبت کرده باشد**، که آن‌وقت کارِ خودِ کاربر از بین می‌رفت.
- **هیچ کنترلِ تازه‌ای به کارت اضافه نشد:** خودِ نشانِ وضعیت دکمه شد. صفحه‌ای که ده‌ها چک دارد با یک منوی جدا کنارِ هر کارت شلوغ می‌شد؛ حالا همان نشانی که کاربر برای *خواندن* وضعیت نگاهش می‌کند، جای *عوض کردنش* هم هست. مودالِ «انتخاب حساب» که از قبل بود، حالا انتخابگرِ سرنوشت هم هست: حسابِ واریز فقط برای «پاس شد»، نامِ گیرنده فقط برای «خرج شد».
- تست در `tests/test_cheque_status.php` است و با دو جهش سنجیده شده: برگرداندنِ `chequeActiveSql()` به `is_settled = 0` تنها (که دقیقاً باگِ دوبار شمردن است) و نادیده گرفتنِ alias.

## ثبت از پیامک بانک — و پیشنهادِ عنوان‌های قبلی

- **پارسر در مرورگر است و هیچ چیزی به سرور نمی‌رود.** PWA و TWA اصلاً به SMS
  دسترسی ندارند، پس کاربر پیامک را می‌چسباند؛ همین محدودیت به سودِ ماست، چون
  متنِ خامِ پیامکِ بانک روی سیم نمی‌رود. **اگر روزی وسوسه شدید این را به یک
  اندپوینت بدهید، دلیلِ نبودنش همین است.**
- `window.parseBankSms()` در `assets/js/app.js` است، **بیرون از
  `DOMContentLoaded` و کاملاً خالص** — هیچ DOM ای نمی‌خواند و نمی‌نویسد. علتش
  آزمودنی بودن است: `tests/test_sms_parse.php` همان فایلِ واقعی را در node
  بارگذاری می‌کند (نه یک کپی)، دقیقاً مثل `test_jalali_parity.php`.
- **⛔ خرابیِ پارسر بی‌صداست.** اگر عددِ «مانده» را به‌جای مبلغ بردارد یا ریال را
  تومان بخواند، هیچ خطایی نمی‌دهد؛ فرم پر می‌شود، کاربر ثبت می‌کند، و ماه بعد
  گزارش غلط است. چهار محافظ دارد و **هر چهار در تست با آزمون جهش سنجیده شده‌اند
  (۱۰ جهش، همه گرفته شد):**
  1. **بدون کلمه‌ی جهت (برداشت/واریز) اصلاً تراکنش نیست.** پیامکِ «موجودی کارت
     شما … ۱۲٬۰۰۰٬۰۰۰ ریال» هم یک عددِ درشت دارد؛ بدون این شرط همان مانده
     به‌عنوان مبلغ پر می‌شد.
  2. **هیچ عددی «به‌طور پیش‌فرض» مبلغ نیست:** باید دست‌کم یکی از سه نشانه را
     داشته باشد — جداکننده‌ی سه‌رقمی، واحدِ پول کنارش، یا نزدیکی به کلمه‌ی
     جهت/«مبلغ». شماره‌ی حسابِ ده‌رقمی و کدِ پیگیری هیچ‌کدام را ندارند.
  3. **شماره‌ی کارت با فاصله پاک می‌شود** (`blankCards`، طولِ متن دست‌نخورده
     می‌ماند تا اندیسِ تاریخ معتبر بماند). بدون آن، `6037` در
     «واریز به 6037-9975-****-1234 مبلغ 500000» مبلغ می‌شد: هم ≥ ۱۰۰۰ است هم
     بلافاصله بعد از کلمه‌ی جهت.
  4. **«مانده / موجودی / سقف / اعتبار» عددِ کنارشان رد می‌شود** — و این جدا از
     رتبه‌بندی لازم است: در «واریز حقوق / مانده حساب: ۵۰٬۰۰۰٬۰۰۰ / مبلغ واریزی
     ۴۵٬۰۰۰٬۰۰۰» مانده به کلمه‌ی جهت **نزدیک‌تر** است.
- **⛔ نبودِ واحد یعنی ریال، نه «نمی‌دانم».** فقط «تومان» و «تومن»ِ صریح از
  تقسیم بر ۱۰ معاف‌اند؛ در هر حالتِ دیگر عدد تقسیم می‌شود و `currency` مقدارِ
  `rial_assumed` می‌گیرد تا پیامِ روی صفحه (زرد، نه سبز) بگوید فرض شده.
  - **نسخه‌ی اول عمداً حدس نمی‌زد و عدد را دست‌نخورده می‌گذاشت.** استدلالش درست
    بود (حدسِ غلط یعنی ده‌برابر شدنِ مبلغ) ولی **فرضش غلط بود**: پیامکِ بانکِ
    ایرانی عملاً همیشه ریال است، پس «دست نزدن» هم خودش یک حدس بود — همان حدسِ
    اشتباه، فقط ساکت‌تر. کاربر هر بار باید یک صفر دستی کم می‌کرد.
  - ⚠ بلوبانک **تومان** می‌نویسد و صریح هم می‌نویسد، پس از این قاعده معاف است.
- **⛔ ادبیاتِ هر بانک فرق می‌کند و فهرستِ رسمی کافی نیست.** بلوبانک به‌جای
  «برداشت» می‌نویسد **«پرید»** و به‌جای «واریز» **«نشست»**. پیش از این پارسر
  هیچ کلمه‌ی جهتی پیدا نمی‌کرد و می‌گفت «تراکنش نیست» — یعنی قابلیت برای
  کاربرِ آن بانک **اصلاً کار نمی‌کرد**، بی‌هیچ خطایی.
- **⛔ مبلغ با «فاصله» از کلمه‌ی جهت انتخاب می‌شود، نه با «سمت».** دو راهِ
  ساده‌تر آزموده و رد شدند، و هر دو را **خودِ تست** گرفت نه بازبینیِ چشمی:
  - *فقط بعد از کلمه‌ی جهت* (نسخه‌ی اول): بلوبانک مبلغ را **قبل** می‌نویسد.
  - *قبل و بعد هم‌ارزش*: در «کارمزد ۵,۰۰۰ ریال، واریز ۴۵,۰۰۰,۰۰۰ ریال» کارمزد
    برنده می‌شد (زودتر در متن).
  - *«بعد» همیشه مقدم بر «قبل»*: در «۲۵۰,۰۰۰ تومان پرید. کارمزد ۵,۰۰۰ تومان»
    کارمزد برنده می‌شد.
  فاصله هر سه را درست جواب می‌دهد، چون در همه‌ی این متن‌ها مبلغِ اصلی به
  کلمه‌ی جهت **چسبیده** و کارمزد یک عبارت دورتر است. دو موردِ آینه‌ای در تست
  هست تا هیچ‌کدام از آن سه قاعده دوباره جا نیفتد.
- **تاریخ از همان `jalali-datepicker.js` رد می‌شود**، نه یک تبدیلِ تازه — وگرنه
  می‌شد پیاده‌سازیِ سومِ تقویم، بیرون از پوششِ `test_jalali_parity`. تاریخِ
  ناموجود (۳۱ آبان، ۱۴۰۳/۱۳/۴۵) با رفت‌وبرگشت رد می‌شود، نه اینکه حدس زده شود.
- **⛔ خواندنِ خودکارِ خودِ پیامک ممکن نیست** و این محدودیتِ سکو است، نه انتخاب:
  نه PWA و نه TWA به SMS دسترسی دارند و هیچ API ای هم برایش نیست (Web OTP فقط
  کدِ ورودِ همان دامنه را می‌دهد، نه پیامکِ بانک). نزدیک‌ترین چیز، دکمه‌ی
  «چسباندن و خواندن» است که خودش کلیپ‌بورد را می‌خواند و بلافاصله پارس می‌کند —
  کاربر پیامک را کپی می‌کند و یک تپ.
  - ⚠ دکمه فقط وقتی رندر می‌شود که `navigator.clipboard.readText` باشد **و**
    `isSecureContext` برقرار باشد؛ روی http دکمه می‌آمد و با زدنش هیچ اتفاقی
    نمی‌افتاد. دکمه‌ی بی‌کار از نبودنش بدتر است.
- **حساب فقط با تطبیقِ چهار رقمِ آخرِ کارت عوض می‌شود.** `walletCardTails()`
  فقط همان چهار رقم را به صفحه می‌دهد، نه شماره‌ی کامل — شماره‌ی کارت حساس است
  و در فهرست حساب‌ها هم نشان داده نمی‌شود.
- **پیشنهادِ عنوان‌های قبلی** (`recentTransactionTitles()` + `recentTitlesDatalist()`)
  هیچ جدول و اندپوینتِ تازه‌ای ندارد و از خودِ `transactions` می‌خواند.
  `JOIN` روی `MAX(id)` است نه `GROUP BY` خام، وگرنه دسته و حساب و مبلغ از
  **ردیف‌های مختلف** می‌آمدند. `<datalist>` است نه `<select>` — همان قاعده‌ی
  `peopleDatalist()`: ورودیِ آزاد باید کار کند.
  - ⚠ دسته **بعد از** `setType()` پر می‌شود: عوض کردنِ نوع فهرستِ دسته‌ها را از
    نو می‌سازد و انتخابِ قبلی را می‌برد.
  - مبلغ فقط وقتی پر می‌شود که کاربر هنوز چیزی ننوشته باشد — نوشته‌ی خودش
    همیشه برنده است.

## یادآوری سررسید — تنها جایی که اپ بی‌آنکه بازش کنید حرف می‌زند

- **دلیل وجودش:** هسته‌ی این برنامه سررسید است (چک، طلب و بدهی، تراکنش دوره‌ای)، ولی تا پیش از این اپ فقط وقتی خبر می‌داد که کاربر خودش بازش کند — یعنی دقیقاً همان لحظه‌ای که دیگر یادآوری لازم ندارد. چکِ برگشتی عارضه‌ی حقوقی دارد و بدهیِ دیرکرد رابطه‌ی خانوادگی را خراب می‌کند.
- `deploy/reminders.php` (نگهبان `PHP_SAPI`) هر صبح با cron اجرا می‌شود، برای هر کاربرِ ایمیل‌دار از `financialEvents()` خلاصه می‌سازد و با `Mailer` می‌فرستد. بدون آرگومان فقط گزارش می‌دهد؛ `--send` واقعاً می‌فرستد؛ `--user ali` فقط یک نفر (برای آزمودن)؛ `--install-cron` زمان‌بندی می‌کند.
- **⛔ پیش‌فرض عمداً روشن است.** این تنها تنظیمی است که ارزشش در نبودنش دیده نمی‌شود: کاربری که نمی‌داند وجود دارد هرگز روشنش نمی‌کند، و بعد چکش برگشت می‌خورد. ولی فقط برای کسی می‌رود که هم ایمیل ثبت کرده باشد هم سررسیدی داشته باشد، پس هیچ‌کس ایمیلِ بی‌ربط نمی‌گیرد. نبودِ ردیف در `notification_prefs` یعنی «هنوز تصمیم نگرفته» — به همین دلیل جدولِ جداست نه ستون روی `users`، وگرنه نمی‌شد «انتخاب نکرده» را از «خاموش گذاشته» تشخیص داد.
- **`REMINDER_DAYS` تنها مرجعِ بازه‌هاست** (مثل `Auth::SESSION_WINDOWS`). فهرستِ دوم نسازید، وگرنه گزینه‌ای که کاربر می‌بیند هنگام ذخیره بی‌صدا به پیش‌فرض برمی‌گردد.
- **`last_sent_on` نگهبانِ «یک ایمیل در روز» است.** بدون آن، یک اشتباهِ زمان‌بندی (cron دو بار، یا اجرای دستی) به هر کاربر چند ایمیل می‌فرستاد — سریع‌ترین راهِ اینکه کاربر یادآوری را خاموش کند یا آدرس را اسپم علامت بزند.
- **متنِ ایمیل در `reminderEmailBody()` است، نه داخل اسکریپت.** آن اسکریپت با cron اجرا می‌شود و خروجی‌اش را کسی نمی‌بیند، پس اگر متن خراب شود بی‌صدا خراب می‌ماند؛ اینجا بدون فرستادنِ هیچ ایمیلی آزمودنی است. **نسخه‌ی متنی اختیاری نیست:** بعضی کلاینت‌ها HTML نشان نمی‌دهند و فیلترهای اسپم به ایمیلِ فقط-HTML سخت‌گیرترند.
- لینک از `appBaseUrl()` ساخته می‌شود نه `HTTP_HOST` — همان قاعده‌ی لینکِ بازیابیِ رمز.
- **کلید فقط وقتی در پروفایل دیده می‌شود که جدولش آمده باشد**، و اگر `MAIL_METHOD` تنظیم نشده باشد صریح می‌گوید ایمیلی نمی‌رود. کلیدی که کار نمی‌کند بدتر از نبودنش است.
- **Web Push عمداً فعلاً نیست:** سرویسِ پوشِ کروم از ایران پایدار نیست و روی iOS فقط در حالت نصب‌شده کار می‌کند. ایمیل هیچ وابستگیِ بیرونی ندارد.
- تست در `tests/test_reminders.php` است (۳۰ بررسی) و با سه جهش سنجیده شده: خاموش کردنِ پیش‌فرض، خالی کردنِ نسخه‌ی متنی، و برداشتنِ نگهبانِ یک‌ایمیل‌در‌روز.

## بکاپ و بازیابی

- `deploy/backup.sh` می‌گیرد، `deploy/restore.sh` **می‌سنجد و برمی‌گرداند**. تا پیش از این فقط اولی بود — و بکاپی که یک بار بازیابی نشده باشد بکاپ نیست، فرضیه است.
- **سنجش با `--admin` اجرا می‌شود، نه با کاربر اپ.** سنجش یک دیتابیس موقت می‌سازد و `hesab_user` طبق قانون جداسازی فقط `GRANT ... ON hesab_db.*` دارد — یعنی نمی‌تواند، و **این عمدی و درست است**. راه درست `sudo bash deploy/restore.sh --admin` است که از سوکت یونیکس با کاربر مدیر وصل می‌شود و دسترسی اپ را دست‌نخورده می‌گذارد. ⚠ وسوسه‌ی اشتباه: «یک GRANT به hesab_user بدهیم» — همان کاری است که قانون منع می‌کند؛ خودِ اسکریپت هم صریحاً هشدار می‌دهد.
- **جدولِ «گم‌شده» معمولاً خرابی نیست.** اگر بکاپ پیش از آخرین migration گرفته شده باشد، جدول‌های تازه در آن نیستند و سنجش رد می‌شود — درست، ولی گمراه‌کننده. پیام خطا همین را می‌گوید و راهنمایی می‌کند اول یک بکاپ تازه بگیرید.
- **حالت پیش‌فرضِ `restore.sh` بی‌خطر است:** روی یک دیتابیس موقت بازیابی می‌کند، فهرست جدول‌ها و تعداد ردیف‌ها را با دیتابیس زنده می‌سنجد، و آخرش موقت را پاک می‌کند (با `trap`، پس حتی اگر وسط کار بمیرد هم جا نمی‌ماند). به داده‌ی زنده دست نمی‌زند.
- بازیابی واقعی فقط با `--into NAME`. اگر مقصد همان دیتابیس زنده باشد، اول یک بکاپ ایمنی می‌گیرد و بعد تأیید تایپی می‌خواهد؛ اگر بکاپ ایمنی نگرفت، اصلاً جلو نمی‌رود.
- مقایسه‌ی فهرست جدول‌ها با `LC_ALL=C sort` مرتب می‌شود، نه با `ORDER BY` دیتابیس. ترتیبِ collation با ترتیبِ بایتیِ `comm` یکی نیست (`wallet_kinds` در برابر `wallets`) و `comm` با ورودی نامرتب فقط هشدار می‌دهد ولی *ادامه می‌دهد* و نتیجه‌اش غلط است — یعنی می‌توانست بکاپِ ناقص را سالم اعلام کند. یک بار در همین آزمون دقیقاً همین شد.
- آزموده شده روی MariaDB: رفت‌وبرگشت کامل، `CHECKSUM TABLE EXTENDED` هر ۲۴ جدول یکسان. مسیرهای شکست هم آزموده شده‌اند: فایل خرابِ gzip، بکاپ خالی، و بکاپی که یک جدول کم دارد — هر سه با کد خروج ۱ رد می‌شوند.

## آمار استفاده — جای «تراکنش همه کاربران»

- **⛔ صفحه‌ی `admin/all-transactions.php` حذف شد.** مدیر همه‌ی تراکنش‌های همه‌ی
  کاربران را با عنوان و مبلغ و طرفِ حساب می‌دید — هم بی‌فایده بود (با فهرستِ
  خرید و قرضِ بقیه هیچ تصمیمی گرفته نمی‌شود) هم بدترین نقضِ حریم خصوصی در کلِ
  اپ، و همان تنها استثنای قاعده‌ی «جداسازی کاربران» بود. حالا استثنایی نمانده.
- جایش `admin/insights.php` + `includes/admin_insights.php` آمد که **عمداً فقط
  شمارش است، نه محتوا**: چند تراکنش، آخرین فعالیت کِی، چند کاربر هر بخش را
  استفاده کرده‌اند. نه عنوان، نه مبلغ، نه طرفِ حساب.
- **⛔ هیچ ردیابیِ تازه‌ای اضافه نشد و هیچ جدولِ لاگی ساخته نمی‌شود.** همه‌ی
  اعداد از داده‌ای که از قبل هست ساخته می‌شوند. اگر روزی وسوسه شدید «رویداد»
  ثبت کنید، بدانید آن یعنی ساختنِ یک ردِ رفتاری از کاربران که تا امروز وجود
  نداشته — و آن‌وقت ادعاهای `privacy.php` هم دیگر درست نیستند.
- **قیفِ شروع اولین کارتِ صفحه است، عمداً.** «چند نفر ثبت‌نام کردند» بی‌معناست
  اگر ندانیم چند نفرشان اولین تراکنش را ثبت کردند. اگر عددِ دوم خیلی کمتر از
  اولی باشد، مسئله‌ی اپ «امکانات» نیست — کاربر در همان دقیقه‌ی اول گیر می‌کند.
- «آخرین فعالیت» از تاریخِ ثبتِ رکوردها می‌آید، **نه از لاگِ ورود**: کسی که اپ
  را باز می‌کند ولی چیزی ثبت نمی‌کند از دیدِ محصول فعال نیست، و ورودها را هم
  نمی‌خواهیم ثبت کنیم.
- ⚠ رشدِ ماهانه روی تاریخِ **میلادی** گروه‌بندی می‌شود و بعد در PHP به ماهِ شمسی
  می‌رود، نه برعکس: ماهِ شمسی وسطِ ماهِ میلادی شروع می‌شود، پس `GROUP BY
  YEAR/MONTH` با برچسبِ شمسی کنارِ هم دروغ می‌گفتند.
- جدولی که هنوز با migration نیامده باشد در `featureAdoption()` رد می‌شود، نه
  اینکه صفحه را بشکند — نصب‌های عقب‌مانده هم باید این صفحه را ببینند.

## لایه‌ها و اسکرول

- هر لایه‌ی `.modal-overlay` / `.sheet-overlay` / `.more-sheet-overlay` که باز شود، `MutationObserver` در `app.js` بدنه را قفل می‌کند. بدون این، کشیدن انگشت روی شیت، صفحه‌ی زیرش را جابه‌جا می‌کرد و کاربر جای خودش را گم می‌کرد.
- **⛔ قفل هیچ ویژگیِ چیدمانی‌ای را عوض نمی‌کند، و این کلِ نکته است.**
  فقط کلاسِ `is-locked` روی `<html>` می‌نشیند که `overflow: hidden`
  می‌دهد، به‌علاوه‌ی یک شنونده‌ی `touchmove` که فقط وقتی جلوی لمس را
  می‌گیرد که هیچ نیای اسکرول‌شونده‌ای در مسیرش نباشد (`passive: false`
  لازم است، وگرنه `preventDefault` نادیده می‌ماند).
  - **دو نسخه‌ی قبلی هر دو غلط بودند و درسش مهم است.** نسخه‌ی اول بدنه
    را `position: fixed` با `top: -Y` می‌کرد — راهِ رایج، ولی روی آیفون
    نوارِ پایینِ شناور را بالا می‌آورد. نسخه‌ی دوم خواست با کشیدنِ
    `min-height` بدنه جبرانش کند و **کار نکرد**، چون فرضش غلط بود:
    عنصرِ `position: fixed` نسبت به **پنجره** جای می‌گیرد، نه نسبت به
    جعبه‌ی `body` — پس بلند و کوتاه کردنِ بدنه اصلاً به آن ربطی ندارد.
  - درسِ کلی‌ترش: وقتی دو بار جبران‌سازی جواب نداد، مسئله را از بین
    ببرید نه اینکه جبرانِ سوم بنویسید. حالا هیچ چیزی روی `body` عوض
    نمی‌شود، پس هیچ چیزی هم نمی‌تواند تکان بخورد — و چون صفحه جابه‌جا
    نمی‌شود، بازگرداندنِ `scrollY` و پرشِ هنگام بستن هم از بین رفت.
  - ⚠ **در مرورگرِ دسکتاپ هیچ‌کدام از این‌ها بازتولید نمی‌شوند** (آنجا
    `env(...)` صفر است و نوارِ مرورگر جمع نمی‌شود)، پس «در کروم درست
    است» اینجا مدرک نیست.

## ⛔ ثبت تراکنش باید روی دسکتاپ هم راه داشته باشد

- شیتِ ثبت تراکنش فقط با `#addTxBtn` باز می‌شد و آن دکمه داخلِ
  `.bottom-nav` بود — که بالای ۹۰۰ پیکسل `display: none !important`
  می‌شود. یعنی **روی کامپیوتر هیچ راهی برای ثبت تراکنش نبود**: هسته‌ی
  اپ، کاملاً از کار افتاده، بی‌هیچ خطایی. روی گوشی بی‌عیب بود، پس
  هیچ تستی و هیچ نگاهی آن را نمی‌گرفت.
- حالا به **کلاسِ `.js-add-tx`** بسته است نه به یک شناسه: دکمه‌ی +
  نوارِ پایین (موبایل) و دکمه‌ی «ثبت تراکنش» نوارِ بالا (`.topbar-add`،
  فقط دسکتاپ) هر دو همان کلاس را دارند. ورودیِ سومی که فردا اضافه شود
  هم بدون دست زدن به `app.js` کار می‌کند.
- **قاعده‌ی کلی: هر قابلیتی که تنها راهش داخلِ `.bottom-nav` باشد، روی
  دسکتاپ وجود ندارد.** هر بار که چیزی به آن نوار اضافه می‌کنید، بپرسید
  روی عرضِ بزرگ از کجا در دسترس است.
- نوار پایین `z-index: 260` دارد، بالاتر از شیت (۲۵۰)، تا دکمه‌ی «بیشتر» موقع باز بودن شیت هم قابل زدن بماند — همان دکمه ضامن باز/بسته است. شیت `padding-bottom` به اندازه‌ی نوار دارد تا زیرش پنهان نشود.
- کارهای مدیریتی در شیت ابزارها زیر یک دکمه‌ی «مدیریت» جمع‌اند که زیرشیت `#adminSheet` را باز می‌کند؛ بقیه‌ی شیت برای مدیر و کاربر عادی یک شکل است.

## حساب‌ها: هیچ پولی بی‌حساب نمی‌ماند

- برنامه برای هر کاربر یک حساب پیش‌فرض می‌سازد به نام **«کیف پول»** (قبلاً «کیف پول نقدی» بود؛ `migration_money_links` تغییر نامش می‌دهد). این همان حسابی است که همه‌ی تراکنش‌های قبلِ راه‌اندازیِ حساب‌ها به آن نسبت داده شدند و پیش‌فرضِ فرم ثبت تراکنش هم هست (`sort_order = 0`).
- `defaultWalletId()` آن را پیدا می‌کند (`kind='cash'` با کمترین `sort_order`؛ اگر نبود، اولین حساب فعال). `resolveWalletId()` نقطه‌ی واحد است: ورودی کاربر را می‌سنجد و اگر نفرستاده یا مال او نیست، حساب پیش‌فرض را برمی‌گرداند. **هر جا پولی جابه‌جا می‌شود از همین رد شوید** — وگرنه مبلغ در هیچ حسابی نمی‌نشیند و کاربر بعداً نمی‌فهمد کجا رفت.
- `walletBalances()` تنها مرجع موجودی است. علاوه بر تراکنش و انتقال و معامله، حالا این‌ها را هم جمع می‌کند:
  - **چک پاس‌شده** (`cheques.settle_wallet_id`): دریافتی مثبت، صادره منفی.
  - **پرداخت طلب/بدهی** (`debt_payments.wallet_id`): وصول طلب مثبت، پرداخت بدهی منفی.
- **هیچ‌کدام از این‌ها ردیف `transactions` نمی‌سازند.** وصول یک طلب درآمد نیست و اگر تراکنش می‌شد، گزارش درآمد/هزینه‌ی آن ماه الکی بالا می‌رفت. تست جدایی در `tests/test_money_links.php` است.
- **همه‌ی جابه‌جایی پولِ طلب و بدهی از `debt_payments` می‌گذرد**، چه پرداخت جزئی باشد چه تسویه‌ی کامل. زدن تیک «تسویه شد» یک ردیف برای *باقیمانده* می‌سازد و با `is_settlement = 1` علامتش می‌زند؛ برداشتن تیک دقیقاً همان‌ها را پس می‌گیرد و پرداخت‌های دستی کاربر دست‌نخورده می‌مانند.
- **تعدیل موجودی** (`api/adjust_wallet.php`) روی `initial_balance` می‌نشیند، نه به شکل تراکنش. موجودی هر حساب محاسبه‌شده است و وقتی با پول واقعی نمی‌خواند باید بشود دستی برابرش کرد؛ ولی یک تعدیل نه درآمد است نه هزینه، پس نباید در گزارش‌ها بیاید. سه حالت دارد: افزودن / کم کردن / «موجودی واقعی این‌قدر است» (تفاوت خودکار حساب می‌شود).
- صفحه‌ی تراکنش‌ها فیلتر `?wallet=ID` دارد و نمای کارتِ هر حساب دو دکمه: «تعدیل موجودی» و «تراکنش‌های این حساب». بدون این دو، موجودی منفی دیده می‌شد ولی هیچ راهی نبود بفهمی از کجا آمده.
- **رنگ در نمودارها معنا دارد، تزئین نیست — ولی تک‌رنگ هم نیست.** `chartPalette($tone, $mode)` یک چرخه‌ی ثابت از هشت هیوی متمایز دارد و فقط *نقطه‌ی شروع* را عوض می‌کند: هزینه از قرمز شروع می‌شود، درآمد و دارایی از سبز. نسخه‌ی اول همه‌ی قاچ‌ها را هم‌خانواده کرده بود (همه سبز یا همه نارنجی) و نمودار خوانده نمی‌شد.
  - ترتیب چرخه دلخواه نیست: با `scripts/validate_palette.js` مهارت dataviz سنجیده شده و هر جفتِ کنارِ هم — در هر دو حالت روشن و شب، و حتی جفتِ اولی/آخری که در دونات به هم می‌رسند — برای چشم عادی و انواع کوررنگی اختلاف کافی دارد. **اگر رنگی را عوض کردید، دوباره اعتبارسنجی کنید.**
  - پله‌های حالت شب همان هشت هیو هستند ولی برای زمینه‌ی تیره دوباره چیده شده‌اند؛ برگرداندن رنگ روشن روی زمینه‌ی تیره کار نمی‌کند (بنفش گم می‌شد، زرد می‌زد توی چشم).
  - نقطه و نوار رنگی در HTML با دو متغیر `--dot-l` و `--dot-d` رنگ می‌گیرند و CSS انتخاب می‌کند؛ نمودارها رنگشان را در جاوااسکریپت می‌گیرند، پس با `registerThemedChart()` ثبت می‌شوند تا با عوض شدن حالت شب دوباره رنگ بگیرند.
  - `registerThemedChart` و `onThemeChange` عمداً **بیرون از `DOMContentLoaded`** در `app.js` تعریف شده‌اند: اسکریپت درون‌صفحه‌ای شنونده‌اش را هنگام تجزیه‌ی HTML ثبت می‌کند، یعنی زودتر از شنونده‌ی `app.js` که با `defer` می‌آید — یک بار همین ترتیب صفحه‌ی گزارش را با «registerThemedChart is not defined» شکست.

## نمای دارایی‌ها: چه چیزی در جمع بیاید

- **بخش معاملات یک ردیفِ جمع‌شده است، نه یکی به‌ازای هر کالا.** «مجموع دارایی‌های بخش معاملات» جمعِ `open_cost` همه‌ی معامله‌های بازِ کاربر است با یک کلید. دلیلش این است که این صفحه نمای *ترکیبِ* دارایی است؛ اسمِ تک‌تکِ معامله‌ها (گوشی، سکه، …) فهرست را شلوغ و درصدها را خرد می‌کرد، در حالی که جای دیدن و فروششان `trades.php` است. دارایی‌های ثبت‌شده عمداً جدا جدا (به تفکیک نوع) می‌مانند، چون خودشان ترکیبِ دارایی‌اند نه یک بخش.
- برچسب کنار نام برداشته شد: دو ردیفِ جمع‌شده خودشان اسمشان را می‌گویند و برچسبِ تکراری فقط عرض می‌گرفت و نام را با «…» می‌برید. در همین صفحه `white-space` نام هم `normal` است تا نامِ بلند به خط دوم برود؛ در گزارش دسته‌بندی که ده‌ها ردیف دارد همچنان `nowrap` است.
- **چک و طلب/بدهی هم هر کدام یک قلمِ خالص‌اند.** «خالص چک‌های در جریان» = دریافتی منهای صادره، و «خالص طلب و بدهی» = باقیمانده‌ی طلب منهای باقیمانده‌ی بدهی. هر دو می‌توانند منفی باشند و آن‌وقت از جمع کل کم می‌کنند.
  - **فقط موارد تسویه‌نشده شمرده می‌شوند، و این قابل مذاکره نیست.** چکِ پاس‌شده و پرداختِ انجام‌شده‌ی بدهی از قبل در `walletBalances()` نشسته‌اند؛ آوردنشان اینجا یعنی دوبار شمردنِ همان پول.
  - قلمِ منفی در نمودار دونات نمی‌آید (قاچِ منفی معنا ندارد) ولی در جمع کل هست. درصد هم فقط وقتی نشان داده می‌شود که هم جمع کل مثبت باشد هم خودِ قلم — وگرنه «۰٪» گمراه‌کننده بود.
- هر قلمِ صفحه‌ی دارایی — دارایی ثبت‌شده، **مجموع دارایی‌های بخش معاملات**، **خالص چک‌ها**، **خالص طلب و بدهی**، و **مجموع حساب‌ها** — یک کلید روشن/خاموش دارد. خاموش کردنش چیزی را پاک نمی‌کند؛ فقط از جمع کل، درصدها و نمودار بیرونش می‌گذارد، تا بشود پرسید «دارایی‌ام بدون طلا چقدر است؟» یا «بدون پولِ توی حساب‌ها چقدر؟».
- انتخاب در `localStorage` همان مرورگر می‌ماند (`daftar_asset_off`)؛ چیزی در دیتابیس ذخیره نمی‌شود چون این یک فیلترِ نگاه است، نه داده.
- جمع و درصدها **در جاوااسکریپت** دوباره حساب می‌شوند، پس PHP فقط حالت «همه روشن» را رندر می‌کند.

## زبان طراحی گزینه‌های دوحالته

- هر گزینه‌ی روشن/خاموشِ اپ یک `<label class="switch">` است با همان سه تکه: `input` پنهان، `.switch-track`، `.switch-knob`. قبلاً هر صفحه چک‌باکس خام مرورگر را نشان می‌داد و هر مرورگر شکل خودش را می‌کشید.
- راست‌به‌چپ: خاموش یعنی دستگیره سمت راست (`inset-inline-start`)، روشن یعنی `translateX(-18px)` به چپ. نسخه‌ی کوچک‌تر برای کنار ردیف‌های فهرست: `.switch-sm`.
- تیک «تسویه شد» / «پاس شد» روی کارت‌های طلب و چک عمداً کلید نیست بلکه چک‌مارک است (`.debt-check`) — کارِ «انجام شد» با تنظیمِ روشن/خاموش فرق دارد — ولی از همان رنگ (`--in`) و همان گردی استفاده می‌کند تا یک خانواده به نظر برسند.

## میله‌ی پیشرفت همیشه از چپ پر می‌شود

- دو خانواده میله در اپ هست و هر دو `direction: ltr` روی *ترک* دارند: `.budget-bar-track` (بودجه، پس‌انداز، پرداخت طلب و بدهی) و `.cat-breakdown-bar-track` (گزارش دسته‌بندی، دارایی).
- **صفحه راست‌به‌چپ است ولی میله‌ی پیشرفت نباید باشد.** بدون آن یک خط، بچه‌ی بلاکیِ داخل ترک از لبه‌ی *راست* شروع می‌کرد و «۳۵٪ پرداخت‌شده» به شکل ۳۵٪ سمت راست دیده می‌شد — خلافِ چیزی که چشم از نوار پیشرفت انتظار دارد، و در کنارِ درصدی که همان‌جا نوشته شده گیج‌کننده بود.
- گرادیان‌های `linear-gradient(90deg, …)` روی خودِ میله فیزیکی‌اند و با جهت صفحه عوض نمی‌شوند، پس دست نخوردند.
- `.switch-track` و `.seg` عمداً بیرون این قاعده‌اند: آن‌ها کنترل‌اند نه نمایش پیشرفت، و جهتشان باید با صفحه بخواند.
- **میله‌ی تازه‌ای که اضافه می‌کنید هم باید همین را داشته باشد.** سنجشش هم آسان است: عرضِ پرشده باید کمتر از ۱۰۰٪ باشد و لبه‌ی چپش با لبه‌ی چپِ ترک یکی — با ۱۰۰٪ اصلاً جهت معلوم نمی‌شود.

## سرعت

- **همه‌ی صفحه‌ها `Cache-Control: no-store, private` می‌گیرند** (در `Auth::initSession()`). بدون این، سافاری روی آیفون نسخه‌ی کش‌شده را نشان می‌داد: کاربر چیزی را حذف می‌کرد، صفحه تازه می‌شد و باز همان عدد قدیمی بود. این عمداً bfcache را هم خاموش می‌کند — رندر سمت سرور ۴ تا ۸ میلی‌ثانیه است، پس ارزشش را دارد.
- **Chart.js در مخزن است** (`assets/js/chart.umd.js`، MIT، نسخه‌ی ۴.۴.۰). قبلاً از jsDelivr می‌آمد و روی اینترنت داخلی همان یک درخواست گاهی چند ثانیه طول می‌کشید یا اصلاً نمی‌رسید و نمودار خالی می‌ماند. حالا از خود سرور می‌آید با gzip (۲۰۵ KB → ۷۰ KB) و کش یک‌ساله‌ی immutable.
- **CSS و JS مستقیم از وب‌سرور می‌آیند و هیچ فایل ثابتی از PHP رد نمی‌شود** (`assetUrls()`). این یک باگ واقعی را بست: نسخه‌ی قبلی همه را از یک اسکریپت PHP (`assets/serve.php`) تحویل می‌داد، پس هر بارگذاری صفحه چهار پروسه‌ی PHP-FPM می‌گرفت (HTML + CSS + JS + chart.js) از `pm.max_children = 5` — و کاربر می‌دید سایت وسط کار چند ثانیه قفل می‌کند. حالا هر بارگذاری **یک** پروسه می‌خواهد. آن اسکریپت و کلید `ASSET_DELIVERY` حذف شدند؛ اگر روزی وسوسه شدید دوباره از PHP تحویلشان بدهید، دلیلش همین است.
  - **دام مسیر فونت:** `@font-face` مسیر نسبی دارد و نسبت به آدرسِ خودِ CSS حل می‌شود، پس `../fonts/…` درست است (`assets/css/` → `assets/fonts/`). کامنت کهنه‌ی بالای همان قاعده در `style.css` عکسِ این را می‌گفت و با حذف `serve.php` اصلاح شد.
- **pool پی‌اچ‌پی از `ondemand`/۵ به `dynamic`/۱۲ رفت** با دو پروسه‌ی همیشه گرم (`deploy/vps-setup.sh`). روی نصب موجود: `sudo bash deploy/tune.sh --apply` — فقط `pool.d/hesab.conf` را می‌نویسد و php-fpm را `reload` می‌کند (نه `restart`)، و اگر تست پیکربندی رد شود خودش برمی‌گرداند.
- **`assets/manifest.php` هدر کش و ETag دارد**؛ بدون آن مرورگر با هر ناوبری دوباره می‌گرفتش و یک پروسه‌ی PHP دیگر می‌خورد.
- **اسکریپت‌ها با `defer` در `<head>`اند**، نه ته `<body>`: دانلودشان موازی با خواندن HTML شروع می‌شود ولی اجرا مثل قبل بعد از ساخته‌شدن صفحه است. فونت هم `preload` دارد چون داخل CSS تعریف شده و مرورگر دیر می‌فهمید لازمش دارد.
- **داشبورد از ۸ کوئری به ۳ رسید:** جمع روزانه‌ی کل تاریخچه یک بار خوانده می‌شود و آمار امروز/هفته/ماه/سال/بازه‌ی دلخواه و شمارش‌ها همه در PHP از رویش ساخته می‌شوند.
- **پرسش‌های ساختار دیتابیس یک کوئری‌اند، نه پنج تا.** `schemaMap()` یک بار در هر درخواست کل نقشه‌ی جدول/ستون را می‌خواند و `tableExists()` / `tableHasColumn()` / `tradesTablesExist()` از حافظه جواب می‌گیرند. نسخه‌ی قبلی برای هر پرسش یک کوئری جدا به `information_schema` می‌زد و فقط داشبورد پنج‌تایش را می‌زد. عمداً بین درخواست‌ها کش نمی‌شود تا بعد از اجرای یک migration تازه، همان درخواست بعدی ساختار واقعی را ببیند.
- **`activeWallets()` فهرست حساب‌ها را در همان درخواست کش می‌کند.** شیت ثبت تراکنش در فوتر هر صفحه است و همان فهرستی را می‌خواست که خودِ صفحه هم گرفته بود — دو بار پشت سر هم یک کوئری.
- شمارش واقعی بعد از این دو: خانه ۷ کوئری، داشبورد ۱۵، حساب‌ها ۱۰، چک‌ها ۱۲، معاملات ۷. رندر سمت سرور ۴ تا ۸ میلی‌ثانیه و HTML گزیپ‌شده حدود ۸ کیلوبایت.
- **هر تنظیمی که ظاهرِ منو را عوض می‌کند هم باید بلافاصله دیده شود.** کلید «بخش معاملات» در پروفایل بعد از ذخیره صفحه را تازه می‌کند، چون شیت «بیشتر» و نوار کناری با حالت قبلی رندر شده‌اند؛ بی‌تازه‌سازی، دکمه‌ی معاملات تا ناوبری بعدی سر جایش می‌ماند و کاربر فکر می‌کند کلید کار نکرد. همین قاعده برای افزودن/حذف در «فهرست‌های من» هم هست.
- **هر تغییر پول باید بلافاصله دیده شود.** حذف تراکنش/طلب/چک فقط ردیف را برنمی‌دارد؛ صفحه هم تازه می‌شود، چون جمع‌ها و موجودی بالای صفحه از روی همان رکورد ساخته شده‌اند و بی‌تازه‌سازی کاربر فکر می‌کند حذف انجام نشده.

### `deploy/perf-report.sh` — «کند است» یک علامت است، نه یک علت

- اجرا: `sudo bash deploy/perf-report.sh` — و برای سنجش صفحه‌های داخلی `--login نام‌کاربری` (رمز پرسیده می‌شود و روی صفحه نمی‌آید). فقط می‌خواند: هیچ فایلی نمی‌نویسد، هیچ سرویسی را `reload`/`restart` نمی‌کند.
- **دلیل وجودش:** «کند است» می‌تواند چهار علتِ کاملاً جدا داشته باشد — PHP کند (opcache، کوئری)، پروسه‌های کم (صف شدن)، فایل‌های بزرگ/بی‌کش، یا شبکه. راهِ هر کدام فرق می‌کند و حدس زدن بینشان هزینه‌ی چند روز کار اشتباه دارد. اسکریپت هر چهار را جدا اندازه می‌گیرد.
- **مهم‌ترین عددش «زمان پاسخ از روی خودِ سرور» است** (بخش ۸): `time_starttransfer` با curl از روی خودِ ماشین، پس شبکه‌ی بین گوشی و سرور در آن نیست. کوچک بودنِ این عدد در کنارِ کندیِ محسوس روی گوشی یعنی مسئله شبکه است و **هیچ بهینه‌سازیِ کدی حلش نمی‌کند**. کمترینِ سه نمونه گرفته می‌شود، نه میانگین، تا یک نوسان اتفاقی داوری را خراب نکند.
- **opcache را باید در SAPI ای سنجید که سایت با آن اجرا می‌شود (fpm)، نه در خط فرمان** — این دو `php.ini` جدا دارند. اسکریپت با `-c` همان `php.ini` و با `PHP_INI_SCAN_DIR` همان `conf.d`ی fpm می‌سنجد.
  - و **اگر نتوانست بسنجد، ادعا نمی‌کند خاموش است**: `extension_loaded()` جدا بررسی می‌شود. نسخه‌ی اول اگر باینریِ نسخه‌دار (`php8.3`) نبود، `ini_get` رشته‌ی خالی می‌داد و اسکریپت «opcache خاموش است» اعلام می‌کرد — یعنی بزرگ‌ترین یافته‌ی گزارش می‌توانست کاملاً ساختگی باشد.
- **رمز دیتابیس از راه `--defaults-extra-file` می‌رود، نه `mysql -pرمز`** — که هر کاربر دیگری روی همین سرور در `ps aux` می‌بیند. روی این VPS سرویس‌های دیگری هم هستند، پس این تفاوت واقعی است. فایل با `chmod 600` ساخته و با یک `trap` مشترک پاک می‌شود (دو `trap EXIT` جدا همدیگر را می‌کشند).
- مسیرها با متغیر محیطی قابل جایگزینی‌اند (`APP_DIR`, `PHP_ROOT`, `SITE_FILE`, `DOMAIN`, `SCHEME`) — نه برای انعطاف، بلکه چون بدون آن هیچ‌کدام از این شاخه‌ها **آزمودنی نبودند**. همه‌ی شاخه‌ها (opcache روشن/خاموش/نامعلوم، pool کم‌ظرفیت، صفحه‌ی کند) با ساختنِ همان وضعیت واقعاً اجرا و دیده شده‌اند.
- تلاش ناموفقِ `--login` مثل هر تلاش دیگری در سدِ `LoginThrottle` شمرده می‌شود؛ پیام خطا خودش راهِ باز کردن قفل را می‌گوید.

## بخش معاملات (خرید و فروش)

- دفتری **جدا از درآمد/هزینه** برای خرید و فروش (`trades.php`). معامله هرگز ردیف `transactions` نمی‌سازد — سودش فقط در صفحه‌ی خودش است و گزارش‌های درآمد/هزینه دست‌نخورده می‌مانند. تست جدایی در `tests/test_trades.php` است.
- جدول‌ها: `trades` (خرید؛ qty اعشاری برای طلا/ارز) و `trade_sales` (فروش‌ها؛ فروش جزئی ممکن است). ساخته‌شده با `migration_trades.sql`.
- **پیش‌فرض خاموش؛ هر کاربر برای خودش** روشن می‌کند (ستون `users.trades_enabled`، کلید در پروفایل، اندپوینت `api/toggle_trades.php`). خاموش کردن داده را پاک نمی‌کند.
- منطق سود فقط در `tradesWithProgress()` است: بهای واحد = (خرید + هزینه‌ی جانبی) / تعداد؛ سود قطعی = جمع فروش‌ها − بهای واحد × فروخته‌شده. هزینه‌ی جانبی به نسبت فروخته‌شده اثر می‌گذارد.
- **سود هر فروش به حسابداری می‌رود** (`syncTradeProfitTransactions`): به ازای هر فروش، یک تراکنش با دسته‌ی «سود معاملات»/«زیان معاملات» برای *سهم سود* ساخته می‌شود (نه کل مبلغ). تراکنش سود عمداً **بدون حساب (wallet)** است چون جابه‌جایی پول را خودِ فروش در `walletBalances` حساب می‌کند — وگرنه دوبار جمع می‌شد. پیوند در `trade_sales.profit_tx_id` است (`migration_trades2`)؛ ویرایش/حذف فروش یا خرید، تراکنش سود را هم به‌روز/حذف می‌کند.
- **دارایی و معامله دو روی یک سکه‌اند:** صفحه‌ی `my-assets.php` نمای کلی همه‌ی دارایی‌هاست (دارایی‌های ثبت‌شده + کالای معاملاتیِ نفروخته) با نمودار و درصد — ولی **هیچ اقدام خرید/فروشی آنجا نیست**. در `trades.php` سه زبانه هست: موجودی / دارایی / سوابق معاملات. زبانه‌ی «دارایی» همان رکوردهای `assets` را می‌آورد و `api/sell_asset.php` فروششان را ثبت می‌کند: مقدار از `assets` کم می‌شود و همان مقدار به‌صورت یک معامله‌ی بسته‌شده در `trades` می‌نشیند (بهای خرید از `unit_price` همان دارایی). آن معامله عمداً `buy_wallet_id` ندارد، چون پولِ خرید قبلاً و جای دیگری خرج شده.
- **کم و زیاد کردن مقدار دارایی فقط در `my-assets.php`؛ خرید و فروش فقط در `trades.php`.** این مرز عمدی است — اگر بشکند، یک قلم از دو جا قابل تغییر می‌شود و اعداد از هم می‌پاشند.
- تنها نقطه‌ی تماس با بقیه‌ی اپ `walletBalances()` است: اگر معامله به حسابی وصل باشد، خرید کم و فروش اضافه می‌شود (با JOIN، نه ردیف تراکنش). `side_costs` عمداً روی موجودی اثر ندارد چون معلوم نیست از کجا پرداخت شده.
- **حسابِ فروش پیش‌فرض «کیف پول» است، نه «بدون حساب».** فرم فروش دیگر گزینه‌ی «بدون واریز» ندارد: پول فروش باید جایی بنشیند، وگرنه سود در حسابداری می‌آمد ولی هیچ حسابی زیاد نمی‌شد. کاربر می‌تواند همان فروش را ویرایش کند و به حساب واقعی ببرد. سمت سرور از `resolveWalletId()` می‌گذرد؛ سمت مرورگر `resetSelect()` فهرست را به گزینه‌ی پیش‌فرضِ HTML برمی‌گرداند (مقداردهی دستی به `'0'` انتخاب را خالی می‌گذاشت).
- ورودی تعداد با `sanitizeQty()` خوانده می‌شود (ارقام و ممیز فارسی را می‌فهمد) — `sanitizeAmount` نقطه‌ی اعشار را می‌اندازد و برای تعداد به کار نمی‌آید.
- فروش بیش از مانده با `SELECT ... FOR UPDATE` در `api/sell_trade.php` بسته شده.

## ورود با ایمیل و بازیابی رمز

- **تصویر صفحه‌ی ورود عکس پروفایل همان کاربر است**، اگر دستگاه نام کاربری‌اش را به خاطر داشته باشد (`Auth::getRememberedUsername()`). اگر عکسی ثبت نکرده یا دستگاه کسی را به خاطر ندارد، آیکن برنامه در **همان قاب گرد** می‌نشیند — شکل قاب در هر دو حالت یکی است.

- ایمیل هنگام **ثبت‌نام و ساخت کاربر الزامی است** (`setup.php`, `admin/users.php`, `api/update_profile.php`, `profile.php`). ویرایش کاربر توسط مدیر عمداً الزامی نیست تا حساب‌های قدیمیِ بدون ایمیل قفل نشوند.
- ورود با **نام کاربری یا ایمیل** انجام می‌شود (`Auth::attemptLogin()`). پیام خطای «رمز غلط» و «حساب وجود ندارد» یکسان است تا وجود حساب لو نرود.
- توکن بازیابی الگوی selector + validator دارد (مثل `trusted_devices`)؛ اعتبار ۶۰ دقیقه، حداکثر ۳ درخواست برای هر کاربر و ۱۰ برای هر IP.
- **همه‌ی زمان‌ها در دیتابیس ساخته و مقایسه می‌شوند** (`DATE_ADD(NOW(), ...)`, `expires_at <= NOW()`). یک بار که PHP زمان را می‌ساخت و MySQL مقایسه می‌کرد، لینک بازیابی بلافاصله «منقضی» می‌شد چون `Auth::initSession()` منطقه‌ی زمانی PHP را عوض می‌کرد. تست رگرسیونش در `tests/test_password_reset.php` است.
- لینک ایمیل از `APP_URL` ساخته می‌شود، نه از `$_SERVER['HTTP_HOST']` (`appBaseUrl()`) — وگرنه با Host header جعلی می‌شد لینک بازیابی را به سایت مهاجم برد.
- بدون تنظیم `MAIL_METHOD`، صفحه‌ی فراموشی رمز صادقانه می‌گوید در دسترس نیست به‌جای اینکه وانمود کند ایمیل فرستاده شد.
- **ایمیل هرگز با ورودی خالی پاک نمی‌شود.** تنها مسیر نوشتن ایمیل تابع `saveUserEmail()` در `functions.php` است (پنل مدیر، `api/update_profile.php` و `deploy/user-admin.php` همه از همان استفاده می‌کنند). رشته‌ی خالی یعنی «دست نزن»؛ پاک کردن فقط با `$allowClear = true` صریح ممکن است. دلیلش یک باگ واقعی است: فرم ویرایش کاربر یکی است و مقدار فعلی ایمیل را جاوااسکریپت از `data-email` پر می‌کند؛ هر بار که آن اسکریپت اجرا نمی‌شد، یک ذخیره‌ی ساده ایمیل ثبت‌شده را بی‌سروصدا NULL می‌کرد و کاربر فقط می‌دید «ایمیل نمی‌مونه». تست رگرسیونش در `tests/test_user_email.php` است.
- وجود ستون `email` را با `usersHaveEmailColumn()` بپرسید، نه با کوئری دستیِ `information_schema` — نتیجه در همان درخواست کش می‌شود.
- **در پشتی همیشه هست:** `php deploy/user-admin.php --reset ali` مستقیم با دیتابیس کار می‌کند و به ایمیل وابسته نیست. فقط از CLI اجرا می‌شود (`PHP_SAPI !== 'cli'` → 404).

## طرحِ اشتراک — مدلِ کامل، اجرای خاموش

- **⛔ امروز هیچ قابلیتی محدود نمی‌شود، و این عمدی است.** نصب‌هایی که الان کار
  می‌کنند همه چیز را دارند؛ اگر یک به‌روزرسانی ناگهان چیزی را از کاربرِ فعلی
  بگیرد، بدترین کاری است که می‌شود کرد. `planEnforced()` پیش‌فرض خاموش است و فقط
  مالکِ نصب روشنش می‌کند. یعنی این لایه فعلاً فقط **می‌شمارد و ثبت می‌کند**.
- **`planAllows()` تنها جای این تصمیم است.** هر گیتِ تازه‌ای باید از همان رد شود،
  وگرنه روشن کردنِ اجرا بعضی جاها اثر می‌کند و بعضی جاها نه. فهرستِ Pro-only
  عمداً کوتاه است (`trades`, `api`, `reminders`): **هسته‌ی اپ — تراکنش، حساب،
  چک، طلب و بدهی — هرگز پولی نمی‌شود**، وگرنه اپ برای کسی که پول نمی‌دهد
  بی‌فایده است و اصلاً امتحانش نمی‌کند.
- **⛔ انقضا با تاریخ است (`users.pro_until`)، نه با کلیدِ روشن/خاموش.** با
  بولین، اولین باری که کسی تمدید نکند باید یک کارِ زمان‌بندی‌شده خاموشش کند — و
  آن کار روزی از کار می‌افتد و همه بی‌سروصدا Pro می‌مانند. با تاریخ، انقضا خودش
  اتفاق می‌افتد و هیچ cron ای لازم نیست.
- **⛔ تمدید از بیشترِ «امروز» و «انقضای فعلی» شروع می‌شود**
  (`GREATEST(COALESCE(pro_until, CURDATE()), CURDATE())`). اگر همیشه از امروز
  حساب می‌شد، کسی که یک ماه زودتر تمدید کند روزهای باقی‌مانده‌اش را از دست
  می‌داد — یعنی تنبیهِ کاربرِ خوش‌حساب، و او فقط می‌بیند عددِ روزها کمتر از
  انتظارش است.
- همه‌ی حسابِ تاریخ در **دیتابیس** انجام می‌شود نه در PHP — همان درسی که لینکِ
  بازیابیِ رمز داد (منطقه‌ی زمانیِ PHP و MySQL یکی نبود و لینک بلافاصله «منقضی»
  می‌شد).
- **کارت‌به‌کارت با تأیید دستی، نه درگاه.** درگاه پرداخت ایرانی نماد اعتماد و
  شخصیت حقوقی می‌خواهد؛ تا آن روز این کار می‌کند و هیچ وابستگیِ بیرونی ندارد.
  `payments.method` رشته است تا وقتی درگاه آمد مقدارِ تازه اضافه شود بی‌آنکه
  ساختار عوض شود.
- **اعلامِ پرداخت فقط یک ادعاست** تا وقتی مدیر تأییدش کند؛ هیچ چیزی روی
  `pro_until` نمی‌نشیند. یک پرداختِ در انتظار در هر زمان مجاز است، وگرنه صفحه‌ی
  مدیر پر از تکراری می‌شود.
- **پرداخت‌ها حذف نمی‌شوند**، فقط وضعیتشان عوض می‌شود — تاریخچه‌ی مالی بین شما و
  کاربر باید بماند، حتی پرداختِ ردشده.
- اگر قیمت یا شماره‌ی کارت تنظیم نشده باشد، `pro.php` صریح می‌گوید هنوز راه
  نیفتاده — فرمی که کار نمی‌کند از نبودنش بدتر است.
- تست در `tests/test_plans.php` است (۳۰ بررسی) و با شش جهش سنجیده شده: تمدید از
  امروز (خوردنِ روزهای باقی‌مانده)، پیش‌فرضِ روشنِ اجرا، بستنِ هسته‌ی اپ، تأییدِ
  دوباره‌ی یک پرداخت، نادیده گرفتنِ ضریبِ تخفیف، و نادیده گرفتنِ انقضا.

## ثبت‌نام خودسرویس — پشتِ کلیدِ مدیر

- **⛔ پیش‌فرض خاموش، و این قابل مذاکره نیست.** هر نصبی که تا امروز بالا آمده یک
  دفترِ خصوصی است که مدیرش خودش کاربرها را می‌سازد. روشن شدنِ خودکارِ ثبت‌نام با
  یک `git pull` — در ساکت‌ترین حالتِ ممکن — آن دفتر را به روی اینترنت باز
  می‌کرد. کلید در `admin/users.php` است (`app_settings.allow_signup`).
- **کلیدِ خاموش یعنی صفحه‌ی `register.php` ۴۰۴ می‌دهد، نه «غیرفعال است».** پیام
  «غیرفعال» به یک اسکنر می‌گوید اینجا اپی هست که ثبت‌نام دارد و فقط خاموش است.
  لینکِ صفحه‌ی ورود هم فقط وقتی روشن باشد رندر می‌شود.
- **⛔ `createUserAccount()` و `validateNewUser()` تنها مسیرِ ساختِ کاربرند** و هم
  ثبت‌نامِ خودسرویس از آن‌ها رد می‌شود هم پنل مدیر. پیش از این پنل مدیر نسخه‌ی
  خودش را داشت؛ با دو نسخه شدن، قاعده‌ای مثل «کاربر تازه باید کیف پول داشته
  باشد» از یکی می‌افتاد و آن‌وقت اولین تراکنشِ آن کاربر در هیچ حسابی نمی‌نشست.
- **ایمیل در ثبت‌نام الزامی است، برخلافِ ویرایشِ کاربر در پنل مدیر.** کسی که خودش
  ثبت‌نام می‌کند مدیری ندارد که رمزش را برایش عوض کند؛ بدون ایمیل، اولین
  فراموشیِ رمز یعنی از دست رفتنِ حساب.
  - ⚠ شرطِ صریحِ «خالی» فقط برای **پیام** است، نه برای رد کردن: `filter_var`
    رشته‌ی خالی را هم رد می‌کند. برداشتنِ آن شرط تست را نمی‌شکست تا وقتی که
    تست خودِ **متنِ پیام** را نسنجید — حالا می‌سنجد.
- سدِ ثبت‌نامِ انبوه از همان جدولِ `login_attempts` استفاده می‌کند با کلیدِ ثابتِ
  `__signup__`، پس با شمارشِ ورودِ ناموفق قاطی نمی‌شود و جدولِ تازه‌ای لازم ندارد.
- تست در `tests/test_signup.php` است (۲۸ بررسی) و **صفحه را با HTTP هم می‌سنجد**،
  نه فقط تابع را: «تنظیم خاموش است» با «صفحه واقعاً بسته است» یکی نیست — همان
  درسی که قاعده‌ی nginx در `api/v1` داد. با پنج جهش سنجیده شده، از جمله روشن
  کردنِ پیش‌فرض و برداشتنِ کیف پولِ پیش‌فرض.

## ورود با کد پیامکی — پشتِ دو کلیدِ جدا

- **⛔ پیش‌فرض خاموش، و دو شرطِ جدا لازم است:** کلیدِ مدیر
  (`app_settings.allow_sms_login`، در `admin/users.php`) **و**
  `SMS_METHOD` در `config.php`. هیچ‌کدام بی‌دیگری کار نمی‌کند.
  دلیلش این است که این قابلیت یک **راهِ ورودِ دومِ کاملِ** به هر حسابی
  است؛ روشن شدنش با یک `git pull` یعنی دری که مالکِ نصب نمی‌داند وجود
  دارد. `SmsLogin::available()` هر سه شرط (کلید، جدول، پنل) را با هم
  می‌سنجد و صفحه‌ها فقط همان را می‌پرسند.
- **کلیدِ خاموش یعنی `sms-login.php` ۴۰۴ می‌دهد**، نه «غیرفعال است» —
  همان قاعده‌ی `register.php`. لینکش در صفحه‌ی ورود هم فقط وقتی رندر
  می‌شود که واقعاً کار کند، وگرنه لینک به بن‌بست می‌برد.
- **چهار محافظ در `SmsLogin`، و هیچ‌کدام اختیاری نیست:**
  1. **⛔ سقفِ تلاش روی خودِ کد** (`sms_codes.attempts`، سقف ۵). کدِ
     ۵ رقمی ۱۰۰٬۰۰۰ حالت دارد؛ بدون این، مهاجمی که شماره را می‌داند یک
     کد می‌گیرد و آزادانه حدس می‌زند — یعنی ورودِ پیامکی از ورودِ با
     رمز **ضعیف‌تر** می‌شد. مهم‌ترین محافظِ کلِ قابلیت.
  2. **یک‌بارمصرف بودن** (`used_at`): وگرنه هر کسی که متنِ پیامک را
     ببیند تا آخرِ پنجره‌ی ۳ دقیقه‌ای وارد می‌شود.
  3. **سقفِ درخواست** (۳ در ساعت برای هر شماره، ۱۰ برای هر IP، و ۶۰
     ثانیه فاصله). اینجا مسئله فقط امنیت نیست، **پول** است: هر پیامک
     هزینه دارد و یک اندپوینتِ بی‌سقف اعتبارِ پنل را در چند دقیقه خالی
     می‌کند. این سقف **پیش از** پیدا کردنِ کاربر سنجیده می‌شود، وگرنه
     شماره‌های تصادفی از آن رد می‌شدند.
  4. **پیامِ یکسان** برای شماره‌ی موجود و ناموجود، وگرنه این صفحه یک
     ابزارِ آماده برای فهمیدنِ «چه کسی کاربرِ این سایت است» می‌شود. برای
     شماره‌ی ناموجود هیچ ردیفی هم ساخته نمی‌شود.
- **کدِ خام هرگز ذخیره نمی‌شود، فقط `sha256`** — همان الگوی `api_tokens`
  و `password_resets`. کدی که کوتاه است و در بکاپ خام بماند، هر کسی که
  یک بار به دیتابیس برسد می‌تواند در همان چند دقیقه با آن وارد شود.
- **کدهای بازِ قبلیِ همان شماره سوزانده می‌شوند.** دو کدِ همزمان معتبر
  یعنی دو برابر شدنِ شانسِ حدس، بی‌هیچ سودی.
- **⛔ `SmsLogin::normalizePhone()` تنها جایی است که شماره معنا می‌گیرد**
  (مثل `categoryScopeSql()`). `+98`، `0098`، `98…`، `9…`، فاصله، خط‌تیره
  و ارقامِ فارسی همه به `09xxxxxxxxx` می‌رسند. اگر ثبتِ شماره و ورود با
  شماره از دو نرمال‌سازی رد شوند، تطبیق **بی‌صدا** شکست می‌خورد: کد
  فرستاده می‌شود، کاربر می‌زندش، و «پیدا نشد». نوشتنِ شماره هم فقط از
  `saveUserPhone()` می‌گذرد که خودش از همان تابع رد می‌شود.
- **`Auth::establishSession()` از `attemptLogin()` جدا شد** — به همان
  دلیلی که `verifyCredentials()` جدا شده بود: با آمدنِ ورودِ پیامکی باید
  بار دوم نوشته می‌شد و دو نسخه از «ورود» دیر یا زود از هم دور می‌افتند.
  کلیدِ جاافتاده در یکی (مثلاً `session_minutes`) یعنی مهلتِ نشست برای آن
  مسیر بی‌صدا به پیش‌فرض برمی‌گردد.
- سدِ عمومیِ حدس از همان `LoginThrottle` می‌آید، با کلیدِ **شماره** نه
  نام کاربری: با نام کاربری، مهاجم می‌توانست با درخواستِ کد، ورودِ **با
  رمزِ** همان حساب را هم قفل کند. راهِ باز کردنش هم همان همیشگی است
  (`deploy/user-admin.php --unlock`).
- **⛔ تنظیمِ پنل از صفحه‌ی مدیر هم ممکن است، ولی `config.php` برنده
  است.** `smsSetting()` اول ثابتِ `config.php` را می‌بیند و فقط اگر
  خالی بود سراغِ `app_settings` می‌رود. دو دلیل: کسی که فقط می‌خواهد
  پنلِ کاوه‌نگارش را وصل کند نباید مجبور باشد با SSH یک فایل PHP را
  ویرایش کند (وگرنه این قابلیت عملاً وصل نمی‌شود)، ولی هم‌زمان کسی که
  به پنل مدیر دسترسی پیدا کند نباید بتواند پیامک‌ها را به پنلِ خودش
  ببرد.
  - ⚠ کلیدِ ذخیره‌شده در `app_settings` **در بکاپِ دیتابیس هم می‌آید**.
    فرم همین را صریح می‌گوید و راهِ جایگزین (`config.php`) را نشان
    می‌دهد.
  - فرم مقدارِ فعلیِ کلید و رمز را **نشان نمی‌دهد**، فقط «ثبت شده».
    خالی گذاشتنشان یعنی «دست نزن» (قاعده‌ی `saveUserEmail`)؛ پاک کردن
    با تایپِ یک خط تیره.
  - `Sms::missingFor()` می‌گوید هر پنل چه چیزی کم دارد، و همان زیرِ
    منوی انتخابِ پنل نوشته می‌شود — «آماده است» یا «کلید API وارد نشده
    است». کلیدی که کار نمی‌کند بدتر از نبودنش است.
- **`includes/sms.php` راه‌اندازِ پنل است** و کاوه‌نگار، sms.ir و
  ملی‌پیامک را می‌شناسد، به‌علاوه‌ی `log` برای توسعه که هیچ‌جا نمی‌فرستد و
  کدها را در `var/sms.log` می‌نویسد. **روی سرورِ واقعی `log` یعنی کدهای
  ورود در یک فایلِ متنی روی دیسک.**
  - **۲۰۰ گرفتن از پنل کافی نیست:** هر سه پنل وضعیت را داخلِ بدنه
    می‌گذارند، پس بدنه هم خوانده می‌شود. وگرنه «فرستاده شد» گفته می‌شد
    برای پیامکی که پنل رد کرده.
  - هم `curl` هم `stream` پشتیبانی می‌شوند: pool این اپ
    `exec`/`shell_exec` را غیرفعال کرده (قانون جداسازی سرور) پس curlِ
    خط‌فرمانی ممکن نیست، و افزونه‌ی curl روی هر نصبی نیست. اگر هیچ‌کدام
    نبود صریح خطا می‌دهد — سکوت یعنی کدی که نرفته ولی «موفق» شده.
- **شماره اختیاری است، برخلافِ ایمیل.** کاربری که ورودِ پیامکی نمی‌خواهد
  نباید مجبور شود شماره بدهد. فیلدش در پروفایل فقط وقتی دیده می‌شود که
  قابلیت واقعاً در دسترس باشد، و خالی گذاشتنش شماره‌ی قبلی را **پاک
  نمی‌کند** (همان قاعده‌ی `saveUserEmail`).
- **Web Push و ورود با OTP روی api/v1 عمداً نیامدند.** آن لایه توکنِ
  ۹۰ روزه دارد و اضافه کردنِ یک مسیرِ ورودِ دومِ بدون‌رمز به آن، سطحِ حمله
  را بی‌دلیل بزرگ می‌کند.
- تست در `tests/test_sms_login.php` است (۳۷ بررسی) و با **ده جهش**
  سنجیده شده — همه گرفته شدند: پیش‌فرضِ روشن، برداشتنِ سقفِ تلاشِ کد،
  یک‌بارمصرف نبودنِ کد، پیامِ متفاوت برای شماره‌ی ناموجود، فرستادنِ کد
  برای حسابِ غیرفعال، برداشتنِ سقفِ ساعتی، نسوزاندنِ کدهای قبلی، خرابیِ
  نرمال‌سازی، ذخیره‌ی کدِ خام، و نادیده گرفتنِ انقضا.
  - ⚠ نکته‌ی مهمِ خودِ تست: سنجشِ سقفِ تلاشِ کد باید سابقه‌ی
    `LoginThrottle` را بین تلاش‌ها پاک کند، وگرنه سدِ **عمومی** (که سقفش
    کمتر است) اول می‌گیرد و تست حتی با برداشتنِ سقفِ کد هم سبز می‌ماند —
    یعنی چیزی را می‌سنجید که فکر می‌کردیم.

## بکاپ: `backup.php` و پسوندِ `.sthesab`

- **⛔ صفحه‌ی جدا دارد چون بن‌بست می‌ساخت.** پیش از این دکمه‌ی خروجی یک
  فرمِ POST مستقیم به `api/export_data.php` بود. مسیرِ موفق مشکلی
  نداشت (مرورگر فایل را می‌گیرد و صفحه عوض نمی‌شود)، ولی هر **شکستی**
  کاربر را در یک صفحه‌ی سفید با یک خطِ JSON رها می‌کرد — بی‌منو،
  بی‌دکمه، و در اپِ نصب‌شده حتی بی‌دکمه‌ی بازگشتِ مرورگر.
- دو لایه جلویش را می‌گیرد و **هر دو لازم‌اند**: `backup.php` دکمه‌ی
  «بازگشت» دارد که همیشه سرِ جایش است (چه بکاپ بگیرد چه پشیمان شود)، و
  خودِ اندپوینت هم با `exportFail()` در هر شکستی به همان‌جا برمی‌گردد.
  - ⚠ استثناء: **بدونِ ورود همچنان ۴۰۱ می‌دهد، نه هدایت.** قاعده‌ی
    `api/` این است و `test_api_auth` هر ۵۹ اندپوینت را با هم می‌سنجد.
    کاربرِ واقعی هم به آنجا نمی‌رسد چون `backup.php` خودش
    `requireLogin()` دارد.
  - ⚠ `Csrf::isJsonRequest()` **خصوصی** است و از بیرون صدا زده نمی‌شود؛
    `exportFail()` همان شرط را خودش دارد (هم `X-Requested-With` هم
    `Accept`). یک بار فراموش شد و اندپوینت ۵۰۰ داد.
- **`backupFileName()` تنها جای ساختنِ نامِ فایل است**: فقط تاریخِ شمسیِ
  همان روز و پسوندِ `.sthesab` — `1405-06-12.sthesab`. کوتاه است تا در
  فهرستِ دانلودِ گوشی کامل خوانده شود، و نام روی صفحه هم **پیش از**
  زدنِ دکمه نشان داده می‌شود تا کاربر بداند دنبالِ چه بگردد.
  - درونش JSON است ولی پسوندِ `.json` یعنی مرورگر و پیام‌رسان سعی
    می‌کنند بازش کنند و کاربر یک دیوارِ متن می‌بیند. `Content-Type` هم
    به همین دلیل `application/octet-stream` شد.
  - ⚠ ارقام **لاتین**اند و `/` به `-` می‌رود: نامِ حاوی ارقامِ فارسی در
    سرآیندِ `filename=` غیرمجاز است و مرورگر بی‌سروصدا کنارش می‌گذارد.

## خروجیِ کاملِ داده و حذفِ حساب

- **دلیل وجودشان:** کاربری که حس کند داده‌اش گروگان است پول نمی‌دهد. «هر وقت
  خواستی همه‌اش را ببر و هر وقت خواستی پاکش کن» تنها چیزی است که این حس را از
  بین می‌برد — برای فروش پیش‌نیاز است نه تزئین. هر دو در پروفایل، بخش «داده‌ی من».
- **⛔ فهرستِ جدول‌ها از خودِ دیتابیس کشف می‌شود** (`userDataTables()`: هر جدولی
  که ستون `user_id` دارد)، نه از یک آرایه‌ی دستی. فهرستِ دستی دیر یا زود عقب
  می‌افتد و اینجا عقب افتادنش دو خرابیِ بی‌صدا می‌سازد: خروجی‌ای که کامل نیست، و
  حذفی که چیزی جا می‌گذارد.
- خروجی JSON است، ستون‌های رمزشده در آن **باز** می‌شوند (فایلی که نمی‌شود خواند
  خروجی نیست)، و `password_hash` و جدول‌های اعتبارنامه (`api_tokens`,
  `password_resets`, `trusted_devices`) در آن نمی‌آیند — آن‌ها داده‌ی کاربر
  نیستند و فقط سطحِ حمله را بزرگ می‌کنند.
  - ⚠ نامِ فایل با **ارقام لاتین** ساخته می‌شود. نامِ حاوی ارقامِ فارسی در سرآیندِ
    `filename=` غیرمجاز است و مرورگر بی‌سروصدا کنارش می‌گذارد و فایل را
    «download» ذخیره می‌کند. با کروم دیده شد.
  - ⚠ بافرِ gzip که `db.php` روشن می‌کند باید پیش از ارسالِ فایل بسته شود، وگرنه
    چیزی که ذخیره می‌شود دوبار فشرده است و باز نمی‌شود.
- حذف سه سد دارد: رمز فعلی، تایپِ عبارتِ «حذف حساب»، و اینکه آخرین مدیر نتواند
  خودش را حذف کند (`countOtherActiveAdmins()` که به همین مناسبت از
  `admin/users.php` به `functions.php` منتقل شد — قاعده‌ی دامنه است نه جزئیاتِ
  یک صفحه).
- **⛔ و یک سدِ چهارم داخلِ خودِ `deleteUserAccount()`:** پیش از `commit` شمرده
  می‌شود که از هر جدول بیش از سهمِ همین کاربر حذف نشده باشد؛ اگر شد، rollback.
  - **دلیلش یک خرابیِ واقعی است.** هنگام آزمونِ جهش، نسخه‌ی جهش‌یافته‌ی این تابع
    (بدونِ شرطِ `user_id`) روی دیتابیسِ **توسعه** اجرا شد و **کامیت کرد**. تست
    بعدش خرابی را دید، ولی داده از قبل رفته بود. یک تستِ خوب جلوی حذفِ
    کامیت‌شده را نمی‌گیرد؛ خودِ کد باید بگیرد. با اجرای همان جهش روی یک
    دیتابیسِ موقت سنجیده شد: حالا رد می‌شود و چیزی از دست نمی‌رود.
  - **⛔ جهش‌سنجیِ `tests/test_user_data.php` را روی دیتابیسِ دارای داده اجرا
    نکنید.** همین هشدار بالای خودِ فایل هم هست.
- ترتیبِ حذف از روی کلیدهای خارجی حدس زده نمی‌شود بلکه **چند دور** تلاش می‌شود؛
  دوری که هیچ پیشرفتی نداشته باشد یعنی واقعاً گیر کرده و با خطا برمی‌گردد.
  ساختنِ فهرستِ ترتیب دستی همان «فهرستِ دوم» می‌شد.
- `privacy.php` عمداً کوتاه و بی‌تعارف است و **هر ادعایش باید در کد قابل نشان
  دادن باشد**. اگر روزی یکی از آن‌ها عوض شد، این صفحه هم باید همان روز عوض شود.
  وضعیتِ رمزنگاری را از `Crypto::available()` می‌خواند، نه از یک جمله‌ی ثابت.
- **نسخه از `var/version.txt` خوانده می‌شود** که `deploy.sh` هنگام استقرار
  می‌نویسد. از `git` خوانده نمی‌شود چون pool این اپ `exec`/`shell_exec` را
  غیرفعال کرده (قانون جداسازی سرور).
- تست در `tests/test_user_data.php` است (۱۹ بررسی) و با چهار جهش سنجیده شده:
  فهرستِ دستیِ ناقص، ماندنِ هشِ رمز در خروجی، حذف بدونِ شرطِ `user_id`، و آمدنِ
  جدول‌های اعتبارنامه در خروجی.

### ⛔ رمزنگاریِ شماره کارت، شماره حساب و شبا

- **پیش‌فرض خاموش، و این عمدی است.** بدون `APP_ENCRYPTION_KEY` همه چیز دقیقاً
  مثل قبل کار می‌کند (خام). روشن کردنش: `php deploy/encrypt-cards.php --setup`
  که کلید می‌سازد، در `config/config.php` می‌نویسد، و از کانفیگ قبلی بکاپ
  می‌گیرد. بعدش `--apply` ردیف‌های موجود را مهاجرت می‌دهد.
- **⛔ کلید گم شود، داده رفته است.** بکاپِ دیتابیس **بدونِ کلید قابل بازیابی
  نیست** — کلید در `config/config.php` است که در دامپ نمی‌آید. این عمدی است:
  اگر کلید کنارِ دیتابیس بماند، رمزنگاری بی‌معناست. `restore.sh` بعد از بازیابی
  اگر مقدارِ رمزشده ببیند صریحاً هشدار می‌دهد.
- **`Crypto` (`includes/crypto.php`) تنها جای این کار است.** قالب:
  `enc:v1:` + base64(نانس ‖ متنِ رمزشده) با `sodium_crypto_secretbox`
  (XSalsa20-Poly1305، احراز اصالت‌دار).
  - **رمزگشاییِ چیزی که رمز نشده، خودِ همان است.** ردیف‌های قدیمی خام‌اند و باید
    بخوانند؛ بدون این، روشن کردنِ رمزنگاری همه‌ی شماره‌کارت‌های موجود را
    ناپدید می‌کرد.
  - شکستِ رمزگشایی `null` می‌دهد نه رشته‌ی آشغال — آشغال می‌رفت داخل فرم و
    کاربر ذخیره‌اش می‌کرد.
  - نانس تصادفی است: با نانسِ ثابت، دو حسابِ هم‌شماره مقدارِ ذخیره‌شده‌ی یکسان
    می‌گرفتند و از روی خودِ دیتابیس می‌شد فهمید کدام کاربران یک کارت دارند.
- **جاهایی که باید از `Crypto` رد شوند:** نوشتن در `api/save_wallet.php`،
  خواندن در `walletBalances()` و `walletCardTails()`. هر کوئریِ تازه‌ای که این
  ستون‌ها را بیاورد هم باید `Crypto::decryptRow()` بخورد، وگرنه کاربر رشته‌ی
  `enc:v1:…` را داخل فرمِ خودش می‌بیند و با ذخیره کردنش شماره‌اش را نابود
  می‌کند. ⚠ در `walletCardTails()` رمزگشایی باید **پیش از** برداشتنِ ارقام
  باشد، وگرنه «از پیامک بانک» حساب را با چهار رقمِ تصادفی اشتباه می‌گیرد.
- **ستون‌ها با `migration_wallet_encrypt` به ۲۵۵ پهن می‌شوند.** بدون آن، روی
  پیکربندیِ غیر-strict مقدار **بی‌صدا بریده** می‌شد و رمزگشایی برای همیشه شکست
  می‌خورد. شاهدِ این migration در `migrate.sh` از نوع تازه‌ی
  `جدول.ستون>=طول` است؛ شاهدِ «وجود ستون» اینجا همیشه درست می‌بود و
  `--verify` دقیقاً همان دروغی را می‌گفت که برای گرفتنش ساخته شده.
- `deploy/encrypt-cards.php` سه محافظ دارد و هیچ‌کدام اختیاری نیست: هر ردیف
  **پیش از نوشتن** رفت‌وبرگشت آزموده می‌شود، همه در یک تراکنش‌اند، و بعد از
  نوشتن همه‌ی مقدارها دوباره خوانده و با اصل مقایسه می‌شوند. `--decrypt-all`
  راهِ برگشت است.
- تست در `tests/test_crypto.php` است (۱۹ بررسی) و با پنج جهش سنجیده شده: نانسِ
  ثابت، `null` کردنِ مقدارِ خام، لایه‌ی دومِ رمز، نادیده گرفتنِ شکستِ رمزگشایی،
  و بی‌اثر کردنِ `decryptRow`.

### ⛔ تگِ پایانِ PHP داخل کامنت

- نوشتنِ آن تگ داخل یک کامنتِ `//` هم PHP را **می‌بندد** و بقیه‌ی فایل HTML
  حساب می‌شود. یک بار در `deploy/encrypt-cards.php` همین شد (در جمله‌ای که
  توضیح می‌داد «پیش از تگِ پایانِ PHP اضافه می‌شود») و خطای «Unclosed {» داد که
  ۵۰ خط بالاتر را نشان می‌داد. **اینجا شانس آوردیم؛** در فایلی که آکولادهایش
  بالانس باشد هیچ خطایی نمی‌دهد و فقط نصفِ فایل بی‌صدا اجرا نمی‌شود — یا سورس
  به مرورگر می‌رود. قاعده ۱۵ در `test_api_contract.php` این را می‌سنجد.

### ⛔ رمز که عوض شد، هر دسترسیِ قبلی باطل می‌شود

- **`revokeAllAccessFor()` تنها جای این کار است** (مثل `categoryScopeSql()`). هر
  چهار مسیرِ نوشتنِ رمز از آن رد می‌شوند: `api/change_password.php`,
  `includes/password_reset.php`, `admin/users.php`, `deploy/user-admin.php`.
- **چرا لازم است:** توکنِ `api/v1` نه به رمز وابسته است نه به نشست — ۹۰ روز زنده
  می‌ماند و رمزِ تازه هیچ اثری رویش ندارد. کاربری که حسابش لو رفته رمز را عوض
  می‌کند، پیام «رمز عوض شد» می‌گیرد، خیالش راحت می‌شود، و مهاجم همچنان با اپ
  موبایل داخل است. **هیچ خطایی، هیچ نشانه‌ای.** پیش از این فقط `trusted_devices`
  باطل می‌شد.
- **⚠ ترتیبِ `require` داخلِ خودِ تابع اهمیت دارد و یک بار شکست:** نسخه‌ی اول اول
  `Auth::revokeAllDevices()` را صدا می‌زد و بعد `api_auth.php` را لود می‌کرد.
  `password_reset.php` تنها `db/mailer/functions` را لود می‌کند، پس آنجا کلاس
  `Auth` وجود نداشت و فراخوانی خطای کشنده می‌داد — که در همان فایل با
  `catch (Throwable)` بلعیده می‌شد و بازیابیِ رمز بی‌صدا **هیچ** دسترسی‌ای را
  باطل نمی‌کرد. `test_password_reset` گرفتش.
- دو تست جدا و هر دو لازم: قاعده ۱۴ در `test_api_contract.php` *شکل* را می‌سنجد
  (هر فایلی که `UPDATE users SET … password_hash` دارد باید تابع را صدا بزند، پس
  مسیرِ پنجمی که فردا اضافه شود جا نمی‌ماند)، و `tests/test_token_revoke.php`
  *رفتار* را: توکنِ واقعی می‌سازد، ثابت می‌کند کار می‌کند، رمز را عوض می‌کند، و
  ثابت می‌کند در یک **پروسه‌ی جدا** دیگر کار نمی‌کند (کشِ ایستای
  `ApiAuth::$current` وگرنه جوابِ کهنه می‌دهد). با سه جهش سنجیده شده، از جمله
  برداشتنِ شرطِ `user_id` که تغییر رمزِ یک نفر همه‌ی کاربران را بیرون می‌انداخت.

### فایل‌های خط‌فرمانی داخل ریشه‌ی وب

`tests/` و `deploy/` داخل `/opt/hesab/app` هستند، یعنی داخل ریشه‌ی وب. دو لایه جلویشان را می‌گیرد و **هر دو لازم‌اند**:

1. سایت nginx این مسیرها را می‌بندد: `/deploy/`, `/tests/`, `/config/`, `/includes/`, `.git`, فایل‌های نقطه‌دار، و پسوندهای `.sql` / `.md` / `.sh`.
2. **خودِ فایل نگهبان دارد:** `if (PHP_SAPI !== 'cli') { http_response_code(404); exit; }`.

لایه‌ی دوم اختیاری نیست. لایه‌ی اول می‌تواند از دست برود — certbot فایل سایت را بازنویسی می‌کند، یا کسی روی هاست دیگری نصب می‌کند — و آن‌وقت باز کردن `https://…/tests/test_trades.php` در مرورگر، تست را روی دیتابیس **واقعی** اجرا می‌کرد: کاربر و کیف پول و معامله می‌ساخت و پاک می‌کرد، بدون هیچ احراز هویتی. `test_api_contract.php` وجود این نگهبان را روی همه‌ی `tests/test_*.php` و `deploy/*.php` می‌سنجد.

> در متن راهنمای اسکریپت‌ها placeholder به شکل `<...>` ننویسید. کاربر آن را عیناً کپی می‌کند و bash علامت `<` را تغییرمسیر ورودی می‌فهمد و فرمان با `syntax error near unexpected token` می‌میرد. به‌جایش مقدار نمونه‌ی واقعی بنویسید (`--set-email ali ali@gmail.com`).

## API نسخه‌دار برای اپ‌های موبایل (`api/v1`)

مرجع کامل برای کسی که اپ می‌نویسد: **`docs/API.md`**.

- **`api/` و `api/v1/` دو چیز کاملاً جدا هستند.** `api/` لایه‌ی داخلیِ AJAX خودِ سایت است: با نشست و کوکی و CSRF کار می‌کند، آزادانه با صفحه‌ها عوض می‌شود، و چهار تای آن (`day_detail`, `category_transactions`, `savings_history`, `transaction_attachments`) اصلاً HTML برمی‌گردانند نه JSON. **اپ موبایل هرگز نباید به آن‌ها وصل شود.**
- **قاعده‌ی نسخه، دلیلِ وجودِ این جداسازی است:** اپی که روی گوشی کاربر نصب شده را نمی‌شود مجبور به به‌روزرسانی کرد. اگر اپ روی `api/save_wallet.php` می‌نشست، هر تغییر در سایت همان لحظه هر اپ نصب‌شده‌ای را می‌شکست. در v1 **افزودن** مجاز است (کلید تازه، پارامتر اختیاری، اندپوینت تازه) و **تغییر یا حذف** ممنوع؛ هر تغییرِ شکننده → `api/v2/`.
- **احراز هویت با توکن است، نه نشست.** `includes/api_auth.php` با همان الگوی selector/validator ای که `trusted_devices` و `password_resets` دارند. توکن خام هرگز ذخیره نمی‌شود، فقط `sha256`. عمر ۹۰ روز، حداکثر ۱۰ توکن زنده برای هر کاربر.
  - **`Auth::userId()` و `cachedCategories()` در v1 ممنوع‌اند** چون کاربر را از نشست می‌گیرند و درخواست API نشستی ندارد — شناسه صفر می‌شد و کاربر فقط دسته‌های پیش‌فرض را می‌دید. قاعده ۹ در `test_api_contract.php` این را می‌سنجد (با توکنایزر، وگرنه همین توضیح، تست را می‌شکست — یک بار واقعاً شکست).
- **`Auth::verifyCredentials()` از `attemptLogin()` جدا شد.** اولی فقط رمز را می‌سنجد و به نشست دست نمی‌زند؛ دومی همان را صدا می‌زند و بعد نشست می‌سازد. بدون این جداسازی، منطق سدِ حدس رمز باید برای API دوباره نوشته می‌شد و دو نسخه‌اش از هم دور می‌افتادند.
- **منطق نوشتن تراکنش در `includes/transactions.php` است و وب و API هر دو از همان‌جا رد می‌شوند.** پیش از این داخل `api/add_transaction.php` و `api/update_transaction.php` بود؛ با آمدن v1 باید بار دوم نوشته می‌شد و دو نسخه از منطقِ پول دیر یا زود از هم دور می‌افتند. آن توابع هیچ چیز چاپ نمی‌کنند و `exit` نمی‌کنند — فقط نتیجه برمی‌گردانند تا هر لایه پاکت خودش را بسازد.
- **تاریخ همیشه هم میلادی و هم شمسیِ آماده فرستاده می‌شود** (`Api::date()`). اگر اپ خودش تبدیل کند، می‌شود پیاده‌سازی سومِ تقویم شمسی — بیرون از پوشش `test_jalali_parity` — و دیر یا زود یک روز اختلاف پیدا می‌کند.
- **مبلغ همیشه عدد صحیح است** (`Api::money()`). ستون‌ها `BIGINT` اند؛ اگر به `double` برود، مقدارهای بزرگ دقتشان را از دست می‌دهند.
- **⛔ قاعده‌ی nginx هرگز `^~` نمی‌گیرد.** `^~` ارزیابیِ location های regex را متوقف می‌کند و `location ~ \.php$` هم regex است — یعنی nginx فایل PHP را به‌جای اجرا **خام** تحویل می‌دهد. یک بار روی سرور واقعاً همین شد: `/api/v1/ping` سورس کامل `index.php` را برگرداند و API اصلاً کار نکرد. `nginx -t` سبز بود و `reload` هم موفق، پس هیچ ابزاری جلویش را نگرفت. با nginx واقعی A/B شد: با `^~` سورس لو می‌رود، با prefix ساده به PHP می‌رسد. `deploy/nginx-api.sh` حالا بعد از اعمال خودِ `/api/v1/ping` را صدا می‌زند و اگر سورس یا چیزی جز JSON دید، پیکربندی را برمی‌گرداند؛ قاعده ۱۰ در `test_api_contract.php` هم `^~` را ممنوع می‌کند.
  - درسِ کلی‌ترش: **«پیکربندی معتبر است» با «درست کار می‌کند» یکی نیست.** هر اسکریپتی که پیکربندی وب‌سرور را عوض می‌کند باید آخرش خودِ نتیجه را بسنجد، نه فقط `nginx -t` را.
- **دو شکل آدرس، و فقط دومی بی‌نیاز از nginx است.** `/api/v1/transactions` به قاعده‌ی nginx نیاز دارد (`deploy/nginx-api.sh --apply`)؛ `/api/v1/index.php?p=transactions` همیشه کار می‌کند چون آدرس واقعی خودِ `index.php` است و به `location ~ \.php$` می‌خورد. **این دوگانگی عمدی است:** اگر certbot یا کسی فایل سایت را بازنویسی کند، آن قاعده از دست می‌رود و اپ‌های نصب‌شده نباید بخوابند.
  - ⚠ **`/api/v1/index.php/transactions` روی nginx کار نمی‌کند** و اولین نسخه‌ی سند اشتباه ادعا کرده بود که کار می‌کند. `~ \.php$` با آدرسی که به `/transactions` ختم می‌شود تطابق ندارد؛ آن شکل فقط روی سرور توسعه‌ی PHP جواب می‌دهد. با nginx واقعی سنجیده شد: `?p=` می‌رسد، `index.php/…` نمی‌رسد.
- **سنجشِ بعد از `reload` باید صبر داشته باشد.** `systemctl reload nginx` به‌محضِ فرستادنِ سیگنال برمی‌گردد؛ خودِ nginx غیرهمزمان بارگذاری می‌کند و پروسه‌های کارگرِ قدیمی تا صدها میلی‌ثانیه بعد هنوز با پیکربندیِ قبلی جواب می‌دهند. سنجشِ فوری همان پاسخِ قدیمی را می‌گرفت و `nginx-api.sh` پیکربندیِ **درست** را برمی‌گرداند — دو بار روی سرور واقعی همین شد و وقت زیادی گرفت. با nginx واقعی اندازه‌گیری شد: تا **۱۵۰ میلی‌ثانیه** پاسخِ قدیمی می‌آمد. حالا سنجش تا ۱۰ ثانیه تکرار می‌شود و فقط اگر تا آخر درست نشد رول‌بک می‌کند (مگر سورس خام که همان‌جا خطر است).
- **سنجشِ اسکریپت‌ها با `--resolve` باشد، نه `-H "Host: …"`.** روی این سرور چند سایت روی یک nginx هستند. `curl -H "Host: x" https://127.0.0.1` هیچ SNI ای نمی‌فرستد، پس nginx بلوکِ پیش‌فرض را برای دست‌دادن TLS برمی‌دارد و پاسخ ممکن است از سایتِ دیگری باشد — یک بار همین باعث شد `nginx-api.sh` پیکربندیِ **درست** را به‌خیالِ خراب بودن برگرداند.
- تست رفتاری در `tests/test_api_v1.php` است و قربانی را خودش می‌سازد. مهم‌ترین بخشش جداسازی کاربران است و با آزمون جهش سنجیده شده: برداشتن بررسی مالکیت یا فیلترِ `user_id` هر دو تست را می‌شکند.

## اپ اندروید (`mobile/`) — پوسته‌ی TWA روی همان PWA

**اپ هیچ کد برنامه‌ای ندارد و نباید داشته باشد.** یک *Trusted Web Activity*
است: پوسته‌ای که به اندروید می‌گوید کروم `hesab.stland.ir` را تمام‌صفحه و
بدون نوار آدرس باز کند. خودِ برنامه همین سایت PHP است. راهنمای کامل در
`mobile/README.md`.

- **اول یک اپ فلاتر ساخته شد و کنار گذاشته شد.** APK اش **۵۱ مگابایت** درآمد
  (موتور گرافیکی فلاتر برای هر سه معماری؛ کدِ ما شاید ۳۰۰ کیلوبایت از آن بود)
  در برابر زیر ۱ مگابایت برای TWA. ولی مسئله‌ی اصلی حجم نبود: آن اپ **۴ صفحه**
  داشت و سایت **همه چیز** — چک، طلب و بدهی، بودجه، پس‌انداز، دارایی، معاملات،
  گزارش‌ها. یعنی دو نسخه از یک برنامه که هر قابلیت باید دو بار نوشته می‌شد.
- **`api/v1` سر جایش ماند و حذف نشد.** اگر روزی اپ بومی لازم شد (اعلان، ویجت،
  کار آفلاینِ جدی) آن لایه با تست‌هایش آماده است.
- **⛔ سه چیز به هم گره خورده‌اند و شکستنشان بی‌صداست:** کلیدِ امضا در
  `KEYSTORE_BASE64`، اثر انگشتِ همان کلید در `.well-known/assetlinks.json`، و
  در دسترس بودنِ آن فایل روی دامنه. اگر یکی بخواند و بقیه نه، اپ **نصب می‌شود
  ولی با نوار آدرس بالا** — همه چیز سبز است و فقط کاربر می‌فهمد.
  - به همین دلیل workflow بعد از ساخت، اثر انگشتِ خودِ APK را با
    `keytool -printcert -jarfile` می‌خواند و با `assetlinks.json` می‌سنجد؛
    نخواند، ساخت را می‌شکند. **«ساخت موفق شد» با «درست کار می‌کند» یکی نیست** —
    همان درسی که قاعده‌ی nginx در `api/v1` داد.
  - امضا با کلیدِ debug اصلاً کار نمی‌کند: CI هر بار یک کلید تازه می‌سازد، پس
    اثر انگشت هر ساخت عوض می‌شود.
  - قاعده‌ی nginx `location ~ /\.(?!well-known)` عمداً `.well-known` را استثنا
    کرده، پس این مسیر از قبل باز است و تنظیم تازه‌ای نمی‌خواهد.
- **آدرس سرور فقط یک جا نوشته شده:** دو `manifestPlaceholders` در
  `mobile/app/build.gradle.kts`. `AndroidManifest.xml` هر دو را از همان‌جا
  می‌خواند. دامنه‌ی تازه یعنی `assetlinks.json` تازه روی همان دامنه.
- **`.github/workflows/android.yml` باید در ریشه‌ی مخزن بماند، نه در
  `mobile/`.** گیت‌هاب فقط `.github/workflows/` ریشه را می‌خواند؛ یک پله
  پایین‌تر **بی‌هیچ خطایی** نادیده گرفته می‌شود و زبانه‌ی Actions خالی می‌ماند.
  - فیلتر `paths` هم لازم است، وگرنه هر اصلاح PHP یک ساخت راه می‌اندازد که
    خروجی‌اش با قبلی مو نمی‌زند.
  - ⚠ `path:` در `upload-artifact` از **ریشه‌ی workspace** حساب می‌شود، نه از
    `working-directory`.
- ساخت روی گیت‌هاب انجام می‌شود چون Android SDK فقط از `dl.google.com` می‌آید
  و آن میزبان در محیط توسعه مسدود است. خروجی هم artifact می‌شود هم Release؛
  **Release برای نصب روی گوشی است**، چون artifact همیشه zip دانلود می‌شود و
  اندروید بدون فایل‌منیجر بازش نمی‌کند.
- **`deploy.sh` کل مخزن را روی سرور می‌ریزد**، پس `mobile/` هم آنجا می‌رود؛
  `vps-setup.sh` مثل `/deploy/` و `/tests/` از وب مسدودش می‌کند. ولی
  `.well-known/` **نباید** مسدود شود — بدون آن تأیید TWA کار نمی‌کند.

---

## پیش از هر push

```bash
bash tests/run.sh
```
کد خروج باید ۰ باشد. (بررسی نحو داخل همین مجموعه هست، پس `php -l` جدا لازم نیست.)

> **درج متغیر کنار گیومه‌ی فارسی:** `"کاربر «$username» فعال شد"` غلط است. PHP بایت‌های ≥ 0x80 را جزء نام متغیر می‌داند، پس نام متغیر `$username»` خوانده می‌شود و پیام وسطش خالی درمی‌آید. همیشه `{$username}` بنویسید. `php -l` این را نمی‌گیرد؛ تست `test_api_contract.php` با توکنایزر می‌گیرد.
