<?php
/**
 * معرفیِ اولیه — چند اسلاید، و همیشه یک راهِ بستن.
 *
 * ⛔ **فقط برای دفترِ خالی رندر می‌شود.** معرفیِ برنامه به کسی که
 *    شش ماه است از آن استفاده می‌کند، نه یک کمک است و نه بی‌ضرر:
 *    دقیقاً همان «هشدارِ همیشگی» است که آدم را عادت می‌دهد هر چیزی که
 *    اپ نشان می‌دهد را ببندد بی‌آنکه بخواند. شرطش را `index.php`
 *    می‌گذارد و همان‌جا هم هیچ کوئریِ تازه‌ای لازم ندارد — از روی
 *    فهرستی که از قبل خوانده شده.
 *
 * ⛔ **«حالتِ رد شدن» خواسته‌ی صریحِ مالکِ نصب بود، و اینجا یعنی هر
 *    راهِ خروجی نشانه را می‌زند** — نه فقط رسیدن به اسلایدِ آخر. کسی که
 *    اسلایدِ دوم را می‌بندد یعنی «نمی‌خواهم»؛ اگر فقط اسلایدِ آخر
 *    نشانه‌گذاری می‌کرد، همان آدم دفعه‌ی بعد دوباره همین را می‌دید و
 *    معرفی به یک مزاحم تبدیل می‌شد.
 *
 * ⛔ **`.modal-overlay` است، نه یک لایه‌ی تازه.** آن کلاس از قبل
 *    حاشیه‌ی امن (`env(safe-area-inset-*)`) و اسکرولِ لایه را درست
 *    دارد، و `watchOverlays()` هم بدنه را قفل می‌کند. لایه‌ی دوم یعنی
 *    دوباره افتادن در همان سه باگی که `CLAUDE.md` برایشان نوشته دارد.
 *    ⚠ و چون `display: none` پیش‌فرضِ خودِ کلاس است، هیچ ویژگیِ
 *      `hidden` ای رندر نمی‌شود و قاعده ۱۸ اصلاً موضوعیت ندارد.
 *
 * ⛔ **تنها مرجعِ اسلایدها همین آرایه است.** با یک فهرستِ دومِ
 *    جاوااسکریپتی، نقطه‌ها و اسلایدها دیر یا زود از هم می‌افتادند —
 *    همان قاعده‌ی `Auth::SESSION_WINDOWS` و `DUE_TABS`.
 *
 * ⚠ متن‌ها عمداً درباره‌ی **کاری که کاربر می‌کند** حرف می‌زنند، نه
 *   درباره‌ی «امکانات». فهرستِ امکانات چیزی است که کاربر رد می‌کند.
 */

/** ⛔ تنها مرجع. نقطه‌ها هم از روی همین شمرده می‌شوند. */
const INTRO_SLIDES = [
    [
        'icon'  => '✍️',
        'title' => 'خرج و درآمد را همان لحظه ثبت کنید',
        'body'  => 'با دکمه‌ی + مبلغ را می‌زنید و دسته‌بندی را از همان '
                 . 'ردیفِ بالای فرم انتخاب می‌کنید. عنوان اجباری نیست.',
    ],
    [
        'icon'  => '💳',
        'title' => 'حساب‌ها، چک، طلب و بدهی',
        'body'  => 'هر حساب موجودیِ خودش را دارد و چک و قسط هم سررسیدشان '
                 . 'را. برنامه پیش از رسیدنِ موعد خبر می‌دهد.',
    ],
    [
        'icon'  => '📊',
        'title' => 'ببینید پول کجا رفت',
        'body'  => 'گزارشِ دسته‌بندی، خالص دارایی و «پول قابل خرج» همه از '
                 . 'همان چیزی ساخته می‌شوند که ثبت کرده‌اید.',
    ],
    [
        'icon'  => '🔒',
        'title' => 'داده‌ی شما مالِ خودتان است',
        'body'  => 'هر وقت خواستید یک فایل پشتیبان بگیرید، و هر وقت '
                 . 'خواستید کلِ حساب را پاک کنید. هیچ چیزی گروگان نیست.',
    ],
];
?>
<div class="modal-overlay intro-overlay" id="introModal">
    <div class="modal-box intro-box">
        <div class="modal-header">
            <h3>آشنایی کوتاه با <?= h(defined('APP_NAME') ? APP_NAME : 'حساب لند') ?></h3>
            <?php /* ⚠ «بستن» از همان اسلایدِ اول هست، نه فقط در آخر:
                     کسی که نمی‌خواهد نباید مجبور باشد تا ته برود. */ ?>
            <button type="button" class="modal-close" id="introClose"
                    aria-label="بستن">&times;</button>
        </div>

        <div class="intro-slides">
            <?php foreach (INTRO_SLIDES as $i => $s): ?>
            <div class="intro-slide<?= $i === 0 ? ' is-on' : '' ?>">
                <div class="intro-icon" aria-hidden="true"><?= h($s['icon']) ?></div>
                <h4 class="intro-title"><?= h($s['title']) ?></h4>
                <p class="intro-body"><?= h($s['body']) ?></p>
            </div>
            <?php endforeach; ?>
        </div>

        <?php /* نقطه‌ها فقط با بیش از یک اسلاید معنا دارند — همان قاعده‌ی
                 اسلایدرِ خانه و نوارِ صفحه‌بندی. */ ?>
        <?php if (count(INTRO_SLIDES) > 1): ?>
        <div class="intro-dots" id="introDots">
            <?php foreach (INTRO_SLIDES as $i => $s): ?>
            <span class="intro-dot<?= $i === 0 ? ' is-on' : '' ?>"></span>
            <?php endforeach; ?>
        </div>
        <?php endif; ?>

        <div class="modal-actions">
            <button type="button" class="btn btn-secondary" id="introPrev">قبلی</button>
            <button type="button" class="btn btn-primary" id="introNext">بعدی</button>
        </div>
    </div>
</div>
