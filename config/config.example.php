<?php
/**
 * فایل تنظیمات دیتابیس
 * این فایل را کپی کرده و به نام config.php ذخیره کنید (در همین پوشه: config/config.php)
 * سپس مقادیر زیر را با اطلاعات هاست خودتان جایگزین کنید
 */

define('DB_HOST', 'localhost');
define('DB_NAME', 'your_database');
define('DB_USER', 'your_username');
define('DB_PASSWORD', 'your_password');
define('DB_CHARSET', 'utf8mb4');

define('APP_NAME', 'حساب لند');
define('APP_TIMEZONE', 'Asia/Tehran');
define('APP_CURRENCY', 'تومان');

define('APP_SECRET_KEY', 'CHANGE_THIS_TO_A_RANDOM_SECRET_STRING_1234567890');

/**
 * کلیدِ رمزنگاریِ داده‌ی حساس (شماره کارت، شماره حساب، شبا).
 *
 * ⛔ خالی بگذارید تا رمزنگاری خاموش بماند و همه چیز دقیقاً مثل قبل کار
 *    کند. برای روشن کردنش این را اجرا کنید — کلید را خودش می‌سازد و
 *    ردیف‌های موجود را هم مهاجرت می‌دهد:
 *
 *        php deploy/encrypt-cards.php --setup
 *
 * ⛔ کلید گم شود، داده رفته است. بکاپِ دیتابیس **بدونِ این کلید قابل
 *    بازیابی نیست**؛ یک نسخه‌اش را جای امنی بیرون از سرور نگه دارید.
 *    (این عمدی است: اگر کلید کنارِ دیتابیس بماند، رمزنگاری بی‌معناست.)
 */
define('APP_ENCRYPTION_KEY', '');

define('APP_FORCE_HTTPS', false);

/**
 * محیط و سطحِ لاگ.
 *
 * APP_ENV:   'prod' | 'staging' | 'dev' — در هر خطِ لاگ نوشته می‌شود
 *            (`env`) و در `dev` سطحِ پیش‌فرض `debug` است. نبودنِ این
 *            ثابت یعنی `prod` (سمتِ امن).
 * LOG_LEVEL: 'debug' | 'info' | 'warn' | 'error' — کمترین سطحی که در
 *            `var/log/` نوشته می‌شود. خالی یعنی `info` (و در dev: debug).
 *            اگر لاگ روزانه بزرگ شد، `warn` فقط مشکل‌ها را نگه می‌دارد.
 *
 * لاگ‌ها در `var/log/web-YYYY-MM-DD.log` (وب) و `cli-…` (cron و خط
 * فرمان) می‌نشینند و ۱۴ روز می‌مانند. هیچ رمز، توکن، شماره کارت یا
 * ایمیلی در آن‌ها نوشته نمی‌شود. خلاصه: `php deploy/log-report.php`
 */
define('APP_ENV', 'prod');
define('LOG_LEVEL', '');

/**
 * آیا سایت پشتِ یک CDN (مثلاً کلادفلر) است؟
 *
 * ⛔ این فقط برای **راست ماندنِ صفحه‌ی حریم خصوصی** است، نه برای هیچ
 *    تصمیمِ فنی‌ای. پشتِ CDN، ارتباطِ رمزنگاری‌شده‌ی کاربر روی لبه‌ی آن
 *    شبکه باز می‌شود — یعنی رمز و شماره کارت از دیدشان رد می‌شود. اگر
 *    `privacy.php` همچنان بگوید «هیچ چیزی به سرویس بیرونی نمی‌رود»،
 *    دروغ گفته.
 *
 * ⚠ خالی/false گذاشتنش چیزی را خاموش **نمی‌کند**: برنامه سرآیندِ خودِ
 *   CDN را هم می‌سنجد و اگر دید، همان را می‌گوید. این مقدار فقط
 *   می‌تواند تشخیص را **قطعی** کند — برای وقتی که سرآیند عوض شود یا
 *   CDN دیگری جلو باشد.
 *
 * (جایگزینیِ آی‌پیِ کاربر کارِ nginx است، نه این:
 *  `sudo bash deploy/nginx-realip.sh --apply`)
 */
define('APP_BEHIND_CDN', false);

/**
 * اگر پروژه را مستقیم روی ریشه دامنه/ساب‌دامنه نصب کرده‌اید، این مقدار را خالی بگذارید.
 * اگر پروژه داخل یک زیرپوشه نصب شده (مثلاً https://example.com/hesabland)، مسیر را اینجا بنویسید: '/hesabland'
 * این مقدار برای درست‌کار کردن لینک‌ها و فایل‌های CSS/JS در صفحات پنل مدیریت (admin/) لازم است.
 */
define('APP_BASE_PATH', '');

/**
 * آدرس مطلق اپ — برای لینک‌هایی که در ایمیل می‌روند.
 *
 * ⚠️ این را حتماً پر کنید. اگر خالی بماند، لینک بازیابی رمز از روی
 * سرآیند Host درخواست ساخته می‌شود و آن سرآیند را خودِ درخواست‌کننده
 * تعیین می‌کند — یعنی کسی می‌تواند کاری کند که لینک بازیابیِ حساب شما
 * به سایت او اشاره کند.
 */
define('APP_URL', 'https://example.com');

/**
 * ارسال ایمیل — برای بازیابی رمز.
 *
 * MAIL_METHOD یکی از این‌ها:
 *   ''      خاموش. صفحه‌ی بازیابی می‌گوید در دسترس نیست.
 *   'mail'  تابع mail() خود PHP. نیاز به MTA روی سرور دارد و روی
 *           VPS تازه معمولاً یا نصب نیست یا ایمیلش به اسپم می‌رود.
 *   'smtp'  اتصال به یک سرور SMTP. قابل اتکاتر.
 */
define('MAIL_METHOD', '');

define('MAIL_FROM', 'no-reply@example.com');
define('MAIL_FROM_NAME', 'حساب لند');

// فقط وقتی MAIL_METHOD = 'smtp'
define('SMTP_HOST', '');
define('SMTP_PORT', 587);
define('SMTP_SECURE', 'tls');   // tls | ssl | none
define('SMTP_USER', '');
define('SMTP_PASS', '');
define('SMTP_EHLO', 'example.com');

/**
 * ارسال پیامک — فقط برای «ورود با کد پیامکی».
 *
 * ⛔ خالی گذاشتنش یعنی این قابلیت وجود ندارد، و این پیش‌فرضِ درست است:
 *    ورود با پیامک یک راهِ ورودِ دوم به هر حسابی است و باید تصمیمِ صریحِ
 *    مالکِ نصب باشد. کلیدِ مدیر در «مدیریت کاربران» هم جدا لازم است؛
 *    یکی از این دو بدونِ دیگری کاری نمی‌کند.
 *
 * SMS_METHOD یکی از این‌ها:
 *   ''            خاموش.
 *   'kavenegar'   کاوه‌نگار — SMS_API_KEY لازم است.
 *   'smsir'       sms.ir — SMS_API_KEY و SMS_SENDER (شماره خط) لازم است.
 *   'melipayamak' ملی‌پیامک — SMS_USER و SMS_PASS لازم است.
 *   'log'         ⛔ فقط توسعه: هیچ پیامکی فرستاده نمی‌شود و کدها در
 *                 var/sms.log نوشته می‌شوند. روی سرورِ واقعی یعنی
 *                 کدهای ورود در یک فایلِ متنی روی دیسک.
 *
 * هر سه پنل حساب و اعتبار و خطِ تأییدشده می‌خواهند.
 */
define('SMS_METHOD', '');
define('SMS_API_KEY', '');
define('SMS_SENDER', '');       // شماره‌ی خط (کاوه‌نگار اختیاری، sms.ir لازم)
define('SMS_USER', '');         // فقط ملی‌پیامک
define('SMS_PASS', '');         // فقط ملی‌پیامک

/**
 * نوعِ حسابِ ملی‌پیامک — فقط وقتی SMS_METHOD = 'melipayamak'.
 *
 * ⛔ این دو حساب آدرسِ متفاوتی دارند و اشتباه گرفتنشان یعنی پیامک
 *    فرستاده نمی‌شود، با پیامی که علتِ واقعی را نمی‌گوید:
 *
 *   'console'  حسابِ جدید. فقط «کلید وب‌سرویس» دارد (SMS_API_KEY) و از
 *              console.melipayamak.com می‌رود.
 *   'panel'    حسابِ قدیمی. نام کاربری و رمزِ پنل دارد (SMS_USER و
 *              SMS_PASS) و از rest.payamak-panel.com می‌رود.
 *
 * خالی بگذارید تا از روی پر بودنِ SMS_USER حدس زده شود (رفتارِ قبلی،
 * برای نصب‌هایی که از قبل کار می‌کنند). در پنل مدیر هم قابل انتخاب است.
 */
define('SMS_MELI_MODE', '');

/**
 * الگوی پیامکِ خدماتی (اختیاری ولی توصیه‌شده).
 *
 * ⛔ برای کدِ ورود، پنل‌های ایرانی «الگو» دارند که از قبل تأییدش
 *    می‌کنند: ارزان‌تر است، **به خطِ اختصاصی نیاز ندارد**، و شبانه‌روزی
 *    می‌رود — در حالی که پیامکِ متن‌آزاد ممکن است اصلاً تحویل نشود.
 *
 *   ملی‌پیامک   → `bodyId` عددیِ «خدمات پایه»ای که در پنل ساخته‌اید.
 *   کاوه‌نگار    → نامِ الگو در `verify/lookup`.
 *   sms.ir      → `templateId` عددی؛ نامِ پارامترِ الگو باید `CODE` باشد.
 *
 * خالی بگذارید تا پیامکِ متن‌آزاد با شماره خط فرستاده شود.
 */
define('SMS_PATTERN', '');

/**
 * متنِ پیامکِ کدِ ورود — **فقط حالتِ متن آزاد**.
 * با الگو بی‌اثر است، چون متن را خودِ الگو دارد.
 * `{code}` جای کد و `{ttl}` جای مهلت (دقیقه) می‌نشیند.
 * خالی = متنِ پیش‌فرضِ برنامه.
 */
define('SMS_TEXT', '');

/* ═══════════════ سهمِ سهامدارِ حسابداریِ فروشگاه ═══════════════ */

/**
 * آدرسِ اندپوینتِ فقط‌خواندنیِ «سهم سهامداران» در حسابداری فروشگاه.
 *
 * ⛔ **خالی یعنی کلِ قابلیت خاموش است، و پیش‌فرض عمداً همین است.** این
 *    فایل وارد گیت نمی‌شود، پس هیچ نصبی با یک `git pull` ناگهان شروع
 *    نمی‌کند به صدا زدنِ یک سرویسِ دیگر.
 *
 * ⛔ آدرس روی **همان سرور** است و از `127.0.0.1` خوانده می‌شود، نه از
 *    اینترنت: هیچ داده‌ای از این نصب بیرون نمی‌رود. فقط خوانده می‌شود.
 *    (باز کردنِ مستقیمِ فایلِ دیتابیسِ آن سیستم ممنوع است — قانونِ
 *    استقلالِ پروژه در `CLAUDE.md`.)
 *
 * نمونه: http://127.0.0.1:3000/api/shareholding
 */
define('STORE_API_URL', '');

/**
 * توکنِ همان اندپوینت — دقیقاً همان مقداری که در `.env` سمتِ فروشگاه
 * زیرِ `SHARE_API_TOKEN` گذاشته شده. کمتر از ۲۴ کاراکتر آن‌طرف پذیرفته
 * نمی‌شود.
 *
 * ⚠ این توکن فقط **خواندن** می‌دهد؛ با آن نمی‌شود در حسابداری فروشگاه
 *   چیزی نوشت. ولی همان خواندن هم مانده و سودِ همه‌ی سهامدارها را
 *   می‌دهد، پس مثل هر رمزِ دیگری در همین فایل می‌ماند و جای دیگری
 *   نوشته نمی‌شود.
 */
define('STORE_API_TOKEN', '');
